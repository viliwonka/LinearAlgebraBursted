# Doc-bloat scan — 2026-07-25

Read-only scan for domain-exposition bloat (prose that teaches the reader the subject matter
instead of stating the contract of the code). Per `docs/dev/brief-doc-bloat-scan.md`. This is
NOT the jargon sweep and NOT the dev-history sweep (both already done separately).

Scope scanned: `Assets/LinearAlgebra/CodeGen/TemplateSource/**` (all ~280 template files),
`Assets/LinearAlgebra/Benchmarks/*.cs` (~40 files), `Assets/Demos/**` (~20 files),
`docs/features/*.md` (~22 files). `Source/`, `SourceTests/Generated/`, `Benchmarks/Generated/`
excluded (codegen output). `DEVLOG.md`, `docs/dev/`, `docs/audit/` excluded (exempt by design).

## Findings

| file:line | category | lines | verdict |
|---|---|---|---|
| _pending — being assembled from parallel scan agents_ | | | |

_(draft in progress)_
