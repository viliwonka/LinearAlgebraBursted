# Release scan 2026-07-12 — area: lp

Scanned 9 template files (core). Findings: total 5 — confirmed 5, uncertain 0, unverified 0, refuted 0; by severity: high 0, medium 0, low 5.

## Scope

- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.Info.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.Cache.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.RevisedSimplex.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.DualSimplex.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.InteriorPoint.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.BarrodaleRoberts.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.FrischNewton.fProxy.cs
- Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.Sparse.fProxy.cs

## Findings

### 1. [low/naming/CONFIRMED] Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.Info.cs:36 — XML doc marks LPMethod.Simplex as the default backend, but LP.solve's actual default parameter is RevisedSimplex.

**Evidence**

```
Line 34-36: `Simplex = 0` doc ends "...robust at modest dense sizes. Default." — but LP.fProxy.cs line 52 declares `LPMethod method = LPMethod.RevisedSimplex` and line 43-47 documents RevisedSimplex as the default. The enum doc contradicts the real default.
```

The enum member doc claims Simplex is the default while the actual `LP.solve` parameter default is `LPMethod.RevisedSimplex`.

**Verifier**

LP.Info.cs:34-36 marks `LPMethod.Simplex` as "Default.", but LP.fProxy.cs:49-52 declares `LP.solve(..., LPMethod method = LPMethod.RevisedSimplex, ...)` and its `<param name="method">` at lines 43-47 explicitly states "default RevisedSimplex - fastest exact backend...". Two authoritative XML docs contradict each other and the actual parameter default. Low severity, but a real user-facing naming/doc bug in a pre-release audit. Suggested fix: strip "Default." from Simplex's summary and, if desired, append it to RevisedSimplex's summary.

**Suggested fix**

Remove "Default." from the Simplex enum member doc (or move it to RevisedSimplex), matching the actual `= LPMethod.RevisedSimplex` parameter default.

### 2. [low/naming/CONFIRMED] Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.Info.cs:29 — LPMethod summary says "Both reach the same optimal vertex" but four backends now exist (Simplex, InteriorPoint, RevisedSimplex, DualSimplex).

**Evidence**

```
Line 28-30: "Which backend LP.solve uses. Both reach the same optimal vertex on a bounded, feasible LP..." The word "Both" is stale — the enum below defines four members. LP.fProxy.cs header line 17-19 has the same staleness ("Two backends ... Simplex ... InteriorPoint").
```

The summary's "Both" predates the addition of RevisedSimplex and DualSimplex; the matching header comment in LP.fProxy.cs is equally stale.

**Verifier**

LP.Info.cs line 28 XML summary says "Both reach the same optimal vertex" while the LPMethod enum on lines 32-56 declares four members (Simplex, InteriorPoint, RevisedSimplex, DualSimplex). LP.fProxy.cs line 17 has the matching staleness — "Two backends" — and only lists Simplex + InteriorPoint, omitting RevisedSimplex and DualSimplex introduced later. Both are public-facing header comments, so the contradiction is a real documentation defect (low severity but genuine).

**Suggested fix**

Reword "Both" to "All" (and update LP.fProxy.cs lines 17-19 to list all four backends), or drop the count.

### 3. [low/naming/CONFIRMED] Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.RevisedSimplex.fProxy.cs:439 — Code comment carries development history, which the project comment policy relegates to DEVLOG.md.

**Evidence**

```
Line 439-442: "Warm-start overload -- added for LPMethod.DualSimplex's HiGHS-style composition ... Non-breaking: the fresh-start overload above now simply builds the all-logical basis/status and forwards here, so its behavior and public surface are unchanged."
```

"added for" / "Non-breaking" / "now simply" is change-history narration, not a contract.

**Verifier**

Lines 439-442 of LP.RevisedSimplex.fProxy.cs contain phrases "added for LPMethod.DualSimplex's HiGHS-style composition" and "Non-breaking: the fresh-start overload above now simply builds ... so its behavior and public surface are unchanged" — pure origin/change-history narration. CLAUDE.md's strict comment policy explicitly enumerates development history as DEVLOG.md material, not code. The immediately-following paragraph (lines 444-448) already states the actual contract (basis/status validity, caller ownership, phase-1/2 selection) and the fresh-start overload's header (lines 420-423) already documents the forwarding relationship contractually, so the history paragraph is both non-contract and redundant.

**Suggested fix**

Reduce to the contract (what the overload requires of basis/status and that it forwards); move the "added for / non-breaking" history to the folder DEVLOG.md.

### 4. [low/naming/CONFIRMED] Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.BarrodaleRoberts.fProxy.cs:240 — Code comment contains performance verdicts and rejected-alternative narration, which the comment policy sends to DEVLOG.md.

**Evidence**

```
Line 240-243: "Left AS-IS: this collection pass costs O(m) ... A from-scratch column-major shadow of T would also have to stay in sync ... not worth it here."
```

Rejected-alternative/perf-reasoning belongs in DEVLOG per CLAUDE.md's strict comment policy.

**Verifier**

Lines 237-243 verbatim contain a rejected-alternative narration ("A from-scratch column-major shadow of T ... not worth it here") plus a complexity/perf verdict ("O(m*iters), asymptotically dominated by the O(m*n^2) BRPivot elimination sweep"). CLAUDE.md's strict comment policy names "benchmark results, perf verdicts, rejected alternatives" as categories that MUST live in DEVLOG.md, not in code comments. The contract-worthy row-major access-stride explanation of T[i,enter] should stay; the "Left AS-IS ... not worth it here" rationale must be moved. Severity is correctly low (style/policy, not correctness).

**Suggested fix**

Keep only the access-pattern contract; relocate the "left as-is / not worth it" rationale and complexity argument to the folder DEVLOG.md.

### 5. [low/naming/CONFIRMED] Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.DualSimplex.fProxy.cs:519 — Code comment contains a rejected-alternative performance verdict (comment-policy violation).

**Evidence**

```
Line 519-525: "...LEFT AS-IS deliberately ... Routing it through a dense Mmul(...) would touch all N columns unconditionally -- a regression whenever flipCount << N, the common case -- for a loop that was never the O(mN) bottleneck."
```

This is a perf verdict / rejected alternative, per policy a DEVLOG entry.

**Verifier**

Lines 518-525 in Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LP.DualSimplex.fProxy.cs contain a "LEFT AS-IS deliberately" note plus explicit rejected-alternative reasoning ("Routing it through a dense Mmul... would touch all N columns unconditionally -- a regression whenever flipCount << N") and a perf verdict comparing to "the O(mN) PRICE passes." CLAUDE.md's comment policy names exactly these categories -- perf verdicts and rejected alternatives -- as DEVLOG-only material. The contract-only remnant (flip application is O(flipCount*m)) is the acceptable trim.

**Suggested fix**

Trim to the behavioral note (flip application is O(flipCount*m)); move the regression/rejected-Mmul reasoning to DEVLOG.md.

## Scanner notes

Reviewed all nine LP template files in full. No high/medium numerical, logical, or pointer defects found. Specifically verified: (1) memory — every Allocator.Temp buffer in simplexCore, RevisedPrimalCore, DualSimplexPivotCore, interiorCore, ladFrischNewtonCore, ladBarrodaleRobertsCore, standardFormInterior, and the LP.solve basis/cache overloads is disposed on all break/return paths; the cache-aware dual overload correctly does NOT dispose caller-owned B/P/eta/weight. (2) codegen — the choose-markers expand sanely for both float and double (LAD threshold 512|4096, Harris tie window 1e-6f|1e-12, dual ratio tie 1e-5f|1e-9); tolerances are floored so the double path is not machine-tight. These files are fProxy-only (float/double), so int/uint overflow/division pitfalls do not apply. (3) Cross-checked sign/index conventions in the Frisch-Newton port (x=-yBest, affine/corrector dz/dw formulas), the Mehrotra IPM predictor/corrector RHS assembly (dense interiorCore matches sparse standardFormInterior), the dual-simplex leaving-variable status assignment and DSE weight update, and the LAD split-variable reformulation objective — all consistent. The only defects are the documentation/comment-policy items above (all low). There are additional comment-policy borderline cases (e.g. benchmark verdicts embedded in LP.fProxy.cs's public XML doc for the RevisedSimplex default, and "Measured, per-dtype crossover" at LP.fProxy.cs:316) not itemized separately.
