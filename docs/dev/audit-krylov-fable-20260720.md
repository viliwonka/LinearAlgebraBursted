# Krylov per-solver Fable audit (2026-07-20)

59 agents (28 review + 31 verify), fable model. 30 confirmed correctness bugs, 138 optimization ideas. Findings are advisory (Fable-found + Fable-verified); confirm before acting.

## Confirmed correctness bugs (verify.real==true)
### [HIGH] minresQLP - Warm start silently discarded: QLP branch entered at iters==1 rebuilds x from a zeroed xl2
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:234
Line 234's condition `Acond < TranCond && flag != flag0 && QLPiter == 0` is false on every healthy iteration (flag == flag0 until a guard fires), so the QLP else-branch runs from iters==1 in EVERY solve. At iters==1: line 246 xl2.zeroInPlace(), line 247 `if (iters > 1)` skips the x-capture, then line 305 x.CopyFrom(in xl2) overwrites x with 0 + u*w. The Lanczos was seeded with r0 = b - A*x0 (lines 79-81), so the solver converges to dx = pinv(A)*(b - A*x0) and RETURNS dx, not x0 + dx. Failure: any warm-started call (x0 != 0, e.g. re-solve in a control loop) returns wrong x; the exit honesty guard then sees finalRnorm = ||A*x0|| and downgrades to MaxIterations — wrong x AND misleading status. The DEVLOG warm-start proof (OP/DEVLOG.md:501-512) claims QLP-at-iters==1 is 'provably unreachable' via Acond < TranCond, but that argument assumes the branch condition is `flag == flag0`; the next DEVLOG entry (513-523) itself states the ported `!=` condition runs QLP 'from iteration 1 onward'. The Python reference (reference/square/minresQLP.py:348) has the same inverted condition but supports no warm start (x built from 0 against r2=b), so it is only wrong HERE. No test warm-starts minresQLP (all use zeroed arena.fProxyVec). Fix: seed xl2 from x when the QLP phase begins at iters==1 (xl2.CopyFrom(in x) instead of zeroInPlace at line 246 for the iters==1 case — at iters==1 ul=ul2=0 so xl2 must carry x0 alone), and/or flip line 234 to `flag == flag0` (see optimizations), which also makes the DEVLOG unreachability proof actually hold for healthy solves; the xl2 seed is still needed for the guard-fired-at-iteration-1 edge.
**verify:** CONFIRMED by direct code trace. Line 234's condition (Acond < TranCond && flag != flag0 && QLPiter == 0) is false on every healthy iteration because flag == flag0 until a guard/stopping test fires, so the QLP else-branch runs from iters==1 in every normal solve — the OP/DEVLOG.md entry at lines 513-523 explicitly says so, contradicting the warm-start 'provably unreachable' proof at lines 501-512. At iters==1 the branch zeroes xl2 (line 246), the only x0-capture (line 263, xl2.CopyFrom(in x)) is behind `if (iters > 1)` and is skipped, and line 305 x.CopyFrom(in xl2) overwrites x with u*w (ul2=ul=0 at iters==1), destroying x0 permanently (QLPiter>=1 blocks any later capture). Since the Lanczos is seeded with r0 = b - A*x0 (lines 79-81), the solver returns dx = A⁺(b - A*x0) instead of x0 + dx. Concrete failure: A=I, b=e1, x0=e2 returns e1-e2 instead of e1; the honesty guard (lines 387-391) then sees finalRnorm ≈ ||A*x0|| and reports MaxIterations — wrong x AND misleading status. Warm start is contractual (doc lines 22-23: 'x is a warm-startable initial guess'). The Python reference (reference/square/minresQLP.py:348) has the same != condition but no warm start (x built from 0), so it is only wrong here. No test warm-starts minresQLP (battery uses x0=0), explaining the green suite. Only mitigation: the honesty guard prevents a false Converged status, but the returned x is still wrong.

### [HIGH] Krylov.BiCGStab - Failed half-step trial verify corrupts v for the next iteration's p-recurrence
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:131
Lines 131-132 overwrite v with A*(trial x) - b to test the half-step exit. The comment claims 'v: fully consumed forming rv above' and 't/v get overwritten below regardless' — but only t is rewritten later in the iteration (lines 145/150); v is NOT. When trialRR > threshold the code falls through, finishes the iteration, and the NEXT iteration's line 99 `p.addScaledInPlace(-omega, v)` consumes this stale v (≈ -s_old in exact arithmetic) instead of the Krylov v = A·M⁻¹p_k, so p_new = r + beta*(p - omega*v) is computed with the wrong v — a wrong recurrence from then on. Scenario: ill-conditioned A where the recurrence ss dips under tol*tol*bb but the true residual has drifted above it (exactly the case the verify exists for) → every subsequent direction is polluted; the solver can stall to MaxIterations or hit spurious Breakdown (true-residual verify prevents false Converged, but convergence is destroyed and the rnorm reported at MaxIterations/Breakdown comes from the now-meaningless recurrence rr). Fix: on the failed-verify fall-through, restore v with one extra apply (identity: A.Apply(in p, ref v); else A.Apply(in pHat, ref v)) — rare path, cheap; under a real M, sHat is also idle until line 149 and could hold A*trial instead, but under the identity there is no spare buffer, so the re-apply is the uniform fix.
**verify:** CONFIRMED by direct code trace of Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs. Lines 131-132 overwrite v with A*(trial x)-b for the half-step verify. On failed verify (trialRR > threshold, line 139 fall-through) the rest of iteration k writes only t (145/150), x (168-177), and r (180) — v is never restored. The next iteration's line 99 `p.addScaledInPlace(-omega, v)` consumes this corrupted v (≈ -s_old) where the recurrence requires v = A·M⁻¹p_k, so p_new is built with the wrong vector and the BiCGSTAB biorthogonality/recurrence is broken from then on. The line-139 comment 't/v get overwritten below regardless' is wrong for v (overwritten only after line 99 of the NEXT iteration). Path is reachable exactly in the ill-conditioned recurrence-drift case the verify was added for. Severity bound: r = b - Ax invariant is still maintained and every Converged return passes a true-residual check, so no silent wrong-answer Converged — the observable failure is stall to MaxIterations or spurious Breakdown on inputs a correct implementation would solve. A second instance of the same flaw exists at line 186 (VerifyTrueResidual clobbers v, 'v is idle here' comment at 184, same fall-through consumption at next line 99). Proposed fix (re-apply A to p/pHat into v on the failed-verify fall-through) is correct; under the identity preconditioner there is indeed no spare buffer, so the re-apply is the uniform fix.

### [HIGH] Krylov.BiCGStab - Failed end-of-iteration VerifyTrueResidual leaves v = A*x, corrupting the next p-update
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:186
Line 186 calls VerifyTrueResidual(in A, in b, in x, ref v, ref r) (Krylov.Solve.Common.fProxy.cs:20-27 fully overwrites the Ax scratch: v := A*x). On a failed verify (recomputed rr > threshold) the loop continues — r is intentionally refreshed to the true residual per the comment, but v now holds A*x, not A·M⁻¹p_k, and the next iteration's line 99 `p.addScaledInPlace(-omega, v)` uses it → wrong search direction, same failure mode as the trial-site bug (stall/late convergence on exactly the drift-prone systems). The comment 'v is idle here' is wrong across the iteration boundary — this pattern is copied from cg (Krylov.CG.fProxy.cs:203) where Ap really is dead because CG recomputes Ap at the top of each iteration before first use; BiCGStab reads v before rewriting it. Fix is free here: t IS genuinely idle at line 186 (last read line 180, next write line 129/145/150 before any read) and is already in the distinct-buffer set, so pass `ref t` as the Ax scratch instead of `ref v`.
**verify:** CONFIRMED by direct dataflow trace of Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs. VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:23) overwrites v := A*x at line 186; on a failed verify (fresh rr > threshold) the loop continues with no restart, and the NEXT iteration reads v at line 99 (p.addScaledInPlace(-omega, v)) BEFORE v is rewritten at line 105/110. The recurrence needs v = A·M⁻¹p_k there, so the search direction p is polluted by -beta·omega·(A*x) — the "v is idle here" comment at line 184 is false across the iteration boundary. The CG analogy fails exactly as claimed: CG recomputes Ap before first use each iteration; BiCGStab consumes old v first. The proposed fix is valid: t is genuinely idle at line 186 (last read line 180, always written at 129/145/150 before any subsequent read) and is already in the distinct-buffer set. Concrete failure: drift-prone ill-conditioned nonsymmetric systems where tracked rr passes but true residual fails → corrupted p → stall/late convergence/spurious MaxIterations, deterministically. Caveat on severity: no false-Converged is possible (every Converged exit is backed by a fresh true-residual check) and no crash/nondeterminism — the wrong result is convergence failure from corrupted iteration state, not a silently wrong x. The trial-verify site (line 131) has the same cross-boundary v corruption, consistent with the claim's cross-reference.

### [HIGH] bgcrodr — Krylov.Block.GCRODR - AUblk allocated UNINITIALIZED then ACCUMULATED into — garbage/NaN folded into the recycled subspace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:424
Line 424: `AUblk = new fProxyMxN(kcurAtEntry, n, Allocator.Temp, true)` — the 4th ctor arg is `bool uninit` (fProxyMxN.cs:117: true => NativeArrayOptions.UninitializedMemory; bgmres's own Wbuf/HQscratch comments confirm false = cleared). Lines 425-427 then build AU via AddScaledRowToRow, which does `dst[dstRow,c] += scale*src[srcRow,c]` — i.e. it ACCUMULATES on top of uninitialized memory. Scenario: any recycling solve reaching its 2nd deflation rebuild (kcurAtEntry > 0, i.e. 3+ cycles — completely ordinary) computes AUblk = garbage + C*Ru, which poisons Fmat/Gmat/Pgram (lines 444-455), the LU/QR eigensolves, and the rebuilt U/C/Ru; the next cycle's recycle projection then folds a garbage correction into X. Under the project's 0xNaN uninitialized-fill audit this is deterministic NaN poisoning of X (violating the doc's 'never NaN' at line 105); with finite Temp-reuse garbage it silently corrupts the recycled subspace and convergence. Scalar gcrodr is clean here (GCRODR.fProxy.cs:294 uses a cleared `new fProxyN(n)`). Fix: pass uninit=false (or ZeroPrefix AUblk before the accumulate).
**verify:** CONFIRMED. Line 424 allocates AUblk with uninit=true, which fProxyMxN.cs:117-125 maps to NativeArrayOptions.UninitializedMemory. Lines 425-427 then build AU purely via AddScaledRowToRow, which is `dst[dstRow,c] += scale*src[srcRow,c]` (lines 28-31) — an accumulate onto uninitialized Temp memory with no zeroing in between. Reachable in an ordinary scenario: kcur starts 0 (line 172) and becomes >0 at line 555 after the first successful deflation rebuild, so hadOldU is true on the 2nd+ rebuild — i.e., any recycling solve running 3+ restart cycles. The garbage AUblk feeds Fmat/Gmat (lines 444/455 via ResolveCombinedCol returning AUblk rows), the LU/eigensolves, and the rebuilt U/C/Ru (lines 531-553), breaking the A·U=C·Ru invariant; the next cycle's recycle correction (lines 388-404) folds the corruption into X. The downstream numerical guards (luInfo.Solved/haveEig/kSafe) only catch garbage that makes the dense solves fail — finite Temp-reuse leftovers pass silently (wrong, run-dependent X = nondeterminism), and NaN-pattern garbage poisons X outright. Scalar gcrodr uses a cleared allocation at the analogous site, confirming intent. Fix: uninit=false or zero AUblk before accumulating.

### [HIGH] btfqmr - Zero or already-solved RHS row causes immediate whole-solve Breakdown
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:211
B with one all-zero row (padding a fixed-s batch) or a warm-started X whose row is already the exact solution, other rows ordinary: bbAll>0 so the line-137 early-out doesn't fire; Rhat0 row = 0, tau[j]=0, rho[j]=0, V row = A*0 = 0. The row is counted converged at init (bound 0 <= thresh) but convergedInit<s, so the loop runs; at k=0 vtrstar[j]=RowDot(Rhat0,V)=0 and AnyZeroOrNaN (line 212) reports Breakdown for the WHOLE solve with itersDone=0 and X untouched — every other, perfectly solvable row fails too. Scalar tfqmr handles the same input as instant Converged (bb==0 / tau<=thresh paths), so this breaks the documented 's INDEPENDENT copies of scalar tfqmr' contract. Fix: freeze rows with bound[j]<=thresh[j] (skip their updates and exclude them from the AnyZeroOrNaN/AnyNaN breakdown scans) — trivially correct here because rows never mix.
**verify:** Verified by tracing the actual code. A batch with one all-zero RHS row (or a warm-started row whose residual is exactly zero) and other ordinary rows passes both aggregate early-outs (bbAll>0 at line 137; convergedInit<s at line 164), enters the loop with Rhat0/U/W/V row j exactly zero, and at k=0 vtrstar[j]=RowDot(Rhat0,V,j)=0 triggers AnyZeroOrNaN at line 212 → whole-solve Breakdown with itersDone=0 and X untouched (first X write is line 238, never reached). All other solvable rows fail with it. Scalar tfqmr (Krylov.TFQMR.fProxy.cs lines 75-79, 90-91) returns instant Converged on the same per-row input, so this violates the documented "s INDEPENDENT copies of scalar tfqmr / same per-row rigor and breakdown guards" contract; converged rows are scanned by every breakdown check with no freezing/exclusion anywhere in the file. Zero-row padding makes the trigger exact (A·0=0, B[j,c]-0=0), no floating-point coincidence needed. Real bug: spurious Breakdown (no solution returned) on a solvable, contract-supported input.

### [MED] minresQLP - Honesty guard nullifies the solver's min-length least-squares mode (flags 2/4) and backward-error convergence
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:388
Lines 387-391 downgrade Converged to MaxIterations whenever finalRnorm > 64*tol*beta1 (raw ||b-Ax||/||b|| scale). But the solver's advertised specialty (docstring lines 17-19, 28-29; DEVLOG flag mapping, OP/DEVLOG.md:533-542) is the min-length LEAST-SQUARES solution of an incompatible singular system — reference flags 2/4 (relAresl <= tol / chk2 <= 1, lines 352/354). For such systems the fresh true residual EQUALS the LS residual, which is O(||b_perp||), generally >> 64*tol*||b||. Failure: singular symmetric A, b with a range-complement component of relative size > 64*tol -> solver computes the correct min-length LS x, internal flag 2/4, then the guard reports MaxIterations; the documented 'Converged for min-length least-squares solve' contract is unreachable except for nearly-compatible b. Secondary: for compatible but ill-conditioned systems, flag 1 uses the backward-stable criterion rnorm <= tol*(Anorm*xnorm + beta1) (line 338); when Anorm*xnorm >> beta1 a genuinely backward-converged solve can also be downgraded. Fix: make the guard flag-aware — for the LS flags verify smallness of ||A*r|| (one extra Apply on the final residual, mirroring the internally-tracked relAresl) instead of ||r||; keep the raw ||r|| check only for the compatible-solve flags (1/3/-1/5).
**verify:** VERIFIED REAL. The honesty guard (Krylov.MINRESQLP.fProxy.cs:387-391) downgrades Converged to MaxIterations whenever the fresh raw residual exceeds 64*tol*beta1. But the solver's internal LS flags 2/4 (lines 352/354) fire on the ||A*r||-based criterion (relAresl/chk2), which is the CORRECT convergence test for the advertised min-length least-squares mode on an incompatible singular system — where the true residual equals ||b_perp|| and never approaches 64*tol*||b||. Concrete failing input: A=diag(1,1,0), b=(1,1,1), x0=0, default tol=sqrtEps (double): solver computes the correct min-length LS x*=(1,1,0), flag 2/4 fires (||A*r||=0), finalRnorm=1 >> 64*tol*sqrt(3)~1.7e-6, guard reports MaxIterations. Even the flag-0 shortcut (b entirely in null(A), lines 160-167) is downgraded. This directly contradicts the docstring contract (lines 17-19, 28-30: 'Converged for ... min-length least-squares solve') and the DEVLOG flag mapping (OP/DEVLOG.md ~533-537, flags 2/4 -> Converged). Not intended: the guard was added a day after the solver (DEVLOG #53, 2026-07-20) solely to fix a COMPATIBLE ill-conditioned false-Converged (Rosser); its rationale never mentions the LS mode and the docstring still promises the now-unreachable outcome. Not caught by tests: KrylovVerifyAtExitTests only covers Rosser + well-conditioned SPD, and the battery invoker Forbids IllConditioned (Invokers:259) — no test exercises an incompatible singular system. Result: the documented specialty of MINRES-QLP is unreachable except for near-compatible b; callers following the contract discard a correct solution as failed. The claim's secondary point (flag-1 backward-converged downgrades) is weaker — that is the scenario the guard deliberately targets — but the primary LS defect is a genuine wrong-status bug on concrete inputs. Suggested fix direction matches the code structure: make the guard flag-aware (verify ||A*r|| smallness for flags 2/4/0 via one extra Apply on the final residual; keep the raw ||r|| check for flags 1/3/-1/5).

### [MED] Krylov.GMRES - Silent false-Converged: no verify-at-exit true-residual check
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:111
When the inner loop hits resnorm = |g[j+1]| <= thresh (line 108/111), x is updated and the outer loop exits with Converged (line 144) purely on the rotated-rhs ESTIMATE. Restarted MGS-GMRES is exactly the solver where loss of orthogonality in V makes |g[j+1]| drift optimistically below the true ||b - Ax||; an ill-conditioned A can then return Converged while the true residual is above tol*bnorm. Every sibling single-RHS solver (cg at Krylov.CG.fProxy.cs:203, fcg:108, minres:197/212, minresQLP:374, biCGStab:186, idr:174/227) routes through VerifyTrueResidual in Krylov.Solve.Common.fProxy.cs:20 before committing Converged — gmres and fgmres are the only ones that don't (task tracker #60 confirms this is a known, still-unfixed gap). Fix: after the post-loop x update on the converged path, recompute r = b - Ax (w and V[0] are free scratch at that point, matching VerifyTrueResidual's distinct-buffer contract) and only keep converged=true if the fresh residual passes; otherwise fall through and keep restarting while total < maxIter. Note the restart-entry check (line 78-80) IS a true-residual check, so only the mid-cycle exit needs the gate.
**verify:** Confirmed against the actual code. The mid-cycle exit at Krylov.GMRES.fProxy.cs:108-111 sets converged=true purely on the rotated-rhs estimate |g[j+1]|; after the x update (lines 115-136) the outer loop exits and line 144 returns IterativeSolveStatus.Converged with resnorm still the estimate — no recomputation of b - Ax on that path (only the restart-entry check at lines 78-80 is a true residual, and it is not reached after a mid-cycle break). The doc contract at line 19 promises the TRUE residual satisfies ‖b − Ax‖ ≤ tol·‖b‖, so Converged overclaims. In floating point |g[j+1]| equals the true residual only in exact arithmetic; MGS loss of orthogonality plus back-solve/accumulation rounding makes it drift optimistically low by ~κ(A)·ε, which is concretely reachable in the generated float instantiation with the default tol = sqrtEps (~3.4e-4) on an ill-conditioned A (κ ≳ 1e4, e.g. Hilbert-type): true residual above tol·bnorm, status Converged. Grep confirms every sibling single-RHS solver (CG:203, FCG:108, BiCGStab:186, IDR:174/227, MINRES:197/212, MINRESQLP:374) gates Converged through VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20); GMRES has no reference to it, so this is an inconsistency with the library's own verify-at-exit convention, not an intended weaker contract. Silent false-Converged bug: wrong (unconverged) x returned with Converged status on concrete ill-conditioned inputs.

### [MED] Krylov.GMRES - Breakdown rr==0 declares Converged then divides by H[j,j]=0, poisoning x with Inf/NaN
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs:52
In GivensApplyAndGenerate, if the rotated H[j,j] (a) and the new subdiagonal H[j+1,j] (bb2) are BOTH zero — e.g. A (or A*Minv) maps v_j to the span of V[0..j] with the rotated diagonal cancelling to 0; extreme cases: near-zero operator, M.Apply producing ~0, singular A at breakdown — then rr=0 takes the else branch (c=1, s=0), so g[j+1] = -s*gj = 0 and gmres (Krylov.GMRES.fProxy.cs:108-111) sees resnorm=0 <= thresh and sets converged=true. HessenbergBackSolve (Arnoldi.Common:70) then computes y[i] = sum / H[i,i] with H[j,j]=rr=0 → Inf/NaN propagates through y into x (GMRES line 123/132), and the solver returns status Converged with resnorm 0 and a NaN-contaminated x. The file's own header (lines 11-13) notes gcrodr keeps a pivotGuard-based breakdown test precisely here; gmres/fgmres have none. Guard: on rr <= (breakdown eps)*|scale| either cap k at j (solve the leading j-by-j system, which excludes the zero pivot) or return Degenerate (enum already exists from the LOBPCG fix). The verify-at-exit fix alone is NOT sufficient — once x is NaN it never recovers on subsequent restarts.
**verify:** CONFIRMED by reading the actual code. GivensApplyAndGenerate (Arnoldi.Common:49-60) has no breakdown guard: when the rotated diagonal a and subdiagonal bb2 are both exactly 0, rr=0 takes the else branch (c=1, s=0), stores H[j,j]=0, and sets g[j+1]=0 while g[j] stays nonzero. gmres (GMRES:108-111) then sees resnorm=|g[j+1]|=0<=thresh and sets converged=true, after which HessenbergBackSolve (Arnoldi.Common:70) computes y[j]=g[j]/H[j,j]=nonzero/0=Inf, and x.addScaledInPlace (GMRES:123/132) writes Inf/NaN into x. Returns status Converged with resnorm 0 and poisoned x. Bulletproof concrete input exact in IEEE arithmetic: A = zero matrix (contract only requires square, not nonsingular), b != 0, x0 = 0, tol < 1 — every intermediate is exactly 0, so the path is hit deterministically; also reproduced in exact arithmetic by diag(1,0) with b=(1,1)^T. No guard exists elsewhere: no verify-at-exit, no NaN check, and the file's own header (lines 11-13) states only gcrodr carries a pivotGuard breakdown test, gmres/fgmres do not. Even under a 'nonsingular A assumed' reading, the honest outcome would be Degenerate/MaxIterations, not Converged+resnorm=0+NaN x — this is precisely the silent false-Converged bug class the project already fixed in LOBPCG. fgmres shares the same helpers and is equally exposed.

### [MED] Krylov.FGMRES - Silent false-Converged: inner-loop exit trusts the Givens residual estimate, no verify-at-exit
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FGMRES.fProxy.cs:121
The converged exit at line 121 uses resnorm = |g[j+1]| (Arnoldi/Givens estimate), then updates x (lines 125-144) and returns Converged at line 156 without ever recomputing b - Ax. Only the restart-boundary check (lines 82-87) is a true residual; a mid-cycle estimate exit is never verified. In FGMRES the estimate's validity additionally depends on the stored z_j actually satisfying w_j = A z_j to floating-point accuracy each step -- with an ill-conditioned A or an aggressive inner-iterative M the estimate can underestimate the true residual, so the solver returns Converged while ||b - Ax|| > tol*||b||. Every other single-RHS solver in the family (cg, fcg, minres x2, minresQLP, biCGStab, idr x2) routes through the shared VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20) for exactly this reason; gmres/fgmres are the only ones that don't. Fix is cheap: one extra A.Apply into w + v0 on the converged path (or fall back into the restart loop when the verify fails), reusing VerifyTrueResidual.
**verify:** Verified in Krylov.FGMRES.fProxy.cs: the line-121 exit sets converged=true on the Givens estimate |g[j+1]| alone; after the x-update (125-144) the while condition (!converged) exits and line 156 returns Converged — no b-Ax recompute exists on that path (the lines 82-87 true-residual check only runs at restart-cycle entry, never after converged is set). Grep confirms the asymmetry: VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20, whose header says its purpose is exactly to not trust estimated residuals) is used by CG, FCG, MINRES (x2, including a fall-through-and-keep-iterating site), MINRESQLP, BiCGStab, and IDR (x2); gmres/fgmres alone bypass it. The doc contract at line 28 states convergence as the TRUE residual (‖b−Ax‖ ≤ tol·‖b‖), so an estimate-only Converged violates the documented contract. Concrete failure: float instantiation, ill-conditioned A (κ~1e5-1e6), tol below attainable accuracy — the Givens estimate decreases monotonically past thresh while the true residual stagnates at ~eps·κ‖b‖ (Paige/Rozložník/Strakoš), so the solver returns Converged with ‖b−Ax‖ ≫ tol·‖b‖; FGMRES's inner-iterative M aggravates the estimate/true gap. This is the project's own named "silent false-Converged" trap class and the fix is cheap (reuse VerifyTrueResidual with the free w/V[0] scratch on the converged path).

### [MED] Krylov.FGMRES - No breakdown guard on a zero Arnoldi column: NaN/Inf solution returned as Converged with rnorm 0
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FGMRES.fProxy.cs:113
If w = A(M^-1 v_j) comes back (numerically) zero -- realistic for FGMRES specifically, since the per-step M is arbitrary and an inner iterative solve can stagnate and return z_j = 0, or A z_j = 0 for a singular A -- then ArnoldiMGSStep (Krylov.Arnoldi.Common.fProxy.cs:18-36) writes an all-zero H column, gets hj1 = 0, and per the `if (hj1 > 0)` at line 29 silently skips writing V[j+1] with no breakdown flag. GivensApplyAndGenerate then hits rr == 0 (Common:52-53), takes c=1, s=0, so g[j+1] = -s*g[j] = 0 -> resnorm = 0 <= thresh -> converged = true at FGMRES:121. HessenbergBackSolve (Common:70) then divides by H[j,j] = rr = 0, giving y[j] = +/-Inf, and x.addScaledInPlace propagates Inf/NaN into x -- returned as Status.Converged with rnorm 0. Sub-case: hj1 == 0 with resnorm still > thresh does NOT break the inner loop, so iteration j+1 orthogonalizes against an unwritten V[j+1] -- zero on the first cycle, but a STALE basis vector from the previous cycle on any restart >= 2, corrupting the Arnoldi recurrence with no signal. The Common header comment (lines 10-13) itself notes gcrodr keeps a pivotGuard breakdown test that gmres/fgmres lack. Fix: after ArnoldiMGSStep, treat hj1 == 0 (or <= eps-scaled) as happy-breakdown/degenerate -- back-solve with the current k and exit the cycle instead of generating the rr==0 rotation; guard the H[i,i] divide.
**verify:** Verified against the actual code. When w = A(M^-1 v_j) is exactly zero — concretely reachable with A = diag(1,0), b = (0,1), x0 = 0 (v0 in null(A)), or in flexible mode with an inner-solve preconditioner returning z_j = 0 — ArnoldiMGSStep (Krylov.Arnoldi.Common.fProxy.cs:27-29) writes an all-zero H column, hj1 = 0, and silently skips V[j+1] with no breakdown flag. GivensApplyAndGenerate then hits rr == 0 (line 50-53), takes c=1,s=0, sets H[j,j]=0 and g[j+1]=0, so FGMRES:118-121 sets resnorm=0 <= thresh and converged=true. HessenbergBackSolve (Common:71) then divides g[j] (which is > thresh, i.e. nonzero) by H[j,j]=0 giving y[j]=Inf, and 0*Inf=NaN contaminates earlier y rows; x.addScaledInPlace (FGMRES:129-144) propagates Inf/NaN into x. Returned as Status.Converged with rnorm 0 at FGMRES:156, with no VerifyTrueResidual or NaN guard anywhere in the routine — a silent false-Converged with a garbage solution. The legitimate happy-breakdown case (hj1=0 but column nonzero, rr>0) is fine, but the code conflates the degenerate all-zero-column case with it. The stale-V[j+1]-on-restart sub-case is also present as described. Real bug.

### [MED] Krylov.TFQMR - Converged exit is never verified against a fresh true residual, unlike every sibling solver
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.TFQMR.fProxy.cs:141
cg, fcg, biCGStab, idr, minres and minresQLP all route their Converged/final exits through Krylov.Solve.Common VerifyTrueResidual (e.g. Krylov.BiCGStab.fProxy.cs:186); tfqmr returns Converged purely from the tracked bound tau*sqrt(k+1) (lines 140-142). The doc (lines 30-33) claims this bound 'guarantees the true residual is within tol*||b||', but Freund's bound is rigorous only in EXACT arithmetic. Failure scenario (float instantiation, ill-conditioned nonsymmetric A): a near-orthogonal shadow dot makes vtrstar tiny-but-nonzero, so the ==0 guard at line 115 passes and alpha becomes huge; the QMR smoothing still forces tau_{m+1} = tau_m*theta/sqrt(1+theta^2) < tau_m monotonically, so after the huge x/w excursion the recurrence-tracked tau keeps shrinking while the TRUE ||b-Ax|| has decoupled by O(eps*|alpha|*||d||) roundoff -> bound crosses thresh -> silent false-Converged. Fix = reuse VerifyTrueResidual at the bound<=thresh exit. Note none of the 7 scratch vectors is dead at that point (au/v/d/u/w all feed later half-steps), so a biCGStab-style continue-on-failed-verify needs either 1 extra scratch or an unconditional verify-then-report at exit; the DEVLOG's 500-trial sweep (zero false-Converged) mitigates but doesn't discharge the float/ill-conditioned regime.
**verify:** Verified against the actual template. (1) The Converged exit at Krylov.TFQMR.fProxy.cs:141-142 returns solely from the recurrence-tracked bound tau*sqrt(k+1); no exit path recomputes b-Ax, and grep confirms tfqmr is the only Krylov solver not routed through Krylov.Solve.Common VerifyTrueResidual (used by CG:203, FCG:108, BiCGStab:186, IDR:174/227, MINRES:197/212, MINRESQLP:374 — the sibling comparison in the claim is exactly accurate). (2) The breakdown guards at lines 115/119 are exact ==0/NaN checks, so a tiny near-orthogonal shadow dot vtrstar passes and alpha=rho/vtrstar becomes huge — the claimed trigger is admitted, not guarded. (3) tau=tau*theta/sqrt(1+theta^2) at line 135 is unconditionally monotone nonincreasing regardless of the true residual, while the huge-alpha excursion injects O(eps*|eta|*||d||) rounding error per half-step into x (u -= alpha*v, w -= alpha*au, d accumulates (theta^2/alpha)*eta history) — the standard attainable-accuracy residual-gap of CGS-derived product methods. Freund's bound is rigorous only in exact arithmetic, so the doc at lines 30-33 ("a Converged exit still guarantees the true residual is within tol*||b||") is an unsound guarantee in floating point, i.e. this is not an intended honest contract but an incorrect one. (4) The DEVLOG's 500-trial sweep (DEVLOG.md:445-447) was a standalone Python (double-precision) port on random matrices — it does not cover the float instantiation or adversarial near-breakdown spectra, so it mitigates but does not discharge the claim. (5) By this codebase's own convention (silent false-Converged is a flagged defect class, and every sibling verifies), tfqmr is a genuine outlier. Concrete failure class: float instantiation, ill-conditioned nonsymmetric A with a near-breakdown shadow dot → tracked bound crosses thresh while true ||b-Ax|| has decoupled → silent false Converged with a wrong x. Medium severity is right: needs an adversarial input, but the input class is real and the fix (unconditional verify-at-exit; au is dead once the return is taken) is cheap.

### [MED] Krylov.gcrodr - Silent false-Converged: terminal-cycle status trusts the Givens estimate, and the clamped Ru correction is applied AFTER that estimate
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:225
Converged is decided at line 225 from resnorm=|g[j+1]|. The recycle correction x -= U*zproj (lines 259-274) is applied AFTER that, and its Ru back-solve silently clamps zproj[i]=0 when |Ru[i,i]| <= pivotGuard (line 272). When a clamp trips on the final cycle, the C-component of the residual is NOT cancelled: true ||b-Ax|| exceeds the reported resnorm by the un-cancelled |(B y - Ru zproj)_i| coefficient, yet the solver returns Converged with the smaller pre-correction estimate. The DEVLOG's mitigation ('paired with the fresh-residual-recompute so this never desyncs x from the tracked residual') only fires at the START of the NEXT cycle -- which never runs once converged=true. Even without the clamp, |g| drifts from the true residual via MGS orthogonality loss over m steps. This contradicts the XML contract at line 41 ('never a false Converged'). Fix: call the existing VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20, already used by cg/fcg/minres/minresQLP/biCGStab/idr) at the Converged exit -- one extra matvec, scratch V[0]+w are free there; ties into pending task #60 (gmres/fgmres verify-at-exit gate).
**verify:** CONFIRMED against Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs. Converged is decided at line 225 from the Givens estimate |g[j+1]|, which only measures the C-orthogonal (projected) residual. The recycle correction x -= U·zproj (lines 259-274) runs AFTER that decision even on the terminal cycle, and line 272 silently clamps zproj[i]=0 when |Ru[i,i]| <= pivotGuard; a clamp leaves the C-component C·(By − Ru·zproj) un-cancelled, so true ‖b−Ax‖ can exceed thresh while status=Converged and resnorm reports the smaller pre-correction estimate. The fresh-residual recompute only runs at the start of the NEXT cycle, which the while(!converged) condition (line 117) prevents; the breakdown path recomputes a true residual (lines 239-248) but the Converged path does not, and VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20, used by cg/fcg/biCGStab/idr/minres/minresQLP) is absent here. The clamp is reachable: the build-time truncation guard rGuard=eps·100·(|Rnew[0,0]|+1) (line 413) and the solve-time clamp pivotGuard=eps·100·(bnorm+1) (line 70) scale on different quantities, and harmonic-Ritz selection targets the smallest |θ| (near-null space), so Ru diagonals in the gap (accepted at build, clamped at solve) are constructible (e.g. float, Rnew[0,0]≈1e-2 accepts 1e-4, bnorm≈10 clamps at ≈1.3e-4). This contradicts the explicit contract at lines 39-40 ('never a false Converged'), so it is a real wrong-result bug on a concrete (narrow: recycling active + mid-cycle convergence + guard-gap Ru diagonal) input class. The suggested fix — VerifyTrueResidual at the Converged exit — matches the pattern already used by the sibling solvers.

### [MED] Krylov.gcrodr - pivotGuard is b-scaled but guards ||A||-scaled quantities: well-conditioned small-norm systems return spurious Breakdown
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:70
pivotGuard = 100*eps*(bnorm+1) (line 70) guards the Arnoldi subdiagonal hj1 (line 190), the Hessenberg back-substitution pivot (line 236), and the Ru diagonals (lines 135, 272) -- all quantities that scale with ||A||, not ||b||. Concrete failure (float): A = 1e-6*I, b = ones. pivotGuard ~ 2.4e-5. Cycle 1: H[0,0]=1e-6, hj1~0 -> arnoldiDone; Givens gives g[1]=0 -> converged=true at line 225; back-substitution then sees |H[0,0]|=1e-6 <= pivotGuard -> breakdown=true, and the status ternary at line 459 prefers Breakdown over Converged -> an exactly-solvable one-step system returns Breakdown with x untouched and rnorm=||b||. Any well-conditioned A with ||A|| <~ 1e-5*||b||/||x-scale|| (float) trips this; conversely for huge-||A|| systems the guard is effectively zero. Fix: scale the H/hj1 guards by the running Hessenberg magnitude (e.g. eps * max |H| in the active column/block) and the Ru guard by max |Ru[i,i]|; also skip the back-substitution breakdown promotion when converged is already true (or verify-then-decide, per the previous finding).
**verify:** Verified against Krylov.GCRODR.fProxy.cs. pivotGuard (line 70) = 100*eps*(||b||+1) is ||b||-scaled (absolute floor ~1.2e-5 float) but guards ||A||-scaled quantities: Arnoldi subdiagonal hj1 (line 190), Hessenberg back-sub pivot H[i,i] (line 236), Ru diagonals (lines 135/272). Concrete float trace A=1e-6*I, b=ones(4): guard~3.58e-5; j=0 gives H[0,0]=1e-6, hj1~1e-13 -> arnoldiDone; Givens resnorm~2e-7 <= thresh -> converged=true (line 225); back-substitution then trips |1e-6| <= guard -> breakdown=true, x never updated, resnorm reset to ||b|| (lines 239-247); status ternary (line 459) prefers Breakdown over Converged. A kappa=1, exactly-solvable system returns Breakdown with x untouched — and fires regardless of tol since back-sub runs even when converged. The OP DEVLOG confirms this guard is unique to gcrodr/bgcrodr (gmres/fgmres lack it and solve the same input), contradicting the doc's own 'recycle=0 is bit-identical to gmres' claim; the DEVLOG documents the guard's intent but not this scale mismatch, and no other check rescues the result. Same mis-scaled guard exists in Krylov.Block.GCRODR.fProxy.cs:190. Minor claim errata (g[1]~2e-7 not exactly 0; returned resnorm is an honest fresh ||b||, so it's a spurious hard failure rather than a silently wrong x) don't change the verdict: real, reachable, wrong behavior on a well-conditioned input.

### [MED] Krylov.CRAIG - Converged status not reconciled against the certified-exact residual that CraigInfo already computes
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIG.fProxy.cs:104
Line 104 declares Converged from the recurrence estimate rnorm = |beta*z| (line 102). CraigInfo (lines 123-131) then pays for a certified-exact lstsqResidual audit (one Apply + ApplyT) and stores the TRUE rnorm into the returned struct — but never compares it to tol*bnorm, and the status stays Converged. On an ill-conditioned / near-rank-deficient A the Golub-Kahan recurrence identity drifts in finite precision and |beta_{k+1} z_k| can underestimate the true ‖b-Ax‖, so the caller gets Solved==true / implicit-bool true while the struct's own exact rnorm exceeds tol*‖b‖ — exactly the silent false-Converged pattern the square-solver family guards with VerifyTrueResidual (see SolveInfo.cs comment: 'cg verify a claimed Converged exit with one fresh r = b-Ax first'; CG.fProxy.cs:203, MINRES.fProxy.cs:212). Unlike lsqr/lsmr (tracked-only, where a verify would cost two extra matvecs), CRAIG has already paid for the exact residual, so the fix is free: pass tol and bnorm into CraigInfo and downgrade a claimed-Converged status when info.rnorm > tol*bnorm (matching gmres/fgmres verify-at-exit gate work already queued as task #60).
**verify:** CONFIRMED against the actual code. Line 104 gates Converged on the recurrence estimate rnorm=|beta*z| (line 102), where z is a pure multiplicative recurrence (line 94) decoupled from the computed x — the classic tracked-vs-true residual gap; since CRAIG is CG-on-AAT (kappa^2), on ill-conditioned full-row-rank A the estimate keeps shrinking below tol*bnorm while the true ||b-Ax|| stagnates at ~eps*kappa(A)*||b||; with default tol=sqrtEps this needs only kappa >~ 3e3 in float. CraigInfo (lines 123-131) then computes the certified-exact residual via lstsqResidual and stores the TRUE rnorm in the returned struct but sets status unconditionally — tol/bnorm are never passed in — so the caller gets Solved==true while the struct's own exact rnorm > tol*bnorm, violating the XML contract (line 24: 'Converges when ||b-Ax|| <= tol*||b||', with rnorm documented certified-exact). This is the exact false-Converged class the library guards elsewhere (VerifyTrueResidual in CG:203, MINRES:197/212, BiCGStab, FCG, IDR, MINRESQLP). Decisive: the CRAIG DEVLOG entry (2026-07-20) explicitly asserts the safety contract 'never a false Converged' — the line-104 drift path falsifies the author's own invariant, so this is an unhandled gap, not an accepted tracked-only contract (that applies to lsqr/lsmr, whose LstsqInfo norms are documented as running values; a verify there would cost extra matvecs, whereas CRAIG has already paid for the exact residual, making the fix free). Scope: x is not corrupted; the wrong result is the deterministic false Solved==true / implicit-bool true that callers branch on. Real, medium severity.

### [MED] Krylov.CRAIGMR - Iterate-0 ArNorm is alpha; should be alpha*beta (off by a factor of ‖b‖)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIGMR.fProxy.cs:94
Line 94 sets ArNorm = alpha with the line-88/89 comment claiming ‖Aᵀ(b-Ax)‖ = alpha at x=0. The true identity is ‖Aᵀr₀‖ = ‖Aᵀb‖ = ‖β₁·Aᵀu₁‖ = α₁β₁ (v₁ is unit). Siblings agree: Krylov.LSQR.fProxy.cs:99 sets arnorm = alpha*beta and Krylov.LSMR.fProxy.cs:104 returns alpha*beta for the iterate-0 value; the file's own per-iteration recurrence (line 144, ArNorm = αnew·βnew·|ζ|/ρ ≡ αnew·|c|·|ζ̄new|, the standard LSQR ‖Aᵀr‖ identity) also extrapolates to α·β at k=0, not α. Failure scenario: any exit during the FIRST loop pass — Converged at k+1=1 (line 128; common on easy systems that converge in one iteration), or the k=0 Breakdown paths at lines 109/135 — returns LstsqInfo.Arnorm wrong by a factor of bnorm; a caller gating on Arnorm-based stationarity (e.g. comparing against tol·‖A‖·‖r‖ style bounds, or auditing against lstsqResidual) sees a value ‖b‖× too small (‖b‖<1: too large). x and rnorm are unaffected. Fix: line 94 → ArNorm = alpha * beta, and correct the line-89 comment.
**verify:** Confirmed real. At x=0, r0=b=beta*u1, so ||A^T r0|| = ||beta*A^T u1|| = ||beta*alpha*v1|| = alpha*beta (v1 unit, normalized at line 84) — line 94's ArNorm = alpha and the line 88-89 comment drop the beta factor. Siblings agree: Krylov.LSQR.fProxy.cs:96-99 sets arnorm = alpha*beta ("tracks ||A^T r|| = alpha*beta pre-loop") and Krylov.LSMR.fProxy.cs:102-104 returns alpha*beta at iterate 0; CRAIGMR's own per-iteration recurrence (line 144, alphaNew*betaNew*|zeta|/rho) also carries both factors. The wrong value escapes: the Converged check (line 127-128) runs BEFORE the ArNorm recurrence update (line 144), so a first-pass Converged exit returns the stale line-94 value. Concrete input: A=[[1]], b=[2] → converges at k+1=1 with Arnorm=1 instead of alpha*beta=2 (off by ||b||). The k=0 line-109 Breakdown path also returns it (though only reachable via NaN since rhobar=alpha>0 is guarded). x, rnorm, and status are unaffected — the bug is confined to the documented LstsqInfo.Arnorm diagnostic field, so severity is medium, but it is a genuinely wrong returned value on a common path. Fix: line 94 → ArNorm = alpha * beta, and fix the comment.

### [MED] Krylov.LSQR - Damped solves can return a NEGATIVE LstsqInfo.Arnorm (abs placed on c only, not on phibar)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSQR.fProxy.cs:154
Line 154: arnorm = phibar * alpha * math.abs(c). With damp != 0, rhobar1 = sqrt(rhobar^2+damp^2) is always > 0, so c = rhobar1/rho > 0 and line 146 makes rhobar = -c*alpha < 0 after iteration 1; the next iteration's damping fold cs1 = rhobar/rhobar1 (line 132) is then NEGATIVE and flips phibar's sign — phibar alternates sign every damped iteration thereafter. Undamped, phibar stays >= 0 (sn >= 0) and abs(c) suffices, but damped, arnorm goes negative on alternating iterations. Concrete failure: lsqr with damp > 0 terminating at iterations == 2 (Converged at line 157, or Breakdown/MaxIterations at 141/161/164) returns LstsqInfo.Arnorm < 0 — a negative norm; any caller test of the form info.Arnorm <= bound passes spuriously. The internal convergence test (arnorm*arnorm <= threshold, line 156) squares it and is unaffected, as are x and rnorm. Reference LSQR (Paige-Saunders/SciPy) uses arnorm = alfa*abs(tau) with abs over the WHOLE product. Fix: arnorm = math.abs(phibar * alpha * c). Sibling check: Krylov.LSMR.fProxy.cs uses the same tracked-norm family and should be audited for the same abs placement.
**verify:** Verified against the actual code. With damp>0: line 146 sets rhobar = -c*alpha < 0 after iteration 1 (c>0 because rhobar1=sqrt(rhobar^2+damp^2)>0, alpha>0). Next iteration's damping fold (line 132) multiplies phibar by rhobar/rhobar1 < 0, flipping phibar negative; line 148 (sn>=0) preserves the sign. Line 154 'arnorm = phibar * alpha * math.abs(c)' puts abs only on c, so arnorm < 0 on alternating damped iterations. LstsqInfoTracked (Krylov.Lstsq.Common.fProxy.cs:58) copies Arnorm verbatim — no abs/sanitization — so any damped solve returning at an even iteration (Converged line 157, Breakdown line 161, MaxIterations line 164) hands the caller LstsqInfo.Arnorm < 0, violating the documented norm contract (SolveInfo.cs:25-28) and making 'Arnorm <= bound' caller checks pass spuriously. Concrete repro: lsqr(A, b, ref x, maxIter=2, tol tiny, damp>0) → MaxIterations with negative Arnorm. Reference LSQR (SciPy: arnorm = alfa*abs(tau), tau = sn*phi) takes abs over the whole product, confirming the fix math.abs(phibar*alpha*c). Undamped path is unaffected (cs1 branch skipped, phibar stays >= 0), and x/rnorm/convergence decisions are unaffected (the internal test squares arnorm) — bug is a wrong reported diagnostic, damped-only, exactly as claimed.

### [MED] Krylov.Block.CG - In-loop Converged is recurrence-residual only — no true-residual verify at exit
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CG.fProxy.cs:102
At Krylov.Block.CG.fProxy.cs:102-103 the Converged status (and the returned maxRnorm) come from the recurrence residual R updated by 'R -= alpha^T Q' (line 100), never re-checked against a fresh B - A*X. On an ill-conditioned SPD A with tight tol, the recurrence R drifts from the true residual, so bcg can return status=Converged while the actual ||B[j] - (A X)[j]|| exceeds tol*||B[j]|| — the classic silent false-Converged. Every scalar sibling gates this (VerifyTrueResidual at Krylov.CG.fProxy.cs:203, Krylov.MINRES.fProxy.cs:212, Krylov.BiCGStab.fProxy.cs:186, Krylov.IDR.fProxy.cs:227 via Krylov.Solve.Common.fProxy.cs:20); no block solver has a block equivalent yet (matches open task #60 'verify-at-exit gate'). Fix = on the Converged path, one extra A.ApplyBlock(X -> T) + BlockMaxResidualRecompute-style check (helper already exists at Krylov.Block.Common.fProxy.cs:83), downgrading to MaxIterations/continuing if it fails. Note: this solver is slated for removal per the user ruling (keep bfbcg + bcgrq), so the fix may be better spent on the survivors — but they should be checked for the same gap.
**verify:** Confirmed by reading Krylov.Block.CG.fProxy.cs: R is recomputed as B - A*X only at init (lines 68-71); the in-loop Converged exit (lines 102-103) and returned maxRnorm come solely from the recurrence-updated R (line 100, R -= alpha^T Q), with no fresh A.ApplyBlock(X) check on the cleanup path. The library's own contract (Krylov.Solve.Common.fProxy.cs VerifyTrueResidual, used by CG:203, FCG:108, MINRES:197/212, MINRESQLP:374, BiCGStab:186, IDR:174/227) is that Converged must survive a fresh true-residual recompute; bcg lacks the block equivalent even though BlockMaxResidualRecompute (Krylov.Block.Common.fProxy.cs:83) already exists. Concrete failure: with default tol=sqrtEps, any SPD A with kappa >~ 1/sqrtEps (>~1e8 double, ~1e4 float) puts tol*||B|| below the attainable-accuracy floor O(kappa*eps*||A||*||X||), so the recurrence residual passes CountConverged while the true residual ||B[j]-(A X)[j]|| stagnates above tol*||B[j]|| — silent false-Converged plus under-reported maxRnorm. Aggravators specific to bcg: BlockSolveSPD silently ridges P^T A P (inexact alpha widens recurrence/true drift). Deterministic wrong result, not a crash. The pending removal of bcg (keep bfbcg/bcgrq ruling) affects where to spend the fix, not whether the bug is real.

### [MED] Krylov.Block.MINRES - tol == 0 with nonzero B silently reports Converged and overwrites X with B
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:292
Lines 284-299: thr[j] = tol*tol*||B[j]||^2, then the allZero early-exit fires when EVERY thr[j] == 0. That conflates two cases: B entirely zero (intended: X=B=0 is the exact solution) and tol == 0 with any B (thr[j] = 0*bb = 0 for all j). In the second case the solver copies B into X (garbage), returns status=Converged, iters=0 — a silent false-Converged that also destroys a warm-started X. The cleanup tail does recompute the true residual, so BlockSolveInfo.converged will honestly read 0, but status says Converged and X is clobbered. No other block solver has this shortcut (bcg/bfbcg/bbiCGStab/bgmres all just form R and CountConverged), so this is bminres-specific. Fix: gate the shortcut on the per-row bb (B actually zero), e.g. track 'all bb == 0' in the thr loop, not 'all thr == 0'; optionally also validate tol > 0 alongside maxIter at line 232.
**verify:** Verified in the actual code. Line 288 sets thr[j] = tol*tol*bb, so tol == 0 makes every thr[j] exactly 0 regardless of B; the allZero test at line 292 checks thr[j] != 0, so with tol == 0 and any nonzero B the shortcut at 293-298 fires: CopyBlock(B -> X) clobbers X (including warm starts) with B, status = Converged, iters = 0. tol is never validated (only maxIter at line 232). The cleanup tail (434-438) recomputes the true residual and sets the honest converged count (which will read 0), but never demotes status — so the returned BlockSolveInfo says status = Converged while X = B is garbage, violating the file's own XML-doc contract (lines 205-207) that Converged is always confirmed by a fresh B - A·X check. Concrete failure: A = 2I, B = [[1,1]], tol = 0, maxIter = 10 -> returns Converged, iters = 0, X = [[1,1]] instead of [[0.5,0.5]]. tol = 0 is a conventional "run to maxIter" input, and without the shortcut the loop would run honestly to MaxIterations (probe sumsq > 0 never passes). The proposed fix (gate the shortcut on per-row bb == 0, i.e. B actually zero, optionally validate tol > 0) is correct. Real bug, bminres-specific.

### [MED] Krylov.Block.MINRES - BuildOmega's Gamma/Rz singularity checks use an ABSOLUTE threshold — scale-dependent breakdown detection
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:140
Lines 139-140 and 167-168: |Gamma[i,i]| <= Consts.fProxyZeroThreshold (absolute: 1e-6f float, 1e-14 double). Gamma is the R-factor of [Gbar; Beta^T], whose magnitude scales with ||A||. Failure both ways: (a) a well-conditioned system with small units (float, ||A|| ~ 1e-5) makes every Gamma diagonal < 1e-6 → spurious Breakdown on iteration 0; (b) a large-scaled system (||A|| ~ 1e6) lets a genuinely rank-deficient Gamma (relative magnitude 1e-9, absolute 1e-3) pass — resurrecting exactly the silent wrong-MaxIterations mode the 2026-07-20 DEVLOG fix added this check to catch. The Rz check (line 168) is fine as absolute (Rz is the R-factor of a projected unit seed, O(1) by construction), but Gamma's should be RELATIVE to its own max |diagonal|, mirroring TriNearSingular in Krylov.Block.Common.fProxy.cs:331 and LQRPRank's relative-tol convention.
**verify:** VERIFIED in code: BuildOmega (Krylov.Block.MINRES.fProxy.cs:139-140) gates Gamma's diagonal on the ABSOLUTE Consts.fProxyZeroThreshold (1e-6 float / 1e-14 double), and Gamma provably scales with ||A||: Vcur is orthonormal, so Alfa/Beta ~ ||A||, and Gbar/Dbar come from orthogonal (norm-preserving) OmegaOld rotations, so the R-factor of [Gbar;Beta^T] is ~||A||. Every sibling check is relative (LQRPRank and TriNearSingular in Krylov.Block.Common scale by the diagonal max; Bidiag scales the same constant by LInf(A)), and the solver's convergence test is relative (tol²·||B||²) — BuildOmega's absolute gate is the lone scale-dependent piece, breaking MINRES's scale invariance. Concrete failure (a): float, A = 1e-7·diag(1..n) — well-conditioned, κ=n — every Gamma diagonal ~1e-7 ≤ 1e-6, gammaOk=false skips both completion seeds, BuildOmega returns false, and bminres returns Breakdown at iters=0 with X untouched; scaling A,B by 100 flips the same system to Converged. (The claim's ||A||~1e-5 example is an order too generous — that passes — but ||A|| ≲ 1e-6 float trips it, so the mechanism holds.) Failure (b): the 2026-07-20 DEVLOG entry confirms this check exists to catch a silent wrong-MaxIterations mode (Phibar decays geometrically without closing the true residual); at ||A||~1e6 a relatively rank-deficient Gamma (abs diag 1e-3) passes the gate and resurrects that mode (downstream pivoted LU won't reject a 1e-3 pivot). Mitigations: Converged is always verified by a fresh true-residual check and cleanup reports honest maxRnorm, so neither mode yields a wrong X labeled Converged — and DEVLOG defers 'the Gamma near-singularity case' to task #50, so robustness incompleteness is known. But scale-dependence is documented nowhere as a contract, deviates from the library's own relative-tolerance convention, and failure (a) is a hard functional defect on valid input today (spurious Breakdown at iteration 0 for a well-conditioned solvable system, purely unit-dependent). The claim's Rz carve-out is also correct (Z1=(I−QyQyᵀ)Z0 from a unit seed is O(1)). Real bug, medium severity: wrong solver outcome (refusal) on concrete well-scaled-but-small-unit inputs, plus defeat of the breakdown detector it was added to be.

### [MED] Krylov.Block.BiCGStab - No verify-at-exit: Converged status, converged count, and maxRnorm are reported purely from the recurrence residual, never re-checked against a fresh B - A*X
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BiCGStab.fProxy.cs:170
BiCGStab's recurrence residual (R -= V*alpha at line 167, R := S - omega*T at line 195) is the classic drift-prone one — which is exactly why the library's own scalar twin gates Converged behind VerifyTrueResidual (Krylov.BiCGStab.fProxy.cs:186), bminres does a MANDATORY fresh ApplyBlock verify before trusting its cheap probe (Krylov.Block.MINRES.fProxy.cs:413-427) plus an unconditional cleanup recompute (:434-438), and bcgrq recomputes the residual fresh at cleanup as 'an exit-time sanity check' (Krylov.Block.BCGrQ.fProxy.cs:168-178). bbiCGStab has none of these: the Converged exits at lines 170 and 198, and the final BlockSolveInfo at line 221 (converged/maxRnorm), all trust the recurrence R. Failure scenario: ill-conditioned nonsymmetric A with tight tol and many iterations — accumulated round-off makes the recurrence R pass tol^2*||B[j]||^2 while the true residual B - A*X does not; caller receives a silent false Converged (or an over-counted `converged` / understated maxRnorm at MaxIterations/Breakdown exits). Fix is cheap and buffer-neutral: at the cleanup label, ApplyBlock(X -> T) (T is idle at every exit point), form B - T, and CountConverged into converged/maxr, mirroring bcgrq's cleanup block; optionally also gate the two in-loop Converged exits like bminres does. Note pending task #60 (gmres/fgmres verify-at-exit gate) shows this is a known family gap, not a deliberate block-family policy.
**verify:** Verified against the actual code. bbiCGStab reports Converged (lines 170, 198), converged count, and maxRnorm (line 221) purely from CountConverged on the recurrence residual R (updated via R -= V*alpha at 167 and R := S - omega*T at 195); the only fresh B - A*X is the initial residual (line 133), and the cleanup label (214-221) does no recompute. Every cited sibling was confirmed: scalar biCGStab gates Converged behind VerifyTrueResidual (Krylov.BiCGStab.fProxy.cs:186, commented 'Verify-at-exit'), bminres does a mandatory fresh ApplyBlock verify before Converged (Krylov.Block.MINRES.fProxy.cs:413-428) plus an unconditional cleanup recompute (434-438), and bcgrq recomputes fresh at cleanup as 'an exit-time sanity check' (Krylov.Block.BCGrQ.fProxy.cs:168-178); VerifyTrueResidual is also used by CG/FCG/MINRES/MINRESQLP/IDR — the family contract is true-residual-backed Converged, and bbiCGStab is the sole outlier. BiCGStab's recurrence residual is the classic drift case (the stated reason the scalar twin verifies), so on ill-conditioned nonsymmetric A with tight tol the recurrence R can pass tol^2*||B[j]||^2 while B - A*X does not, producing a silent false Converged (or over-counted converged / understated maxRnorm at MaxIterations/Breakdown exits) that nothing downstream corrects. The proposed fix is feasible and buffer-neutral: T is dead at every path into cleanup. This matches the project's own listed trap 'silent false-Converged'. Severity is medium: correct on well-conditioned systems, wrong status only in the drift regime — but that is exactly the regime the claim scopes.

### [MED] Krylov.Block.IDR - No verify-at-exit: Converged declared purely off the recurrence residual (silent false-Converged)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.IDR.fProxy.cs:226
Scalar idr — which this solver's doc says it 'exactly mirrors' — guards BOTH convergence exits with VerifyTrueResidual (Krylov.IDR.fProxy.cs:174 and :227, helper in Krylov.Solve.Common.fProxy.cs:20): on rr<=threshold it recomputes r=b-Ax fresh and only reports Converged if the TRUE residual passes, else continues with the corrected R. bidr has no block analog: at lines 141, 226, and 272 status=Converged comes straight from CountConverged over the recurrence residual R, which is updated incrementally (R -= beta^T*Gk at :221, R -= om*Q at :266) across many sweeps and can drift from B-A*X on ill-conditioned nonsymmetric systems — IDR is exactly the family where this drift is worst. Failure scenario: after enough sweeps the recurrence R dips under thr while ||B-A*X|| does not -> caller gets status=Converged and a too-small maxRnorm, X silently unconverged. The V and Q workspaces are idle at all three exit sites (same shape as scalar's reuse of V/Q), so a block verify (A.ApplyBlock(X)->Q; Q=B-Q; recount vs thr) costs one extra ApplyBlock at exit only. Note: sibling bbiCGStab shares this gap (family pattern; verify-at-exit gates are tracked separately for gmres/fgmres), but bidr uniquely deviates from a scalar counterpart that already has the guard, and maxRnorm at :286 is likewise the unverified recurrence value.
**verify:** CONFIRMED. bidr (Krylov.Block.IDR.fProxy.cs) declares status=Converged at lines 226 and 272 straight from CountConverged over the recurrence residual R, which is updated incrementally (R -= beta^T*Gk at :221, R -= om*Q at :266) with no fresh B-A*X recompute anywhere after initialization (:133); maxRnorm at :286 is the same unverified value. Scalar idr — which bidr's doc says it "exactly mirrors" — guards BOTH corresponding exits with VerifyTrueResidual (Krylov.IDR.fProxy.cs:174 and :227), recomputing r=b-Ax fresh and continuing with the corrected R on a failed verify, so the drop of the guard is a deviation from the direct scalar counterpart, not a family convention. The block-solver family standard confirms verify-at-exit is the contract: bminres does a MANDATORY fresh verify (:413), bcgrq/bfbcg recompute the residual fresh from the final X, bcraig/bcraigmr check fresh ||B-AX|| every round ("never a false Converged"), bgmres/bfgmres/gcrodr recompute fresh at restarts (GCRODR even has a DEVLOG-documented fix for reporting a stale estimate). The only exception, btfqmr, documents a rigorous quasi-residual UPPER bound guaranteeing the true residual — IDR's recurrence residual has no such bound, and IDR is the family with the worst known updated-vs-true residual drift (which is why the scalar guard exists). The freshly-written DEVLOG section for Krylov.Block.IDR documents every other deviation (storage, deflation, seed) but never mentions skipping verify — omission, not intended contract; nothing downstream re-verifies. Concrete failure: ill-conditioned nonsymmetric A, tight tol, many sweeps → recurrence R dips under thr while true ||B-A*X|| does not → caller gets Converged + too-small maxRnorm with X silently unconverged. Fix is cheap as claimed: Q (and V) are idle at both exit sites, so one ApplyBlock(X)->Q, Q=B-Q, recount vs thr at exit mirrors the scalar pattern. One imprecision in the claim: the third cited site (:141) checks a freshly computed residual (R = B-A*X from :133) and is actually fine — this does not weaken the two real sites.

### [MED] bgmres - Unguarded triangular LS solve: singular/near-singular Rls poisons X with NaN/Inf, no Breakdown detection, NaN masked by comparison polarity
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:531
BlockLSResolveAndCheck (Krylov.Block.Common.fProxy.cs:517-547) calls QR.decompSolve, whose Blas.triUpper divides by Rls's diagonal unguarded (QR.fProxy.cs:703-717). If the accumulated block-Hessenberg prefix is (near-)singular — reachable for near-singular A or at a happy-breakdown/deflation edge — Y gets Inf/NaN. The Pythagorean check does NOT see this (resid2 is computed from G/QtG, not Y), so cycleConverged can be TRUE in the very same step (exact-convergence + rank-collapsed H is precisely the happy-breakdown edge), after which the commit (GMRES.fProxy.cs:178-200) adds NaN·V into X, destroying even the warm-start/previous-cycle progress, and the solver returns status=Converged with NaN X. On the non-converged path the poison is only caught one cycle later as an LQRP rank-0 'Breakdown', and NaN masking makes the diagnostics lie: CountConverged's 'rn > worst' (Common:128) and the check's 'resid2 > thr[c]' (Common:544) are both false for NaN, so maxRnorm reports 0. bgcrodr deliberately keeps 'a NaN/Inf breakdown scan between the dense-QR solve and the Pythagorean check' (Common:463-465 comment); bgmres/bfgmres have no equivalent. Fix: TriNearSingular (Common:331-344) on Rls's diagonal, or a NaN/Inf scan of Yv, before committing — return Breakdown without touching X.
**verify:** CONFIRMED real. Every load-bearing element of the claim checks out in the actual code. (1) Unguarded divide: BlockLSResolveAndCheck (Krylov.Block.Common.fProxy.cs:531) calls QR.decompSolve (QR.fProxy.cs:703-717), whose doc literally says "Always reports Success — assumes a valid (non-singular) R"; it forwards to the multi-RHS Blas.triUpper (Blas.Triangular.fProxy.cs:164: `fProxy inv = (fProxy)1 / U[r, r];`) — no pivot guard, no rank check. Exact-zero diagonal gives Inf, then 0*Inf=NaN in the axpy updates. (2) Reachability: bgmres's contract is "general (nonsymmetric) square A" with no nonsingularity requirement; on happy breakdown (wj1==0) the accumulated H prefix is SQUARE (totalRows=off[k+1]=off[k]+0=totalCols), and for a singular A the square H is singular — e.g. A=diag(0,1), B=(1,1), restart>=2 closes the Krylov space at step 2 with a singular 2x2 H. bgcrodr's own guard comment (GCRODR:334-336) states this exact scenario: "A rank-deficient least-squares system (e.g. A maps this whole cycle's basis to 0, as a singular/zero operator can) can leave Yv non-finite". (3) Masked false Converged: the Pythagorean check (Common:537-544) uses only Gactive/QtG, never Yv — and in the square-H happy-breakdown case Q is square-orthogonal so gg-qq≈0 and cycleConverged is true UNCONDITIONALLY, in the same step Yv is poisoned. The commit (GMRES.fProxy.cs:178-200) then adds NaN*V into X (destroying warm-start progress) and breaks with status=Converged. (4) Lying diagnostics: the final fresh-residual pass (GMRES:209-212) computes rr=NaN from NaN X; CountConverged's `rr <= thr[j]` and `rn > worst` (Common:127-129) are both false for NaN, so the returned BlockSolveInfo is status=Converged, converged=0, maxRnorm=0, X=all-NaN — a silent false-Converged, exactly the trap class flagged in the brief. (5) Guarded-elsewhere check fails in the bug's favor: sibling bgcrodr has the exact NaN/Inf scan of Yv between decompSolve and the Pythagorean check (GCRODR.fProxy.cs:337-340, breaking to status=Breakdown WITHOUT touching X) and documents "never NaN, never a false Converged" (GCRODR:105-106); Common:462-465 explicitly notes that scan as a bgcrodr-only difference — bgmres (and bfgmres, which shares BlockLSResolveAndCheck) have no equivalent. bgcrodr also guards its other triangular solve via BackSubUpperBlock's pivotGuard (GCRODR:58-66). The proposed fix (NaN/Inf scan of Yv or TriNearSingular on Rls's diagonal before commit, return Breakdown without touching X) matches the sibling's shipped pattern. Only nits: the cited CountConverged line is 129 not 128, and on the NON-converged path the poison also propagates X→NaN at commit (caught next cycle as rank-0 Breakdown, but X is already destroyed) — neither affects the verdict.

### [MED] bgmres - Exit path never downgrades status: can return status=Converged while its own fresh true-residual check reports converged < s
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GMRES.fProxy.cs:209
cycleConverged comes from the Pythagorean identity over the PROJECTED residual: gg = ||G0 column||² (Common:539-540) measures only V0's projection of r0. When the initial LQRP deflates (w[0] < s, GMRES.fProxy.cs:132), the un-projected component ||(I−V0ᵀV0)r0_c|| is silently dropped — for a small-norm RHS column whose per-column threshold tol²·||B_c||² is below the LQRP rank tolerance (which scales with the LARGEST row, |L[0,0]|), the check underestimates and can fire falsely; MGS orthogonality loss adds the same-direction error. The function already pays for the defense — a fresh R0 = B − A·X recompute at exit (GMRES.fProxy.cs:209-212) — but only updates `converged`/`maxRnorm`; `status` keeps the value set at line 202, so the struct can report status=Converged with converged < s and nothing forcing the caller to cross-check. This is exactly the 'Converged status that doesn't survive a fresh true-residual check' pattern, and matches the still-open gmres/fgmres verify-at-exit gate (task #60) — bgmres and bfgmres (identical code shape, FGMRES:208-218) share the gap. Fix is two lines: after line 212, if status == Converged && converged < s, downgrade (MaxIterations or a dedicated status).
**verify:** CONFIRMED. In bgmres (Krylov.Block.GMRES.fProxy.cs), status=Converged can be set at line 202 from cycleConverged, which BlockLSResolveAndCheck (Common:517-547) computes via the Pythagorean identity over the PROJECTED residual: G is seeded as G0=V0·R0^T (line 139) with only w[0] rows, so when the initial LQRP deflates (w[0]<s, line 132) the un-projected component of R0 is dropped from the check. The deflation cut (LQRPRank, Common:208-219) scales with the LARGEST residual row (max(s,n)·zeroThreshold·|L[0,0]|) while the pass threshold scales per-column with tol²·‖B_c‖² — so a small-norm RHS column mixed with a large-residual one can have its entire residual direction deflated below the rank tolerance yet far above its own convergence threshold (float, n≈1000: cut ~1e-4 vs required ~1e-8), making cycleConverged fire falsely; MGS orthogonality drift adds same-direction error. The exit path (lines 209-212) recomputes the fresh true residual and refreshes `converged`/`maxRnorm` but never reconciles `status`, so the returned BlockSolveInfo can carry status=Converged with converged<rhs. That violates the struct's own documented contract (BlockSolveInfo.cs:34-35: "Converged iff ALL rhs columns converged") and makes Solved/implicit-bool report success on a non-converged solve — a self-contradictory, observable wrong result. Not guarded elsewhere: scalar CG/MINRES/BiCGStab/IDR gate Converged on VerifyTrueResidual, and open task #60 (gmres/fgmres verify-at-exit gate) confirms this gap class is a recognized bug, not an intended contract. bfgmres (FGMRES.fProxy.cs:208-218) shares the identical shape. Severity med: X, converged, and maxRnorm are honest; only status/Solved lie. Two-line downgrade fix after the exit recompute is correct.

### [MED] bfgmres - Converged status not gated on the final true-residual recompute (silent false-Converged)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:208
status=Converged is decided solely by BlockLSResolveAndCheck's Pythagorean LS-residual identity over Hbuf/Gbuf (lines 171-176, committed at 208), which is exact only insofar as MGS2 orthogonality of V and the LQRP deflation threshold hold. Lines 213-218 then DO recompute the fresh true residual from the final X and store it into `converged`/`maxRnorm` — but `status` is never reconciled. Failure scenario: ill-conditioned nonsymmetric A with long restart cycles (MGS2 orthogonality loss) or deflation dropping directions right at the LQRPRank threshold -> the LS identity reports every column under thr while the true residual of some column j exceeds thr[j]; the returned BlockSolveInfo says status=Converged with converged < s, and a caller branching on status alone accepts a bad X. The fix pattern already exists: the final CountConverged result is in hand, so demote status (Converged -> MaxIterations/NotConverged) when converged < s — the same verify-at-exit gate tracked as pending task #60 for gmres/fgmres; bgmres (Krylov.Block.GMRES.fProxy.cs:202/209-212) shares the identical gap, so gate both siblings together to preserve their documented identity-fold bit-identity.
**verify:** CONFIRMED in Krylov.Block.FGMRES.fProxy.cs: status=Converged is set at line 208 purely from BlockLSResolveAndCheck's Pythagorean LS-residual identity (line 171), which drifts from the true residual under MGS2 orthogonality loss or LQRP deflation at the rank threshold. Lines 215-218 DO recompute the fresh true residual at the final X and overwrite `converged`/`maxRnorm`, but `status` is never reconciled — so on an ill-conditioned nonsymmetric A (especially the float instantiation, long restart cycles, or deflation right at the LQRPRank cut) the function can return a self-contradictory BlockSolveInfo: status=Converged with converged<s and maxRnorm above tolerance. This violates BlockSolveInfo's own documented contract (BlockSolveInfo.cs:34-36: status is Converged "iff ALL rhs columns converged"), and both `Solved` and the implicit bool conversion report success on a failed solve — a silent false-Converged, not an intended contract. Not guarded elsewhere: the top-of-cycle true-residual check (lines 113-118) only covers restart-boundary exits; the common mid-cycle exit (line 208) is unverified, and no VerifyTrueResidual call exists in either block-GMRES file. It also contradicts the library's established verify-at-exit convention (MINRES:197-200 explicitly falls through and keeps iterating on a failed verify; CG:203, BiCGStab:186, FCG:108, IDR:174/227, MINRESQLP:374 all gate Converged on a fresh true residual). Sibling gap confirmed: Krylov.Block.GMRES.fProxy.cs has the identical structure (status=Converged at 202, unreconciled fresh CountConverged at 212). Fix is the one-liner the claim suggests: demote status when the already-computed final converged < s, applied to both siblings to preserve the identity-fold bit-identity.

### [MED] bgcrodr — Krylov.Block.GCRODR - Full-width GEMM at line 397 reads NEVER-WRITTEN uninitialized Yscratch rows — the in-code '0*stale = 0' justification fails for 0*NaN/Inf
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:397
Yscratch is allocated uninit (line 154, flag true, mirroring bgmres) but unlike bgmres — which only ever reads rows < off[k] — bgcrodr's commit correction does `Blas.dot(in Bview, in Yscratch, ...)` over Yscratch's FULL m*s rows (line 397). The comment at 388-394 argues stale rows beyond totalColsFinal contribute 0*stale = 0 because Bmat's columns there are zero; that holds for finite stale values from an earlier wider cycle, but rows >= max(off[k]) over ALL cycles so far are never written at all and hold uninitialized memory — 0*NaN = NaN, 0*Inf = NaN. Scenario: recycling solve (kcur>0 from cycle 2 on) where every cycle so far ran narrower than m*s (rank-deflated widths w[j] < s — common for correlated RHS — or an early wj1==0 / mid-cycle-convergence break): the correction CtrView2 picks up NaN, BackSubUpper divides it, corr2 goes NaN, X is poisoned at the final commit. Deterministic failure under the 0xNaN debug fill; probabilistic (Temp-reuse-dependent) in release. Fix: allocate Yscratch cleared in this solver (line 154 true->false; one-time m*s*s zero cost), which also makes the 388-394 comment's invariant actually true.
**verify:** Verified against actual code. (1) Yscratch IS uninitialized: fProxyMxN ctor maps uninit=true to NativeArrayOptions.UninitializedMemory (fProxyMxN.cs:125), and GCRODR line 154 passes true. (2) Only writer is QR.decompSolve through RowsView(Yscratch, totalCols) (line 331-332), a prefix view (Length=rows*s) — rows beyond the max totalCols over all cycles are NEVER written. (3) Blas.dot(Bview, Yscratch, CtrView2, false, false) at line 397 is a plain GEMM contracting the full m*s inner dimension (OP.Dot.fProxy.cs:208→151) — it reads every Yscratch row. (4) The comment's '0*stale=0' defense only holds for finite stale values; never-written Temp memory can hold NaN/Inf bit patterns, and 0*NaN=NaN poisons CtrView2 → BackSubUpperBlock (NaN Rhs propagates through sum/diag; pivot guard doesn't catch it) → corr2 → X at line 403. (5) Reachable: with correlated RHS rows the deflating-LQRP rank keeps w[j]<s every cycle, so totalCols<m*s permanently and tail rows stay unwritten; from cycle 2 on (kcur>0) the full-width read executes. The NaN check at 337-340 only inspects rows<totalCols, and in the cycleConverged path the solver returns status=Converged with NaN-contaminated X — violating its own documented 'never NaN, never a false Converged' contract (lines 105-106). Contrast confirmed: bgmres/bfgmres only ever read Yscratch[0..off[k]) (Krylov.Block.Common.fProxy.cs:515), so the mirrored uninit allocation is safe there but not in bgcrodr. One embellishment: there is no in-tree 0xNaN debug fill (grep found none), so failure is probabilistic on Temp-allocator garbage rather than deterministic — but it remains a real uninitialized-memory read flowing into the returned solution (latent nondeterminism / NaN result / false Converged). Fix (line 154 true→false, clearing the m*s x s buffer once) is correct.

### [MED] bgcrodr — Krylov.Block.GCRODR - False-Converged window: the Pythagorean cycle check assumes exact C-component cancellation that the pivotGuard-zeroed Ru back-sub silently breaks; status never re-verified against the fresh final residual
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:352
The LS residual identity (lines 345-354) equals the true residual only if the commit's recycle correction exactly cancels the C-component C*(Bmat*Y) (lines 388-403). BackSubUpperBlock writes 0 for any row with |Ru[i,i]| <= pivotGuard (line 71) — then the C*(Bmat*Y) term is NOT cancelled, the true residual exceeds the LS estimate, yet cycleConverged still sets status=Converged (line 407). This is made reachable by a scale mismatch: pivotGuard = eps*100*(||B||_F+1) (line 190) scales with the RHS, while Ru's diagonals scale with ||A*U|| (~||A||, U near-unit). Concrete: float, ||B||_F ~ 1e6, ||A|| ~ 1 => pivotGuard ~ 10 >> every Ru diagonal => the correction is skipped every cycle while Bmat != 0, and a mid-cycle Pythagorean pass declares Converged with a true residual that fails thr. The tail (lines 575-578) already computes the fresh true residual and honest `converged` count but never downgrades `status`, so the returned struct says Converged with converged < s — directly contradicting the line-106 contract 'never a false Converged'. Cheap fix: after line 578, if status==Converged && converged < s, downgrade (mirrors the verify-at-exit gate pattern in Krylov.Solve.Common); separately consider scaling pivotGuard for the Ru solves to Ru's own max diagonal rather than ||B||.
**verify:** CONFIRMED false-Converged window in Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs. The Pythagorean cycle check (lines 345-354) equals the true residual only if the commit's recycle correction exactly cancels the C-component: A·V_j = C·Bij + V·H by construction (lines 274-299), and the commit (lines 388-403) subtracts U·Zproj2 where Ru·Zproj2 must equal Bmat·Y for the C·(Bmat·Y) term to cancel. BackSubUpperBlock zeroes any row with |Ru[i,i]| <= pivotGuard (line 71), breaking that cancellation; Bmat never enters the LS check, so the leftover C-component is invisible to cycleConverged, which still sets status=Converged (line 407). Reachability via scale mismatch is genuine: pivotGuard = eps*100*(||B||_F+1) (line 190) scales with the RHS, while Ru's diagonals scale with ||A·U|| ~ ||A|| and are only guaranteed above the recycle-build's own correctly-scaled rGuard (line 544) — so float with ||B||_F ~ 1e6, ||A|| ~ 1 gives pivotGuard ~ 12 > every Ru diagonal, zeroing the correction every cycle on a perfectly well-conditioned Ru. The uncancelled term scales with Y ~ O(||B||) and generically exceeds thr for nonsymmetric A with C^T·A·V != 0, while the V-space LS estimate passes. The tail (lines 575-578) recomputes the fresh true residual and honest converged count but never downgrades status, so the function returns status=Converged with converged < s — contradicting the line-106 contract 'never a false Converged' and deviating from the library's verify-at-exit pattern (VerifyTrueResidual in Krylov.Solve.Common.fProxy.cs:20, used by MINRES line 212, MINRESQLP line 374, CG/BiCGStab/IDR). The cycle-start use of the same guarded back-sub (line 217) is harmless (R0 recomputed fresh at line 223); only the commit path lies. Wrong result: caller receives Converged for RHS columns whose true residual fails tol. Claim's proposed fix (downgrade status after line 578 when converged < s; rescale pivotGuard for Ru solves to Ru's own diagonal scale) is correct.

### [MED] btfqmr - Converged-but-still-iterating rows drive spurious late Breakdown (no row freezing)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:245
Rows with different conditioning: row j converges at iter 10 but the solve continues to iter 200 for slower rows. Row j keeps being advanced (doc: 'every row is advanced every half-step'), its tau/rho/w shrink toward machine zero; in float, rhoNew=RowDot(Rhat0,W) or rho underflows to exact 0, or tau[j] hits exact 0 — then AnyZeroOrNaN on vtrstar (line 212), alpha (line 216), tau (line 245), or rhoLast (line 259) reports Breakdown for the entire solve even though every row was either converged or progressing fine. Note the perverse case: tau[j]==0 means row j is solved EXACTLY, yet line 245 turns that into whole-solve Breakdown unless all rows happen to be converged at line 243. Same fix as above: exclude converged rows from updates and breakdown scans.
**verify:** VERIFIED REAL. Read Krylov.Block.TFQMR.fProxy.cs in full. The code confirms the claim's mechanics: (1) no row freezing — every row is advanced every half-step (doc line 96-97 says so explicitly), so a converged row j keeps having its tau/rho/W updated while slower rows iterate; (2) all four breakdown scans (vtrstar line 212, alpha line 216, tau line 245, rhoLast line 259) run AnyZeroOrNaN over ALL s rows, including converged ones, and any hit aborts the WHOLE solve with Breakdown. The only escape is line 243-244: converged==s must hold at that exact half-step.

Concrete failing input (exact, not just underflow): let A be diagonal with a block of 1.0 entries and a block of widely-spread entries; B row 0 supported only on the unit-eigenvalue coordinates, B row 1 on the ill-conditioned ones. At k=0: alpha[0]=rho/vtrstar=1 exactly, AU row 0 == Rhat0 row 0 exactly (multiplication by 1.0 is exact), so W row 0 = Rhat0 - AU = exactly 0, thetaI=0, tau[0]=0. bound[0]=0 <= thresh[0] so row 0 is CONVERGED (in fact solved exactly — X row 0 receives the exact solution at line 238). Row 1 is not yet converged, so line 244 doesn't fire, and line 245's AnyZeroOrNaN(tau) sees tau[0]==0 and reports Breakdown for the entire solve at iteration 1, leaving X row 1 far from its solution. Result: a benign, well-posed input where s independent scalar tfqmr solves would all converge instead returns status=Breakdown with an unsolved row — a wrong result in the sense the harness defines (spurious failure status; the batched method violates its own equivalence-to-s-scalar-solves purpose).

The gradual-float version also holds: over ~200 extra iterations tau[j] shrinks multiplicatively (tau *= thetaI*c) toward exact 0 in float, and rhoNew[j]=RowDot(Rhat0,W) underflows to exact 0 → beta=0 → rho=0 → next even step alpha[j]=0 → Breakdown at line 216. No guard elsewhere excludes converged rows from these scans.

Not an intended contract: the XML doc (line 97-98) defines the tau breakdown as "tau collapsing BEFORE its bound catches it" — but tau[j]==0 implies bound[j]=0 <= thresh[j], i.e. the bound HAS caught it, so the code fires the breakdown in precisely the case the doc says it shouldn't. The documented "breakdown on ANY row aborts the whole solve" contract is about genuinely broken rows, not exactly-solved ones. The claimed fix direction (exclude converged rows from updates/breakdown scans, i.e. row freezing/locking) is the correct remedy. File: C:\Users\viliv\Documents\LinearAlgebraBursted\Assets\LinearAlgebra\CodeGen\TemplateSource\OP\Krylov.Block.TFQMR.fProxy.cs, lines 205-277 (checks at 212, 216, 243-246, 259).

### [MED] blsmr - Converged has no verify-at-exit gate; clamped subtractive ||A^T R||^2 estimator is noise-dominated at the stopping point
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:234
Convergence (lines 232-237) is decided ONLY by nrmATR2 = max(0, nrmATR2 - ||phi_k||_F^2), a long subtraction chain seeded from ||phi0||^2. At default tol=sqrtEps the threshold is eps*||A^T B||_F^2 -- the same magnitude as the cancellation floor of the chain -- so near the stopping point the estimate is roundoff-dominated, and the math.max clamp at line 234 masks negative drift: on a stagnating/ill-conditioned solve the strictly-positive ||phi_k||^2 subtractions can drive the estimate to 0 and return Converged (converged=s) while the true ||A^T(B-AX)||_F is still above tol*||A^T B||_F. The cleanup already computes Rfinal = A*X (line 261) but only for maxRnorm reporting -- the Converged claim is never re-verified. This is exactly the silent-false-Converged class the codebase has been closing: MINRES deleted its no-verify short-circuit and MINRES-QLP got a raw-scale downgrade guard (OP/DEVLOG.md #53 entries, 2026-07-20), gmres/fgmres are pending (#60), and the sibling ports bcraig/bcraigmr deliberately recompute a fresh residual every round instead of trusting the free block-lifted shortcut (their DEVLOG entries call the block generalization of the scalar identity 'unverified'). Fix is cheap and localized to the Converged exit: one BlockApplyOpT of (B - Rfinal) + BlockFrobDot vs threshold in cleanup, downgrading to MaxIterations on failure. Note the same estimator noise also causes the DEVLOG-documented opposite symptom (float reports MaxIterations though X is actually solved), so a fresh check at the MaxIterations exit could also upgrade honestly-converged floats. File: Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:232-237, 259-263.
**verify:** CONFIRMED against the actual code. Krylov.Block.LSMR.fProxy.cs decides Converged solely from nrmATR2 = max(0, nrmATR2 - ||phi_k||_F^2) (lines 232-237), a clamped subtractive chain seeded at ||A^T B||_F^2, with default threshold eps*||A^T B||_F^2 (tol=sqrtEps) — exactly the chain's cancellation floor, so the estimate is roundoff-dominated at the stopping point; the max(0,·) clamp means any fp over-subtraction on a stagnating/ill-conditioned solve (phi not decaying, breakdown guards only catch numerically-singular factors) pins the estimate to 0 and returns Converged with converged=s while true ||A^T(B-AX)||_F is still above tol*||A^T B||_F. Cleanup computes Rfinal=A*X (line 261) but only for maxRnorm reporting — the Converged status is never re-verified, violating the file's own XML contract "never a false Converged" (line 33). Corroboration from OP/DEVLOG.md: (1) the blsmr entry already documents this estimator being wrong at threshold in float (opposite direction — reports MaxIterations though X is solved — forcing a double-only test assert), empirically establishing the noise; (2) the #53 campaign fixed this exact silent-false-Converged class in MINRES/MINRESQLP/biCGStab/idr with concrete Rosser reproductions, and blsmr escaped that pass; (3) siblings bcraig/bcraigmr deliberately recompute fresh residuals every round because the block-lifted shortcut is "unverified", and the bcgls entry contrasts its exact test with "blsmr's conservative float convergence flag". The only counterargument (exact-arithmetic identity guarantees sum||phi||^2 <= ||phi0||^2 so the clamp never fires) fails in floating point — the DEVLOG documents the identity already failing in float on this file. Concrete failing input class: ill-conditioned tall system in float at default tol. The proposed fix (gate Converged in cleanup on one BlockApplyOpT of B-Rfinal + BlockFrobDot vs threshold, downgrade to MaxIterations) is cheap, localized, and matches the codebase's established #53 pattern.

### [MED] bcraig — Krylov.Block.CRAIG - Missing X/B distinct-buffer guard: aliased X silently destroys B and returns false Converged with X=0
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIG.fProxy.cs:50
bcraig zeroes X (lines 50-51) BEFORE reading B for the tolerance scale bSqF = BlockFrobDot(B,B) (line 83). If the caller aliases X and B (possible whenever A is square, m == n, so both are s x n), the zeroing wipes B, bSqF becomes 0, and the solver returns status=Converged with X=0 and maxRnorm=0 for a nonzero original B — a silent wrong answer. This violates the library's own established convention: scalar craig guards exactly this via RequireDistinctBuffers including x vs b (Krylov.CRAIG.fProxy.cs:49-56), and bbiCGStab guards X vs B (Krylov.Block.BiCGStab.fProxy.cs:93-99). Fix: add a RequireDistinctBuffers(X.Data.Ptr, B.Data.Ptr) check next to the shape checks (~line 48). Sibling blsmr shares the same gap (Krylov.Block.LSMR.fProxy.cs:48-49) but is out of this review's scope.
**verify:** Verified in Krylov.Block.CRAIG.fProxy.cs: X is zeroed at lines 50-51 before B is first read at line 83 (bSqF = BlockFrobDot(B,B)), and the function contains no RequireDistinctBuffers call. When A is square (m==n, explicitly allowed by the line-42 check and the forwarder doc "wide or square"), B (s×m) and X (s×n) have identical shapes, so a caller can pass the same fProxyMxN as both the `in B` and `ref X` arguments; the struct carries a raw buffer pointer, so zeroing X wipes B. Then bSqF==0 triggers the line-84 early-out: status=Converged, X=0, and cleanup's residual recompute compares the zeroed B against A·0=0, yielding maxRnorm=0 and converged=s — a silent false Converged that even the exit residual check cannot detect because the input B was destroyed, contradicting the doc's "never a false Converged" promise. Grep confirms this is an omission, not a contract: scalar craig guards x/b (Krylov.CRAIG.fProxy.cs:55) and block siblings bbiCGStab (:99), bminres (:244), btfqmr (:133) all include X/B in RequireDistinctBuffers via the shared Krylov.Guards.cs helper. Concrete repro: bcraig(identity 3×3, in M, ref M) with nonzero 1×3 M returns Converged with M zeroed instead of the correct X=M.

### [MED] bcraigmr - Missing X/B distinct-buffer guard: aliased call silently destroys B and returns false Converged with X=0
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIGMR.fProxy.cs:47
bcraigmr zeroes X at entry (lines 47-48) before reading B (BlockFrobDot at line 103, LQ.decomp(B,...) at line 115). For a square system (m == n, allowed since A.Rows <= A.Cols), X and B have identical s x m shapes, so a caller can legally pass the same buffer for both (solve-onto-B style). Then the zeroing wipes B, bSqF == 0 at line 104, and the solver returns status=Converged, converged=s, X=0, maxRnorm=0 -- a silent, plausible-looking wrong answer with no exception and no Breakdown. The library already treats this as a guarded contract: scalar craigmr calls RequireDistinctBuffers on x/b (Krylov.CRAIGMR.fProxy.cs:51-58) and block bminres guards X vs B (Krylov.Block.MINRES.fProxy.cs:238-245, Krylov.Guards.cs helper). Fix: add the same RequireDistinctBuffers(X.Data.Ptr, B.Data.Ptr) check after the shape validation. Same gap exists in the siblings bcraig (Krylov.Block.CRAIG.fProxy.cs:50) and blsmr/bcgls, but this file is the one under review.
**verify:** Every element of the claim checks out against the actual code. bcraigmr zeroes X at lines 47-48 before B is first read (BlockFrobDot at 103, LQ.decomp(B) at 115). For a square system (m==n, explicitly allowed by line 39's check A.Rows <= A.Cols), X and B have identical s x m shapes, so all shape validations (42-44) pass on an aliased call. Concrete failure: pass the same buffer as B and X for a nonsingular square A with nonzero B — the zeroing wipes B, bSqF==0 triggers the trivial-solution branch, and the solver returns status=Converged, converged=s, maxRnorm=0 with X=0, a silent wrong answer; the exit-time fresh-residual recompute is defeated because B itself was destroyed. The library treats X/B aliasing as a guarded contract everywhere else: scalar craigmr includes x/b in RequireDistinctBuffers (Krylov.CRAIGMR.fProxy.cs:57), and block siblings bminres (:244), bbiCGStab (:99), btfqmr (:133) all guard X vs B. bcraigmr (and bcraig) omit the guard, so instead of the convention's ArgumentException the caller gets a false Converged and a destroyed B. Real guard-omission bug producing a wrong result on a concrete legal input.

## Optimizations
### [HIGH/deterministic-reorder] minresQLP - Flip line 234 to `flag == flag0` to resurrect the cheap MINRES phase (currently dead code)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:234
The ported `flag != flag0` makes every healthy iteration take the QLP update (~12 full n-passes: lines 288-307 scaledCopy/addScaled/mul chains + CopyFrom + 3 axpys) instead of the MINRES update (2 passes: Blas.combine3 + addScaledInPlace, lines 236-238). The SISC 2011 paper's own TranCond semantics ('trancond > 1/eps leads to MINRES iterates throughout, trancond = 1 generates MINRES-QLP from the start') only make sense with `flag == flag0`; as ported, TranCond=1e7 is dead and the one reachable MINRES-branch case (iters==1 with guard-fired flag 6/9) applies the NON-robust update exactly when QLP is needed. Flipping restores ~5-6x less vector traffic per healthy iteration and fixes the warm-start bug (correctness #1) for healthy solves. Determinism: deterministic run-to-run but NOT bit-identical to current output (mathematically equivalent update path, different rounding) — needs the pre-1.0 waiver + battery re-baseline. Cross-check the official Stanford SOL MATLAB minresQLP.m before flipping (the stashed Python port at reference/square/minresQLP.py:348 carries the same inverted condition, so fidelity-to-Python argues nothing here); note DEVLOG OP/DEVLOG.md:513-523 already documents this as a known reference flaw ported as-is.

### [HIGH/deterministic-reorder] Krylov.gcrodr - Deflation Gram build: O(d^2 * n) explicit dots reduce to n-FREE small-matrix products via the projected Arnoldi relation
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:304
Lines 304-326 compute Fmat/Gmat/Pgram with ~2.5*d^2 length-n Blas.dot calls per cycle (defaults d = kcur+p ~ 40 -> ~4000 n-dots, several times the whole cycle's Arnoldi dot cost of ~m^2/2), plus the kcur^2 length-n axpy AU=C*Ru materialization at lines 291-297. But AP lies exactly in span[C, V_{0..p}]: A*U = C*Ru and A*Z_j = C*B[:,j] + V*Hbar[:,j] (that IS the projected Arnoldi recurrence, valid for the flexible path too since AV[j] is captured pre-projection at line 168). Hence Fmat = [[Ru^T Ru, Ru^T B],[B^T Ru, B^T B + Hbar^T Hbar]] with ZERO length-n work, and Gmat/Pgram only need the small cross blocks C^T U, V^T U, U^T U (plus Zv blocks when flexible; identity path gets Pgram's V-block = I and C^T V = 0 free). Cuts per-cycle n-dots from ~2.5 d^2 to ~k(k+p) and deletes the AU rebuild entirely. The folder DEVLOG already earmarks exactly this ('Fmat's top-left block reduces to Ru_old^T Ru_old ... a future perf pass'). Cheaper fallback tier: keep explicit dots but store U/C/V/Zv/AV as contiguous fProxyMxN (rows = vectors) and form each Gram as ONE GEMM via Blas.dot(in M, in M, ref G, false, true) -- the exact BlockGram pattern at Krylov.Block.Common.fProxy.cs:19-22. Either way the sums re-associate: deterministic-reorder, pre-1.0 waiver.

### [HIGH/bit-identical] Krylov.Block.CG - BlockAdd / BlockZplusT hand-rolled scalar loops -> UnsafeOP.axpy / compAdd SIMD kernels
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
Per iteration bcg does three full s-x-n passes through these scalar helpers: BlockAdd for 'X += T' and 'R -= T' (Krylov.Block.CG.fProxy.cs:99-100) and BlockZplusT for 'P = Zeff + Q' (lines 121-122). Both helpers (Krylov.Block.Common.fProxy.cs:36-49) are plain scalar element loops over contiguous storage. BlockAdd(Y,T,sign) is exactly y += a*x elementwise = UnsafeOP.axpy (already SIMD via OP.Component addScaledInPlace, OP.Component.fProxy.cs:85-90); BlockZplusT can be MemCpy(dst,Z) + UnsafeOP.compAdd (subInPlace/addInPlace pattern, OP.Component.fProxy.cs:67-81), or a trivial new fused dst=a+b kernel next to compAdd. Elementwise maps, same per-element rounding -> bit-identical. Benefits every block solver sharing these helpers (bcgrq, bfbcg, block Arnoldi's BlockAdd at Common:487).

### [HIGH/bit-identical] Krylov.Block.BFBCG - Per-iteration LQRP.decomp allocates an s-x-n Temp workspace + Pivot every iteration — hoist via the existing fProxyLQRPCache overload
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BFBCG.fProxy.cs:171
FactorLiveSearch (called at BFBCG lines 101 and 171, body at Krylov.Block.Common.fProxy.cs:255-266) uses the allocating LQRP.decomp overload, which news W = fProxyMxN(sLive, n, Temp) + v(n) and copies the whole block every call (LQRP.fProxy.cs:268-271), plus a new Pivot(sLive, Temp) per call. That is an O(s*n) allocation+copy per iteration; inside a Burst job Temp Dispose is a no-op, so for large n these allocations also accumulate for the job's lifetime. LQRP already ships a zero-alloc workspace overload, LQRP.decomp(..., ref fProxyLQRPCache ws) at LQRP.fProxy.cs:287 — hoist one cache (sized s x n) and one Pivot(s) next to the other Temp scratch at lines 53-60 and thread them through FactorLiveSearch/FactorLiveResidual. Caveat: RequireLQRPWorkspace(ws, m, n) is checked per call (LQRP.fProxy.cs:300) — since sLive shrinks, either take exact-size views into the cache's W/v or relax the check to >=; verify before wiring. Same computation on same inputs -> bit-identical. (This is pending task #25's 'LQRP kernel' item; benefits bcgrq too since the helper is shared.)

### [HIGH/bit-identical] Krylov.Block.MINRES - BlockAdd is a hand-rolled scalar loop — reroute to UnsafeOP.axpy (addScaledInPlace)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
Krylov.Block.Common.fProxy.cs:36-41 BlockAdd does a scalar 'yp[i] += sign * tp[i]' over the contiguous s*n block. bminres calls it 5x per iteration on s x n data (lines 342, 346, 395, 396, 407) — the dominant elementwise traffic after ApplyBlock/GEMM. The existing SIMD kernel UnsafeOP.axpy (UnsafeOP.fProxy.cs:1746, exposed as OP.Component addScaledInPlace) computes exactly y += a*x elementwise; an elementwise map reorder is bit-identical per the determinism rules. Since post-#47 Data.Length == M_Rows*N_Cols, BlockAdd can simply forward to UnsafeOP.axpy(Y.Data.Ptr, T.Data.Ptr, sign, len). Bonus: benefits every block solver sharing BlockAdd, not just bminres.

### [HIGH/bit-identical] Krylov.Block.BiCGStab - BlockAdd is a scalar element loop called 5x per iteration on s*n data — reroute to the existing SIMD axpy (OP.Component addScaledInPlace / UnsafeOP.axpy)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
BlockAdd (Common:36-41) is `for i: yp[i] += sign*tp[i]` over the contiguous block. bbiCGStab calls it 5 times per iteration on s x n blocks (BiCGStab lines 166, 167, 193/194, 195, 209). fProxyMxN implements IUnsafefProxyArray (fProxyMxN.cs:14) with the universal Data.Length == M_Rows*N_Cols invariant, so the body can be exactly `Y.addScaledInPlace(sign, T)` -> UnsafeOP.axpy (the SIMD kernel already used by scalar biCGStab at Krylov.BiCGStab.fProxy.cs:170-176). Elementwise map, no reduction reorder -> bit-identical. Shared-helper caveat: BlockAdd is also used by bcg/bcgrq/bfbcg/bgmres etc., so this one edit speeds the whole block family (and stays bit-identical for all of them).

### [HIGH/bit-identical] Krylov.Block.IDR - BlockAdd is a hand-rolled scalar loop on the hottest per-step path — reroute to the existing SIMD axpy kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
BlockAdd (Common:36-41) is 'for(i<len) yp[i] += sign*tp[i]' — exactly UnsafeOP.axpy (UnsafeOP.fProxy.cs:1746, [NoAlias] SIMD) / OP.Component addScaledInPlace (OP.Component.fProxy.cs:85, generic over IUnsafefProxyArray which fProxyMxN already implements, fProxyMxN.cs:14). bidr calls BlockAdd on m*n-sized blocks ~6-8 times per k-step (V/Q accumulation :178-179, V=R-V :183, Uk build :188/:195, bi-ortho :206-207, R/X commit :221-222, end-of-sweep :266-268) plus m*m at :165/:233 — this is the dominant non-GEMM per-iteration cost. Replace the body with UnsafeOP.axpy(yp, tp, sign, (int)len). axpy is a pure elementwise map, so the reroute is BIT-IDENTICAL (free). Fix lives in Common so every block solver (bbiCGStab, bgmres, btfqmr, ...) wins simultaneously. Same treatment for BlockScaleInPlace (Common:425, used at bidr:182) via the SIMD scale/mul elementwise kernel — also bit-identical.

### [HIGH/deterministic-reorder] bgcrodr — Krylov.Block.GCRODR - Harmonic-Ritz F/G/Pgram build is O(d^2*n) purely SCALAR dots — gather the combined basis once and use 3 GEMMs
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:434
Lines 434-457 do ~2*d^2 RowDotAt calls (RowDotAt, line 14-19, is a plain scalar accumulate loop — no SIMD) plus d^2 redundant ResolveCombinedCol re-resolutions, with d up to recycle + m*s (e.g. 130) and each dot O(n). Reroute: copy the d combined rows ONCE into two d x n scratch blocks Pg/APg (each source row is contiguous in its owning block, so this is d row-memcpys via CopyBlock-style spans, O(d*n)), then Fmat = APg*APg^T, Pgram = Pg*Pg^T, Gmat = APg*Pg^T — three calls to the existing Blas.dot GEMM (the exact BlockCrossGram shape from Krylov.Block.Common.fProxy.cs:398), symmetrizing F/P as BlockGram (Common:19) already does. This is a deterministic-reorder change (scalar sequential dot -> GEMM tree). Minimal-change fallback: replace RowDotAt's body with the fused SIMD Blas.vecDot (UnsafeOP.fProxy.cs) on the two row pointers — same category, smaller win. Same gather also feeds opt #2 and the AUblk build (lines 425-427: kcur^2 scalar row-axpys == one Ru^T*Cblk GEMM via BlockCTV on a compacted kcur x kcur Ru copy — note Ru's true stride is `recycle`, so a compaction copy, not a View, is required, per this file's own line-390 stride warning).

### [HIGH/deterministic-reorder] btfqmr - Fuse W -= alpha*AU + ||W_row||^2 into UnsafeOP.axpyNormSq per row
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:232
Lines 224 and 232 do RowAddScaled(W,i,-alpha[i],AU) then math.sqrt(RowDot(W,W,i,n)) — exactly the axpyNormSq fusion (UnsafeOP.fProxy.cs:1782, exposed as Blas.axpyNormSq). Rows are contiguous: pass W.Data.Ptr + (long)i*N_Cols and AU.Data.Ptr + (long)i*N_Cols. This is the hottest O(s*n) work in every half-step. The kernel contract says the fused reduction is bit-identical to plain-update-then-vecDot, but vs the CURRENT scalar sequential RowDot the reduction is a deterministic-reorder change (pre-1.0 waiver); the update half is bit-identical.

### [HIGH/deterministic-reorder] btfqmr - RowDot is a hand-rolled scalar dot — reroute to UnsafeOP.vecDot on row pointers
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:36
RowDot (lines 36-41) is used for tau init (159), the s bi-orthogonality dots vtrstar (211), and rhoNew (258) — all dots of contiguous length-n rows. Replace with UnsafeOP.vecDot(P.Data.Ptr + (long)row*N_Cols, Q.Data.Ptr + (long)row*N_Cols, n), the exact pattern already shipped in LOBPCG.fProxy.cs:1121 and the DEVLOG 'reduction-reroute batch 6'. ~2-4x on the dot-bound half-steps. Deterministic-reorder (tree reduction vs sequential sum). Do NOT replace the s row-dots with a GEMM — only the diagonal of Rhat0*V^T is needed, GEMM would compute s^2 dots.

### [HIGH/bit-identical] btfqmr - RowAddScaled / RowScaleAddSelf / RowScaleInPlace scalar loops should be SIMD row kernels
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:18
Lines 18-33: hand-rolled per-element axpy/aypx/scale over contiguous rows, called in every half-step at 224 (via O1), 229-230 (D update), 238 (X += eta*D), 251 (U -= alpha*V), 269-271 (U/V updates). These are elementwise maps — SIMD-ing them is bit-identical (free). Reuse the fProxyW/fProxy4 elementwise machinery behind OP.Component addScaledInPlace/scaleAddInPlace/mulInPlace by adding thin row-pointer overloads (UnsafeOP already has the scaledCopy shape at UnsafeOP.fProxy.cs:1980 to model them on), rather than new one-off kernels.

### [HIGH/deterministic-reorder] blsmr - Per-row Apply/ApplyT marshalling forfeits GEMM/spMM for the dominant per-iteration cost
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:154
Both block operator applications per iteration (lines 154, 167; also 118, 133, 261) go through BlockApplyOp/BlockApplyOpT (Krylov.Block.Common.fProxy.cs:300-325), which copies each row into rowN/rowM scalar-by-scalar, runs s independent GEMVs, and copies back -- 4*s*(n+m) marshalling copies per iteration plus s separate kernel launches. For the dense forwarder (fProxyDenseOperator) the whole block apply is one existing GEMM: AV_row = Blas.dot(V, Adense, false, true) (matMatDotTransB, row-contiguous both operands) and ATU_row = Blas.dot(U, Adense, false, false); for BSR it is the already-block-unrolled BSR.spMM. Requires a rectangular ApplyBlock/ApplyTBlock on IfProxyLinearOperator (currently documented square/symmetric-only) or a TOp-specialized fast path. This is the dominant per-iteration cost of blsmr and benefits bcgls/bcraig/bcraigmr identically. Determinism: contraction order in the GEMM row-dot kernels may differ from per-row GEMV accumulation -> deterministic-reorder (BSR.spMM may in fact be bit-identical per-row, unverified). Interim bit-identical half-win: replace the scalar row copy loops in BlockApplyOp/BlockApplyOpT with UnsafeUtility.MemCpy (rows are contiguous in row-major storage).

### [HIGH/deterministic-reorder] bcgls - Dense-A overload pays per-row GEMV + marshalling copies; Q = A·P and S = Aᵀ·R are single GEMMs
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CGLS.fProxy.cs:83
The dense forwarder (line 129, fProxyDenseOperator) funnels through the generic per-row BlockApplyOp/BlockApplyOpT (Krylov.Block.Common.fProxy.cs:300/314): per iteration that is 2·s separate GEMVs plus 2·s·(n+m) scalar copy elements through rowN/rowM scratch. In row convention these are one GEMM each: Q = Blas.dot(P, A, Q, false, true) (Q_rows = P_rows·Aᵀ) and S = Blas.dot(R, A, S, false, false) (S_rows = R_rows·A) — OP.Dot.fProxy.cs:208 already supports both transpose flags and carries the 8x16 register-tiled matMatDot. Specialize bcgls for a dense fProxyMxN A (or give fProxyDenseOperator a rectangular ApplyBlock/ApplyBlockT pair), keeping the generic per-row path for matrix-free TOp. Deterministic-reorder: GEMM tiling reorders each row-dot vs the sequential GEMV, so results shift in last-ulp (pre-1.0 waiver category).

### [HIGH/unknown] bcraig — Krylov.Block.CRAIG - Gate the per-round fresh ||B - A X||_F check on the free block identity ||LB_{j+1}^T Y_j||_F — saves ~1/3 of all operator applies
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIG.fProxy.cs:155
Each round does 3 full block operator applies: A V (line 124), A^T U (line 132), and the fresh-residual A X (line 155). The DEVLOG documents deliberately not TRUSTING a recurrence shortcut (correctness over speed, never-false-Converged) — but the identity ||B - A X_j||_F = ||LB_{j+1}^T Y_j||_F (residual = -U_{j+1} LB_{j+1}^T Y_j with U_{j+1} row-orthonormal; needs no commutation) can be used as a cheap GATE instead of a replacement: pipeline next round's Step A (lines 124-128) before the convergence check, compute est = ||LBnew^T Ynew||_F from an s x s GEMM + BlockFrobDot (the same product line 140 computes anyway for the next coefficient solve), and only run the expensive fresh BlockApplyOp(A, X) confirm when est <= threshold. The fresh check remains the SOLE authority for Converged, preserving the documented no-false-Converged contract. Determinism: on the normal path the exit round and X are unchanged; if the estimate drifts large in finite precision, convergence detection can be delayed by a round (different but still-valid exit iterate), hence 'unknown' rather than bit-identical. Cross-check the folder DEVLOG entry (Krylov.Block.CRAIG, 2026-07-20) before implementing — it rejects trusting the shortcut, not gating with it.

### [HIGH/deterministic-reorder] bcraigmr - Gate the per-round fresh-residual recompute with the free ||ZetaBarNew||_F estimate (cuts ~1/3 of all operator applications)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIGMR.fProxy.cs:180
Lines 180-184 run BlockApplyOp(A, X) -- s full A.Apply calls -- every round purely for the stopping test, on top of Step A's s Applies and Step B's s ApplyTs, i.e. +50% operator cost per round. The scalar sibling gets rNorm free as |zetabar| (Krylov.CRAIGMR.fProxy.cs:117); the block analog ||ZetaBarNew||_F is already computed at line 160 for free. The DEVLOG documents the always-recompute as deliberate because the block identity is UNVERIFIED -- but gating does not need it to be exact: use ||ZetaBarNew||_F (via BlockFrobDot, with a safety factor) only as a cheap predictor, and run the expensive fresh ||B-AX||_F check ONLY when the estimate crosses threshold, keeping the fresh check as the sole Converged gate (no false-convergence risk, honesty preserved). X's trajectory is unchanged; only the round at which the true check fires (hence possibly the exit round, if the estimate is loose) can shift -- not bit-identical, deterministic run-to-run.

### [MED/bit-identical] Krylov.FCG - Fuse the two per-iteration dots over z (rznew and <z,rOld>) into one dual-dot pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FCG.fProxy.cs:116
Line 116 computes rznew = Blas.dot(r, z) and line 123 computes Blas.dot(z, rOld) — two separate full passes that each stream z (z is read twice per iteration). No dual-dot kernel exists in UnsafeOP.fProxy.cs (only vecDot at line 242 / vecDotRange at 287), so this needs a NEW small kernel (e.g. UnsafeOP.vecDot2(z, r, rOld, n, out d1, out d2) exposed as Blas.dot2), built in the same style as the existing fused kernels (axpyNormSq:1782, updateXR:1914). If each of the two accumulator sets mirrors vecDot's exact lane/accumulation shape (the library's established 'bit-identical-to-composition' contract for its fused kernels, per OP/DEVLOG.md:1752), the result is bit-identical to the two separate Blas.dot calls. Saves one length-n stream read of z per iteration. Note the breakdown guard at line 118 only consumes rznew, so both dots can be computed before the guard with no behavior change.

### [MED/bit-identical] Krylov.FCG - Replace the per-iteration rOld.CopyFrom(r) with an out-of-place r-update plus buffer swap
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FCG.fProxy.cs:101
Line 101 does a full length-n copy rOld.CopyFrom(in r) every iteration solely so the pre-update residual survives the in-place Blas.updateXR at line 104. Instead: add an out-of-place variant of UnsafeOP.updateXR (UnsafeOP.fProxy.cs:1914) that writes r_new = r - alpha*q into a separate [NoAlias] destination (rOld's buffer) while leaving r intact, then swap the local fProxyN handles r<->rOld (they are pointer-wrapping structs; swapping locals is safe — the verify-at-exit at line 108 and M.Apply at 114 go through the same locals). Same arithmetic, same accumulation order for the folded ||r||^2 => bit-identical; eliminates one full n-length read+write (the copy) per iteration. CAUTION on the tempting bigger rewrite: beta's numerator rznew - <z,rOld> equals -alpha*<z,Ap> in exact arithmetic (r_new - r_old = -alpha*Ap), which would delete rOld entirely, but that identity is broken on the verify-continue path (line 108 replaces r with the fresh true residual when the verify FAILS and iteration continues), and the explicit-difference form is the numerically robust one — do not substitute it.

### [MED/bit-identical] Krylov.MINRES - Fuse per-iteration identity-path y-update + beta dot into Blas.axpyNormSq
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRES.fProxy.cs:138
Line 138 `y.addScaledInPlace(-(alfa / beta), r2)` is immediately followed (after the pure handle swap at 144, which moves no data) by line 152 `betaNewSq = Blas.dot(r2, r2)` on that SAME just-updated buffer. In the identity path this is exactly the axpyNormSq shape: replace with `betaNewSq = Blas.axpyNormSq(-(alfa / beta), r2, ref y)` before the swap (Blas.Fused.fProxy.cs:17; contract: 'Bit-identical to addScaledInPlace followed by Blas.dot(y,y) -- same accumulation order, fused into one pass'). Saves one full O(n) read pass per iteration on the plain-MINRES hot path. Preconditioned path keeps the split form since its beta dot is Blas.dot(r2, z) (no fused kernel returns a cross-dot). BiCGStab (Krylov.BiCGStab.fProxy.cs:121,180) and IDR already use this exact pattern.

### [MED/bit-identical] minresQLP - Fuse the Lanczos axpy + betan dot into Blas.axpyNormSq on the identity path
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:143
Line 143 `r3.addScaledInPlace(-(alfa / beta), r2)` followed (after the pointer swap) by line 150 `betanSq = Blas.dot(r2, r2)` is exactly the axpy+normSq pair the existing fused kernel covers: in the M.IsIdentity branch replace with `betanSq = Blas.axpyNormSq(-(alfa / beta), r2, ref r3)` before the swap (Blas.Fused.fProxy.cs:17 -> UnsafeOP.fProxy.cs:1782). The kernel's documented contract (UnsafeOP.fProxy.cs:1779-1780) is BIT-IDENTICAL to axpy-then-vecDot, so this is a free pass saved (one full read of the updated vector) per iteration on the common unpreconditioned path. The preconditioned branch keeps the plain axpy (its betan comes from the M-weighted dot, line 155). Same reroute applies to the sibling Krylov.MINRES.fProxy.cs:138+152, outside this review's scope.

### [MED/bit-identical] minresQLP - Fuse the QLP x-update (CopyFrom + two axpys) into one Blas.combine3 pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:305
Lines 305-307 `x.CopyFrom(in xl2); x.addScaledInPlace(ul, wl); x.addScaledInPlace(u, w)` = 3 passes; replace with `Blas.combine3(ref x, xl2, ul, wl, u, w, (fProxy)1)` = 1 pass. The kernel (UnsafeOP.fProxy.cs:1991-1994) computes w[i] = s*(v[i] + a*w1[i] + b*w2[i]) left-to-right, so with s = 1 (exact multiply) the per-element operation order is identical to the current copy-then-two-axpys sequence — bit-identical. Impact is med while the solver runs always-QLP (every iteration, see correctness #1 / optimization #1); drops to low (QLP tail only) once line 234 is flipped.

### [MED/bit-identical] Krylov.BiCGStab - Fuse dot(t,t) and dot(t,r) into one dual-reduction pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:153
Lines 153 and 161 read t twice (and r once) per iteration for tt = dot(t,t) and ts = dot(t,r) — the classic BiCGStab omega pair. No existing kernel does a dual reduction (vecDot/axpyNormSq/xpayNormSq each produce one), so this needs a NEW UnsafeOP kernel (e.g. dotAndNormSq(t, r, n) -> (t·r, t·t)) that runs two independent accumulator chains, each replicating vecDot's exact lane/accumulator order — then each result is bit-identical to the separate Blas.dot calls it replaces. Saves one full O(n) read pass over t per iteration; the biggest per-iteration vector-op win left in this solver.

### [MED/bit-identical] Krylov.GMRES - Fuse the last MGS axpy with the norm via Blas/UnsafeOP.axpyNormSq (per inner iteration)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs:27
ArnoldiMGSStep does w.addScaledInPlace(-hij, vi) for i=0..j, then a separate full pass hj1 = sqrt(Blas.dot(w, w)) (line 27). Peel the i==j iteration and replace its axpy + the following dot with UnsafeOP.axpyNormSq(w, V[j], -hjj, n) — the kernel's contract (UnsafeOP.fProxy.cs:1779-1782) guarantees bit-identical to axpy-then-vecDot. Eliminates one full read pass over w per inner Arnoldi step (the hottest loop, executed every one of the maxIter inner iterations). Shared with fgmres — change lands there too, also bit-identical.

### [MED/bit-identical] Krylov.GMRES - Replace CopyFrom + scalar *= loop with one UnsafeOP.scaledCopy pass (per inner iteration)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs:33
vj1.CopyFrom(in w); for (i<n) vj1[i] *= invh (Arnoldi.Common:33-34) is two passes over n, the second a scalar indexer loop. UnsafeOP.scaledCopy(vj1.Data.Ptr, w.Data.Ptr, invh, n) (UnsafeOP.fProxy.cs:1980, [NoAlias]-safe here — vj1 and w are distinct buffers) does it in one SIMD pass. Elementwise map, same per-element operation → bit-identical. Runs once per inner Arnoldi step.

### [MED/bit-identical] Krylov.FGMRES - Arnoldi normalize: CopyFrom + scalar indexer loop -> one fused Blas.scaledCopy pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs:33
ArnoldiMGSStep normalizes with vj1.CopyFrom(in w) followed by a scalar per-element indexer loop `for i: vj1[i] *= invh` (lines 33-34). Replace both with Blas.scaledCopy(invh, in w, ref vj1) (Blas.Fused.fProxy.cs:53 -> UnsafeOP.scaledCopy, [NoAlias] pointer kernel, Burst-vectorized): one pass instead of memcpy + a per-element indexer loop, and it removes per-access indexer overhead. y[i] = invh * w[i] vs (copy w[i]) *= invh is IEEE-multiply commutative -> bit-identical. Runs every inner iteration (n-length), and the file is shared, so gmres gets the same win in lockstep (preserving the documented fgmres==gmres bit-identity under identity).

### [MED/bit-identical] Krylov.IDR - ns2 = Blas.dot(R, R) at end-of-sweep recomputes a value already held in rr
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.IDR.fProxy.cs:205
Every path into the end-of-sweep step leaves rr == ||R||^2 for the current R: line 164's Blas.axpyNormSq contract (Blas.Fused.fProxy.cs) is bit-identical to update-then-separate-vecDot, and the failed-verify path sets rr = trueRR with R holding exactly that residual (lines 175-176); the k-loop always runs at least once before line 186 (s>=1 and the iter>=maxIter case breaks at line 184). So line 205's ns2 = Blas.dot(R, R) is a redundant full n-length reduction per sweep — replace with ns2 = rr. Bit-identical by the axpyNormSq/vecDot shared-tree contract.

### [MED/deterministic-reorder] Krylov.IDR - s scalar shadow dots (f = P^T R and the new Msys column) could be one contiguous GEMV
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.IDR.fProxy.cs:99
Line 99 (f[i] = Blas.dot(P[i], R), s dots) and line 155 (Msys[i,k] = Blas.dot(P[i], Gk), s-k dots) stream the same right-hand vector against s separately allocated P vectors. Storing P as one contiguous s x n fProxyMxN (row-major) turns each into a single dense mat-vec using the existing SIMD GEMV kernel (the ~3.4-4x campaign kernel) with contiguous row locality and one call instead of s. If instead implemented as per-row UnsafeOP.vecDot over the contiguous rows it is bit-identical; routing through the multi-accumulator GEMV kernel is deterministic-reorder (still run-to-run/cross-arch deterministic, just a different fixed tree than vecDot).

### [MED/bit-identical] Krylov.TFQMR - Fuse w -= alpha*au with ||w||^2 via existing Blas.axpyNormSq (mirrors biCGStab)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.TFQMR.fProxy.cs:125
Line 125 (w.addScaledInPlace(-alpha, au)) and line 133 (Blas.dot(w, w) inside the theta computation) are exactly the axpy+norm-sq pair that Blas.axpyNormSq (Blas.Fused.fProxy.cs:17 -> UnsafeOP.axpyNormSq) fuses into one pass — the same shape biCGStab already uses at Krylov.BiCGStab.fProxy.cs:121/180. The intervening d-update (lines 129-131) does not touch w, so hoist: fProxy ww = Blas.axpyNormSq(-alpha, au, ref w); ... theta = math.sqrt(ww)/tau. The Fused kernels' contract (and DEVLOG bit-identity pinning) makes the fused reduction bit-identical to update-then-vecDot. Saves one full read pass over w on EVERY half-step of the memory-bound inner loop.

### [MED/bit-identical] Krylov.gcrodr - Hoist the ~20 per-cycle Temp allocations in the deflation block out of the restart loop
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:300
Every restart cycle allocates and disposes AU (line 291), Fmat/Gmat/Pgram (300-302), GmatLU/Xsol/piv (328-330), evReal/evImag (335-336), keys/selIdx (345, 355), Zsel/Ntheta/evTmp/Vtmp (369-372), Unew/AUraw (390-391), Rnew (410). All sizes are bounded by dmax = recycle + restart and n, known before the loop -- allocate once next to V/H/cs/sn (lines 73-109) at max size and reuse the active prefix, mirroring how the Arnoldi workspace is already handled. Pure allocation lifetime change: bit-identical.

### [MED/bit-identical] Krylov.gcrodr - Fuse residual-formation and MGS-tail norms into Blas.axpyNormSq (contractually bit-identical)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:144
Three sites do addScaledInPlace followed by a separate Blas.dot of the result: (a) lines 123-124 + 144 (v0 = b - w, then beta^2 = dot(v0,v0)); (b) the second fresh residual at lines 140-141 + 144; (c) the final i==j MGS subtract at lines 184-186 + the dot(w,w) at line 188. Each collapses into one Blas.axpyNormSq call (Blas.Fused.fProxy.cs:17), whose contract states 'Bit-identical to y.addScaledInPlace(a,x) followed by Blas.dot(y,y) -- same accumulation order, fused into one pass'. Saves one full length-n memory pass per site per cycle (sites a/b run every cycle, site c every inner iteration). Free bit-identical win.

### [MED/bit-identical] Krylov.CRAIGMR - Fuse the per-iteration d-update: scaleAddInPlace + divInPlace is two full n-passes that can be one
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIGMR.fProxy.cs:123
Lines 123-124 do d.scaleAddInPlace(-theta, v) (UnsafeOP.aypx, one pass over n) then d.divInPlace(rho) (UnsafeOP.scalDiv, a second pass over n, plain scalar '/=' loop at UnsafeOP.fProxy.cs:2114). Every iteration pays 3 O(n) vector passes on the x/d side (aypx + scalDiv + axpy at line 125) plus the two matvecs. Primary suggestion (bit-identical): add one small fused kernel to UnsafeOP.fProxy.cs next to aypx/axpy — d[i] = (v[i] + (-theta)*d[i]) / rho, elementwise, preserving the exact per-element op order (mul, add, div) — modeled on the existing fused combine3/updateXR style; this removes one full read+write pass over d per iteration with zero rounding change since the same three IEEE ops happen per element in the same order (elementwise map fusion, no reduction reordered). Alternative (NOT bit-identical, deterministic): adopt LSQR's unnormalized-direction form (Krylov.LSQR.fProxy.cs:151-152: x += (phi/rho)*w; w = v - (theta/rho)*w) which eliminates the divide pass entirely using only the existing axpy/aypx kernels — but that reassociates the 1/rho scaling into the scalars and changes rounding, so it needs the pre-1.0 waiver; the new-kernel route is strictly safer.

### [MED/bit-identical] Krylov.LSMR - Fuse the per-iteration hbar/x (and optionally h) updates into one pass — needs a NEW UnsafeOP kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSMR.fProxy.cs:173
Lines 173-177 do three full n-length traversals per iteration: hbar.scaleAddInPlace(-coefHbar, h); x.addScaledInPlace(zeta/(rho*rhobar), hbar); h.scaleAddInPlace(-thetanew/rho, v). No existing fused kernel fits: Blas.updateXR's twin updates are independent, whereas here the x update reads the JUST-updated hbar; Blas.combine3 is the MINRES w-shape. A new UnsafeOP kernel 'for i: t = h[i] + a*hbar[i]; hbar[i] = t; x[i] += b*t; h[i] = v[i] + c*h[i]' collapses 3 read/write passes over 4 n-vectors into 1, purely elementwise — no reduction, so bit-identical under FloatMode strict. This is the only per-iteration win; it matters most when Apply/ApplyT are cheap (very sparse BSR where spMV ~ O(nnz) is comparable to 3n streaming), and is negligible for dense operators.

### [MED/bit-identical] Krylov.Block.CG - R = B - A*X init is a scalar double loop -> MemCpy + UnsafeOP.compSub
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CG.fProxy.cs:70
Krylov.Block.CG.fProxy.cs:70-71 computes R = B - Q with a scalar per-element loop over the whole s-x-n block. Both blocks are contiguous (Data.Length == M_Rows*N_Cols invariant), so this is UnsafeUtility.MemCpy(R, B) followed by the existing SIMD UnsafeOP.compSub (OP.Component subInPlace, OP.Component.fProxy.cs:76-81). Elementwise map -> bit-identical. One-time cost per solve, but it is the largest single scalar loop left in the file body.

### [MED/deterministic-reorder] Krylov.Block.CG - CountConverged and the thr loop: per-row squared norms scalar -> Blas.vecDot on contiguous rows
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:120
thr setup (Krylov.Block.CG.fProxy.cs:61-66) and CountConverged (Krylov.Block.Common.fProxy.cs:120-133, called at bcg:73 and every iteration at bcg:102) both accumulate ||row||^2 with a scalar sequential loop. Rows of the row-major s-x-n block are contiguous, so each is one Blas.vecDot(rowPtr, rowPtr, n) call on the existing fused SIMD kernel (UnsafeOP.fProxy.cs vecDot / addSquares). This reorders a sum -> deterministic-reorder, allowed pre-1.0 under the documented waiver; note CountConverged is shared by the whole block family, so the waiver covers bcgrq/bfbcg/bcgls convergence thresholds too (convergence iteration counts may shift by rounding).

### [MED/bit-identical] Krylov.Block.BCGrQ - Same saSearch x saSearch Gram PQ is Cholesky-factored twice per iteration — reuse FactorGramOnce like bfbcg
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:148
BlockSolveSPD is called at line 119 (alpha) and line 148 (beta) with the SAME PQ view and each call rebuilds work=G+ridge and runs a full CHO.decompInPlace (Krylov.Block.Common.fProxy.cs:55-77). bfbcg already solved this exact pattern: FactorGramOnce (Krylov.Block.Common.fProxy.cs:273) factors once, then CHO.decompSolve is called twice on the shared factor (Krylov.Block.BFBCG.fProxy.cs:123,130,159). The ridge ladder is deterministic, so factoring once yields the identical L — bit-identical, saves one s^3/3 Cholesky + s^2 rebuild per iteration.

### [MED/bit-identical] Krylov.Block.BCGrQ - BlockAdd is a scalar s x n loop — reroute to UnsafeOP.axpy / fProxyComp.addScaledInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:131
S5 (line 131) and S11 (line 157) each stream a full s x n block through Common's BlockAdd (Krylov.Block.Common.fProxy.cs:36-41), a scalar `yp[i] += sign*tp[i]` loop. UnsafeOP.axpy (UnsafeOP.fProxy.cs:1746, [NoAlias] SIMD) / the fProxyComp.addScaledInPlace extension (OP.Component.fProxy.cs:85) is a drop-in: the RowsView arguments satisfy the universal Data.Length==M_Rows*N_Cols invariant, and sign*t + y elementwise is order-preserving — bit-identical. One change in the shared helper speeds every block solver (bcgrq, bfbcg, bgmres Arnoldi, ...).

### [MED/bit-identical] Krylov.Block.BCGrQ - Initial residual and cleanup residual R = B - A·X are scalar indexer loops — memcpy + SIMD compSub
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:82
Lines 82-83 and 174-176 compute R[i,c] = B[i,c] - AP[i,c] elementwise through the indexer. Replace with UnsafeUtility.MemCpy(B -> dst) then fProxyComp.subInPlace (OP.Component.fProxy.cs:76, backed by UnsafeOP.compSub SIMD) over the contiguous s x n block. Pure elementwise map — bit-identical. Cleanup runs on every exit path so this is on the hot verify tail too.

### [MED/bit-identical] Krylov.Block.BCGrQ - CopyBlock scalar double loop on contiguous s x n views — use MemCpy
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:97
Setup line 97 (PaActive -> Psearch) and S11 line 156 (PaActive -> Pnew) copy saSearch/saNew x n blocks through Common's CopyBlock (Krylov.Block.Common.fProxy.cs:147-151), an indexer-per-element loop. Both src and dst are contiguous leading-rows views, so a single UnsafeUtility.MemCpy of rows*n*sizeof(fProxy) (or UnsafeOP.scaledCopy with a=1, UnsafeOP.fProxy.cs:1980) is bit-identical. Same helper also feeds BFBCG and the Arnoldi/LS solvers via BlockLSResolveAndCheck. Related low-cost extra: BlockApplyPre's row marshalling copies (Krylov.Block.Common.fProxy.cs:141,143) can also be MemCpy.

### [MED/deterministic-reorder] Krylov.Block.BCGrQ - Per-row squared-norm reductions (thr setup, LockConvergedRows, CountConverged) are scalar dots — reroute to UnsafeOP.vecDot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:73
thr loop (lines 73-77, one-time), LockConvergedRows (Krylov.Block.Common.fProxy.cs:194-195, runs EVERY iteration doing sLive scalar length-n dots) and CountConverged (Krylov.Block.Common.fProxy.cs:126, exit-time) all accumulate rr sequentially. Rerouting each row to UnsafeOP.vecDot(rowPtr,rowPtr,n) (UnsafeOP.fProxy.cs:242, fused SIMD) is a sum-tree reorder: DETERMINISTIC-REORDER, allowed pre-1.0 under the documented waiver, and it can flip a lock/convergence decision only at last-ulp thresholds. Per the reduction-reroute two-tier policy this is the sum/dot tier, not the free tier.

### [MED/bit-identical] Krylov.Block.BFBCG - Setup copies M R0 into P before factoring — reuse the existing FactorLiveResidual helper and drop the s-x-n CopyBlock
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BFBCG.fProxy.cs:93
Lines 89-103: the setup writes M R0 (or R0) into P via CopyBlock, then FactorLiveSearch(P, ...) hands it to LQRP.decomp — which immediately makes its own internal copy (LQRP.fProxy.cs:271), so the P staging copy is pure overhead; raw P0 is never read again (the loop rebuilds P from Z + beta^T Phat before its next factorization). Krylov.Block.Common.fProxy.cs:226-250 already has FactorLiveResidual(in R, in M, ref Z, ...) that applies the preconditioner into Z and feeds the live view straight into LQRP.decomp for exactly this setup step (bcgrq's pattern). Replacing lines 90-101 with one FactorLiveResidual call removes one full s x n copy (two in the preconditioned path counting the Z->P copy). LQRP receives bit-identical input values -> bit-identical result.

### [MED/deterministic-reorder] Krylov.Block.BFBCG - Scalar per-row sum-of-squares reductions (thr setup, LockConvergedRows, CountConverged) — reroute onto UnsafeOP.addSquares/vecDot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BFBCG.fProxy.cs:76
The thr loop at lines 73-78 and, per iteration, LockConvergedRows (Krylov.Block.Common.fProxy.cs:191-196) plus the cleanup CountConverged (Common:120-133) each accumulate ||row||^2 with a scalar bounds-checked-indexer loop over n. Rows of fProxyMxN are contiguous (row-major), so each is one UnsafeOP.addSquares(acc, rowPtr, n) or vecDot(rowPtr, rowPtr, n) call (UnsafeOP.fProxy.cs:2000/242) — the same SIMD width-8 reduction the single-RHS solvers use. LockConvergedRows is O(sLive*n) scalar work every iteration, comparable to one BlockCTV. This reorders a sum -> deterministic-reorder, pre-1.0 waiver + DEVLOG note required; change thr, lock, and CountConverged together so residuals and thresholds stay on one summation convention. Helpers are shared with bcgrq — same waiver covers both.

### [MED/bit-identical] Krylov.Block.MINRES - Hoist per-iteration Temp allocations out of the k-loop (BuildOmega scratch, LU pivot, UnpivotBetaRows tmp)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:129
Every iteration allocates and disposes: BuildOmega's 8 fixed-size buffers Y/Qy/Z0/T/QyT/Z1/Qperp/Rz (lines 129-147, all s2 x s or s x s), the LU Pivot pivS (line 399), and UnpivotBetaRows' s x s tmp (line 47, one call per normalization). All sizes are loop-invariant — allocate them once next to Alfa..Res2 in the setup block (lines 248-267) and pass by ref, matching how the solver already hoists T/thr/Pnorm. Pure allocation-site change, bit-identical. Also honors the doc header's 'zero-alloc' claim more truthfully and avoids per-iteration Temp allocator churn at high maxIter.

### [MED/bit-identical] Krylov.Block.MINRES - Residual-formation loops T = B - T should reuse UnsafeOP.aypx (scaleAddInPlace with a = -1)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:303
Three scalar double loops compute 'dst[i,c] = B[i,c] - dst[i,c]' over contiguous s x n blocks: initial residual (lines 303-304 into Wk), probe-confirm (424-425 into T), cleanup verify (436-437 into T). scaleAddInPlace (OP.Component.fProxy.cs:94 -> UnsafeOP.aypx, y = a*y + x) with a = -1 gives y = x - y; IEEE (-1)*t + b rounds identically to b - t, and it's an elementwise map, so the SIMD reroute is bit-identical. The probe-confirm site runs once per iteration near convergence, so this is not just entry/exit cost.

### [MED/deterministic-reorder] Krylov.Block.MINRES - LU with partial pivoting on Gamma — but Gamma is QR's upper-triangular R; use a triangular solve
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:400
Lines 398-401: Gamma comes straight from QR.decomp in BuildOmega, so it is upper triangular and Gamma^T is lower triangular — LU.solveInPlaceTransA re-factorizes an already-triangular matrix (O(s^3) + a per-iteration Pivot alloc + the CopyMat at 398) before its substitution. Reuse the existing multi-RHS kernel Blas.triLower (Blas.Triangular.fProxy.cs:175): TransposeSmall(Gamma -> GammaCopy) then Blas.triLower(ref GammaCopy, ref W) is a single forward substitution. Different elimination/rounding path than pivoted LU, so deterministic-reorder, not bit-identical. Note triLower always reports Success (unguarded zero diagonal) — keep the breakdown gate by relying on BuildOmega's Gamma diagonal check (line 139-140, ideally after fixing it to be relative), or pre-check the diagonal before the solve.

### [MED/deterministic-reorder] Krylov.Block.BiCGStab - BlockFrobDot's two scalar s*n passes per iteration (omega numerator/denominator) — reroute to UnsafeOP.vecDot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:415
BlockFrobDot (Common:415-422) is a sequential scalar accumulate over the contiguous block; bbiCGStab calls it twice per iteration (BiCGStab lines 183-184: tS = <T,R>, tT = <T,T>) — 2*s*n scalar FLOPs on the critical path. Both are single contiguous dots: reroute to UnsafeOP.vecDot(up, vp, len) (the fProxyW/fProxy4 fused SIMD kernel behind Blas.vecDot). This changes the summation tree -> deterministic-reorder (pre-1.0 waiver category), and changes omega slightly, so it needs the documented waiver + baseline refresh. tT stays NaN-safe under the `!(tT > 0)` guard (a NaN sum still fails the comparison).

### [MED/deterministic-reorder] Krylov.Block.BiCGStab - CountConverged (3 calls/iteration) and the thr-init loop do scalar per-row sum-of-squares — reroute each contiguous row to UnsafeOP.vecDot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:120
CountConverged (Common:120-133) accumulates rr = sum R[j,c]^2 scalar-sequentially; bbiCGStab calls it at lines 137, 169, 197 (up to 2x per iteration in steady state), and the tol^2*||B[j]||^2 threshold loop (BiCGStab lines 125-130) is the same pattern one time. Rows are contiguous in the row-major s x n layout, so each becomes UnsafeOP.vecDot(rowPtr, rowPtr, n) (or the addSquares kernel). Reduction reorder -> deterministic-reorder waiver; the convergence decision can flip on borderline columns, so baselines need refreshing. Shared-helper caveat: CountConverged is used by every block solver — one edit, family-wide win, single waiver.

### [MED/deterministic-reorder] Krylov.Block.IDR - CountConverged does O(m*n) scalar sequential row norms every iteration — reroute to UnsafeOP.vecDot per contiguous row
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:120
CountConverged (Common:120-133) computes each row's ||R[j]||^2 with a scalar 'rr += R[j,c]*R[j,c]' loop; it runs once per bidr iteration (:140, :225, :271). fProxyMxN is row-major so row j is a contiguous n-length span — each row norm is one UnsafeOP.vecDot(rowPtr, rowPtr, n) call (UnsafeOP.fProxy.cs:242, the SIMD 2x-fProxy4-accumulator kernel). Same reroute applies to bidr's one-time thr setup loop (Krylov.Block.IDR.fProxy.cs:62-67, identical per-row dot). Category: DETERMINISTIC-REORDER (scalar sequential sum -> SIMD tree reduction changes grouping; pre-1.0 waiver applies), and shared — changes convergence-count round-off for all block solvers using CountConverged.

### [MED/bit-identical] Krylov.Block.IDR - CopyBlock is an element-by-element indexer loop over contiguous buffers — use MemCpy
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:147
CopyBlock (Common:147-151) copies via the [i,c] indexer element-wise. Every bidr call site copies a WHOLE contiguous buffer into another whole contiguous buffer of identical shape (f[i]->sumBlk :161 once per forward-sub row per k-step, Q->Uk :187/:194 m*n per k-step, R->V :240 per sweep, B->X :56), so the body can be a single UnsafeUtility.MemCpy(dst.Data.Ptr, src.Data.Ptr, len*sizeof(fProxy)) when s*n == full extent (or per-row MemCpy otherwise). A copy is trivially BIT-IDENTICAL. The same per-row MemCpy applies to BlockApplyPre's rowIn/rowOut marshalling (Common:141-143), which bidr hits twice per k-step under a real preconditioner (:193, :247).

### [MED/bit-identical] bgmres - Redundant QᵀG GEMM every inner step: BlockLSResolveAndCheck computes Q^T·Gactive twice
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:534
QR.decompSolve (QR.fProxy.cs:713-716) already computes X = QᵀB via Blas.dot(in Q, in B, ref X, transposeA:true) before triUpper destroys it in place; BlockLSResolveAndCheck then recomputes the identical product Blas.dot(in HQ, in Gactive, ref QtG, true, false) at Common:534 just for the Pythagorean check. Restructure: compute QtG once (same Blas.dot call → same bits), copy QtG into Yv, run Blas.triUpper(ref Rls, ref Yv) directly instead of calling decompSolve, and do the Pythagorean check off QtG. Saves one totalRows×totalCols×s GEMM per block step (called every inner step). Bonus in the same function: Yv is only consumed at commit, so the triUpper solve itself can be skipped on steps where allConverged is false and j < m-1 and wj1 > 0 — check first, solve only when the cycle is about to end.

### [MED/bit-identical] bgmres - BlockAdd is a hand-rolled scalar duplicate of the SIMD axpy kernel, sitting in the dominant MGS2 inner loop
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
BlockAdd (Common:36-41) does `yp[i] += sign * tp[i]` in a plain scalar pointer loop. That is exactly UnsafeOP.axpy, already exposed SIMD (fProxyW/fProxy4) via OP.Component addScaledInPlace (OP.Component.fProxy.cs:85-90, y += a*x). BlockAdd is called from BlockArnoldiMGS2Step's projection subtraction (Common:487) — 2 passes × (j+1) blocks per step over w[j]×n, i.e. inside bgmres's dominant O(m·s·n) term — and from the commit loop (GMRES.fProxy.cs:189-198). Elementwise map with identical per-element expression → bit-identical; benefits every block solver sharing the helper. Same reroute applies to BlockScaleInPlace (Common:425-430 → mulInPlace/UnsafeOP scale) for the other users of Common.

### [MED/bit-identical] bgmres - Per-step Temp allocations in the hot loop: LQRP.decomp's allocating overload (s×n scratch per Arnoldi step) plus per-step Pivot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:494
Every BlockArnoldiMGS2Step calls the allocating LQRP.decomp overload (Common:494), which news a w[j]×n scratch W plus v from Allocator.Temp (LQRP.fProxy.cs:268-271) and copies the input into it — every single block step, plus once per cycle at GMRES.fProxy.cs:129. Temp is a bump allocator: a long solve accumulates maxIter·(s·n) elements of arena growth (n=10240, s=4, 500 steps ≈ 80 MB float) that is never reclaimed until scope exit. LQRP has a documented zero-alloc cache overload, `decomp(..., ref fProxyLQRPCache ws)` (LQRP.fProxy.cs:287, 'Semantics identical to the allocating overload') — hoist one fProxyLQRPCache(s, n) next to the other workspace buffers and thread it through BlockArnoldiMGS2Step. Likewise hoist the per-step `new Pivot(w[j], Allocator.Temp)` (Common:491) and per-cycle Ppiv0 (GMRES.fProxy.cs:126) — LQRP resets P internally per its own doc. Only the allocation source changes → bit-identical.

### [MED/bit-identical] bgmres - R0 = B − A·X residual update is a scalar 2D-indexer double loop, twice; should be the SIMD aypx kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GMRES.fProxy.cs:114
GMRES.fProxy.cs:114-115 (per cycle) and 209-211 (exit path) compute R0[i,c] = B[i,c] − R0[i,c] via per-element indexer loops over the full s×n block. This is exactly OP.Component scaleAddInPlace(R0, (fProxy)(-1), B) — y = a·y + x routed to UnsafeOP.aypx (OP.Component.fProxy.cs:92-99, SIMD). b + (−1·r) is IEEE-identical to b − r (exact negation, identical add), elementwise → bit-identical. Both R0 and B are contiguous s×n with matching Data.Length, so the whole-array kernel applies directly.

### [MED/deterministic-reorder] bfgmres - Allocate Z as one contiguous (m*s) x n buffer and collapse the k-GEMM commit loop into a single BlockCTV GEMM
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:184
The commit loop (184-206) does, per cycle: ZeroPrefix(Wcombo) + k iterations of {ExtractRowsAt (indexer copy of a w[i] x s Y slice into YiBuf), BlockCTV (a GEMM whose inner dimension is only w[i] <= s), BlockAdd}. The Y coefficients in Yscratch are ALREADY laid out at row offsets off[i] — exactly the offsets the per-step Z blocks would occupy if Z were one (m*s) x n buffer instead of m separate s x n allocations (line 74-75). With a contiguous Z (per-step writes at row offset off[j] via an offset-write variant of BlockApplyPre, same indexer shape as Common's WriteBlockAt), the whole non-identity commit becomes one call: BlockCTV(RowsView(Yscratch, off[k]), RowsView(Zstack, off[k]), ref Wcombo) — one GEMM with inner dimension off[k]=~ks instead of k tiny-inner-dim GEMMs, and the ZeroPrefix/ExtractRowsAt/YiBuf/BlockAdd traffic disappears. Reuses the existing Blas.dot GEMM (via BlockCTV, Krylov.Block.Common.fProxy.cs:32). Deterministic-reorder: the per-element accumulation order over i-blocks moves into the GEMM's reduction order — pre-1.0 waiver required. Leave the identity path's V[i] loop untouched (V blocks must stay separate LQRP outputs), preserving the documented bit-identity with bgmres under identity.

### [MED/deterministic-reorder] bgcrodr — Krylov.Block.GCRODR - U/AU rebuild is kNew*d scalar row-axpys — two GEMMs over the same gathered blocks (or bit-identical SIMD axpy at minimum)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:524
Lines 524-539 build each new U/AU column with d AddScaledRowInto calls (scalar axpy, line 22-25). With the d x n Pg/APg gather from opt #1: Unew = Zsel^T * Pg and AUnew = Zsel^T * APg are two Blas.dot GEMMs (the BlockCTV shape, Common:32) producing kNew x n row blocks; AUraw's n x kNew column-major layout for QR then needs one transpose copy (or keep as-is and LQ the row block). GEMM route = deterministic-reorder (the l-sum is re-associated). If the structure is kept, a strictly bit-identical win is available: AddScaledRowInto/AddScaledRowToRow's inner loop is an ELEMENTWISE axpy (no reduction), so routing it through the SIMD addScaledInPlace kernel (OP.Component.fProxy.cs) on the contiguous row span is bit-identical per the determinism rules.

### [MED/bit-identical] bgcrodr — Krylov.Block.GCRODR - Entire deflation rebuild runs and is then discarded on the final maxIter-bounded cycle
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:415
When the inner loop exits with total >= maxIter, the code still executes the full deflation update at lines 415-569 (O(d^2*n) Gram build + dense LU + nonsymmetric QR eigensolve + kNew symmetric eigensolves + QR of AUraw), then immediately hits `if (total >= maxIter) break` at line 571 — the rebuilt U/C/Ru are never used and X/outputs are unaffected. Guard the block with `&& total < maxIter` (bit-identical: only dead work removed). This fires on EVERY solve that ends by iteration budget, which is the common case for hard systems.

### [MED/bit-identical] btfqmr - Whole-block scalar loops: Rhat0 = B - AX, D zeroing, V += AU
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:155
Lines 155-156 (Rhat0 = B - V) and 181-182 (D = 0) iterate element-wise over the full contiguous s*n block — use the SIMD elementwise kernels (OP.Component zeroInPlace / a fused sub over the flat s*n span, as NormsOP does with flat Data.Ptr spans). Line 275 RowAddScaled(V, i, 1, AU) per row is a whole-block V += AU: use BlockAdd (Krylov.Block.Common.fProxy.cs:36) or better a SIMD addScaledInPlace over the flat block. All elementwise maps — bit-identical.

### [MED/bit-identical] blsmr - alphabarT is QRCP-factored twice per iteration (redundant recompute)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:224
Lines 213 and 224 both call BlockSolveGeneralWide against the SAME alphabarT: each call re-copies it into coefWork and re-runs the full QRCP factorization (Krylov.Block.Common.fProxy.cs:384-390) -- two identical O(s^3) factorizations + CopyMats per iteration. Factor once (QRCP.decomp into the shared scratch) and back-solve both RHS (RHSphi s x s, RHS_P s x n), mirroring the existing FactorGramOnce factor-once pattern (Krylov.Block.Common.fProxy.cs:273). Identical factorization path on identical input -> merged result is bit-identical. (A further step -- exploiting that alphabark is upper-triangular by construction as Rfull's top-left block, so a plain TRSM would do -- is NOT bit-identical and would lose QRCP's rank check that doubles as the breakdown guard; the factor-once merge is the free half.)

### [MED/bit-identical] blsmr - O(s*n) lag-shift copies use scalar indexer loops instead of MemCpy/CopyFrom
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:250
The end-of-iteration shift does three full s x n block copies (lines 250-255: PLag1->PLag2, Pnew->PLag1, Vnext->V) plus 8 s x s CopyMats (lines 240-254) through per-element indexer loops (CopyBlock/CopyMat, Krylov.Block.Common.fProxy.cs:147-157). All are same-shape contiguous buffers: use fProxyMxN.CopyFrom / UnsafeUtility.MemCpy (the pattern Blas.dot itself uses at OP.Dot.fProxy.cs:191). Pure copy -> bit-identical. (Full pointer-rotation would eliminate the big copies entirely, but the file deliberately documents copy-over-swap for auditability at lines 53-55; note that rationale only claims triviality for the SMALL s x s state -- the s x n blocks are the actual cost -- so if measured worthwhile, rotating just PLag2<-PLag1<-Pnew and V<-Vnext locals is safe here: static method, no IJob struct-copy exposure.)

### [MED/deterministic-reorder] bcgls - CountConverged redoes an s×n norm pass whose results the GEMM just computed as the Gram diagonal
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CGLS.fProxy.cs:97
Line 95's BlockGram(S,S,gammaNew) already produces ‖S[j]‖² as gammaNew[j,j]; line 97's CountConverged then re-walks the whole s×n block scalar-sequentially to recompute exactly those row norms (same duplication at lines 67/71 for gamma). Both call sites also discard the out maxRnorm (maxrS0/maxrS unused), so the per-row math.sqrt is pure waste. Replace with a tiny diagonal check: converged = count of gammaNew[j,j] <= thr[j] — eliminating a full s×n memory pass per iteration. Deterministic-reorder: the GEMM-ordered diagonal differs in last-ulp from CountConverged's scalar sum, so a knife-edge convergence decision could flip; thr (line 69) is already built from the GEMM-ordered gamma[j,j], so the diagonal test is arguably the more self-consistent one.

### [MED/bit-identical] bcgls - BlockAdd/BlockZplusT scalar loops on contiguous blocks should be the SIMD elementwise kernels
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
Three whole-block passes per iteration go through scalar element loops in Krylov.Block.Common.fProxy.cs: BlockAdd (line 36, used at CGLS lines 91-92 on s×n and s×m blocks) and BlockZplusT (line 44, CGLS line 107). BlockAdd(Y, T, ±1) is exactly OP.Component addScaledInPlace(y, a, x) (OP.Component.fProxy.cs:85, SIMD, fProxyMxN implements IUnsafefProxyArray); BlockZplusT is a copy + addScaledInPlace or one new fused SIMD add-into-dst kernel next to the existing UnsafeOP fused family. Elementwise maps reorder nothing (and a=±1 multiplies are exact), so this is bit-identical/free — and since these helpers are shared, bcg/bcgrq/bfbcg/bgmres/bfgmres inherit the win.

### [MED/bit-identical] bcraig — Krylov.Block.CRAIG - Cleanup tail redundantly recomputes A X — Rfinal already holds the fresh residual on every post-init path
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIG.fProxy.cs:164
cleanup (line 164) runs a full BlockApplyOp(A, X, Rfinal) + BlockMaxResidualRecompute(B, Rfinal). But on every path that reaches cleanup after the first residual computation — Converged at 118/159, MaxIterations, and ALL in-loop breakdowns at 129/137/144 (which fire BEFORE that round's X update at 147, so X is unchanged since line 155/114 filled Rfinal) — Rfinal already holds B - A X. Replace with the existing BlockMaxResidualNorm(in Rfinal, s, m) (Krylov.Block.Common.fProxy.cs:102, already used by bcgls), tracking a small 'residValid' flag; only the round-1 breakdown / B==0 paths (where X=0, residual = B) need the fallback. Saves s full operator applies per solve. Bit-identical: the stored residual is computed as (-AX)+B (BlockScaleInPlace then BlockAdd, lines 156-157), which IEEE-equals the recompute's per-element B-(AX), and both reductions cast the same fProxy value to double before squaring.

### [MED/bit-identical] bcraig — Krylov.Block.CRAIG - BlockAdd / BlockScaleInPlace / X-zeroing are hand-rolled scalar loops — route through the existing SIMD elementwise kernels, and fuse the residual negate+add into one aypx pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
fProxyMxN implements IUnsafefProxyArray (fProxyMxN.cs:14) and Data.Length == M_Rows*N_Cols is a universal invariant, so OP.Component.fProxy.cs's SIMD extensions already apply to whole contiguous blocks: (a) X zeroing at lines 50-51 -> X.zeroInPlace(); (b) BlockAdd (Krylov.Block.Common.fProxy.cs:36-41, plain scalar 'yp[i] += sign*tp[i]') at call sites 110, 116, 126, 134, 147, 157 -> y.addScaledInPlace(sign, t) (=axpy, SIMD); (c) BlockScaleInPlace (Common:425-430) at 115, 141, 156 -> mulInPlace(scale) (UnsafeOP.scalMul). Additionally fuse the two-pass residual formation at 115-116 and 156-157 (negate Rfinal, then add B) into a single scaleAddInPlace((fProxy)(-1), B) (=aypx) sweep. All elementwise maps -> bit-identical per the determinism rules ((-1)*y is exact, so even the fused form matches). These are O(s*n)/O(s*m) loops run ~5x per round; the fix lives in Common so blsmr/bcraigmr/bcgls benefit too.

### [MED/deterministic-reorder] bcraigmr - Cleanup tail redoes s A.Apply calls although Rfinal already holds a fresh B-AX on the Converged and MaxIterations paths
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIGMR.fProxy.cs:211
Line 211 recomputes BlockApplyOp(A, X, Rfinal) unconditionally, but on the Converged exit (line 184) and the MaxIterations exit, Rfinal already contains a fresh B - A*X computed at lines 180-182 with the final X (Step B at lines 186-206 touches neither X nor Rfinal). Reuse it via the EXISTING BlockMaxResidualNorm helper (Krylov.Block.Common.fProxy.cs:102, already the cleanup reduction for bcgls) instead of BlockMaxResidualRecompute. A freshness flag is REQUIRED, not optional: the init-breakdown paths (lines 116/120), the bSqF==0 path, and a k==0 Step-A breakdown (line 137) reach cleanup with Rfinal stale or uninitialized (allocated uninit:true at line 89) -- keep the recompute on those paths. Saves s Apply calls per solve on the two common exits. The reported maxRnorm diagnostic changes in the last bits (double-precision B-AX subtraction vs the stored fProxy-rounded residual); status/converged unchanged.

### [MED/deterministic-reorder] bcraigmr - Dense-operator fast path: each BlockApplyOp/BlockApplyOpT is s GEMVs with per-row marshalling copies; could be one GEMM
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:300
bcraigmr does 3 block applies per round (Step A line 132, residual check line 180, Step B line 188), each looping s rows through rowN/rowM copy-in, scalar A.Apply, copy-out (Krylov.Block.Common.fProxy.cs:300-325). For the dense forwarder (fProxyDenseOperator, line 234) each whole block apply is one GEMM: AVrows = V*A^T via Blas.dot(in V, in Adense, ref AV, false, true) and ATUrows = U*A via Blas.dot(in U, in Adense, ref ATU, false, false) -- reusing the existing optimized OP.Dot GEMM (register-tiled per gemm-register-tiling work). The per-row route exists because IfProxyLinearOperator.ApplyBlock is documented symmetric/square-only, so this needs a rectangular-dense fast path (e.g. a dense-aware overload or an ApplyBlockRect capability), not a change to the generic fallback. GEMM tiling reorders the dot accumulations vs scalar Apply -> deterministic-reorder (pre-1.0 waiver). Dominant win for dense A; BSR spMM could get the analogous treatment later.

### [MED/bit-identical] bcraigmr - BlockAdd / BlockScaleInPlace scalar loops -> existing SIMD elementwise kernels (bit-identical), BlockFrobDot -> vecDot (reorder)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
Per round bcraigmr calls BlockAdd 6x on s*m / s*n contiguous blocks (lines 134, 169, 176, 182, 190) and BlockScaleInPlace + 2x BlockFrobDot for the residual test. BlockAdd (Krylov.Block.Common.fProxy.cs:36-41) is a scalar 'yp[i] += sign*tp[i]' loop -- exactly OP.Component addScaledInPlace (OP.Component.fProxy.cs:85, backed by SIMD UnsafeOP.axpy at UnsafeOP.fProxy.cs:1746); fProxyMxN implements IUnsafefProxyArray (fProxyMxN.cs:14) so Y.addScaledInPlace(sign, T) is a drop-in and BIT-IDENTICAL (pure elementwise map, no reduction). Same for BlockScaleInPlace (Common:425) -> the SIMD scale kernel, and the X-zeroing loop at lines 47-48 -> zeroInPlace. BlockFrobDot (Common:415) is a sequential scalar accumulation over the contiguous buffer -> UnsafeOP.vecDot (UnsafeOP.fProxy.cs:242, fProxyW/fProxy4 fused kernel) -- that one is a reduction-tree reorder, so DETERMINISTIC-REORDER under the pre-1.0 waiver (matches the reduction-reroute program's two-tier rule). These helpers are shared, so every block solver in the family benefits.

### [LOW/bit-identical] Krylov.CG - Initial-residual block duplicates VerifyTrueResidual — reuse the shared helper
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CG.fProxy.cs:160
Lines 161-163 + 167 (A.Apply(in x, ref Ap); r.CopyFrom(in b); r.addScaledInPlace(-1, Ap); rr = Blas.dot(r, r)) are byte-for-byte the body of Krylov.Solve.Common's VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20-27). Replace with rr = VerifyTrueResidual(in A, in b, in x, ref Ap, ref r) — identical operation sequence and accumulation order, so bit-identical; zero perf change, pure dedup (one fewer hand-rolled copy of the residual recipe to keep in sync).

### [LOW/bit-identical] Krylov.CG - Preconditioned path streams r and z twice per iteration (M.Apply then separate Blas.dot) — an IfProxyPreconditioner.ApplyDot would fuse them
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CG.fProxy.cs:216
Each non-identity iteration does M.Apply(in r, ref z) then Blas.dot(r, z) (lines 216-217, and once at setup lines 180-182) — the dot re-reads both r and z (~2n extra streamed loads per iteration). The operator side already solves exactly this shape: IfProxyLinearOperator.ApplyDot (Interfaces/LinearOperator.fProxy.cs:33), specialized as Blas.dotSelf / BSR.spMVDot. Mirroring it — add ApplyDot to IfProxyPreconditioner with a default Apply+Blas.dot body (bit-identical, per the fused-kernel contract that the fused reduction half matches plain-update-then-vecDot) and specialize where the preconditioner's final write pass can carry the accumulator (block-Jacobi, FSAI's second triangular apply) — would save one 2-vector streaming pass per iteration. NEW interface member (no existing preconditioner-side kernel to reuse), and A.Apply usually dominates traffic on sparse systems, hence low impact; only worth it as part of a wider pass since it touches every IfProxyPreconditioner implementor and every preconditioned solver could then adopt it (pcg-family, fcg).

### [LOW/bit-identical] Krylov.MINRES - Fuse initial residual formation + norm into Blas.axpyNormSq
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRES.fProxy.cs:84
Lines 84+87: `r1.addScaledInPlace((fProxy)(-1), y)` then `trueRR0 = Blas.dot(r1, r1)` is again the axpyNormSq shape — replace with `trueRR0 = Blas.axpyNormSq((fProxy)(-1), y, ref r1)` (bit-identical per the kernel's documented contract). One-time O(n) saving. The identical copy+axpy+dot pattern also lives in the shared VerifyTrueResidual helper (Krylov.Solve.Common.fProxy.cs:23-26); fusing it THERE additionally speeds every solver's verify-at-exit, still bit-identical.

### [LOW/bit-identical] Krylov.MINRES - Replace hand-rolled scalar w/w1/w2 zeroing loop with zeroInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRES.fProxy.cs:115
Line 115 `for (int i = 0; i < A.Rows; i++) { w[i] = 0; w1[i] = 0; w2[i] = 0; }` is a scalar indexer loop over three strided buffers. Replace with `w.zeroInPlace(); w1.zeroInPlace(); w2.zeroInPlace();` (OP.Component.fProxy.cs:16, a single UnsafeUtility.MemClear each). Zero-writing is an elementwise map, so this is bit-identical/free. One-time O(3n) cost, so low impact, but it is the exact hand-rolled-loop-vs-existing-kernel pattern and drops the per-element bounds/indexer overhead.

### [LOW/bit-identical] minresQLP - QLP branch two-term combos (a*x + b*y) are 2 passes each; needs a new combine2 map kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.MINRESQLP.fProxy.cs:290
The QLP right-reflection does four scaledCopy/mulInPlace + addScaledInPlace pairs per iteration (lines 290-297, also 281-284, and the one-time transfer lines 251-260), each two full passes for dst = a*x + b*y. No existing kernel expresses this: combine3 (UnsafeOP.fProxy.cs:1991) forces a unit-coefficient v-slot plus an outer scale s, and rewriting a*x+b*y as s*(x + (b/a)y) changes rounding and divides by a possibly-zero rotation coefficient — hence a NEW elementwise `combine2` kernel (dst[i] = a*x[i] + b*y[i], plain [NoAlias] map like scaledCopy, Burst auto-vectorizes) is the right shape. Per-element arithmetic identical to the two-pass sequence -> bit-identical, halves QLP-update passes. Only worth adding if the QLP phase stays hot; after the line-234 flip it is tail-only, so rank this behind the other three.

### [LOW/bit-identical] Krylov.BiCGStab - Replace the scalar p/v zero-init loop with zeroInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:86
Hand-rolled `for (i...) { p[i]=0; v[i]=0; }` should be `p.zeroInPlace(); v.zeroInPlace();` (OP.Component.fProxy.cs:16, SIMD elementwise map, already used by MINRESQLP:128). Elementwise store reorder — bit-identical, one-time O(n).

### [LOW/bit-identical] Krylov.BiCGStab - Fuse the initial residual build with its norm via Blas.axpyNormSq
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:73
Lines 71-79 do r.CopyFrom(b); r.addScaledInPlace(-1, v); then a separate rr = Blas.dot(r, r) — three passes. Replace the last two with `rr = Blas.axpyNormSq((fProxy)(-1), v, ref r)` (Blas.Fused.fProxy.cs:17), whose stated contract is that the fused reduction is BIT-IDENTICAL to plain-update-then-vecDot; the solver already uses it at lines 121/180. One-time saving of one O(n) pass.

### [LOW/deterministic-reorder] Krylov.BiCGStab - Fuse the two-pass p-update (p -= omega*v; p = beta*p + r) into one pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:99
Lines 99-100 traverse p twice per iteration. The single-pass form p[i] = r[i] + beta*p[i] - (beta*omega)*v[i] matches combine3's shape (UnsafeOP.fProxy.cs:1991, w = s*(v + a*w1 + b*w2)) BUT combine3 cannot be reused as-is: (a) it is [NoAlias] on all pointers and here the destination aliases w1 (p in/out), and (b) the refactored rounding (beta*p - (beta*omega)*v vs beta*(p - omega*v)) differs — so this needs a new in-place alias-tolerant kernel and is a deterministic-reorder change (pre-1.0 waiver), not bit-identical. Saves one O(n) read+write pass over p per iteration.

### [LOW/bit-identical] Krylov.BiCGStab - Fuse the twin x-update (x += alpha*p; x += omega*s) into one pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.BiCGStab.fProxy.cs:170
Lines 170-171 (and 175-176 in the preconditioned branch) make two O(n) read+write passes over x. No existing kernel fits: updateXR (UnsafeOP.fProxy.cs:1914) writes two destinations with one shared scalar; combine3 is [NoAlias]-blocked for an in-place destination. A new kernel x[i] = (x[i] + a*p[i]) + b*q[i] evaluated in exactly that association is bit-identical to the current two sequential axpys while halving x traffic. Only worth doing if the new kernel is shared with other twin-update solvers; otherwise skip.

### [LOW/bit-identical] Krylov.GMRES - v0 normalization: scalar indexer loop -> mulInPlace (UnsafeOP.scalMul)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:83
for (i<n) v0[i] *= invBeta is a hand-rolled scalar loop through the fProxyN indexer. v0.mulInPlace(invBeta) already exists (OP.Component.fProxy.cs:41 -> UnsafeOP.scalMul, UnsafeOP.fProxy.cs:2099) and Burst auto-vectorizes the raw-pointer form. Elementwise scale by the same scalar → bit-identical regardless of order. Once per restart.

### [LOW/bit-identical] Krylov.GMRES - Zeroing loops -> zeroInPlace (MemClear)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:128
for (i<n) w[i] = 0 (line 128, preconditioned solution-update path) should be w.zeroInPlace() (OP.Component.fProxy.cs:16, UnsafeUtility.MemClear). Same for the m+1-element g reset at line 84 (g.zeroInPlace(), tiny but free). Writing the same 0 bits → bit-identical. Once per restart.

### [LOW/bit-identical] Krylov.GMRES - Fuse restart residual axpy + norm via axpyNormSq
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:77
v0.CopyFrom(b); v0.addScaledInPlace(-1, w); beta = sqrt(Blas.dot(v0, v0)) (lines 76-78) — the axpy and the dot are one axpyNormSq(v0, w, -1, n) call (bit-identical per the kernel contract at UnsafeOP.fProxy.cs:1779), saving one full pass over v0. Once per restart; same shape VerifyTrueResidual's callers use.

### [LOW/bit-identical] Krylov.GMRES - x.addScaledInPlace(1, zt) -> x.addInPlace(zt)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:135
Adding with an explicit scale of 1 goes through axpy's per-element multiply; x.addInPlace(zt) (OP.Component.fProxy.cs:67 -> UnsafeOP.compAdd) skips it. 1*z == z exactly in IEEE, so bit-identical. Once per restart; trivial win, mostly intent clarity.

### [LOW/deterministic-reorder] Krylov.GMRES - Solution update: k sequential axpys could be one GEMV if V were stored contiguously
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.fProxy.cs:120
x += sum_i y[i] V[i] is k full-vector axpy passes (lines 120-124 identity path, 129-133 preconditioned) = k reads of n each. Storing V as one contiguous (m+1) x n fProxyMxN (rows = basis vectors) instead of an UnsafeList of separate fProxyN would let this be a single x += V^T y GEMV-style pass and would also improve Arnoldi locality. Per-element accumulation order over i changes (sequential axpy chain -> per-element dot / SIMD tree) → deterministic-reorder, pre-1.0 waiver territory. Once per restart and requires a storage refactor touching ArnoldiMGSStep's V[i] access — only worth it bundled with a broader V-layout change, not standalone.

### [LOW/bit-identical] Krylov.FGMRES - Fuse the last MGS projection axpy with the norm reduction via Blas.axpyNormSq
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs:25
In ArnoldiMGSStep the final projection (i == j) does w.addScaledInPlace(-hij, vi) and then a separate Blas.dot(w, w) at line 27. The i==j step can be Blas.axpyNormSq(-hij, in vi, ref w) instead, whose contract (Blas.Fused.fProxy.cs:15-17) is explicitly bit-identical to addScaledInPlace followed by Blas.dot(y,y) -- same accumulation order, fused into one pass. Saves one full n-length read of w per inner iteration. Shared file: gmres benefits identically.

### [LOW/bit-identical] Krylov.FGMRES - Restart-vector normalize is a hand-rolled scalar indexer loop -> fProxyComp.mulInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FGMRES.fProxy.cs:90
`for (int i = 0; i < n; i++) v0[i] *= invBeta;` goes element-by-element through the fProxyN indexer. Replace with v0.mulInPlace(invBeta) (OP.Component.fProxy.cs:41 -> UnsafeOP.scalMul, [NoAlias] pointer loop that Burst auto-vectorizes). The kernel performs the identical `target[i] *= s` per element -> bit-identical (elementwise map). Once per restart only, so low impact; mirror the identical line in Krylov.GMRES.fProxy.cs:83 to keep the two templates in lockstep. (The g-clear loop at line 91 could likewise be g.zeroInPlace(), negligible.)

### [LOW/bit-identical] Krylov.FGMRES - Restart residual computation duplicates VerifyTrueResidual -- reuse it (also the hook for the verify-at-exit fix)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.FGMRES.fProxy.cs:82
Lines 82-85 (A.Apply(x, w); v0.CopyFrom(b); v0.addScaledInPlace(-1, w); beta = sqrt(Blas.dot(v0, v0))) are op-for-op the shared VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20-27) with Ax=w, r=v0. Rerouting is a pure DRY/reuse change (identical operation sequence -> bit-identical) and puts the exact helper needed for the false-Converged fix (correctness finding 1) already in the call pattern. Same duplication exists in gmres.

### [LOW/bit-identical] Krylov.IDR - Reuse Blas.updateXR for the twin x/R update at both convergence sites
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.IDR.fProxy.cs:164
Lines 164-165 (rr = Blas.axpyNormSq(-beta, Gk, ref R); x.addScaledInPlace(beta, Uk)) and lines 217-219 (rr = Blas.axpyNormSq(-om, Q, ref R); x.addScaledInPlace(om, V or VHat)) are exactly the existing CG-family twin update Blas.updateXR(a, p, ref x, q, ref R) = x += a*p; R -= a*q; return ||R||^2 (Blas.Fused.fProxy.cs:40, UnsafeOP.fProxy.cs:1914), whose doc guarantees bit-identity to addScaledInPlace + addScaledInPlace(-a) + Blas.dot. Gain is call/code-shape only (updateXR's x-half is its own loop, so memory traffic is unchanged) — worthwhile mainly for consistency with the other Krylov bodies.

### [LOW/bit-identical] Krylov.IDR - Fuse the paired V/Q (and Gk/Uk) axpys sharing one scalar into a twin-axpy kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.IDR.fProxy.cs:121
Lines 121-125 do V.addScaledInPlace(c[i], G[i]) and Q.addScaledInPlace(c[i], U[i]) as 2(s-k-1) separate passes, and lines 149-150 do the same Gk/Uk pair with -alpha. A small new UnsafeOP twin-axpy (updateXR's two-destination shape minus the reduction: y1 += a*x1; y2 += a*x2 in one call) keeps the exact per-element order and per-i sequence, so it is bit-identical; it halves loop/call overhead and lets Burst interleave two independent streams. No existing kernel fits (updateXR forces a subtract plus an unwanted ||r||^2 reduction), hence the new-kernel justification.

### [LOW/bit-identical] Krylov.IDR - nt2 = dot(Q,Q) and ts = dot(Q,R) are two passes over Q; a fused dual-reduction saves one
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.IDR.fProxy.cs:204
Lines 198 and 204 stream Q twice per sweep for the omega computation. A new fused kernel computing both sums in one pass, each accumulator keeping vecDot's existing fProxyW/fProxy4 tree independently (same pattern axpyNormSq already uses for its fused half), is bit-identical to the two separate Blas.dot calls and removes one n-length traversal per sweep. Combined with the ns2 = rr fix above, the end-of-sweep step drops from four reductions over n to two.

### [LOW/deterministic-reorder] Krylov.TFQMR - Odd-tail v update: 3 passes -> 2 by using the nested Horner form with the existing scaleAddInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.TFQMR.fProxy.cs:165
Lines 165-169 compute v = beta^2*v + beta*au_old + au_new as three passes over v (mulInPlace, addScaledInPlace, addInPlace). Freund's/scipy's nested form v = au_new + beta*(au_old + beta*v) needs only two applications of the EXISTING scaleAddInPlace (aypx) kernel: v.scaleAddInPlace(beta, au) before the au refresh (v = beta*v + au_old), then v.scaleAddInPlace(beta, au) after it (v = beta*(...) + au_new) — same buffers, no extra scratch, and arguably more faithful to the reference recurrence. Saves one full pass over v every full step (every 2 half-steps). Not bit-identical: same algebra, different elementwise rounding order (no reduction is reordered, but bits change), so it falls under the documented deterministic-change waiver.

### [LOW/bit-identical] Krylov.TFQMR - Hand-rolled scalar zero loop for d should be d.zeroInPlace()
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.TFQMR.fProxy.cs:101
for (int i = 0; i < A.Rows; i++) d[i] = 0 is a scalar loop; OP.Component.fProxy.cs:16 provides the SIMD zeroInPlace elementwise map used elsewhere. Zeroing is an elementwise map, so the reroute is bit-identical (free). Setup-only cost, so impact is small — this is mainly kernel-reuse consistency.

### [LOW/bit-identical] Krylov.TFQMR - rhoLast is always equal to rho at its only use — drop the variable
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.TFQMR.fProxy.cs:152
rho is only reassigned at the odd tail (line 162), and every odd half-step is immediately preceded by an even tail that sets rhoLast = rho (line 152), with rhoLast also initialized to rho (line 105). So at line 159 beta = rhoNew/rhoLast, rhoLast == rho identically — the variable and the line-152 store are dead weight, and the line-157 rhoLast==0 guard is unreachable in a distinct way (rho==0 would already have produced alpha==0 -> Breakdown at the preceding even step). Replacing rhoLast with rho is a pure simplification, bit-identical; keep the ==0/isnan guard on rho itself.

### [LOW/bit-identical] Krylov.gcrodr - Scalar normalize loops -> mulInPlace / scaledCopy SIMD kernels
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:149
Line 149 'for (i) v0[i] *= invBeta' is a hand-rolled scalar loop; OP.Component's mulInPlace(this T, fProxy s) (OP.Component.fProxy.cs:41) is the SIMD elementwise kernel for exactly this -- elementwise map, bit-identical. Lines 195-196 do vj1.CopyFrom(w) then the same scalar scale loop -- replace both passes with one Blas.scaledCopy(invh, w, ref vj1) (Blas.Fused.fProxy.cs:53, [NoAlias] kernel, w and vj1 are distinct), the pattern MINRES already uses (Krylov.MINRES.fProxy.cs:129). Same per-element product, bit-identical. (gmres shares the line-149 pattern at its line 83 -- same fix applies there.)

### [LOW/deterministic-reorder] Krylov.gcrodr - Breakdown-path residual: reuse VerifyTrueResidual instead of the hand-rolled scalar loop
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GCRODR.fProxy.cs:243
Lines 243-246 hand-roll A.Apply + a scalar sequential 'rr2 += d*d' loop. This is exactly the shape the DRY pass extracted into VerifyTrueResidual (Krylov.Solve.Common.fProxy.cs:20) -- call it with V[0] and w as the two distinct scratch vectors (both dead on this path). Replaces the scalar sequential sum with the SIMD vecDot tree: deterministic-reorder (pre-1.0 waiver), plus consistency with the cg/minres/idr family. Note the shared-Arnoldi helpers themselves (ArnoldiMGSStep/GivensApplyAndGenerate/HessenbergBackSolve) are deliberately NOT reused here per the documented DEVLOG ruling ('leave on its own copy', whole-solver-level decision) -- not re-proposed.

### [LOW/bit-identical] Krylov.CRAIG - Scalar zeroing loop for x should reuse the SIMD zeroInPlace kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIG.fProxy.cs:59
Line 59 `for (int i = 0; i < x.N; i++) x[i] = (fProxy)0;` is a scalar indexer loop (per-element bounds-checked); replace with `x.zeroInPlace()` (OP.Component.fProxy.cs:16, generic over IUnsafefProxyArray, SIMD memclear) — same pattern MINRESQLP uses (Krylov.MINRESQLP.fProxy.cs:128). Writing zeros is elementwise, so bit-identical. Once per solve, O(n).

### [LOW/bit-identical] Krylov.CRAIG - Scalar zeroing loop for v should reuse zeroInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIG.fProxy.cs:72
Line 72 `for (int j = 0; j < v.N; j++) v[j] = (fProxy)0;` — same as above: replace with `v.zeroInPlace()`. Bit-identical, once per solve.

### [LOW/unknown] Krylov.CRAIG - u init: CopyFrom + divInPlace is two full passes; Blas.scaledCopy is one fused pass
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIG.fProxy.cs:67
Lines 67-68 `u.CopyFrom(in b); u.divInPlace(bnorm);` traverse the length-m buffer twice. `Blas.scaledCopy(1/bnorm, b, ref u)` (Blas.Fused.fProxy.cs:53 → UnsafeOP.scaledCopy SIMD) does it in one pass — the exact idiom MINRES/MINRESQLP already use for normalization (Krylov.MINRES.fProxy.cs:129 `Blas.scaledCopy(1/beta, r2, ref v)`). CAVEAT on determinism category: this swaps per-element division for multiply-by-reciprocal — not a reorder (still elementwise, cross-arch deterministic under IEEE-strict), but NOT bit-identical to current output (different rounding), and the changed u_1 propagates through the whole solve. One-time cost so perf gain is marginal; take it only for idiom consistency with MINRES, or add a dividing scaledCopy variant if bit-compat with current output matters.

### [LOW/bit-identical] Krylov.CRAIGMR - Init writes Aᵀu into tmpN then copies to v — ApplyT can target v directly
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIGMR.fProxy.cs:75
Lines 75-76: A.ApplyT(in u, ref tmpN); v.CopyFrom(in tmpN). Unlike the in-loop GolubKahanVStep (Krylov.Bidiag.Common.fProxy.cs:28-33), which genuinely needs tmpN as the ApplyT destination before the fused xpayNormSq into v, the init step has no -beta*v term, so ApplyT can write straight into v: A.ApplyT(in u, ref v); alpha = math.sqrt(Blas.dot(v, v)). Drops one n-length copy, one time. Bit-identical (same values land in v, Blas.dot already routes to the SIMD UnsafeOP.vecDot via OP.Dot.fProxy.cs:22). u (input) and v (output) are guaranteed distinct by the line-57 RequireDistinctBuffers guard. Note Krylov.LSQR.fProxy.cs:85-86 has the identical pattern — if fixed here, fix the sibling in the same pass.

### [LOW/bit-identical] Krylov.CRAIGMR - Scalar indexer zero-loops for x and d should use the SIMD zeroInPlace kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIGMR.fProxy.cs:61
Lines 61-62 zero x and d with per-element indexer loops (for (i...) x[i] = 0), going through the fProxyN indexer instead of the existing SIMD elementwise map. The generic zeroInPlace<T> (OP.Component.fProxy.cs:16) exists exactly for this and is already the house pattern in Krylov.MINRESQLP.fProxy.cs:128 (w.zeroInPlace(); wl.zeroInPlace(); ...). Replace with x.zeroInPlace(); d.zeroInPlace(). Writing zeros is trivially bit-identical. One-time cost only (not per-iteration), hence low impact — but it is a straight reuse of a shared kernel over a hand-rolled scalar loop. Krylov.CRAIG.fProxy.cs:59,72 has the same pattern if a sibling sweep is done.

### [LOW/deterministic-reorder] Krylov.CRAIGMR - Optional: u = b/bnorm via one-pass Blas.scaledCopy instead of CopyFrom + divInPlace (rounding change)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.CRAIGMR.fProxy.cs:70
Lines 70-71: u.CopyFrom(in b); u.divInPlace(bnorm) is two passes over m. Blas.scaledCopy(1/bnorm, b, ref u) (Blas.Fused.fProxy.cs:53 → UnsafeOP.scaledCopy, the pattern Krylov.MINRES.fProxy.cs:129 already uses for exactly this normalize-into) does it in one pass — but it replaces per-element IEEE division (UnsafeOP.scalDiv at UnsafeOP.fProxy.cs:2114 genuinely does target[i] /= s) with multiply-by-reciprocal, which is deterministic but NOT bit-identical (up to 1-ulp-ish per element, and it perturbs the whole downstream Krylov trajectory). One-time init cost, so the win is marginal; Krylov.CRAIG.fProxy.cs:67-68 and LSQR use the same CopyFrom+divInPlace idiom, so leaving it as-is for cross-solver consistency is also defensible. Flagged for completeness; only worth doing as a deliberate library-wide normalize-convention decision under the pre-1.0 waiver, not as a local tweak.

### [LOW/bit-identical] Krylov.LSQR - Fuse the initial residual build u = b - A*x with its norm via Blas.xpayNormSq (3 m-length passes -> 1)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSQR.fProxy.cs:72
Lines 72-76 do A.Apply(x, tmpM); u.CopyFrom(b); u.addScaledInPlace(-1, tmpM); beta = sqrt(Blas.dot(u,u)) — a copy pass, an axpy pass, and a dot pass over length m. Replace with A.Apply(in x, ref u); beta = math.sqrt(Blas.xpayNormSq((fProxy)(-1), b, ref u)) (Blas.Fused.fProxy.cs:27, kernel UnsafeOP.xpayNormSq). u = (-1)*u + b equals b + (-1)*tmpM bit-for-bit (exact *(-1), commutative IEEE add), and the kernel's documented contract says the fused reduction is bit-identical to scaleAddInPlace + Blas.dot. Also frees tmpM at init. Init-only cost, hence low impact.

### [LOW/bit-identical] Krylov.LSQR - Redundant init copy: ApplyT into tmpN then v.CopyFrom(tmpN) — apply directly into v
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSQR.fProxy.cs:85
Lines 85-86: A.ApplyT(in u, ref tmpN); v.CopyFrom(in tmpN). Unlike the in-loop GolubKahanVStep (which needs tmpN because v holds prior state for the fused xpay), at init v holds nothing — A.ApplyT(in u, ref v) produces the identical bytes and drops one full n-length copy pass. v is already guaranteed distinct from u by RequireDistinctBuffers (line 55).

### [LOW/bit-identical] Krylov.LSQR - Scalar element-loop zeroing -> existing SIMD zeroInPlace (3 sites)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSQR.fProxy.cs:64
Line 64 (for i: x[i]=0 in the atbSq==0 early exit), line 304 and line 324 (lsqrJacobi dense/BSR cold-start zeroing) hand-roll per-element indexer loops. Reuse the existing SIMD kernel x.zeroInPlace() (OP.Component.fProxy.cs:16). Zeroing is elementwise so the reroute is bit-identical/free per the determinism rules; also avoids the per-element indexer bounds path.

### [LOW/bit-identical] Krylov.LSQR - Per-iteration x += (phi/rho)*w and w = -(theta/rho)*w + v each traverse w — fusable into one pass (new kernel)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSQR.fProxy.cs:151
Lines 151-152 read w twice per iteration (addScaledInPlace then scaleAddInPlace over length n). No existing fused kernel matches: Blas.updateXR (Blas.Fused.fProxy.cs:40) has the wrong shape (its second update writes r from a distinct q AND folds a reduction; here w is an aypx onto itself with no reduction wanted). A small new UnsafeOP kernel updateXW(a, ref x, b, ref w, in v): { x[i] += a*w_old; w[i] = b*w_old + v[i]; } would halve w traffic; both updates are elementwise with no reduction, so fusion reorders nothing — bit-identical. Impact is low because each iteration is dominated by the Apply + ApplyT matvecs (this saves ~1 of ~6 vector passes; only matters for very sparse A where nnz ~ n). Same pattern exists in LSMR if adopted.

### [LOW/bit-identical] Krylov.LSMR - Init residual: reuse Blas.xpayNormSq instead of Apply→tmpM + CopyFrom + addScaledInPlace + separate dot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSMR.fProxy.cs:78
Lines 78-82 compute u = b - A·x as A.Apply(x, ref tmpM); u.CopyFrom(b); u.addScaledInPlace(-1, tmpM); beta = sqrt(Blas.dot(u,u)) — four passes over m-length data after the Apply. Replace with A.Apply(in x, ref u); beta = math.sqrt(Blas.xpayNormSq((fProxy)(-1), b, ref u)) — the exact fused kernel GolubKahanUStep already uses (Krylov.Bidiag.Common.fProxy.cs:23). xpayNormSq's documented contract (Blas.Fused.fProxy.cs:25-26) is bit-identical to scaleAddInPlace + dot, and b + (-1)*Ax vs (-1)*Ax + b is the same mul+add rounding under FloatMode strict (no fma contraction), so the change is bit-identical. Init-only, hence low impact; lsqr lines 72-76 have the identical pattern if this is applied family-wide.

### [LOW/bit-identical] Krylov.LSMR - Redundant tmpN round-trip at v init: ApplyT directly into v
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSMR.fProxy.cs:91
Lines 91-92: A.ApplyT(in u, ref tmpN); v.CopyFrom(in tmpN). RequireDistinctBuffers (line 61) already guarantees v is distinct from u, so A.ApplyT(in u, ref v) writes v directly and drops one full n-length copy. Trivially bit-identical (same values, one fewer memcpy). Init-only. Same pattern exists in lsqr lines 85-86.

### [LOW/bit-identical] Krylov.LSMR - Hand-rolled scalar zero loops should reuse the SIMD zeroInPlace kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LSMR.fProxy.cs:108
Line 108 (for i: hbar[i]=0), line 70 (x zeroing on the atbSq==0 early-out), and the cold-start x zeroing in lsmrJacobi at lines 347 and 367 are scalar indexer loops. OP.Component.fProxy.cs:16 zeroInPlace is the existing SIMD elementwise kernel for exactly this; elementwise stores are bit-identical by the project's own rules. All are one-time init code, so impact is low, but it is direct existing-kernel reuse and drops the per-element bounds-checked indexer.

### [LOW/bit-identical] Krylov.Block.CG - CopyBlock / CopyMat scalar element copies -> UnsafeUtility.MemCpy
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:147
CopyBlock and CopyMat (Krylov.Block.Common.fProxy.cs:147-157) are scalar per-element copies; bcg calls them at lines 79/85 (P = Z init), and per iteration at 95, 115 (coef <- RZ/RZnew, s x s) and 124 (RZ <- RZnew). All operands are contiguous full buffers, so both collapse to one UnsafeUtility.MemCpy. Pure copy -> bit-identical. The s x s ones are tiny; the s x n CopyBlock init and the shared use inside BlockLSResolveAndCheck (Common:526) are the ones that matter.

### [LOW/bit-identical] Krylov.Block.CG - BlockApplyPre row marshalling: scalar copy loops -> MemCpy (rows are contiguous)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:139
BlockApplyPre (Krylov.Block.Common.fProxy.cs:139-143) copies each R row into rowIn and rowOut back into Z with scalar loops, 2*s row copies of length n per iteration in the preconditioned path (bcg:84,110). Row j of the row-major block is contiguous, so both copies are UnsafeUtility.MemCpy; the M.Apply call itself is untouched -> bit-identical. (A fProxyN row-view over the block would eliminate the copies entirely, but only if a non-owning vector view exists; MemCpy is the safe rewire.)

### [LOW/deterministic-reorder] Krylov.Block.CG - Identity path recomputes s row norms that diag(RZnew) already contains
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CG.fProxy.cs:107
Under the identity preconditioner, each iteration computes per-row ||R[j]||^2 twice: once scalar in CountConverged (bcg:102) and again as the diagonal of RZnew = R R^T via the GEMM BlockGram (bcg:107). Reading diag(RZnew) for the convergence test would drop one full s-x-n reduction pass per iteration, but requires hoisting the Gram above the convergence check and swaps the scalar-sequential sums for GEMM-ordered ones -> deterministic-reorder, and it forces one extra (wasted) Gram on the final iteration. Only worth it if the block-CG comparison pass (task #25) shows CountConverged on the profile; otherwise skip.

### [LOW/bit-identical] Krylov.Block.BCGrQ - FactorLiveResidual allocates a Temp Pivot every call (2x per iteration cadence) — hoist into bcgrq's scratch
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BCGrQ.fProxy.cs:138
Each call at lines 90 and 138 news a Pivot(sLive, Allocator.Temp) inside FactorLiveResidual (Krylov.Block.Common.fProxy.cs:230) whose contents are discarded (only the L diagonal is used for rank). Allocate one Pivot(s) next to thr/Live/alphaBuf (line 53-60) and pass it by ref, or reuse a max-size persistent pivot. Temp bump-allocs are cheap so impact is low, but it is the only remaining per-iteration allocation in the solver. Bit-identical.

### [LOW/bit-identical] Krylov.Block.BFBCG - R = B - A X setup and cleanup residual use scalar indexer double-loops — reuse OP.Component.subInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BFBCG.fProxy.cs:82
Lines 82-83 (R = B - AP) and 191-193 (Rfinal = B - Rfinal) are elementwise scalar loops through the [i,c] indexer over the full s x n block. fProxyMxN implements IUnsafefProxyArray (fProxyMxN.cs:14), so the SIMD elementwise kernels apply directly: copy B into the destination (CopyBlock/MemCpy) then subInPlace(AP) (OP.Component.fProxy.cs:76), or add a dst=a-b variant next to BlockZplusT in Common. Elementwise subtraction with unchanged per-element operands -> bit-identical. Each site runs once per solve, so impact is modest; the same treatment applies to Common's BlockAdd/BlockZplusT/CopyBlock scalar loops (Common:36-49,147-151), which DO run per iteration and map to addScaledInPlace/MemCpy — worth folding into the same pass (shared with bcgrq/bgmres et al.).

### [LOW/bit-identical] Krylov.Block.BFBCG - beta sign-flip scalar double-loop — one mulInPlace(-1) on the view
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BFBCG.fProxy.cs:160
Lines 160-162 negate the saSearch x sLive beta with a scalar [i,j] indexer loop. The RectView is contiguous, so OP.Component.mulInPlace(beta, (fProxy)(-1)) (OP.Component.fProxy.cs:41) does it in one SIMD call; IEEE negation is exact -> bit-identical. Tiny block (<= s x s), so this is a readability/consistency win more than a measured one.

### [LOW/deterministic-reorder] Krylov.Block.MINRES - Phibar RHS update multiplies a half-zero 2s x s stack through a full 2s x 2s GEMM
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:382
Lines 382-387: PhibarStack is zeroed, Phibar copied into its top s rows, then Res2 = OmegaNew^T * PhibarStack. The bottom s rows are always exactly zero, so Res2 == (OmegaNew's top s rows)^T * Phibar: Blas.dot(RowsView(OmegaNew, s), Phibar, Res2, true, false) halves the GEMM's k-dimension and deletes the per-iteration zero-fill + CopyRowsAt. Dropping the exact-zero accumulation terms changes the accumulation tree (and can flip a -0.0), so deterministic-reorder.

### [LOW/deterministic-reorder] Krylov.Block.MINRES - thr init and CountConverged row sums are scalar sequential — reroute to the fused addSquares/vecDot kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:287
Lines 284-289 (thr[j] from a scalar sum of B[j,c]^2) and Krylov.Block.Common.fProxy.cs:120-133 (CountConverged's per-row rr) both walk contiguous rows scalar-sequentially; rows of a row-major fProxyMxN are contiguous, so each is one call to the existing SIMD reduction (UnsafeOP addSquares / Blas.vecDot on a row pointer). Sum-tree reorder = deterministic-reorder under the pre-1.0 waiver. Low impact here (entry + probe-confirm + cleanup, not the inner loop), but CountConverged is shared by the whole block family so the win multiplies.

### [LOW/bit-identical] Krylov.Block.MINRES - Per-iteration scalar zero-fills could use the zeroInPlace SIMD map
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.MINRES.fProxy.cs:362
M2's full 2s x 2s zero (lines 362-363, per iteration), the W/W1/W2 seed zeroing (332-333, once), and the Vout row-zeroing inside BlockNormalizeIdentity (70-72) are scalar loops; OP.Component zeroInPlace / a memclear over the contiguous block is bit-identical (elementwise). Small absolute win (s^2 / s*n once), listed for completeness while touching these lines anyway.

### [LOW/bit-identical] Krylov.Block.BiCGStab - Initial residual and shadow-residual/P init use scalar double loops — use MemCpy + the SIMD compSub instead
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.BiCGStab.fProxy.cs:134
Lines 134-135 (R = B - T) are a scalar per-element loop: replace with UnsafeUtility.MemCpy(R <- B) then R.subInPlace(T) (OP.Component:76-81 -> UnsafeOP.compSub, SIMD elementwise -> bit-identical). Likewise CopyBlock (Common:147-151), called at lines 140-141 for Rhat0/P init, is a scalar double loop over a contiguous block -> UnsafeUtility.MemCpy (pure copy, bit-identical). One-time cost per solve, so low impact, but zero-risk.

### [LOW/bit-identical] Krylov.Block.BiCGStab - BlockZplusT (P := R + Tmp, 1x/iteration) is a scalar loop — MemCpy + SIMD compAdd
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:44
BlockZplusT (Common:44-49), called at BiCGStab line 211 for the search-direction update, is `dp[i] = zp[i] + tp[i]` scalar over s*n. Replace with UnsafeUtility.MemCpy(dst <- Z) then dst.addInPlace(T) (OP.Component:67-73 -> UnsafeOP.compAdd). Elementwise -> bit-identical. Also used by the bcg-family P updates, so it compounds with the BlockAdd reroute.

### [LOW/bit-identical] Krylov.Block.BiCGStab - BlockApplyPre marshals each row element-by-element — the rows are contiguous, use MemCpy for the in/out copies
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:135
BlockApplyPre (Common:135-145), called twice per iteration in the preconditioned path (BiCGStab lines 152, 179), copies R[i,:] -> rowIn and rowOut -> Z[i,:] with scalar per-element loops. Row-major rows are contiguous, so both copies are UnsafeUtility.MemCpy of n elements (pure copies -> bit-identical). Only matters for the non-identity-preconditioner path, and M.Apply usually dominates, hence low. BlockScaleInPlace(Rhs, -1) at BiCGStab line 202 is the same scalar-loop pattern but only s x s — routing it through the SIMD elementwise scale (mulInPlace) is bit-identical but negligible.

### [LOW/deterministic-reorder] Krylov.Block.IDR - BlockFrobDot is a scalar sequential reduction — reroute to UnsafeOP.vecDot over the contiguous block
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:415
BlockFrobDot (Common:415-422) is 'acc += up[i]*vp[i]' over the contiguous m*n block — exactly UnsafeOP.vecDot(up, vp, len) (UnsafeOP.fProxy.cs:242, SIMD). bidr calls it at :53 (setup) and three times per end-of-sweep step (:251 Q.Q, :255 Q.R, :256 R.R). Category: DETERMINISTIC-REORDER (sequential -> SIMD tree sum). Low impact for bidr (only 3 calls per s+1 iterations) but shared with bbiCGStab/btfqmr omega steps.

### [LOW/deterministic-reorder] Krylov.Block.IDR - ns2 = BlockFrobDot(R,R) at end-of-sweep recomputes the O(m*n) residual norm CountConverged just computed
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.IDR.fProxy.cs:256
R is unchanged between the CountConverged call at :225 (which computed every per-row rr) and the ns2 = BlockFrobDot(R,R) at :256; ns2 is just the sum of those per-row rr values. Extending CountConverged with an out total (sum of row rr, sequential over rows) lets bidr drop the extra O(m*n) pass. Category: DETERMINISTIC-REORDER only in the sense that sum-of-row-sums groups differently than the current single sequential pass over the flat buffer (row-boundary regrouping); if instead CountConverged's row order is summed sequentially, the change is a fixed documented grouping, still not bit-identical to today's flat pass. Minor win: one m*n pass per sweep.

### [LOW/bit-identical] Krylov.Block.IDR - Initial residual R = B - A*X formed with a scalar indexer loop — one aypx elementwise kernel call
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.IDR.fProxy.cs:134
Lines 134-135 do 'R[i,col] = B[i,col] - R[i,col]' element-by-element via the indexer. Since fProxyMxN implements IUnsafefProxyArray, this is exactly the existing SIMD scaleAddInPlace (y = a*y + x with a = -1; OP.Component.fProxy.cs:94) applied once to the whole contiguous block: R.scaleAddInPlace((fProxy)(-1), B). Elementwise map -> BIT-IDENTICAL. One-time cost per call, so low impact, but it is a pure reuse of an existing kernel (scalar idr already uses the fProxyN twin of this exact pattern at Krylov.IDR.fProxy.cs:87).

### [LOW/deterministic-reorder] bgmres - CountConverged and the thr setup do scalar sequential row sums-of-squares; reroute to the SIMD dot kernel
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:120
CountConverged (Common:120-133, called twice per cycle + at exit) and the per-column threshold loop (GMRES.fProxy.cs:97-102, once) accumulate rr += R[j,c]² through the 2D indexer, scalar-sequentially. Each row is contiguous, so this is one Blas.vecDot (UnsafeOP fused SIMD, 2×fProxy4 accumulators) per row — the same reroute already done for StatsCore/NormsOP row norms. Tree reduction reorders the sum → deterministic-reorder (pre-1.0 waiver required, and thresholds/convergence counts can flip in the last ulp). O(s·n) per call vs the O(m·s·n) Arnoldi, hence low impact — but the change lands in Common and pays across all block solvers using CountConverged.

### [LOW/bit-identical] bgmres - Indexer-based zero/copy marshalling: ZeroPrefix full-extent clears, CopyBlock row copies, BlockApplyPre row staging
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GMRES.fProxy.cs:123
ZeroPrefix at GMRES.fProxy.cs:123-124 covers Hbuf/Gbuf's ENTIRE contiguous buffer and at line 181 all of Wcombo — replace with OP.Component zeroInPlace (OP.Component.fProxy.cs:16, SIMD/memclear) instead of an M×N indexer loop. CopyBlock inside BlockLSResolveAndCheck (Common:526, every step) copies contiguous rows from Hbuf's m·s stride into packed HQ — per-row UnsafeUtility.MemCpy. BlockApplyPre (Common:135-145, the whole preconditioned path) stages each contiguous row through rowIn/rowOut with scalar element loops — both copies are straight MemCpy. All are elementwise moves of identical values → bit-identical.

### [LOW/deterministic-reorder] bgmres - Every-step from-scratch dense re-QR of the growing block Hessenberg — O((j·s)³) per step, O(m⁴s³) per cycle
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:517
BlockLSResolveAndCheck re-factors the full accumulated prefix every inner step (GMRES.fProxy.cs:168; the 'periodic' in the docstring is in practice every step). Cost sums to ~m⁴s³/4 per cycle, vs the Arnoldi's 2m²s²n — negligible for large n but dominant for small-n/large-m or large-s configurations. The docstring declares 'not incremental block-Givens' as a deliberate design choice, so this is a flag-for-re-evaluation, not a defect: an incremental block-Givens/Householder update touching only the new block column (the shared GivensApplyAndGenerate machinery scalar gmres uses is the s=1 special case) drops it to O((ms)²s) per step. Reformulating the LS factorization reorders the arithmetic → deterministic-reorder, needs the documented waiver. The cheap half of this win (skip the triUpper Y-solve on non-final steps) is bit-identical and folded into the redundant-GEMM finding above.

### [LOW/bit-identical] bfgmres - R0 = B - A X residual formation is an indexer-based scalar double loop (twice)
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:114
Lines 114-115 (per restart cycle) and 216-217 (exit recompute) do R0[i,c] = B[i,c] - R0[i,c] through the bounds/arena-checked indexer over a fully contiguous s x n block. This is a pure elementwise map, so a flat raw-pointer/SIMD version is bit-identical (no reduction). No existing kernel has the reversed dst = src - dst shape (OP.Component subInPlace is dst -= src), so add a tiny shared unsafe helper in Krylov.Block.Common.fProxy.cs styled after BlockZplusT (Common:44-49) — every block solver (bgmres, bbiCGStab, ...) hand-rolls this same loop, so it is also a DRY candidate in the task #57 mold. Per-cycle cost is only O(s*n), hence low impact, but the raw-pointer hoist campaign showed indexer loops carry ~20x constant-factor overhead.

### [LOW/deterministic-reorder] bfgmres - Per-row ||B[j]||^2 threshold loop should reuse the SIMD dot kernel on contiguous rows
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:97
Lines 97-102 accumulate bb += B[j,c]*B[j,c] scalar-sequentially; fProxyMxN is row-major so row j is a contiguous unit-stride span — this is exactly the UnsafeOP.fProxy.cs vecDot/addSquares SIMD reduction exposed via Blas (LQRP.fProxy.cs's own rowSumSq at line 40 already does the raw-pointer multi-accumulator version of this). Runs once per solve (O(s*n)), so low impact. Deterministic-reorder: sequential sum -> SIMD tree changes the reduction order (pre-1.0 waiver). The same shape lives in Common's CountConverged (Krylov.Block.Common.fProxy.cs:120-133, called at lines 117/218 here) — reroute both together or neither, since thr and the convergence sums should stay consistent in rounding behavior; note OP/DEVLOG.md:14-15 records this rr += shape as deliberately uniform across the block family, so do it as a family-wide pass, not a one-file edit.

### [LOW/bit-identical] bfgmres - Full-extent ZeroPrefix calls should be zeroInPlace/MemClear
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:123
ZeroPrefix(ref Hbuf, (m+1)*s, m*s) and ZeroPrefix(ref Gbuf, (m+1)*s, s) (123-124, per restart cycle) and ZeroPrefix(ref Wcombo, s, n) (187, per commit) all zero the buffer's ENTIRE true extent (rows*cols == allocated dims), yet Common's ZeroPrefix (Krylov.Block.Common.fProxy.cs:455-460) walks the checked indexer element by element. When the extent equals the full buffer these are contiguous and the existing OP.Component zeroInPlace SIMD kernel (or UnsafeUtility.MemClear) is a drop-in, bit-identical replacement (writing zeros is order-free). Keep ZeroPrefix for genuinely partial extents.

### [LOW/bit-identical] bfgmres - Ppiv0 Pivot allocated per restart cycle; BlockApplyPre row marshalling is indexer-copied
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.FGMRES.fProxy.cs:126
Two allocation/copy nits: (a) `new Pivot(s, Allocator.Temp)` at line 126 is allocated and disposed once per restart cycle (and Common's BlockArnoldiMGS2Step does the same per Arnoldi step at Common:491) — LQRP.decomp fully initializes the pivot, so a single Pivot(s) hoisted above the while loop can be reused; Temp disposal is a near-no-op, so this is Temp-arena growth hygiene on long solves, not speed. (b) BlockApplyPre (Common:135-145, called at line 158 every non-identity Arnoldi step) copies each contiguous row in and out through the checked indexer; both copies are UnsafeUtility.MemCpy candidates (bit-identical, pure copies). Both changes are shared-code edits affecting the whole block family, not just bfgmres.

### [LOW/bit-identical] bgcrodr — Krylov.Block.GCRODR - Residual formation R0 = B - A*X is a hand-rolled scalar double loop, 3 sites — one contiguous SIMD aypx call
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:208
Lines 208-209, 224-225, and 575-577 compute R0[i,c] = B[i,c] - R0[i,c] elementwise in scalar. R0 and B are contiguous s x n blocks, and y = -y + x is exactly the existing SIMD scaleAddInPlace(=aypx) kernel (OP.Component.fProxy.cs) applied to the flat span with a = -1. Elementwise map, no reduction => bit-identical. Same pattern exists in bgmres, so the reroute belongs in a shared Common helper.

### [LOW/deterministic-reorder] bgcrodr — Krylov.Block.GCRODR - B scanned twice for bbFrob and per-row thresholds
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:187
Lines 187-190 scan all of B for bbFrob, then lines 192-197 rescan it per row for thr[j]. One pass computing per-row bb (ideally via the fused Blas.vecDot / addSquares row kernels, UnsafeOP.fProxy.cs) and accumulating bbFrob += bb halves the traffic; accumulating row subtotals re-associates the global sum => deterministic-reorder (bbFrob only feeds pivotGuard, a threshold). Trivial cost either way; only worth doing while touching the file.

### [LOW/bit-identical] bgcrodr — Krylov.Block.GCRODR - ~17 Temp allocations per deflation cycle could be hoisted to once-per-solve max-size buffers
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.GCRODR.fProxy.cs:430
Fmat/Gmat/Pgram/GmatLU/Xsol/evReal/evImag/keys/selIdx/Zsel/Ntheta/evTmp/Vtmp/AUblk/Unew/AUraw/Rnew are allocated and disposed inside every cycle's deflation block (lines 421-568). d is bounded by recycle + m*s, so all can be allocated once before the while loop at dMax and narrowed per cycle with the existing View/RowsView/RectView helpers (square d x d views are safe: leading d*d elements, full rewrite each use — but note any PARTIAL-write buffer would hit the line-390 stride trap, so keep Zsel/AUraw exact-size or fully rewritten). Temp is a bump allocator so the win is modest (allocator growth + clear traffic), bit-identical. Mirrors the once-per-solve workspace style the rest of the file already uses.

### [LOW/deterministic-reorder] btfqmr - B is read twice at setup: BlockFrobDot(B,B) then per-row norms recompute the same data
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.TFQMR.fProxy.cs:136
Line 136 computes the full-block Frobenius dot only for the ==0 early-out, then lines 144-148 re-scan all of B for per-row thresh. Compute per-row bb once (UnsafeOP.vecDot per contiguous row) and sum them for the zero test — the ==0 decision is order-independent (sum of nonnegative terms), the thresh values change summation order (deterministic-reorder). Also hoist math.sqrt((fProxy)(k+1)) out of the per-row loop at line 240 (bit-identical), and note tau[i]^2 at line 192 already avoids re-dotting Rhat0 — keep that.

### [LOW/bit-identical] blsmr - BlockAdd/BlockScaleInPlace re-implement existing SIMD elementwise kernels as scalar loops
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:36
BlockAdd (Y += sign*T, Krylov.Block.Common.fProxy.cs:36-41) is exactly OP.Component addScaledInPlace (UnsafeOP.axpy, [NoAlias] SIMD) over the contiguous block, and BlockScaleInPlace (Common:425-430) is the existing SIMD scale/mulInPlace map. blsmr calls BlockAdd 7x and BlockScaleInPlace 2x per iteration on s x n / s x s blocks (lines 156, 164, 169, 184, 189, 212, 216, 220-223, 230). Elementwise maps have no reduction -> SIMD rewrite is bit-identical, and fixing the shared helper benefits every block solver.

### [LOW/deterministic-reorder] blsmr - BlockFrobDot scalar sequential reduction could reroute to the fused SIMD vecDot
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:415
BlockFrobDot (Krylov.Block.Common.fProxy.cs:415-422) is a scalar sequential accumulate over the contiguous buffer; UnsafeOP.vecDot (via Blas) is the existing fused SIMD reduction over the same flat range. Used at blsmr lines 119 (s x n, once), 148 and 233 (s x s per iteration). Reduction tree reorder -> deterministic-reorder (pre-1.0 waiver category), NOT bit-identical -- and note line 233 feeds the convergence recurrence, so this reroute changes the Converged iteration count marginally; gate it accordingly.

### [LOW/bit-identical] blsmr - ATB buffer is redundant (Sbar already serves as the A^T-block scratch) and X-zeroing bypasses zeroInPlace
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.LSMR.fProxy.cs:110
ATB (s x n, line 110) is used once at line 118 solely to compute atbSqF, then stays allocated for the whole solve; line 133 already demonstrates the pattern of reusing Sbar as A^T-apply scratch -- reuse Sbar at 118 too and delete ATB (saves an s x n Temp allocation, zero numerical change). Similarly the X zeroing at lines 48-49 is a scalar indexer loop where the existing SIMD zeroInPlace extension (OP.Component.fProxy.cs:16) applies. Both bit-identical.

### [LOW/bit-identical] bcgls - One-time init: scalar X zeroing and per-element CopyBlock
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CGLS.fProxy.cs:42
Lines 42-43 zero X via a per-element indexer double loop — X.zeroInPlace() (OP.Component.fProxy.cs:16) is the existing SIMD kernel for exactly this. The two init copies CopyBlock(B→R) at line 65 (s×m) and CopyBlock(S→P) at line 79 (s×n) route through Common's per-element indexer loop (Krylov.Block.Common.fProxy.cs:147); both blocks are contiguous, so UnsafeUtility.MemCpy (or the UnsafeOP scaledCopy kernel) is bit-identical. Outside the iteration loop, so low impact; the same MemCpy upgrade inside CopyBlock would also serve BlockLSResolveAndCheck and the other block solvers.

### [LOW/bit-identical] bcgls - Row marshalling in BlockApplyOp/BlockApplyOpT copies rows element-by-element
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:307
For the matrix-free TOp path that the GEMM specialization can't cover, the per-row gather/scatter loops (Krylov.Block.Common.fProxy.cs:307/309 and 321/323) copy contiguous row-major rows into/out of rowN/rowM one element at a time via bounds-checked indexers — 2·s·(n+m) elements per iteration for bcgls (lines 83, 94). A UnsafeUtility.MemCpy of each contiguous row is bit-identical. Secondary to the Apply cost itself, but free.

### [LOW/deterministic-reorder] bcraig — Krylov.Block.CRAIG - BlockFrobDot is a scalar sequential reduction — reroute to the SIMD UnsafeOP.vecDot on the flattened contiguous block
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:415
BlockFrobDot (Krylov.Block.Common.fProxy.cs:415-422) accumulates up[i]*vp[i] in a single scalar chain. The block is contiguous, so it can call UnsafeOP.vecDot(U.Data.Ptr, V.Data.Ptr, len) (UnsafeOP.fProxy.cs:242, the fused fProxyW/fProxy4 SIMD kernel) — the same self-dot pattern scalar craig already uses via Blas.dot(b,b) (Krylov.CRAIG.fProxy.cs:61). bcraig call sites: line 83 (bSqF, once) and 117/158 (rSqF, per round, O(s*m)). Multi-accumulator tree reduction => deterministic-reorder, allowed pre-1.0 under the documented waiver, NOT bit-identical — it shifts the convergence-round decision boundary by round-off, so land it with the waiver note. Shared helper: also changes blsmr/bcraigmr/bidr streams.

### [LOW/bit-identical] bcraig — Krylov.Block.CRAIG - Per-round LQ.decomp Temp allocations — hoist two fProxyLQCache workspaces
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIG.fProxy.cs:128
The allocating LQ.decomp overload (LQ.fProxy.cs:345-395) Temp-allocates a full working copy W (s x m or s x n) plus a length-n v EVERY call; bcraig calls it twice per round (lines 128, 136) plus twice in init (95, 99). Hoist two workspaces via the existing zero-alloc overload LQ.decomp(..., ref fProxyLQCache ws) (LQ.fProxy.cs:402): one sized (s, m) for the Wbar factorizations, one (s, n) for the Sbar ones, allocated once next to the other Temp buffers. Bit-identical as long as s < Consts.fProxyLqBlockMinM (256 float / 512 double): below that gate the allocating overload runs the same unblocked lqKernel the ws overload always runs; at s >= the gate the allocating overload would have taken the blocked path, so the swap would change bits (and drop the blocked speedup) — in practice block counts s are far below 256, but note it. Allocator.Temp is a bump allocator so the win is the avoided per-call zero/copy setup, not malloc cost — low impact, applies identically to blsmr/bcraigmr.

### [LOW/bit-identical] bcraig — Krylov.Block.CRAIG - CopyMat/CopyBlock and BlockApplyOp row marshalling use indexed element loops — contiguous rows should be MemCpy
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.Common.fProxy.cs:147
End-of-round state carry (lines 150-152) goes through CopyMat/CopyBlock (Krylov.Block.Common.fProxy.cs:147-157), scalar indexed loops over contiguous same-shape row-major buffers — replace with UnsafeUtility.MemCpy (or a shared bulk-copy helper): s*s + s*n elements per round. Same for BlockApplyOp/BlockApplyOpT's per-row copy-in/copy-out marshalling (Common:307-309, 321-323): each block apply copies 2 contiguous rows per RHS scalar-by-scalar, 3 applies per round => ~3*s*(n+m) copied elements; rows of a row-major fProxyMxN and fProxyN storage are both contiguous so MemCpy is a drop-in. Pure copies => bit-identical. (A GEMM-shaped ApplyBlock for the dense/BSR operators would be the bigger win but IfProxyLinearOperator.ApplyBlock is documented symmetric/square-only — the per-row fallback here is the documented design for rectangular A, so only the copy mechanics are flagged.)

### [LOW/bit-identical] bcraigmr - CopyBlock scalar per-element copies (V<-Vnext, D<-Dnew, 2 full s*n blocks per round) -> MemCpy
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIGMR.fProxy.cs:178
CopyBlock (Krylov.Block.Common.fProxy.cs:147) copies element-by-element through the [i,c] indexer; the call sites at lines 178 and 205 copy full contiguous s*n blocks between same-shape buffers, so a single UnsafeUtility.MemCpy over Data.Ptr (or a memcpy fast path inside CopyBlock when both blocks are full-width) is a bit-identical replacement. Same applies to CopyMat's four s*s copies at lines 201-204 (negligible, s small) -- the s*n ones are the ones worth doing.

### [LOW/deterministic-reorder] bcraigmr - D-update solves an exactly-lower-triangular system through full rank-revealing QRCP instead of a forward-substitution TRSM
Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Block.CRAIGMR.fProxy.cs:171
Line 171 solves Rho^T Dnew = RHSD via BlockSolveGeneralWide -> QRCP.solveInPlace (Krylov.Block.Common.fProxy.cs:384-390), but Rho is the upper-triangular R-factor extracted from the QR at line 147, so Rho^T is exactly lower-triangular: a TRSM forward substitution over the s x n RHS (with the existing TriNearSingular(Rho) at line 150 as the breakdown guard) does the same job at roughly half the small-solve cost and no QRCP scratch. CAVEAT: bcraig's DEVLOG documents routing triangular solves through QRCP as a deliberate simplicity/uniformity decision for this family, and s is small -- only worth doing as a family-wide pass with a benchmark, not a one-off here. Different rounding path -> not bit-identical.
