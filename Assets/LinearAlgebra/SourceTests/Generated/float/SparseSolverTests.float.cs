// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceTests/fProxy/SparseSolverTests.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using LinearAlgebra;
using LinearAlgebra.Gallery;
using LinearAlgebra.Sparse;
using NUnit.Framework;
using Unity.Burst;
using Unity.Collections;
using Unity.Jobs;
using Unity.Mathematics;

// Phase-2 sparse-solver test suite: IfloatLinearOperator / floatDenseOperator /
// floatBSROperator / floatBlockJacobi / Krylov.cg&lt;TOp&gt; / Krylov.pcg&lt;TOp,TPre&gt;, plus
// the concrete cg/pcg convenience overloads for floatMxN and floatBSR. Every
// BSR system is cross-checked against the equivalent dense system (same pattern as
// floatSparseBSRTests: build the SAME system in both forms and compare).
//
// Correctness cases run inside a [BurstCompile] IJob (matches floatConjugateGradientTests /
// floatSparseBSRTests). Guard/exception cases run on the managed test thread with
// Assert.Throws, since NUnit's Assert.Throws cannot execute inside a Burst-compiled job.
public class floatSparseSolverTests
{
    [BurstCompile(CompileSynchronously = true)]
    public struct SparseSolverTestJob : IJob
    {
        public enum TestType
        {
            Laplacian1DBSRCGMatchesDenseCG,
            ThreeByThreeBlockSPDConverges,
            DenseForwardingUnchanged,
            PCGMatchesCG,
            PCGBeatsCGIllConditioned,
            BlockJacobiApplyHandComputed,
            WarmStart,
            PcgNonSpdPreconditionerBreaksDown,

            // ---- Phase 3: MINRES / BiCGSTAB / CGLS / LSQR ----
            MinresIndefiniteDenseAndBSR,
            MinresSpdMatchesCG,
            BiCGStabNonSymmetricMatchesLU,
            CglsOverdeterminedConsistentDenseAndBSR,
            LsqrOverdeterminedConsistentDenseAndBSR,
            CglsInconsistentMatchesQR,
            LsqrInconsistentMatchesQR,
            CglsLsqrUnderdeterminedConsistent,

            // ---- LSMR (Fong-Saunders): least-squares with monotone ||A^T r|| ----
            LsmrOverdeterminedConsistentDenseAndBSR,
            LsmrInconsistentMatchesQR,
            LsmrUnderdeterminedMatchesLsqr,
            LsmrMonotonicArnorm,

            // ---- Tikhonov damping (CGLS/LSQR/LSMR): min ||Ax-b||^2 + damp^2||x||^2 ----
            TikhonovDampingMatchesAugmentedQR,

            // ---- CGNE / Craig: minimum-norm solve for consistent under-determined systems ----
            CgneUnderdeterminedMinNormMatchesLsqr,
            CgneWarmStart,
            CgneInconsistentDoesNotConverge,

            // ---- LS diagnostics (LstsqInfo: rnorm/Arnorm/xnorm/iterations/status; free tracked estimates) ----
            LstsqInfoMatchesIndependentRecompute,
            LstsqInfoDampedArnorm,
            LsmrRnormMatchesExact,
            LstsqInfoBSRMatchesDense,

            // ---- AᵀA-Jacobi preconditioner: fewer iterations, same solution ----
            JacobiPreconditionerReducesIterations,
            JacobiConvenienceSolversLSOptimalDenseAndBSR,

            // ---- literature ground truth: Strang best-fit-line, EXACT diagnostics ----
            LstsqInfoStrangLineFitExact,

            // ---- square-solver diagnostics (SolveInfo: rnorm/iterations/status; free tracked ‖r‖) ----
            SolveInfoRnormMatchesResidual,

            // ---- Phase 3 warm-start plumbing (initial residual r = b - A*x0 from the CALLER's x) ----
            MinresWarmStart,
            BiCGStabWarmStart,
            CglsWarmStart,
            LsqrWarmStart,
            LsmrWarmStart,
        }

        public TestType Type;

        // CG/PCG residuals converge to tolerance^2 * ||b||^2 (see Krylov.cg), so per-component
        // error is well below this scaled comparison threshold on both precisions; loose enough
        // that comparing two INDEPENDENTLY-converged solutions (each accurate only to about
        // Consts.floatSqrtEps, not machine epsilon) doesn't false-fail.
        static float Tol() => 1e-3f;

        // Looser threshold for the Phase-3 solvers' cross-checks. These compare TWO
        // independently-converged iterative solutions (or an iterative solution against a direct
        // one) on INDEFINITE / RECTANGULAR / ILL-conditioned systems, whose per-component absolute
        // error can be a few times Consts.floatSqrtEps*scale -- looser than the SPD-CG cases above.
        // The spec explicitly allows loosening to 1e-2f|1e-5 for exactly these iterative-vs-direct
        // comparisons rather than fighting flaky tolerances.
        static float LooseTol() => 1e-2f;

        // Least-squares optimality: the NORMAL-EQUATION residual ||A^T(A x - b)|| must vanish
        // relative to the fixed scale ||A^T b|| (mirrors cgls/lsqr's own convergence reference).
        // This -- NOT ||A x - b|| ~= 0 -- is the correct acceptance criterion for an inconsistent
        // (overdetermined) system, whose residual A x - b is left orthogonal to range(A), nonzero.
        static void AssertLeastSquaresOptimal(in floatMxN A, in floatN x, in floatN b, float relTol)
        {
            var Ax  = Blas.dot(A, x);
            var res = Ax - b;                 // r = A x - b     (length m)
            var atr = Blas.dot(res, A);  // A^T r           (length n)  -- vector*matrix == A^T r
            var atb = Blas.dot(b, A);    // A^T b           (scale reference)
            float atrNorm = math.sqrt(Blas.dot(atr, atr));
            float atbNorm = math.sqrt(Blas.dot(atb, atb));
            Assert.IsTrue(atrNorm <= relTol * atbNorm);
        }

        public void Execute()
        {
            switch (Type)
            {
                case TestType.Laplacian1DBSRCGMatchesDenseCG: Laplacian1DBSRCGMatchesDenseCG(); break;
                case TestType.ThreeByThreeBlockSPDConverges: ThreeByThreeBlockSPDConverges(); break;
                case TestType.DenseForwardingUnchanged: DenseForwardingUnchanged(); break;
                case TestType.PCGMatchesCG: PCGMatchesCG(); break;
                case TestType.PCGBeatsCGIllConditioned: PCGBeatsCGIllConditioned(); break;
                case TestType.BlockJacobiApplyHandComputed: BlockJacobiApplyHandComputed(); break;
                case TestType.WarmStart: WarmStart(); break;
                case TestType.PcgNonSpdPreconditionerBreaksDown: PcgNonSpdPreconditionerBreaksDown(); break;

                case TestType.MinresIndefiniteDenseAndBSR: MinresIndefiniteDenseAndBSR(); break;
                case TestType.MinresSpdMatchesCG: MinresSpdMatchesCG(); break;
                case TestType.BiCGStabNonSymmetricMatchesLU: BiCGStabNonSymmetricMatchesLU(); break;
                case TestType.CglsOverdeterminedConsistentDenseAndBSR: CglsOverdeterminedConsistentDenseAndBSR(); break;
                case TestType.LsqrOverdeterminedConsistentDenseAndBSR: LsqrOverdeterminedConsistentDenseAndBSR(); break;
                case TestType.CglsInconsistentMatchesQR: CglsInconsistentMatchesQR(); break;
                case TestType.LsqrInconsistentMatchesQR: LsqrInconsistentMatchesQR(); break;
                case TestType.CglsLsqrUnderdeterminedConsistent: CglsLsqrUnderdeterminedConsistent(); break;

                case TestType.LsmrOverdeterminedConsistentDenseAndBSR: LsmrOverdeterminedConsistentDenseAndBSR(); break;
                case TestType.LsmrInconsistentMatchesQR: LsmrInconsistentMatchesQR(); break;
                case TestType.LsmrUnderdeterminedMatchesLsqr: LsmrUnderdeterminedMatchesLsqr(); break;
                case TestType.LsmrMonotonicArnorm: LsmrMonotonicArnorm(); break;

                case TestType.TikhonovDampingMatchesAugmentedQR: TikhonovDampingMatchesAugmentedQR(); break;

                case TestType.CgneUnderdeterminedMinNormMatchesLsqr: CgneUnderdeterminedMinNormMatchesLsqr(); break;
                case TestType.CgneWarmStart: CgneWarmStart(); break;
                case TestType.CgneInconsistentDoesNotConverge: CgneInconsistentDoesNotConverge(); break;

                case TestType.LstsqInfoMatchesIndependentRecompute: LstsqInfoMatchesIndependentRecompute(); break;
                case TestType.LstsqInfoDampedArnorm: LstsqInfoDampedArnorm(); break;
                case TestType.LsmrRnormMatchesExact: LsmrRnormMatchesExact(); break;
                case TestType.LstsqInfoBSRMatchesDense: LstsqInfoBSRMatchesDense(); break;
                case TestType.JacobiPreconditionerReducesIterations: JacobiPreconditionerReducesIterations(); break;
                case TestType.JacobiConvenienceSolversLSOptimalDenseAndBSR: JacobiConvenienceSolversLSOptimalDenseAndBSR(); break;
                case TestType.LstsqInfoStrangLineFitExact: LstsqInfoStrangLineFitExact(); break;
                case TestType.SolveInfoRnormMatchesResidual: SolveInfoRnormMatchesResidual(); break;

                case TestType.MinresWarmStart: MinresWarmStart(); break;
                case TestType.BiCGStabWarmStart: BiCGStabWarmStart(); break;
                case TestType.CglsWarmStart: CglsWarmStart(); break;
                case TestType.LsqrWarmStart: LsqrWarmStart(); break;
                case TestType.LsmrWarmStart: LsmrWarmStart(); break;
            }
        }

        // ---- helpers ---------------------------------------------------------------------

        // Same recipe as floatConjugateGradientTests.BuildSPD: A = M^T M + dim*I (strictly SPD,
        // diagonally dominant).
        static floatMxN BuildDenseSPD(ref Arena arena, int dim, uint seed)
        {
            var M = arena.floatRandomMat(dim, dim, -1f, 1f, seed);
            var A = Blas.dot(M, M, true);
            for (int d = 0; d < dim; d++)
                A[d, d] += dim;
            return A;
        }

        // 1x1-block BSR built from a dense matrix's nonzero entries via AddValue. Triplet count
        // is bounded by the caller-supplied nnzHint (sized to the known nonzero pattern) purely
        // as a perf choice -- it avoids a few reallocations of the builder's internal growable
        // lists, nothing more. Growing the builder's lists past capacityHint (triggering one or
        // more UnsafeList reallocations) is safe: the builder's mutable triplet state lives
        // behind a single heap-allocated pointer shared by every value-copy of the struct
        // (including the arena's own tracked copy), so a reallocation on one copy is visible to
        // all of them. See the growth regression tests in SparseBSRTests.float.cs, which build
        // via many-reallocation growth on purpose to prove this.
        static floatBSR DenseToBSR1x1(ref Arena arena, in floatMxN A, int nnzHint)
        {
            var builder = arena.floatBSRBuilder(A.M_Rows, A.N_Cols, 1, 1, math.max(nnzHint, 1));
            for (int r = 0; r < A.M_Rows; r++)
                for (int c = 0; c < A.N_Cols; c++)
                    if (A[r, c] != (float)0)
                        builder.AddValue(r, c, A[r, c]);
            return builder.ToBSR(ref arena);
        }

        static void AssertVecEq(in floatN a, in floatN b, float tol)
        {
            Assert.IsTrue(Analysis.isZero(a - b, tol));
        }

        // got/expected are double so the result-struct norm fields (now double regardless of the
        // solve's precision) pass straight in; float callers just widen. NOT a second float
        // overload -- that would collide with this one in the double generation (CS0111).
        static void AssertClose(double got, double expected, float tol)
            => Assert.IsTrue(math.abs(got - expected) <= tol * ((float)1 + math.abs(expected)));

        // ---- 1. 1D Laplacian tridiagonal as a 1x1-block BSR: CG matches dense CG -----------
        void Laplacian1DBSRCGMatchesDenseCG()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 16;
            var A = arena.floatLaplacian1D(dim);
            // Tridiagonal: at most 3 nonzeros/row -> 3*dim is a safe upper bound.
            var bsm = DenseToBSR1x1(ref arena, in A, 3 * dim);

            var b = arena.floatRandomVec(dim, -1f, 1f, 4242);

            var xDense = arena.floatVec(dim);
            bool okDense = Krylov.cg(in A, in b, ref xDense, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okDense);

            var xBsr = arena.floatVec(dim);
            bool okBsr = Krylov.cg(in bsm, in b, ref xBsr, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);

            AssertVecEq(in xDense, in xBsr, Tol());

            // A*x ~= b for the BSR solve too (spec's explicit acceptance criterion).
            var Ax = BSR.spMV(in bsm, in xBsr);
            AssertVecEq(in Ax, in b, Tol());

            arena.Dispose();
        }

        // ---- 2. 3x3-block SPD system: CG converges, residual within tol -------------------
        //
        // Build a random block matrix M (BR=3, a handful of blocks on a 3x3 block grid), form
        // the dense A = M^T M + eps*I (guaranteed SPD), then re-encode A as a genuine 3x3-block
        // BSR with every block-row/col pair stored (A^T A is generally dense even when M is
        // sparse) -- so CG genuinely walks a multi-block-per-row BSR structure, not just 1x1
        // scalars.
        void ThreeByThreeBlockSPDConverges()
        {
            var arena = new Arena(Allocator.Persistent);

            const int BR = 3;
            const int nb = 3; // 9x9
            int dim = BR * nb;

            var mb = arena.floatBSRBuilder(nb, nb, BR, BR, nb * nb);
            mb.AddBlock(0, 0, arena.floatRandomMat(BR, BR, -1f, 1f, 8001));
            mb.AddBlock(0, 1, arena.floatRandomMat(BR, BR, -1f, 1f, 8002));
            mb.AddBlock(1, 1, arena.floatRandomMat(BR, BR, -1f, 1f, 8003));
            mb.AddBlock(1, 2, arena.floatRandomMat(BR, BR, -1f, 1f, 8004));
            mb.AddBlock(2, 2, arena.floatRandomMat(BR, BR, -1f, 1f, 8005));
            mb.AddBlock(2, 0, arena.floatRandomMat(BR, BR, -1f, 1f, 8006));
            var Mdense = mb.ToBSR(ref arena).ToDense(ref arena);

            var A = Blas.dot(Mdense, Mdense, true);
            for (int i = 0; i < dim; i++)
                A[i, i] += dim;

            var ab = arena.floatBSRBuilder(nb, nb, BR, BR, nb * nb);
            for (int bi = 0; bi < nb; bi++)
                for (int bj = 0; bj < nb; bj++)
                {
                    var blk = arena.floatMat(BR, BR);
                    for (int r = 0; r < BR; r++)
                        for (int c = 0; c < BR; c++)
                            blk[r, c] = A[bi * BR + r, bj * BR + c];
                    ab.AddBlock(bi, bj, in blk);
                }
            var Absm = ab.ToBSR(ref arena);

            var b = arena.floatRandomVec(dim, -1f, 1f, 8100);
            var x = arena.floatVec(dim);
            bool ok = Krylov.cg(in Absm, in b, ref x);
            Assert.IsTrue(ok);

            var Ax = BSR.spMV(in Absm, in x);
            AssertVecEq(in Ax, in b, Tol());

            // Cross-check against the dense reference too.
            var xDense = arena.floatVec(dim);
            bool okDense = Krylov.cg(in A, in b, ref xDense);
            Assert.IsTrue(okDense);
            AssertVecEq(in x, in xDense, Tol());

            arena.Dispose();
        }

        // ---- 3. Dense forwarding unchanged: guards the cg(in floatMxN,...) ----
        //         refactor into cg<floatDenseOperator> -----------------------------------------
        void DenseForwardingUnchanged()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 12;
            var A = BuildDenseSPD(ref arena, dim, 90125);
            var b = arena.floatRandomVec(dim, -1f, 1f, 4242);

            // The (unchanged, public) concrete entry point.
            var xConcrete = arena.floatVec(dim);
            bool okConcrete = Krylov.cg(in A, in b, ref xConcrete);
            Assert.IsTrue(okConcrete);

            // Calling cg<TOp> directly via floatDenseOperator must reproduce the identical
            // result -- this is the single source of truth the concrete overload now forwards
            // into.
            var op = new floatDenseOperator(in A);
            var xGeneric = arena.floatVec(dim);
            var r = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            bool okGeneric = Krylov.cg(in op, in b, ref xGeneric, ref r, ref p, ref Ap, dim, Consts.floatSqrtEps);
            Assert.IsTrue(okGeneric);

            AssertVecEq(in xConcrete, in xGeneric, Tol());

            var Ax = Blas.dot(A, xConcrete);
            AssertVecEq(in Ax, in b, Tol());

            // Independent cross-check against a DIRECT solver on a completely different code path
            // (Householder QR, no Krylov/CG involvement). The xConcrete-vs-xGeneric check above is
            // circular now that both funnel through the same cg<TOp> loop; this pins the CG
            // solution to a truly independent reference. QR.solveInPlace is DESTRUCTIVE (destroys
            // A and b), so it MUST run on fresh copies, not the A/b the CG calls used.
            var A2 = A.Copy();
            var b2 = b.Copy();
            var xQR = arena.floatVec(dim);
            QR.solveInPlace(ref A2, ref b2, ref xQR);
            AssertVecEq(in xConcrete, in xQR, Tol());

            arena.Dispose();
        }

        // ---- 4. PCG correctness: matches CG's solution -------------------------------------
        void PCGMatchesCG()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 12;
            var A = BuildDenseSPD(ref arena, dim, 6001);
            var bsm = DenseToBSR1x1(ref arena, in A, dim * dim);
            var M = arena.floatBlockJacobi(in bsm);
            var b = arena.floatRandomVec(dim, -1f, 1f, 6002);

            var xCG = arena.floatVec(dim);
            bool okCG = Krylov.cg(in bsm, in b, ref xCG);
            Assert.IsTrue(okCG);

            var xPCG = arena.floatVec(dim);
            bool okPCG = Krylov.pcg(in bsm, in M, in b, ref xPCG);
            Assert.IsTrue(okPCG);

            AssertVecEq(in xCG, in xPCG, Tol());

            arena.Dispose();
        }

        // ---- 5. Block-Jacobi PCG needs <= iterations of plain CG on an ill-conditioned,
        //         diagonally-scaled SPD system --------------------------------------------
        //
        // Sym is normalized to a UNIT diagonal ("correlation matrix": Sym = S / sqrt(Sii*Sjj),
        // a diagonal congruence transform, so it stays SPD) before the D*Sym*D rescale. That
        // makes diag(A) EXACTLY d_i^2, so point-Jacobi (BR=1) exactly recovers Sym's own
        // well-conditioned spectrum -- the textbook case where Jacobi preconditioning provably
        // helps. The iteration budget used to find each solver's minimum is 4*dim, not dim:
        // CG's finite-termination property (<=dim iterations) only holds in EXACT arithmetic;
        // floating point on an ill-conditioned system can need more (same 4n cap the
        // GalleryLaplacian1D/MinIJ CG tests use).
        void PCGBeatsCGIllConditioned()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 16;
            var S = BuildDenseSPD(ref arena, dim, 7001);

            var Sym = arena.floatMat(dim, dim);
            for (int r = 0; r < dim; r++)
                for (int c = 0; c < dim; c++)
                    Sym[r, c] = S[r, c] / math.sqrt(S[r, r] * S[c, c]);

            var A = arena.floatMat(dim, dim);
            var d = arena.floatVec(dim);
            for (int i = 0; i < dim; i++)
                d[i] = (i % 2 == 0) ? (float)1 : (float)100; // alternating 1,100

            for (int r = 0; r < dim; r++)
                for (int c = 0; c < dim; c++)
                    A[r, c] = d[r] * Sym[r, c] * d[c];

            var bsm = DenseToBSR1x1(ref arena, in A, dim * dim);
            var M = arena.floatBlockJacobi(in bsm);
            var b = arena.floatRandomVec(dim, -1f, 1f, 7002);

            int maxBudget = 4 * dim;
            int minCG = -1, minPCG = -1;
            for (int budget = 1; budget <= maxBudget && (minCG < 0 || minPCG < 0); budget++)
            {
                if (minCG < 0)
                {
                    var xCG = arena.floatVec(dim);
                    if (Krylov.cg(in bsm, in b, ref xCG, budget, Consts.floatSqrtEps))
                        minCG = budget;
                }
                if (minPCG < 0)
                {
                    var xPCG = arena.floatVec(dim);
                    if (Krylov.pcg(in bsm, in M, in b, ref xPCG, budget, Consts.floatSqrtEps))
                        minPCG = budget;
                }
            }

            Assert.IsTrue(minCG > 0);
            Assert.IsTrue(minPCG > 0);
            Assert.IsTrue(minPCG <= minCG);

            arena.Dispose();
        }

        // ---- 6. Block-Jacobi Apply matches a hand-computed block-diagonal inverse ----------
        void BlockJacobiApplyHandComputed()
        {
            var arena = new Arena(Allocator.Persistent);

            const int BR = 2;
            var builder = arena.floatBSRBuilder(2, 2, BR, BR, 2);
            var d0 = arena.floatMat(BR, BR);
            d0[0, 0] = 4f; d0[0, 1] = 1f;
            d0[1, 0] = 1f; d0[1, 1] = 3f;
            var d1 = arena.floatMat(BR, BR);
            d1[0, 0] = 2f; d1[0, 1] = 0f;
            d1[1, 0] = 0f; d1[1, 1] = 5f;
            builder.AddBlock(0, 0, in d0);
            builder.AddBlock(1, 1, in d1);
            var A = builder.ToBSR(ref arena);

            var M = arena.floatBlockJacobi(in A);

            var r = arena.floatVec(4);
            r[0] = 1f; r[1] = 2f; r[2] = 3f; r[3] = 4f;
            var z = arena.floatVec(4);
            M.Apply(in r, ref z);

            // Hand inverse of d0=[[4,1],[1,3]]: det=11, inv = (1/11)*[[3,-1],[-1,4]].
            float det0 = 11f;
            float z0 = (3f * r[0] - 1f * r[1]) / det0;
            float z1 = (-1f * r[0] + 4f * r[1]) / det0;
            // d1 is diagonal: inv = diag(1/2, 1/5).
            float z2 = r[2] / 2f;
            float z3 = r[3] / 5f;

            Assert.IsTrue(math.abs(z[0] - z0) < Tol());
            Assert.IsTrue(math.abs(z[1] - z1) < Tol());
            Assert.IsTrue(math.abs(z[2] - z2) < Tol());
            Assert.IsTrue(math.abs(z[3] - z3) < Tol());

            arena.Dispose();
        }

        // ---- 7. Warm start: seeding x with the exact solution converges immediately --------
        void WarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 10;
            var A = BuildDenseSPD(ref arena, dim, 9001);
            var bsm = DenseToBSR1x1(ref arena, in A, dim * dim);
            var M = arena.floatBlockJacobi(in bsm);
            var b = arena.floatRandomVec(dim, -1f, 1f, 9002);

            var x = arena.floatVec(dim);
            bool ok = Krylov.pcg(in bsm, in M, in b, ref x);
            Assert.IsTrue(ok);

            // Feed the converged solution back as the initial guess -- a single iteration's
            // worth of budget must still report convergence (matches
            // floatConjugateGradientTests.AlreadyConverged's dense-CG counterpart).
            var xWarm = x.Copy();
            bool okWarm = Krylov.pcg(in bsm, in M, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, Tol());

            // Same check for plain (unpreconditioned) CG.
            var xCG = arena.floatVec(dim);
            bool okCG = Krylov.cg(in bsm, in b, ref xCG);
            Assert.IsTrue(okCG);
            var xCGWarm = xCG.Copy();
            bool okCGWarm = Krylov.cg(in bsm, in b, ref xCGWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okCGWarm);
            AssertVecEq(in xCG, in xCGWarm, Tol());

            arena.Dispose();
        }

        // ---- 8. pcg breakdown guard: a non-SPD preconditioner bails out (returns false) -------
        //
        // The preconditioned inner product <r,z> must stay positive for PCG to be well-defined.
        // floatNegatePreconditioner deliberately returns z = -r, so rzold = <r,-r> = -||r||^2 < 0
        // -- the pcg `if (!(rzold > 0)) return false;` guard must catch it and
        // return false, rather than looping with a wrong-signed alpha/beta (silent divergence /
        // NaN). A itself is a genuine SPD system so the failure is attributable to the
        // preconditioner, not the operator.
        void PcgNonSpdPreconditionerBreaksDown()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 8;
            var A = BuildDenseSPD(ref arena, dim, 5501);
            var op = new floatDenseOperator(in A);
            var pre = new floatNegatePreconditioner();
            var b = arena.floatRandomVec(dim, -1f, 1f, 5502); // nonzero rhs -> not the b==0 shortcut

            var x = arena.floatVec(dim);
            bool ok = Krylov.pcg(in op, in pre, in b, ref x, dim, Consts.floatSqrtEps);
            Assert.IsFalse(ok);

            arena.Dispose();
        }

        // =================================================================================
        // Phase 3 correctness cases
        // =================================================================================

        // ---- MINRES on a symmetric INDEFINITE system (dense + BSR agree) -----------------
        //
        // Laplacian1D (SPD, diag 2 / off-diag -1) shifted by -2 on the diagonal: eigenvalues
        // become 2-2cos(k*pi/(n+1)) - 2 = -2cos(k*pi/(n+1)) for k=1..n, which straddle 0 -> a
        // genuinely mixed-sign (symmetric indefinite) A. dim=16 -> n+1=17 is odd, so k=(n+1)/2
        // is non-integer and NO eigenvalue is exactly 0 (A stays nonsingular). MINRES handles
        // this cleanly where CG's p.Ap>0 curvature requirement would break down.
        void MinresIndefiniteDenseAndBSR()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 16;
            var A = arena.floatLaplacian1D(dim);
            for (int i = 0; i < dim; i++)
                A[i, i] -= (float)2;          // shift diag 2 -> 0: mixed-sign spectrum, indefinite

            var b = arena.floatRandomVec(dim, -1f, 1f, 31001);

            var xDense = arena.floatVec(dim);
            bool okDense = Krylov.minres(in A, in b, ref xDense, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okDense);

            var Ax = Blas.dot(A, xDense);
            AssertVecEq(in Ax, in b, LooseTol());          // A nonsingular -> unique solution, A x ~= b

            // Independent cross-check against a DIRECT LU solve on the SAME indefinite matrix
            // (no Krylov/MINRES involvement) -- pins the iterative solution to a truly independent
            // reference, not just the self-consistent A x ~= b residual. LU.decompInPlace +
            // LU.decompSolve are DESTRUCTIVE, so they run on COPIES. The shifted Laplacian above is
            // constructed to be nonsingular (odd n+1 -> no exactly-zero eigenvalue), so LU succeeds.
            var LUcopy = A.Copy();
            var pivot = new Pivot(dim, Allocator.Temp);
            bool okLU = LU.decompInPlace(ref LUcopy, ref pivot);
            Assert.IsTrue(okLU);
            var xLU = b.Copy();
            LU.decompSolve(ref LUcopy, in pivot, ref xLU);
            AssertVecEq(in xDense, in xLU, LooseTol());
            pivot.Dispose();

            // Same system as a 1x1-block BSR: minres(BSR) must agree with minres(dense).
            var bsm = DenseToBSR1x1(ref arena, in A, 3 * dim);   // tridiagonal (shifted diag=0 dropped)
            var xBsr = arena.floatVec(dim);
            bool okBsr = Krylov.minres(in bsm, in b, ref xBsr, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in xDense, in xBsr, LooseTol());

            var AxBsr = BSR.spMV(in bsm, in xBsr);
            AssertVecEq(in AxBsr, in b, LooseTol());

            // NOTE (spec nice-to-have, NOT asserted): plain CG on this SAME indefinite A breaks
            // down -- Krylov.cg's p.Ap>0 curvature guard fails / returns a much
            // worse residual. MINRES succeeding where CG cannot is the whole point of this case;
            // asserting CG's failure mode is fiddly and left as a documented expectation.

            arena.Dispose();
        }

        // ---- MINRES on a plain SPD system agrees with CG (dense + BSR) --------------------
        void MinresSpdMatchesCG()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 12;
            var A = BuildDenseSPD(ref arena, dim, 32001);
            var b = arena.floatRandomVec(dim, -1f, 1f, 32002);

            var xCG = arena.floatVec(dim);
            bool okCG = Krylov.cg(in A, in b, ref xCG, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okCG);

            var xMin = arena.floatVec(dim);
            bool okMin = Krylov.minres(in A, in b, ref xMin, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okMin);
            AssertVecEq(in xCG, in xMin, LooseTol());       // MINRES == CG on an SPD system

            var Ax = Blas.dot(A, xMin);
            AssertVecEq(in Ax, in b, LooseTol());

            // BSR minres agrees with dense minres.
            var bsm = DenseToBSR1x1(ref arena, in A, dim * dim);
            var xMinBsr = arena.floatVec(dim);
            bool okMinBsr = Krylov.minres(in bsm, in b, ref xMinBsr, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okMinBsr);
            AssertVecEq(in xMin, in xMinBsr, LooseTol());

            arena.Dispose();
        }

        // ---- BiCGSTAB on a NON-symmetric (diagonally-dominant) system --------------------
        //
        // Random off-diagonals in [-1,1], diagonal boosted to dim+1 so |A_ii| > sum_{j!=i}|A_ij|
        // strictly -> nonsingular, unconditionally BiCGSTAB-friendly, and deliberately NOT
        // symmetrized. Cross-checked against a dense DIRECT LU solve on the SAME matrix.
        void BiCGStabNonSymmetricMatchesLU()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 8;
            var A = arena.floatRandomMat(dim, dim, -1f, 1f, 33001);
            for (int i = 0; i < dim; i++)
                A[i, i] += (float)(dim + 1);   // strict diagonal dominance

            var b = arena.floatRandomVec(dim, -1f, 1f, 33002);

            var xBcg = arena.floatVec(dim);
            bool okBcg = Krylov.biCGStab(in A, in b, ref xBcg, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okBcg);
            var Ax = Blas.dot(A, xBcg);
            AssertVecEq(in Ax, in b, LooseTol());

            // Direct LU reference on COPIES (LU.decompInPlace + LU.decompSolve are DESTRUCTIVE).
            var LUcopy = A.Copy();
            var pivot = new Pivot(dim, Allocator.Temp);
            bool okLU = LU.decompInPlace(ref LUcopy, ref pivot);
            Assert.IsTrue(okLU);
            var xLU = b.Copy();
            LU.decompSolve(ref LUcopy, in pivot, ref xLU);
            AssertVecEq(in xBcg, in xLU, LooseTol());
            pivot.Dispose();

            // BSR form agrees with the dense BiCGSTAB solve.
            var bsm = DenseToBSR1x1(ref arena, in A, dim * dim);
            var xBcgBsr = arena.floatVec(dim);
            bool okBcgBsr = Krylov.biCGStab(in bsm, in b, ref xBcgBsr, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(okBcgBsr);
            AssertVecEq(in xBcg, in xBcgBsr, LooseTol());

            arena.Dispose();
        }

        // ---- CGLS on an overdetermined CONSISTENT least-squares problem (dense + BSR) -----
        //
        // b = A*x_true exactly (b in range(A)) -> the least-squares solution is x_true, recovered
        // exactly (within tolerance). m > n.
        void CglsOverdeterminedConsistentDenseAndBSR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 34001);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 34002);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.cgls(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);
            AssertVecEq(in x, in xTrue, LooseTol());

            var Ax = Blas.dot(A, x);
            AssertVecEq(in Ax, in b, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.cgls(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- LSQR on an overdetermined CONSISTENT least-squares problem (dense + BSR) ------
        void LsqrOverdeterminedConsistentDenseAndBSR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 35001);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 35002);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsqr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);
            AssertVecEq(in x, in xTrue, LooseTol());

            var Ax = Blas.dot(A, x);
            AssertVecEq(in Ax, in b, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.lsqr(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- CGLS on an overdetermined INCONSISTENT problem: normal-equation optimality ---
        //
        // Random b generally NOT in range(A) -> ||A x - b|| does NOT go to 0. The correct
        // acceptance criterion is ||A^T(A x - b)|| ~= 0 (residual orthogonal to range(A)),
        // cross-checked against a dense QR least-squares solve on the SAME system.
        void CglsInconsistentMatchesQR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 36001);
            var b = arena.floatRandomVec(m, -1f, 1f, 36002);   // inconsistent

            var x = arena.floatVec(n);
            bool ok = Krylov.cgls(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            AssertLeastSquaresOptimal(in A, in x, in b, LooseTol());

            // Dense QR least-squares reference on COPIES (QR.solveInPlace is DESTRUCTIVE).
            var A2 = A.Copy();
            var b2 = b.Copy();
            var xQR = arena.floatVec(n);
            QR.solveInPlace(ref A2, ref b2, ref xQR);
            AssertVecEq(in x, in xQR, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.cgls(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- LSQR on an overdetermined INCONSISTENT problem: normal-equation optimality ----
        void LsqrInconsistentMatchesQR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 37001);
            var b = arena.floatRandomVec(m, -1f, 1f, 37002);   // inconsistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsqr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            AssertLeastSquaresOptimal(in A, in x, in b, LooseTol());

            var A2 = A.Copy();
            var b2 = b.Copy();
            var xQR = arena.floatVec(n);
            QR.solveInPlace(ref A2, ref b2, ref xQR);
            AssertVecEq(in x, in xQR, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.lsqr(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- CGLS & LSQR on an underdetermined (m < n) CONSISTENT problem (nice-to-have) ---
        //
        // Wide A, b = A*x_gen (consistent) -> infinitely many exact solutions. Starting from
        // x0 = 0, both CGLS and LSQR converge to the SAME (minimum-norm) solution. We assert the
        // easy-to-verify properties -- A x ~= b and both solvers agree -- rather than the min-norm
        // optimality itself (that weaker check is all this nice-to-have case claims).
        void CglsLsqrUnderdeterminedConsistent()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 4, n = 10;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 38001);
            var xGen = arena.floatRandomVec(n, -1f, 1f, 38002);
            var b = Blas.dot(A, xGen);      // consistent

            var xC = arena.floatVec(n);
            bool okC = Krylov.cgls(in A, in b, ref xC, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okC);
            var AxC = Blas.dot(A, xC);
            AssertVecEq(in AxC, in b, LooseTol());

            var xL = arena.floatVec(n);
            bool okL = Krylov.lsqr(in A, in b, ref xL, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okL);
            var AxL = Blas.dot(A, xL);
            AssertVecEq(in AxL, in b, LooseTol());

            // Both start from x0=0 -> both land on the unique minimum-norm solution -> they agree.
            AssertVecEq(in xC, in xL, LooseTol());

            arena.Dispose();
        }

        // =================================================================================
        // Phase 3 warm-start plumbing
        //
        // Every OTHER Phase-3 test starts from a zero-initialized x, so b - A*x0 == b always --
        // a regression that dropped the initial-residual subtraction (r = b - A*x0 silently
        // becoming r = b) would pass all of them. These four seed x with the ALREADY-converged
        // solution and re-solve with maxIter=1: each solver computes r = b - A*x from the
        // CALLER-supplied x and checks it against tolerance in its own pre-loop residual check
        // (Krylov.float.cs), so an already-converged x
        // must report true WITHOUT spending the single iteration -- and x must come back unchanged.
        // Mirrors the CG/PCG WarmStart test above.
        // =================================================================================

        // ---- MINRES warm start (SPD system, sufficient for the warm-start plumbing) ----
        void MinresWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 12;
            var A = BuildDenseSPD(ref arena, dim, 41001);
            var b = arena.floatRandomVec(dim, -1f, 1f, 41002);

            var x = arena.floatVec(dim);
            bool ok = Krylov.minres(in A, in b, ref x, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            // Seed the converged solution back; a single iteration's budget must still report
            // convergence via the pre-loop residual check, and leave x untouched.
            var xWarm = x.Copy();
            bool okWarm = Krylov.minres(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, LooseTol());

            arena.Dispose();
        }

        // ---- BiCGSTAB warm start (random diagonally-dominant non-symmetric A) ----
        void BiCGStabWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int dim = 8;
            var A = arena.floatRandomMat(dim, dim, -1f, 1f, 41101);
            for (int i = 0; i < dim; i++)
                A[i, i] += (float)(dim + 1);   // strict diagonal dominance

            var b = arena.floatRandomVec(dim, -1f, 1f, 41102);

            var x = arena.floatVec(dim);
            bool ok = Krylov.biCGStab(in A, in b, ref x, 4 * dim, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            var xWarm = x.Copy();
            bool okWarm = Krylov.biCGStab(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, LooseTol());

            arena.Dispose();
        }

        // ---- CGLS warm start (overdetermined m>n CONSISTENT system, b = A*xTrue) ----
        void CglsWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 41201);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 41202);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.cgls(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            var xWarm = x.Copy();
            bool okWarm = Krylov.cgls(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, LooseTol());

            arena.Dispose();
        }

        // ---- LSQR warm start (overdetermined m>n CONSISTENT system, b = A*xTrue) ----
        void LsqrWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 41301);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 41302);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsqr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            var xWarm = x.Copy();
            bool okWarm = Krylov.lsqr(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, LooseTol());

            arena.Dispose();
        }

        // ---- LSMR on an overdetermined CONSISTENT least-squares problem (dense + BSR) ------
        //
        // Same fixture and acceptance criterion as CglsOverdeterminedConsistentDenseAndBSR above.
        void LsmrOverdeterminedConsistentDenseAndBSR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 42001);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 42002);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsmr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);
            AssertVecEq(in x, in xTrue, LooseTol());

            var Ax = Blas.dot(A, x);
            AssertVecEq(in Ax, in b, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.lsmr(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- LSMR on an overdetermined INCONSISTENT problem: normal-equation optimality ----
        //
        // Random b generally NOT in range(A). Correct acceptance = ||A^T(A x - b)|| ~= 0, plus a
        // cross-check against the dense QR least-squares solution (the unique minimizer) -- the
        // same oracle as the CGLS/LSQR inconsistent tests, so LSMR must land on the same x.
        void LsmrInconsistentMatchesQR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 42101);
            var b = arena.floatRandomVec(m, -1f, 1f, 42102);   // inconsistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsmr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            AssertLeastSquaresOptimal(in A, in x, in b, LooseTol());

            var A2 = A.Copy();
            var b2 = b.Copy();
            var xQR = arena.floatVec(n);
            QR.solveInPlace(ref A2, ref b2, ref xQR);
            AssertVecEq(in x, in xQR, LooseTol());

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xBsr = arena.floatVec(n);
            bool okBsr = Krylov.lsmr(in bsm, in b, ref xBsr, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okBsr);
            AssertVecEq(in x, in xBsr, LooseTol());

            arena.Dispose();
        }

        // ---- LSMR on an underdetermined (m < n) CONSISTENT problem: matches LSQR ----
        //
        // Same wide-A min-norm setup as CglsLsqrUnderdeterminedConsistent above; here LSMR is
        // cross-checked against the already-tested LSQR solution.
        void LsmrUnderdeterminedMatchesLsqr()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 4, n = 10;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 42201);
            var xGen = arena.floatRandomVec(n, -1f, 1f, 42202);
            var b = Blas.dot(A, xGen);      // consistent

            var xM = arena.floatVec(n);
            bool okM = Krylov.lsmr(in A, in b, ref xM, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okM);
            var AxM = Blas.dot(A, xM);
            AssertVecEq(in AxM, in b, LooseTol());

            var xL = arena.floatVec(n);
            bool okL = Krylov.lsqr(in A, in b, ref xL, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(okL);

            AssertVecEq(in xM, in xL, LooseTol());   // both land on the unique minimum-norm solution

            arena.Dispose();
        }

        // ---- LSMR warm start (overdetermined m>n CONSISTENT system, b = A*xTrue) ----
        void LsmrWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 42301);
            var xTrue = arena.floatRandomVec(n, -1f, 1f, 42302);
            var b = Blas.dot(A, xTrue);      // consistent

            var x = arena.floatVec(n);
            bool ok = Krylov.lsmr(in A, in b, ref x, 8 * n, Consts.floatSqrtEps);
            Assert.IsTrue(ok);

            var xWarm = x.Copy();
            bool okWarm = Krylov.lsmr(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);
            AssertVecEq(in x, in xWarm, LooseTol());

            arena.Dispose();
        }

        // ---- LSMR monotone ||A^T r|| (its distinguishing property vs LSQR) ----
        //
        // LSMR guarantees ||A^T r_k|| decreases MONOTONICALLY. Run it for k = 1..n iterations (fresh
        // zero start each time, tolerance 0 so it never early-stops and runs EXACTLY k iterations),
        // recompute the true ||A^T(A x_k - b)|| externally, and assert the sequence is non-increasing.
        // A recurrence bug that swapped/mis-scaled the MINRES-layer rotation could still converge to
        // the right final x (self-correcting over enough steps) while violating monotonicity -- this
        // pins the property the QR cross-check cannot see. Generous fp slack: only a GROSS violation
        // (a real bug) exceeds it.
        void LsmrMonotonicArnorm()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 12, n = 6;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 43101);
            var b = arena.floatRandomVec(m, -1f, 1f, 43102);   // inconsistent -> ||A^T r|| > 0 for a while

            float prev = float.MaxValue;
            for (int k = 1; k <= n; k++)
            {
                var x = arena.floatVec(n);                     // fresh zero start
                Krylov.lsmr(in A, in b, ref x, k, (float)0);  // tol 0 -> runs exactly k iterations

                var res = Blas.dot(A, x) - b;              // A x - b   (length m)
                var atr = Blas.dot(res, A);                // A^T res   (length n)
                float nrm = math.sqrt(Blas.dot(atr, atr));

                Assert.IsTrue(nrm <= prev + (float)0.02 * prev + (float)1e-4);   // non-increasing (+ fp slack)
                prev = nrm;
            }

            arena.Dispose();
        }

        // ---- Tikhonov damping: CGLS / LSQR / LSMR all solve min ||Ax-b||^2 + damp^2||x||^2 ----
        //
        // Reference = dense QR least-squares on the AUGMENTED system [A; damp*I] x ~= [b; 0], which
        // IS the damped minimizer x = (A^T A + damp^2 I)^-1 A^T b. All three damped solvers (dense
        // AND 1x1-BSR) must land on it. Uses an INCONSISTENT b so damping actually changes the
        // answer (a wrong/no-op damp term would miss the reference). damp = 0.5 is well inside the
        // regime where the regularization is numerically significant but the system stays solvable.
        void TikhonovDampingMatchesAugmentedQR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 43201);
            var b = arena.floatRandomVec(m, -1f, 1f, 43202);   // inconsistent
            float damp = (float)0.5;

            var xref = DampedReferenceQR(ref arena, in A, in b, damp);

            // dense damped solvers
            var xC = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgls(in A, in b, ref xC, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xC, in xref, LooseTol());

            var xL = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqr(in A, in b, ref xL, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xL, in xref, LooseTol());

            var xM = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsmr(in A, in b, ref xM, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xM, in xref, LooseTol());

            // 1x1-BSR damped solvers agree with the same reference
            var bsm = DenseToBSR1x1(ref arena, in A, m * n);

            var xCb = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgls(in bsm, in b, ref xCb, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xCb, in xref, LooseTol());

            var xLb = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqr(in bsm, in b, ref xLb, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xLb, in xref, LooseTol());

            var xMb = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsmr(in bsm, in b, ref xMb, 16 * n, Consts.floatSqrtEps, damp));
            AssertVecEq(in xMb, in xref, LooseTol());

            arena.Dispose();
        }

        // ---- CGNE / Craig: minimum-norm solution of a consistent under-determined system ----
        //
        // Same wide-A min-norm setup as CglsLsqrUnderdeterminedConsistent above; CGNE is
        // cross-checked against LSQR (already tested), on dense AND 1x1-BSR.
        void CgneUnderdeterminedMinNormMatchesLsqr()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 4, n = 10;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 44001);
            var xGen = arena.floatRandomVec(n, -1f, 1f, 44002);
            var b = Blas.dot(A, xGen);      // consistent (b in range(A))

            var xC = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgne(in A, in b, ref xC, 8 * n, Consts.floatSqrtEps));
            var AxC = Blas.dot(A, xC);
            AssertVecEq(in AxC, in b, LooseTol());          // solves the consistent system

            var xL = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqr(in A, in b, ref xL, 8 * n, Consts.floatSqrtEps));
            AssertVecEq(in xC, in xL, LooseTol());          // both land on the unique min-norm solution

            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            var xCb = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgne(in bsm, in b, ref xCb, 8 * n, Consts.floatSqrtEps));
            AssertVecEq(in xC, in xCb, LooseTol());          // BSR agrees with dense CGNE

            arena.Dispose();
        }

        // ---- CGNE warm start (consistent under-determined system) ----
        void CgneWarmStart()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 4, n = 10;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 44101);
            var xGen = arena.floatRandomVec(n, -1f, 1f, 44102);
            var b = Blas.dot(A, xGen);      // consistent

            var x = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgne(in A, in b, ref x, 8 * n, Consts.floatSqrtEps));

            var xWarm = x.Copy();
            bool okWarm = Krylov.cgne(in A, in b, ref xWarm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(okWarm);                            // pre-loop residual check reports convergence
            AssertVecEq(in x, in xWarm, LooseTol());          // and leaves x untouched

            arena.Dispose();
        }

        // ---- CGNE on an INCONSISTENT system: b not in range(A) ----
        // An over-determined (m > n) A with a random b is generically inconsistent (b has a
        // component orthogonal to range(A)). CGNE drives r = b - Ax toward zero, which is
        // impossible here -- ||r|| stalls at the nonzero least-squares residual -- so it runs out
        // of iterations (or breaks down on A^T r == 0 at the LS point) and returns false. CGLS on
        // the SAME system converges to the least-squares minimizer. This pins the behavioral
        // contract that CGNE is for consistent systems only and exercises the false-return path.
        void CgneInconsistentDoesNotConverge()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 10, n = 4;                                 // over-determined
            var A = arena.floatRandomMat(m, n, -1f, 1f, 44201);
            var b = arena.floatRandomVec(m, -1f, 1f, 44202);  // random rhs: generically NOT in range(A)

            var xC = arena.floatVec(n);
            bool okCgne = Krylov.cgne(in A, in b, ref xC, 8 * n, Consts.floatSqrtEps);
            Assert.IsFalse(okCgne);                            // cannot reach zero residual -> false

            // CGLS solves the same system in the least-squares sense (normal equations A^T r = 0).
            var xLs = arena.floatVec(n);
            Assert.IsTrue(Krylov.cgls(in A, in b, ref xLs, 8 * n, Consts.floatSqrtEps));
            AssertLeastSquaresOptimal(in A, in xLs, in b, LooseTol());

            arena.Dispose();
        }

        // ================== LS diagnostics (LstsqInfo) ==================

        // Independently recompute rnorm/Arnorm/xnorm EXACTLY from the returned x (paying a real
        // matvec) and assert the solver's FREE tracked estimates in the info struct match to within
        // LooseTol at convergence. r = b - A x, and A^T(A x - b) = -A^T r so ||A^T(Ax-b)|| == Arnorm;
        // for damp!=0 the reported optimality residual is ||A^T r - damp^2 x|| = ||A^T(Ax-b) + damp^2 x||.
        static void AssertInfoMatches(in floatMxN A, in floatN b, in floatN x, float damp,
                                      in LstsqInfo info, float tol)
        {
            var Ax  = Blas.dot(A, x);
            var res = Ax - b;                        // Ax - b  (= -r), length m
            float rnorm = math.sqrt(Blas.dot(res, res));
            Assert.IsTrue(math.abs(rnorm - info.rnorm) <= tol * ((float)1 + rnorm));

            var g = Blas.dot(res, A);           // A^T(Ax - b), length n  (vector*matrix)
            if (damp != (float)0)
                for (int i = 0; i < g.N; i++) g[i] += (damp * damp) * x[i];   // + damp^2 x
            float arnorm = math.sqrt(Blas.dot(g, g));
            Assert.IsTrue(math.abs(arnorm - info.Arnorm) <= tol * ((float)1 + arnorm));

            float xnorm = math.sqrt(Blas.dot(x, x));
            Assert.IsTrue(math.abs(xnorm - info.xnorm) <= tol * ((float)1 + xnorm));
        }

        // Diagnostics fields match an independent recompute across all three LS solvers, on an
        // INCONSISTENT over-determined system (so rnorm is meaningfully nonzero while Arnorm -> 0).
        void LstsqInfoMatchesIndependentRecompute()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 12, n = 5;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 51001);
            var b = arena.floatRandomVec(m, -1f, 1f, 51002);   // random rhs -> inconsistent
            int maxIter = 8 * n;

            var xC = arena.floatVec(n);
            var infoC = Krylov.cgls(in A, in b, ref xC, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(infoC.Solved);
            Assert.IsTrue(infoC.iterations >= 1 && infoC.iterations <= maxIter);
            AssertInfoMatches(in A, in b, in xC, (float)0, in infoC, LooseTol());

            var xL = arena.floatVec(n);
            var infoL = Krylov.lsqr(in A, in b, ref xL, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(infoL.Solved);
            Assert.IsTrue(infoL.iterations >= 1 && infoL.iterations <= maxIter);
            AssertInfoMatches(in A, in b, in xL, (float)0, in infoL, LooseTol());

            var xM = arena.floatVec(n);
            var infoM = Krylov.lsmr(in A, in b, ref xM, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(infoM.Solved);
            Assert.IsTrue(infoM.iterations >= 1 && infoM.iterations <= maxIter);
            AssertInfoMatches(in A, in b, in xM, (float)0, in infoM, LooseTol());

            // Inconsistent system: residual is nonzero but the normal-equation residual vanishes.
            Assert.IsTrue(infoC.rnorm > (float)0.01);
            Assert.IsTrue(infoC.Arnorm <= LooseTol() * ((float)1 + infoC.rnorm));

            arena.Dispose();
        }

        // Damped diagnostics: with damp != 0 the reported Arnorm is the DAMPED normal-equation
        // residual ||A^T r - damp^2 x||, which the independent recompute must reproduce.
        void LstsqInfoDampedArnorm()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 12, n = 5;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 51101);
            var b = arena.floatRandomVec(m, -1f, 1f, 51102);
            float damp = (float)0.3;
            int maxIter = 8 * n;

            var xC = arena.floatVec(n);
            var infoC = Krylov.cgls(in A, in b, ref xC, maxIter, Consts.floatSqrtEps, damp);
            Assert.IsTrue(infoC.Solved);
            AssertInfoMatches(in A, in b, in xC, damp, in infoC, LooseTol());

            var xL = arena.floatVec(n);
            var infoL = Krylov.lsqr(in A, in b, ref xL, maxIter, Consts.floatSqrtEps, damp);
            Assert.IsTrue(infoL.Solved);
            AssertInfoMatches(in A, in b, in xL, damp, in infoL, LooseTol());

            var xM = arena.floatVec(n);
            var infoM = Krylov.lsmr(in A, in b, ref xM, maxIter, Consts.floatSqrtEps, damp);
            Assert.IsTrue(infoM.Solved);
            AssertInfoMatches(in A, in b, in xM, damp, in infoM, LooseTol());

            arena.Dispose();
        }

        // The LSMR ‖r‖ figure is the one genuinely subtle piece of the free diagnostics: LSMR never
        // holds the residual r = b - A x, so info.rnorm comes from the Fong-Saunders §5.4 scalar
        // recurrence run over the same rotations (O(1)/iteration, no matvec). Pin it against the
        // certified-exact residual (Krylov.lstsqResidual, one real Apply+ApplyT) on BOTH a
        // consistent system (rnorm -> 0) and an inconsistent one (rnorm large) -- the case most
        // likely to expose a recurrence bug.
        void LsmrRnormMatchesExact()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 16, n = 6;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 51401);

            // (a) consistent: b = A x* for a known x* -> exact recovery, rnorm -> 0.
            var xStar = arena.floatRandomVec(n, -1f, 1f, 51402);
            var bCons = Blas.dot(A, xStar);                // bCons = A x*  (consistent)

            // (b) inconsistent: random rhs -> nonzero least-squares residual.
            var bInc = arena.floatRandomVec(m, -1f, 1f, 51403);

            int maxIter = 8 * n;
            var rS = arena.floatVec(m);
            var sS = arena.floatVec(n);

            var xc = arena.floatVec(n);
            var ic = Krylov.lsmr(in A, in bCons, ref xc, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(ic.Solved);
            var exC = Krylov.lstsqResidual(new floatDenseOperator(in A), in bCons, in xc, (float)0, ref rS, ref sS);
            AssertClose(ic.rnorm, exC.rnorm, LooseTol());

            var xi = arena.floatVec(n);
            var ii = Krylov.lsmr(in A, in bInc, ref xi, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(ii.Solved);
            var exI = Krylov.lstsqResidual(new floatDenseOperator(in A), in bInc, in xi, (float)0, ref rS, ref sS);
            Assert.IsTrue(exI.rnorm > (float)0.1);             // genuinely inconsistent
            AssertClose(ii.rnorm, exI.rnorm, LooseTol());

            // (c) mid-flight: force MaxIterations (maxIter=2) so the ‖r‖ recurrence is pinned BEFORE
            // convergence -- where the dnorm += betacheck² accumulation would drift if transcribed
            // wrong. rnorm must still equal the exact residual of the (un-converged) iterate.
            var xf = arena.floatVec(n);
            var if2 = Krylov.lsmr(in A, in bInc, ref xf, 2, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(!if2.Solved && if2.iterations == 2);   // genuinely stopped mid-flight
            var exF = Krylov.lstsqResidual(new floatDenseOperator(in A), in bInc, in xf, (float)0, ref rS, ref sS);
            AssertClose(if2.rnorm, exF.rnorm, LooseTol());

            arena.Dispose();
        }

        // The BSR diagnostic overload reports the same diagnostics (up to iterative tolerance) as the
        // dense one for the SAME system -- confirms the BSR info path (with A^T materialization) feeds
        // lstsqInfo identically.
        void LstsqInfoBSRMatchesDense()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 12, n = 5;
            var A = arena.floatRandomMat(m, n, -1f, 1f, 51301);
            var b = arena.floatRandomVec(m, -1f, 1f, 51302);
            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            int maxIter = 8 * n;

            var xD = arena.floatVec(n);
            var infoD = Krylov.lsqr(in A, in b, ref xD, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(infoD.Solved);

            var xB = arena.floatVec(n);
            var infoB = Krylov.lsqr(in bsm, in b, ref xB, maxIter, Consts.floatSqrtEps, (float)0);
            Assert.IsTrue(infoB.Solved);

            AssertVecEq(in xD, in xB, LooseTol());
            double sr = (float)1 + infoD.rnorm;
            Assert.IsTrue(math.abs(infoD.rnorm  - infoB.rnorm)  <= LooseTol() * sr);
            Assert.IsTrue(math.abs(infoD.Arnorm - infoB.Arnorm) <= LooseTol() * ((float)1 + infoD.Arnorm));
            Assert.IsTrue(math.abs(infoD.xnorm  - infoB.xnorm)  <= LooseTol() * ((float)1 + infoD.xnorm));

            arena.Dispose();
        }

        // ================== AᵀA-Jacobi preconditioner ==================

        // A = Q·diag(s) with Q orthonormal columns (QR of a random tall matrix) and s spanning
        // magnitudes (2^0..2^(n-1)). AᵀA = diag(s²) is badly scaled: the plain solve sees n distinct
        // eigenvalues (needs ~n Krylov steps), while column-Jacobi maps A·D = Q (unit-conditioned),
        // so the preconditioned solve converges almost immediately.
        static floatMxN BuildBadlyScaledOrthonormal(ref Arena arena, int m, int n, uint seed)
        {
            var Q = arena.floatRandomMat(m, n, -1f, 1f, seed);
            var R = arena.floatMat(n, n);
            QR.decompInPlace(ref Q, ref R);           // Q now has orthonormal columns

            var A = arena.floatMat(m, n);
            for (int j = 0; j < n; j++)
            {
                float s = math.pow((float)2, (float)j);
                for (int i = 0; i < m; i++) A[i, j] = Q[i, j] * s;
            }
            return A;
        }

        // The preconditioner STRICTLY reduces iterations on the badly-scaled system, both reach the
        // least-squares optimum, and the lsqrJacobi convenience wrapper matches the composable path.
        void JacobiPreconditionerReducesIterations()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 30, n = 12;
            var A = BuildBadlyScaledOrthonormal(ref arena, m, n, 52001);
            var b = arena.floatRandomVec(m, -1f, 1f, 52002);
            int maxIter = 4 * n;
            float tol = Consts.floatSqrtEps;

            // plain lsqr with diagnostics
            var xPlain = arena.floatVec(n);
            var infoPlain = Krylov.lsqr(in A, in b, ref xPlain, maxIter, tol, (float)0);
            Assert.IsTrue(infoPlain.Solved);

            // preconditioned via the composable diagnostic path (to read the iteration count)
            var d2 = arena.floatVec(n); Blas.columnNormsSquared(in A, ref d2);
            var d  = arena.floatVec(n); Blas.buildJacobiScale(in d2, ref d);
            var scratch = arena.floatVec(n);
            var op = new floatColScaledOperator<floatDenseOperator>(new floatDenseOperator(in A), d, scratch);

            var y    = arena.floatVec(n);
            var u    = arena.floatVec(m);
            var v    = arena.floatVec(n);
            var w    = arena.floatVec(n);
            var tmpM = arena.floatVec(m);
            var tmpN = arena.floatVec(n);
            var infoJac = Krylov.lsqr(op, in b, ref y, ref u, ref v, ref w, ref tmpM, ref tmpN, maxIter, tol, (float)0);
            Assert.IsTrue(infoJac.Solved);
            var xComp = arena.floatVec(n);
            for (int j = 0; j < n; j++) xComp[j] = d[j] * y[j];

            Assert.IsTrue(infoJac.iterations < infoPlain.iterations);   // the preconditioner's payoff

            // Both land on a least-squares-optimal x (‖Aᵀr‖ ≈ 0). (Forward error is NOT compared:
            // the plain solve on this ill-conditioned system is much less accurate per-component.)
            AssertLeastSquaresOptimal(in A, in xComp,  in b, LooseTol());
            AssertLeastSquaresOptimal(in A, in xPlain, in b, LooseTol());

            // The convenience wrapper reproduces the composable path exactly.
            var xConv = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqrJacobi(in A, in b, ref xConv, maxIter, tol));
            AssertVecEq(in xConv, in xComp, LooseTol());

            arena.Dispose();
        }

        // Every *Jacobi convenience wrapper (cgls/lsqr/lsmr, dense AND 1x1-BSR) solves the
        // badly-scaled system to least-squares optimality, and the BSR form matches the dense form.
        void JacobiConvenienceSolversLSOptimalDenseAndBSR()
        {
            var arena = new Arena(Allocator.Persistent);

            int m = 24, n = 8;
            var A = BuildBadlyScaledOrthonormal(ref arena, m, n, 52101);
            var b = arena.floatRandomVec(m, -1f, 1f, 52102);
            var bsm = DenseToBSR1x1(ref arena, in A, m * n);
            int maxIter = 4 * n;
            float tol = Consts.floatSqrtEps;

            // cgls
            var xCd = arena.floatVec(n);
            Assert.IsTrue(Krylov.cglsJacobi(in A, in b, ref xCd, maxIter, tol));
            AssertLeastSquaresOptimal(in A, in xCd, in b, LooseTol());
            var xCb = arena.floatVec(n);
            Assert.IsTrue(Krylov.cglsJacobi(in bsm, in b, ref xCb, maxIter, tol));
            AssertVecEq(in xCd, in xCb, LooseTol());

            // lsqr
            var xLd = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqrJacobi(in A, in b, ref xLd, maxIter, tol));
            AssertLeastSquaresOptimal(in A, in xLd, in b, LooseTol());
            var xLb = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsqrJacobi(in bsm, in b, ref xLb, maxIter, tol));
            AssertVecEq(in xLd, in xLb, LooseTol());

            // lsmr
            var xMd = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsmrJacobi(in A, in b, ref xMd, maxIter, tol));
            AssertLeastSquaresOptimal(in A, in xMd, in b, LooseTol());
            var xMb = arena.floatVec(n);
            Assert.IsTrue(Krylov.lsmrJacobi(in bsm, in b, ref xMb, maxIter, tol));
            AssertVecEq(in xMd, in xMb, LooseTol());

            arena.Dispose();
        }

        // LITERATURE GROUND TRUTH (Strang, Introduction to Linear Algebra -- best-fit line):
        // fit b = C + D t at t = 0,1,2 with data b = (6,0,0). A = [[1,0],[1,1],[1,2]].
        // Normal equations AᵀA x = Aᵀb -> [[3,3],[3,5]] x = [6,0] -> x = (5, -3) EXACTLY.
        // Residual r = b - A x = (1,-2,1): rnorm = sqrt(6), Aᵀr = (0,0) so Arnorm = 0, xnorm = sqrt(34).
        // Pins the diagnostics AND the LS solution against literal hand-computed constants (external
        // ground truth), not another solver.
        void LstsqInfoStrangLineFitExact()
        {
            var arena = new Arena(Allocator.Persistent);

            var A = arena.floatMat(3, 2);
            A[0, 0] = (float)1; A[0, 1] = (float)0;
            A[1, 0] = (float)1; A[1, 1] = (float)1;
            A[2, 0] = (float)1; A[2, 1] = (float)2;
            var b = arena.floatVec(3);
            b[0] = (float)6; b[1] = (float)0; b[2] = (float)0;

            float tol = Consts.floatSqrtEps;
            float expRnorm = math.sqrt((float)6);
            float expXnorm = math.sqrt((float)34);

            // All three solvers must reproduce x = (5,-3) and the exact diagnostics.
            for (int which = 0; which < 3; which++)
            {
                var x = arena.floatVec(2);
                LstsqInfo info;
                if (which == 0)      info = Krylov.cgls(in A, in b, ref x, 50, tol, (float)0);
                else if (which == 1) info = Krylov.lsqr(in A, in b, ref x, 50, tol, (float)0);
                else                 info = Krylov.lsmr(in A, in b, ref x, 50, tol, (float)0);

                Assert.IsTrue(info.Solved);
                AssertClose(x[0], (float)5,    LooseTol());
                AssertClose(x[1], (float)(-3), LooseTol());
                AssertClose(info.rnorm, expRnorm, LooseTol());
                Assert.IsTrue(info.Arnorm <= LooseTol() * ((float)1 + expRnorm));   // Aᵀr = 0 exactly
                AssertClose(info.xnorm, expXnorm, LooseTol());
            }

            arena.Dispose();
        }

        // Independently recompute ‖b - A x‖ (one real matvec) and check the solver's FREE rnorm
        // (a value it already tracked -- never a fresh A*x) matches it.
        static void AssertResidualNorm(in floatMxN A, in floatN b, in floatN x, double rnorm, float tol)
        {
            var Ax = Blas.dot(A, x);
            float acc = (float)0;
            for (int i = 0; i < b.N; i++) { float e = b[i] - Ax[i]; acc += e * e; }
            AssertClose(rnorm, math.sqrt(acc), tol);
        }

        // BSR counterpart: recompute ‖b - A x‖ with the SAME sparse matvec the solver tracked its
        // residual through (spMV), so the check stays in-arithmetic rather than comparing a BSR-
        // tracked rnorm against a dense recompute (whose summation order differs).
        static void AssertResidualNormBSR(in floatBSR A, in floatN b, in floatN x, double rnorm, float tol)
        {
            var Ax = BSR.spMV(in A, in x);
            float acc = (float)0;
            for (int i = 0; i < b.N; i++) { float e = b[i] - Ax[i]; acc += e * e; }
            AssertClose(rnorm, math.sqrt(acc), tol);
        }

        // The square solvers (cg/pcg/minres/biCGStab/cgne) RETURN an SolveInfo
        // whose rnorm is filled from each solver's already-tracked residual -- cg/pcg/cgne a live
        // ‖r‖ (√ of the dot they already form for the convergence test), minres its phibar (the
        // MINRES identity), biCGStab its running ‖r‖ -- never a fresh matvec. Pin that free rnorm
        // against an INDEPENDENTLY recomputed ‖b - A x‖ for every solver + operator shape, plus a
        // forced-MaxIterations mid-flight case where rnorm must still equal the exact residual of
        // the un-converged iterate (that path returns √rsold, the post-update recurrence residual).
        void SolveInfoRnormMatchesResidual()
        {
            var arena = new Arena(Allocator.Persistent);
            float tol = LooseTol();

            // ---- SPD system: cg, pcg (block-Jacobi over BSR), minres ----
            int n = 12;
            var Aspd = BuildDenseSPD(ref arena, n, 52001);
            var bspd = arena.floatRandomVec(n, -1f, 1f, 52002);
            var bsm = DenseToBSR1x1(ref arena, in Aspd, n * n);
            var M = arena.floatBlockJacobi(in bsm);
            int maxIter = 4 * n;

            var xg = arena.floatVec(n);
            var ig = Krylov.cg(in Aspd, in bspd, ref xg, maxIter, Consts.floatSqrtEps);
            Assert.IsTrue(ig.Solved && ig.iterations >= 1 && ig.iterations <= maxIter);
            AssertResidualNorm(in Aspd, in bspd, in xg, ig.rnorm, tol);

            var xp = arena.floatVec(n);
            var ip = Krylov.pcg(in bsm, in M, in bspd, ref xp, maxIter, Consts.floatSqrtEps);
            Assert.IsTrue(ip.Solved && ip.iterations >= 1);
            AssertResidualNormBSR(in bsm, in bspd, in xp, ip.rnorm, tol);   // BSR solve -> BSR recompute

            var xm = arena.floatVec(n);
            var im = Krylov.minres(in Aspd, in bspd, ref xm, maxIter, Consts.floatSqrtEps);
            Assert.IsTrue(im.Solved && im.iterations >= 1);
            AssertResidualNorm(in Aspd, in bspd, in xm, im.rnorm, tol);

            // ---- Non-symmetric diagonally-dominant system: biCGStab ----
            int d = 8;
            var Ansym = arena.floatRandomMat(d, d, -1f, 1f, 52101);
            for (int i = 0; i < d; i++) Ansym[i, i] += (float)(d + 1);   // strict diagonal dominance
            var bns = arena.floatRandomVec(d, -1f, 1f, 52102);
            var xb = arena.floatVec(d);
            var ib = Krylov.biCGStab(in Ansym, in bns, ref xb, 4 * d, Consts.floatSqrtEps);
            Assert.IsTrue(ib.Solved && ib.iterations >= 1);
            AssertResidualNorm(in Ansym, in bns, in xb, ib.rnorm, tol);

            // ---- Underdetermined CONSISTENT system (m < n): cgne (min-norm) ----
            int mm = 6, nn = 10;
            var Aun = arena.floatRandomMat(mm, nn, -1f, 1f, 52201);
            var xStar = arena.floatRandomVec(nn, -1f, 1f, 52202);
            var bun = Blas.dot(Aun, xStar);                          // b = A x*  (in range(A))
            var xn = arena.floatVec(nn);
            var inx = Krylov.cgne(in Aun, in bun, ref xn, 4 * nn, Consts.floatSqrtEps);
            Assert.IsTrue(inx.Solved && inx.iterations >= 1);
            AssertResidualNorm(in Aun, in bun, in xn, inx.rnorm, tol);

            // ---- Mid-flight MaxIterations for EVERY square solver: one (or two) step(s) does NOT
            //      converge, yet rnorm must still equal ‖b - A x‖ of the (updated) un-converged
            //      iterate -- the contract that on MaxIterations x is a valid last iterate, not
            //      undefined. Each returns √(tracked residual) with x fully advanced. ----
            var xh = arena.floatVec(n);
            var ih = Krylov.cg(in Aspd, in bspd, ref xh, 1, Consts.floatSqrtEps);
            Assert.IsTrue(!ih.Solved && ih.status == IterativeSolveStatus.MaxIterations && ih.iterations == 1);
            AssertResidualNorm(in Aspd, in bspd, in xh, ih.rnorm, tol);

            var xhp = arena.floatVec(n);
            var ihp = Krylov.pcg(in bsm, in M, in bspd, ref xhp, 1, Consts.floatSqrtEps);
            Assert.IsTrue(ihp.status == IterativeSolveStatus.MaxIterations && ihp.iterations == 1);
            AssertResidualNormBSR(in bsm, in bspd, in xhp, ihp.rnorm, tol);

            var xhm = arena.floatVec(n);
            var ihm = Krylov.minres(in Aspd, in bspd, ref xhm, 1, Consts.floatSqrtEps);
            Assert.IsTrue(ihm.status == IterativeSolveStatus.MaxIterations && ihm.iterations == 1);
            AssertResidualNorm(in Aspd, in bspd, in xhm, ihm.rnorm, tol);

            var xhb = arena.floatVec(d);
            var ihb = Krylov.biCGStab(in Ansym, in bns, ref xhb, 1, Consts.floatSqrtEps);
            Assert.IsTrue(ihb.status == IterativeSolveStatus.MaxIterations && ihb.iterations == 1);
            AssertResidualNorm(in Ansym, in bns, in xhb, ihb.rnorm, tol);

            var xhn = arena.floatVec(nn);
            var ihn = Krylov.cgne(in Aun, in bun, ref xhn, 1, Consts.floatSqrtEps);
            Assert.IsTrue(ihn.status == IterativeSolveStatus.MaxIterations && ihn.iterations == 1);
            AssertResidualNorm(in Aun, in bun, in xhn, ihn.rnorm, tol);

            // ---- Breakdown path: CG on the indefinite A = diag(1,-1) with b = (1,1) and x₀ = 0
            //      hits p·Ap = 1·1 + 1·(-1) = 0 on the very first step -> Breakdown at iterations=0,
            //      x untouched (= 0). rnorm must be the residual of that x: ‖b - A·0‖ = ‖b‖ = √2. ----
            var Aind = arena.floatMat(2, 2);
            Aind[0, 0] = (float)1; Aind[0, 1] = (float)0;
            Aind[1, 0] = (float)0; Aind[1, 1] = (float)(-1);
            var bind = arena.floatVec(2); bind[0] = (float)1; bind[1] = (float)1;
            var xind = arena.floatVec(2); xind[0] = (float)0; xind[1] = (float)0;
            var iind = Krylov.cg(in Aind, in bind, ref xind, 10, Consts.floatSqrtEps);
            Assert.IsTrue(iind.status == IterativeSolveStatus.Breakdown && iind.iterations == 0);
            AssertResidualNorm(in Aind, in bind, in xind, iind.rnorm, tol);
            AssertClose(iind.rnorm, math.sqrt((float)2), tol);

            // ---- b == 0 shortcut: the unique solution is x = 0, reported as Converged at
            //      iterations=0 with rnorm exactly 0 (no matvec, b copied through). ----
            var bzero = arena.floatVec(n);
            for (int i = 0; i < n; i++) bzero[i] = (float)0;
            var xzero = arena.floatVec(n);
            var izero = Krylov.cg(in Aspd, in bzero, ref xzero, maxIter, Consts.floatSqrtEps);
            Assert.IsTrue(izero.Solved && izero.iterations == 0);
            AssertClose(izero.rnorm, (float)0, tol);

            arena.Dispose();
        }

        // Damped least-squares reference: min ||Ax-b||^2 + damp^2||x||^2 == the plain least-squares
        // solution of the augmented system [A; damp*I] x ~= [b; 0], solved with dense QR. QR.solveInPlace
        // is DESTRUCTIVE, so the augmented matrix/rhs are fresh temporaries.
        static floatN DampedReferenceQR(ref Arena arena, in floatMxN A, in floatN b, float damp)
        {
            int m = A.M_Rows, n = A.N_Cols;
            var Atil = arena.floatMat(m + n, n);
            for (int i = 0; i < m; i++)
                for (int j = 0; j < n; j++)
                    Atil[i, j] = A[i, j];
            for (int i = 0; i < n; i++)
                for (int j = 0; j < n; j++)
                    Atil[m + i, j] = (i == j) ? damp : (float)0;

            var btil = arena.floatVec(m + n);
            for (int i = 0; i < m; i++) btil[i] = b[i];
            for (int i = 0; i < n; i++) btil[m + i] = (float)0;

            var xref = arena.floatVec(n);
            QR.solveInPlace(ref Atil, ref btil, ref xref);
            return xref;
        }
    }

    // Deliberately non-SPD test-double preconditioner: z = M^-1 r := -r, so <r,z> = -||r||^2 <= 0.
    // Used only by PcgNonSpdPreconditionerBreaksDown to exercise pcg's rzold>0 breakdown guard.
    public struct floatNegatePreconditioner : IfloatPreconditioner
    {
        public void Apply(in floatN r, ref floatN z)
        {
            for (int i = 0; i < r.N; i++)
                z[i] = -r[i];
        }
    }

    // ---- correctness cases (Burst) -------------------------------------------------------

    [Test]
    public void Laplacian1DBSRCGMatchesDenseCGTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.Laplacian1DBSRCGMatchesDenseCG }.Run();

    [Test]
    public void ThreeByThreeBlockSPDConvergesTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.ThreeByThreeBlockSPDConverges }.Run();

    [Test]
    public void DenseForwardingUnchangedTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.DenseForwardingUnchanged }.Run();

    [Test]
    public void PCGMatchesCGTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.PCGMatchesCG }.Run();

    [Test]
    public void PCGBeatsCGIllConditionedTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.PCGBeatsCGIllConditioned }.Run();

    [Test]
    public void BlockJacobiApplyHandComputedTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.BlockJacobiApplyHandComputed }.Run();

    [Test]
    public void WarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.WarmStart }.Run();

    [Test]
    public void PcgNonSpdPreconditionerBreaksDownTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.PcgNonSpdPreconditionerBreaksDown }.Run();

    // ---- Phase 3 correctness entry points ----

    [Test]
    public void MinresIndefiniteDenseAndBSRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.MinresIndefiniteDenseAndBSR }.Run();

    [Test]
    public void MinresSpdMatchesCGTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.MinresSpdMatchesCG }.Run();

    [Test]
    public void BiCGStabNonSymmetricMatchesLUTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.BiCGStabNonSymmetricMatchesLU }.Run();

    [Test]
    public void CglsOverdeterminedConsistentDenseAndBSRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CglsOverdeterminedConsistentDenseAndBSR }.Run();

    [Test]
    public void LsqrOverdeterminedConsistentDenseAndBSRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsqrOverdeterminedConsistentDenseAndBSR }.Run();

    [Test]
    public void CglsInconsistentMatchesQRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CglsInconsistentMatchesQR }.Run();

    [Test]
    public void LsqrInconsistentMatchesQRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsqrInconsistentMatchesQR }.Run();

    [Test]
    public void CglsLsqrUnderdeterminedConsistentTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CglsLsqrUnderdeterminedConsistent }.Run();

    // ---- LSMR entry points ----

    [Test]
    public void LsmrOverdeterminedConsistentDenseAndBSRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrOverdeterminedConsistentDenseAndBSR }.Run();

    [Test]
    public void LsmrInconsistentMatchesQRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrInconsistentMatchesQR }.Run();

    [Test]
    public void LsmrUnderdeterminedMatchesLsqrTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrUnderdeterminedMatchesLsqr }.Run();

    [Test]
    public void LsmrMonotonicArnormTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrMonotonicArnorm }.Run();

    [Test]
    public void TikhonovDampingMatchesAugmentedQRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.TikhonovDampingMatchesAugmentedQR }.Run();

    [Test]
    public void CgneUnderdeterminedMinNormMatchesLsqrTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CgneUnderdeterminedMinNormMatchesLsqr }.Run();

    [Test]
    public void CgneWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CgneWarmStart }.Run();

    [Test]
    public void CgneInconsistentDoesNotConvergeTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CgneInconsistentDoesNotConverge }.Run();

    // ---- LS diagnostics entry points ----

    [Test]
    public void LstsqInfoMatchesIndependentRecomputeTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LstsqInfoMatchesIndependentRecompute }.Run();

    [Test]
    public void LstsqInfoDampedArnormTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LstsqInfoDampedArnorm }.Run();

    [Test]
    public void LsmrRnormMatchesExactTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrRnormMatchesExact }.Run();

    [Test]
    public void LstsqInfoBSRMatchesDenseTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LstsqInfoBSRMatchesDense }.Run();

    [Test]
    public void JacobiPreconditionerReducesIterationsTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.JacobiPreconditionerReducesIterations }.Run();

    [Test]
    public void JacobiConvenienceSolversLSOptimalDenseAndBSRTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.JacobiConvenienceSolversLSOptimalDenseAndBSR }.Run();

    [Test]
    public void LstsqInfoStrangLineFitExactTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LstsqInfoStrangLineFitExact }.Run();

    [Test]
    public void SolveInfoRnormMatchesResidualTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.SolveInfoRnormMatchesResidual }.Run();

    // ---- Phase 3 warm-start entry points ----

    [Test]
    public void MinresWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.MinresWarmStart }.Run();

    [Test]
    public void BiCGStabWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.BiCGStabWarmStart }.Run();

    [Test]
    public void CglsWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.CglsWarmStart }.Run();

    [Test]
    public void LsqrWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsqrWarmStart }.Run();

    [Test]
    public void LsmrWarmStartTest()
        => new SparseSolverTestJob { Type = SparseSolverTestJob.TestType.LsmrWarmStart }.Run();

    // ---- guard / exception cases (managed thread; Assert.Throws can't run inside Burst) ----

    static floatBSR BuildSquareBSR(ref Arena arena)
    {
        const int BR = 2, BC = 2;
        var builder = arena.floatBSRBuilder(2, 2, BR, BC, 2);
        builder.AddBlock(0, 0, arena.floatRandomMat(BR, BC, -1f, 1f, 6101));
        builder.AddBlock(1, 1, arena.floatRandomMat(BR, BC, -1f, 1f, 6102));
        return builder.ToBSR(ref arena);
    }

    [Test]
    public void BlockJacobi_MissingDiagonalBlock_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int BR = 2;
            // Only an off-diagonal block -- no block at (0,0) or (1,1).
            var builder = arena.floatBSRBuilder(2, 2, BR, BR, 1);
            builder.AddBlock(0, 1, arena.floatRandomMat(BR, BR, -1f, 1f, 6201));
            var A = builder.ToBSR(ref arena);

            Assert.Throws<ArgumentException>(() => arena.floatBlockJacobi(in A));
        }
        finally { arena.Dispose(); }
    }

    // Numerical breakdown reporting: the out-info overloads return a DirectSolveInfo instead of
    // throwing; the info-less conveniences keep throwing for the same condition.
    [Test]
    public void BlockJacobi_SingularDiagonalBlock_StatusAndThrow()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int BR = 2;
            var builder = arena.floatBSRBuilder(2, 2, BR, BR, 2);
            builder.AddBlock(0, 0, arena.floatDiagonalMat(BR, (float)4));
            builder.AddBlock(1, 1, arena.floatMat(BR, BR));   // all-zero diagonal block: singular
            var A = builder.ToBSR(ref arena);

            var M = arena.floatBlockJacobi(in A, out PreconditionerInfo info);
            Assert.IsFalse(info.Solved);

            Assert.Throws<ArgumentException>(() => arena.floatBlockJacobi(in A));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Preconditioner_StatusOverloads_SuccessPath()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int BR = 2;
            var builder = arena.floatBSRBuilder(2, 2, BR, BR, 2);
            builder.AddBlock(0, 0, arena.floatDiagonalMat(BR, (float)4));
            builder.AddBlock(1, 1, arena.floatDiagonalMat(BR, (float)9));
            var A = builder.ToBSR(ref arena);

            var mJ = arena.floatBlockJacobi(in A, out PreconditionerInfo infoJ);
            Assert.IsTrue(infoJ.Solved);
            Assert.IsTrue(infoJ.attempts == 1);
            var mI = arena.floatIC0(in A, out PreconditionerInfo infoI);
            Assert.IsTrue(infoI.Solved);
            Assert.IsTrue(infoI.shift == 0.0);   // clean SPD build: no rescue shift
            var mL = arena.floatILU0(in A, out PreconditionerInfo infoL);
            Assert.IsTrue(infoL.Solved);
            Assert.IsTrue(infoL.attempts == 1);

            // The successfully-built Jacobi is usable: z = M^-1 r on the 4x4 system.
            var r = arena.floatVec(2 * BR, (float)1);
            var z = arena.floatVec(2 * BR);
            mJ.Apply(in r, ref z);
            Assert.IsTrue(math.abs((float)(z[0] - (float)0.25)) < 1e-6f);
            Assert.IsTrue(math.abs((float)(z[2 * BR - 1] - (float)(1.0 / 9.0))) < 1e-6f);
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void BlockJacobi_NonSquareBSR_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int BR = 2, BC = 3;
            var builder = arena.floatBSRBuilder(2, 2, BR, BC, 1); // BR != BC
            builder.AddBlock(0, 0, arena.floatRandomMat(BR, BC, -1f, 1f, 6301));
            var A = builder.ToBSR(ref arena);

            Assert.Throws<ArgumentException>(() => arena.floatBlockJacobi(in A));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Cg_NonSquareDenseOperator_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            var A = arena.floatMat(3, 4); // non-square
            var op = new floatDenseOperator(in A);
            var b = arena.floatVec(3);
            var x = arena.floatVec(4);
            var r = arena.floatVec(3);
            var p = arena.floatVec(3);
            var Ap = arena.floatVec(3);

            Assert.Throws<ArgumentException>(() =>
                Krylov.cg(in op, in b, ref x, ref r, ref p, ref Ap, 4, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    // ---- scratch-aliasing guards for cg / pcg --------------------------------------------
    //
    // cg/pcg throw if ANY two of their vector arguments share a Data.Ptr (the elementwise axpy
    // scratch updates silently corrupt on aliasing rather than self-checking). The pairs below
    // are chosen to be ones NOT already caught by a downstream Apply/dot guard, so each proves
    // the up-front distinctness check is doing real work. The guard runs before any computation,
    // so the operator matrix's contents are irrelevant -- a bare square floatMat suffices for cg.

    [Test]
    public void Cg_AliasingRAndAp_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int dim = 4;
            var A = arena.floatMat(dim, dim); // square; guard fires before A is read
            var op = new floatDenseOperator(in A);
            var b = arena.floatRandomVec(dim, -1f, 1f, 6501);
            var x = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            var rAlias = Ap; // r aliases Ap (would turn r -= Ap into r -= r == 0: false convergence)
            Assert.Throws<ArgumentException>(() =>
                Krylov.cg(in op, in b, ref x, ref rAlias, ref p, ref Ap, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Cg_AliasingRAndX_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int dim = 4;
            var A = arena.floatMat(dim, dim);
            var op = new floatDenseOperator(in A);
            var b = arena.floatRandomVec(dim, -1f, 1f, 6511);
            var x = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            var rAlias = x; // r aliases x (r.CopyFrom(b) would silently clobber the initial guess)
            Assert.Throws<ArgumentException>(() =>
                Krylov.cg(in op, in b, ref x, ref rAlias, ref p, ref Ap, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Pcg_AliasingRAndX_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            var A = BuildSquareBSR(ref arena);       // 4 x 4, both diagonal blocks present
            var M = arena.floatBlockJacobi(in A);
            int dim = A.M_Rows;
            var b = arena.floatRandomVec(dim, -1f, 1f, 6521);
            var x = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            var z = arena.floatVec(dim);
            var rAlias = x; // r aliases x
            Assert.Throws<ArgumentException>(() =>
                Krylov.pcg(in A, in M, in b, ref x, ref rAlias, ref p, ref Ap, ref z, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Pcg_AliasingZAndX_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            var A = BuildSquareBSR(ref arena);
            var M = arena.floatBlockJacobi(in A);
            int dim = A.M_Rows;
            var b = arena.floatRandomVec(dim, -1f, 1f, 6531);
            var x = arena.floatVec(dim);
            var r = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            var zAlias = x; // z aliases x (not caught by M.Apply's own r/z guard)
            Assert.Throws<ArgumentException>(() =>
                Krylov.pcg(in A, in M, in b, ref x, ref r, ref p, ref Ap, ref zAlias, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    // x aliasing b: the final pair among {r,p,Ap,x,b} / {r,p,Ap,z,x,b}. Benign in the current loop
    // (b isn't reread after the initial residual), but the guard is documented as ALL-pairs-distinct
    // so it must still throw. xAlias is a struct-copy of b (shares Data.Ptr) -- passing them as two
    // distinct locals keeps b as `in` and x as `ref` without an in/ref same-variable conflict.
    [Test]
    public void Cg_AliasingXAndB_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int dim = 4;
            var A = arena.floatMat(dim, dim); // square; guard fires before A is read
            var op = new floatDenseOperator(in A);
            var b = arena.floatRandomVec(dim, -1f, 1f, 6541);
            var xAlias = b; // x aliases b (struct copy shares Data.Ptr)
            var r = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            Assert.Throws<ArgumentException>(() =>
                Krylov.cg(in op, in b, ref xAlias, ref r, ref p, ref Ap, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Pcg_AliasingXAndB_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            var A = BuildSquareBSR(ref arena);
            var M = arena.floatBlockJacobi(in A);
            int dim = A.M_Rows;
            var b = arena.floatRandomVec(dim, -1f, 1f, 6551);
            var xAlias = b; // x aliases b (struct copy shares Data.Ptr)
            var r = arena.floatVec(dim);
            var p = arena.floatVec(dim);
            var Ap = arena.floatVec(dim);
            var z = arena.floatVec(dim);
            Assert.Throws<ArgumentException>(() =>
                Krylov.pcg(in A, in M, in b, ref xAlias, ref r, ref p, ref Ap, ref z, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    // ---- Phase 3 guard / aliasing cases (managed thread; Assert.Throws can't run in Burst) ----
    //
    // MINRES/BiCGSTAB/CGLS/LSQR share cg/pcg's "every vector argument must be a distinct buffer"
    // contract, enforced up front by RequireDistinctBuffers before any computation -- so the
    // operator matrix's contents are irrelevant and a bare zeroed floatMat suffices. 1-2 aliasing
    // cases per solver (matching Phase 2's coverage) prove the guard fires; exhaustive pairwise
    // coverage is not attempted.

    [Test]
    public void Minres_NonSquareDense_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            var A = arena.floatMat(3, 4); // non-square -> minres's A.Rows!=A.Cols guard fires first
            var b = arena.floatVec(3);
            var x = arena.floatVec(3);
            var y  = arena.floatVec(3); var r1 = arena.floatVec(3); var r2 = arena.floatVec(3);
            var v  = arena.floatVec(3); var w  = arena.floatVec(3);
            var w1 = arena.floatVec(3); var w2 = arena.floatVec(3);
            Assert.Throws<ArgumentException>(() =>
                Krylov.minres(in A, in b, ref x, ref y, ref r1, ref r2, ref v, ref w, ref w1, ref w2, 3, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Minres_AliasingR1AndR2_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int dim = 4;
            var A = arena.floatMat(dim, dim); // square; guard fires before A is read
            var b = arena.floatRandomVec(dim, -1f, 1f, 39001);
            var x = arena.floatVec(dim);
            var y  = arena.floatVec(dim);
            var r1 = arena.floatVec(dim);
            var v  = arena.floatVec(dim);
            var w  = arena.floatVec(dim);
            var w1 = arena.floatVec(dim);
            var w2 = arena.floatVec(dim);
            var r2Alias = r1; // r2 aliases r1
            Assert.Throws<ArgumentException>(() =>
                Krylov.minres(in A, in b, ref x, ref y, ref r1, ref r2Alias, ref v, ref w, ref w1, ref w2, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void BiCGStab_AliasingXAndB_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            const int dim = 4;
            var A = arena.floatMat(dim, dim);
            var b = arena.floatRandomVec(dim, -1f, 1f, 39101);
            var xAlias = b; // x aliases b (struct copy shares Data.Ptr)
            var r = arena.floatVec(dim); var rHat0 = arena.floatVec(dim); var p = arena.floatVec(dim);
            var v = arena.floatVec(dim); var t = arena.floatVec(dim);
            Assert.Throws<ArgumentException>(() =>
                Krylov.biCGStab(in A, in b, ref xAlias, ref r, ref rHat0, ref p, ref v, ref t, dim, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Cgls_AliasingRAndQ_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            int m = 5, n = 3;
            var A = arena.floatMat(m, n);      // rectangular; guard fires before A is read
            var b = arena.floatRandomVec(m, -1f, 1f, 39201);
            var x = arena.floatVec(n);
            var r = arena.floatVec(m);
            var s = arena.floatVec(n);
            var p = arena.floatVec(n);
            var qAlias = r; // q aliases r (both length m -> passes the dimension checks, trips the guard)
            Assert.Throws<ArgumentException>(() =>
                Krylov.cgls(in A, in b, ref x, ref r, ref s, ref p, ref qAlias, n, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Lsqr_AliasingUAndTmpM_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            int m = 5, n = 3;
            var A = arena.floatMat(m, n);
            var b = arena.floatRandomVec(m, -1f, 1f, 39301);
            var x = arena.floatVec(n);
            var u = arena.floatVec(m);
            var v = arena.floatVec(n);
            var w = arena.floatVec(n);
            var tmpN = arena.floatVec(n);
            var tmpMAlias = u; // tmpM aliases u (both length m)
            Assert.Throws<ArgumentException>(() =>
                Krylov.lsqr(in A, in b, ref x, ref u, ref v, ref w, ref tmpMAlias, ref tmpN, n, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Lsmr_AliasingHAndHbar_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            int m = 5, n = 3;
            var A = arena.floatMat(m, n);
            var b = arena.floatRandomVec(m, -1f, 1f, 39401);
            var x = arena.floatVec(n);
            var u = arena.floatVec(m);
            var v = arena.floatVec(n);
            var h = arena.floatVec(n);
            var tmpM = arena.floatVec(m);
            var tmpN = arena.floatVec(n);
            var hbarAlias = h; // hbar aliases h (both length n)
            Assert.Throws<ArgumentException>(() =>
                Krylov.lsmr(in A, in b, ref x, ref u, ref v, ref h, ref hbarAlias, ref tmpM, ref tmpN, n, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }

    [Test]
    public void Cgne_AliasingPAndTmpN_Throws()
    {
        var arena = new Arena(Allocator.Persistent);
        try
        {
            int m = 4, n = 6;                   // under-determined
            var A = arena.floatMat(m, n);
            var b = arena.floatRandomVec(m, -1f, 1f, 39501);
            var x = arena.floatVec(n);
            var r = arena.floatVec(m);
            var p = arena.floatVec(n);
            var q = arena.floatVec(m);
            var tmpNAlias = p; // tmpN aliases p (both length n)
            Assert.Throws<ArgumentException>(() =>
                Krylov.cgne(in A, in b, ref x, ref r, ref p, ref q, ref tmpNAlias, n, Consts.floatSqrtEps));
        }
        finally { arena.Dispose(); }
    }
}
