// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceTests/iProxy/CompareTests.iProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using BULA;
using NUnit.Framework;
using System;

using Unity.Burst;
using Unity.Collections;

using Unity.Jobs;

public class longCompareTests
{
    [BurstCompile(CompileSynchronously = true)]
    public struct TestsJob : IJob
    {
        public enum TestType
        {
            VecEquals,
            VecNotEquals,
            VecLess,
            VecLessOrEqual,
            VecGreater,
            VecGreaterOrEqual,

            MatEquals,
            MatNotEquals,
            MatLess,
            MatLessOrEqual,
            MatGreater,
            MatGreaterOrEqual,

            VecRandom,
            MatRandom,

            MatDiagonal,

            VecVecEquals,
            VecVecNotEquals,
            VecVecLess,
            VecVecLessOrEqual,
            VecVecGreater,
            VecVecGreaterOrEqual,
            VecVecRandom,

            MatMatEquals,
            MatMatNotEquals,
            MatMatLess,
            MatMatLessOrEqual,
            MatMatGreater,
            MatMatGreaterOrEqual,
            MatMatRandom,

            VecIsPow2,
            MatIsPow2,
        }

        public TestType Type;

        public void Execute()
        {
            switch (Type)
            {
                case TestType.VecEquals:
                    VecEquals();
                    break;
                case TestType.VecNotEquals:
                    VecNotEquals();
                    break;
                case TestType.VecLess:
                    VecLess();
                    break;
                case TestType.VecLessOrEqual:
                    VecLessOrEqual();
                    break;
                case TestType.VecGreater:
                    VecGreater();
                    break;
                case TestType.VecGreaterOrEqual:
                    VecGreaterOrEqual();
                    break;
                case TestType.VecRandom:
                    VecRandom();
                    break;

                case TestType.MatEquals:
                    MatEquals();
                    break;
                case TestType.MatNotEquals:
                    MatNotEquals();
                    break;
                case TestType.MatLess:
                    MatLess();
                    break;
                case TestType.MatLessOrEqual:
                    MatLessOrEqual();
                    break;
                case TestType.MatGreater:
                    MatGreater();
                    break;
                case TestType.MatGreaterOrEqual:
                    MatGreaterOrEqual();
                    break;

                case TestType.MatRandom:
                    MatRandom();
                    break;
                case TestType.MatDiagonal:
                    MatDiagonal();
                    break;

                case TestType.VecVecEquals:
                    VecVecEquals();
                    break;
                case TestType.VecVecNotEquals:
                    VecVecNotEquals();
                    break;
                case TestType.VecVecLess:
                    VecVecLess();
                    break;
                case TestType.VecVecLessOrEqual:
                    VecVecLessOrEqual();
                    break;
                case TestType.VecVecGreater:
                    VecVecGreater();
                    break;
                case TestType.VecVecGreaterOrEqual:
                    VecVecGreaterOrEqual();
                    break;
                case TestType.VecVecRandom:
                    VecVecRandom();
                    break;

                case TestType.MatMatEquals:
                    MatMatEquals();
                    break;
                case TestType.MatMatNotEquals:
                    MatMatNotEquals();
                    break;
                case TestType.MatMatLess:
                    MatMatLess();
                    break;
                case TestType.MatMatLessOrEqual:
                    MatMatLessOrEqual();
                    break;
                case TestType.MatMatGreater:
                    MatMatGreater();
                    break;
                case TestType.MatMatGreaterOrEqual:
                    MatMatGreaterOrEqual();
                    break;
                case TestType.MatMatRandom:
                    MatMatRandom();
                    break;

                case TestType.VecIsPow2:
                    VecIsPow2();
                    break;
                case TestType.MatIsPow2:
                    MatIsPow2();
                    break;

                default:
                    throw new System.NotImplementedException();
            }
        }

        public void VecEquals()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v == 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            boolVec = v == 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
        }

        public void VecNotEquals()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v != 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));

            boolVec = v != 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));
        }

        public void VecLess()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v < 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));

            boolVec = v < 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));
        }

        public void VecLessOrEqual()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v <= 0;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));
            boolVec = v <= 1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            boolVec = v <= -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
        }

        public void VecGreater()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v > 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));

            boolVec = v > -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));
        }

        public void VecGreaterOrEqual()
        {
            int dim = 16;

            longN v = new longN(dim, Allocator.Temp);

            var boolVec = v >= 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            boolVec = v >= -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            boolVec = v >= 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
        }

        public void MatEquals()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m == 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            boolMat = m == 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));
        }

        public void MatNotEquals()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m != 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            boolMat = m != 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));
        }

        public void MatLess()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m < 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            boolMat = m < 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));
        }

        public void MatLessOrEqual()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m <= 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            boolMat = m <= 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            boolMat = m <= -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));
        }

        public void MatGreater()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m > 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            boolMat = m > -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));
        }

        public void MatGreaterOrEqual()
        {
            int dim = 16;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);

            var boolMat = m >= 0;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            boolMat = m >= -1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            boolMat = m >= 1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));
        }

        public void VecRandom()
        {
            int dim = 64;

            longN v = GenerateOP.longRandomVec(dim, -100, 100, 1451);
            v[0] = 0;

            var boolVec = v == 0;

            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v != 0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v < 0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v > 0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v <= 0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v >= 0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));
        }

        public void MatRandom()
        {
            int dim = 32;

            longMxN m = GenerateOP.longRandomMat(dim, dim, -100, 100, 1451);
            m[0,0] = 0;

            var boolMat = m == 0;

            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m != 0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m < 0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m > 0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m <= 0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m >= 0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));
        }

        public void MatDiagonal()
        {
            int dim = 32;

            longMxN m0 = GenerateOP.longDiagonalMat(dim, 1);

            var boolMat = m0 == 1;

            Assert.IsTrue(Analysis.isDiagonal(boolMat));
            Assert.IsFalse(Analysis.IsAllEqualTo(boolMat, true));
            Assert.IsFalse(Analysis.IsAllEqualTo(boolMat, false));
        }

        public void VecVecEquals()
        {
            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 0, 100);

            var boolVec = v0 == v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0[0] = 1;

            boolVec = v0 == v1;

            Assert.IsFalse(Analysis.IsAllSame(boolVec));
        }

        public void VecVecNotEquals()
        {
            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 200, 300);

            var boolVec = v0 != v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0 = v1;

            boolVec = v0 != v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
        }

        public void VecVecLess()
        {

            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 200, 300);

            var boolVec = v0 < v1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0 = v1;

            boolVec = v0 < v1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
        }

        public void VecVecLessOrEqual()
        {
            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 200, 300);

            var boolVec = v0 <= v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0 = v1;

            boolVec = v0 <= v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0 = GenerateOP.longLinVec(dim, 0, 100);
            v1 = GenerateOP.longLinVec(dim, 100, 0);

            boolVec = v0 <= v1;

            Assert.IsFalse(Analysis.IsAllSame(boolVec));
        }

        public void VecVecGreater()
        {
            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 200, 300);

            var boolVec = v0 > v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));

            v0 = v1;

            boolVec = v0 > v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));

            v0 = GenerateOP.longLinVec(dim, 100, 0);
            v1 = GenerateOP.longLinVec(dim, 0, 100);

            boolVec = v0 > v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v1 > v0;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));
        }

        public void VecVecGreaterOrEqual()
        {
            int dim = 16;

            longN v0 = GenerateOP.longLinVec(dim, 0, 100);
            longN v1 = GenerateOP.longLinVec(dim, 200, 300);

            var boolVec = v0 >= v1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, false));
            v0 = v1;

            boolVec = v0 >= v1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolVec, true));

            v0 = GenerateOP.longLinVec(dim, 1, 0);

            boolVec = v0 >= v1;
            Assert.IsTrue(Analysis.IsAllSame(boolVec));
        }

        public void VecVecRandom()
        {
            int dim = 64;

            longN v0 = GenerateOP.longRandomVec(dim, -100, 100, 1451);
            longN v1 = GenerateOP.longRandomVec(dim, -100, 100, 6421);

            v0[0] = v1[0];
            v0[1] = (long)(1-v1[1]);
            var boolVec = v0 == v1;

            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v0 != v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v0 < v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v0 > v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v0 <= v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));

            boolVec = v0 >= v1;
            Assert.IsFalse(Analysis.IsAllSame(boolVec));
        }

        public void MatMatEquals()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 0, 100);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 0, 100);

            var boolMat = m0 == m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            m0[0,0] = 1;
            m0[1,1] = 1;
            m0[2,2] = 1;
            m0[3,3] = 1;

            boolMat = m0 == m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));
        }

        public void MatMatNotEquals()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);

            var boolMat = m0 != m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            m1 = GenerateOP.longRandomMat(dim, dim, 200, 300, 2131);

            boolMat = m0 != m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));
        }

        public void MatMatLess()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 000, 100, 2131);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 200, 300, 2131);

            var boolMat = m0 < m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            m0 = m1;

            boolMat = m0 < m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));
        }

        public void MatMatLessOrEqual()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 200, 300, 2131);

            var boolMat = m0 <= m1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            m0 = m1;

            boolMat = m0 <= m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));

            m0 = GenerateOP.longRandomMat(dim, dim, 100, 0, 2131);
            m1 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);

            boolMat = m0 <= m1;

            Assert.IsFalse(Analysis.IsAllSame(boolMat));
        }

        public void MatMatGreater()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 200, 300, 2131);

            var boolMat = m0 > m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            m0 = m1;

            boolMat = m0 > m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            m0 = GenerateOP.longRandomMat(dim, dim, 100, 0, 2131);
            m1 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);

            boolMat = m0 > m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m1 > m0;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));
        }

        public void MatMatGreaterOrEqual()
        {
            int dim = 16;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, 0, 100, 2131);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, 200, 300, 2131);

            var boolMat = m0 >= m1;
            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, false));

            m0 = m1;

            boolMat = m0 >= m1;

            Assert.IsTrue(Analysis.IsAllEqualTo(boolMat, true));
            m0 = GenerateOP.longRandomMat(dim, dim, 100, 0, 2131);

            boolMat = m0 >= m1;
            Assert.IsTrue(Analysis.IsAllSame(boolMat));
        }

        public void MatMatRandom()
        {
            int dim = 32;

            longMxN m0 = GenerateOP.longRandomMat(dim, dim, -100, 100, 1451);
            longMxN m1 = GenerateOP.longRandomMat(dim, dim, -100, 100, 6421);

            m0[0,0] = m1[0,0];
            m0[0,1] = (long)(1 - m1[0,1]);
            var boolMat = m0 == m1;

            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m0 != m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m0 < m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m0 > m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m0 <= m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));

            boolMat = m0 >= m1;
            Assert.IsFalse(Analysis.IsAllSame(boolMat));
        }

        // ispow2 is a genuine PREDICATE (not a relational comparison against a second operand), so
        // it lives here alongside the rest of the boolN/boolMxN-producing comparator surface rather
        // than in longCompMathTests/longCompBitsTests - see UnsafeBoolOP.long.cs's ispow2
        // kernel and longN.Comparators.cs/longMxN.Comparators.cs's public ispow2() methods.
        public void VecIsPow2()
        {
            int n = 7;

            longN v = new longN(n, Allocator.Temp);
            v[0] = 0;   // not a power of two
            v[1] = 1;   // 2^0
            v[2] = 2;   // 2^1
            v[3] = 3;   // not a power of two
            v[4] = 4;   // 2^2
            v[5] = -8;  // negative - never a power of two, regardless of its magnitude's bit pattern
            // type MSB pattern (int.MinValue-analog): negative for every SIGNED type, so never a
            // power of two - the adversarial mirror of uint's 0x80000000 -> true (see
            // UIntTypeTests.IsPow2HighBitUnsignedTrue), which relies on the exact same bit pattern
            // being read as POSITIVE (2^31) once there is no sign bit to speak of.
            v[6] = unchecked((long)0x8000000000000000);

            var b = v.ispow2();
            Assert.IsFalse(b[0]);
            Assert.IsTrue(b[1]);
            Assert.IsTrue(b[2]);
            Assert.IsFalse(b[3]);
            Assert.IsTrue(b[4]);
            Assert.IsFalse(b[5]);
            Assert.IsFalse(b[6]);
        }

        public void MatIsPow2()
        {
            int dim = 4;

            longMxN m = new longMxN(dim, dim, Allocator.Temp);
            for (int i = 0; i < m.Length; i++)
                m[i] = (long)(i + 1); // 1..16 - several exact powers of two among them (1,2,4,8,16)

            var b = m.ispow2();

            // Hardcoded expected literals (matching VecIsPow2's style), not a recomputation of the
            // same x>0 && (x&(x-1))==0 formula the kernel itself uses - 1,2,4,8,16 are the powers of
            // two present in 1..16, everything else in that range is not.
            Assert.IsTrue(b[0]);    // 1  == 2^0
            Assert.IsTrue(b[1]);    // 2  == 2^1
            Assert.IsFalse(b[2]);   // 3
            Assert.IsTrue(b[3]);    // 4  == 2^2
            Assert.IsFalse(b[4]);   // 5
            Assert.IsFalse(b[5]);   // 6
            Assert.IsFalse(b[6]);   // 7
            Assert.IsTrue(b[7]);    // 8  == 2^3
            Assert.IsFalse(b[8]);   // 9
            Assert.IsFalse(b[9]);   // 10
            Assert.IsFalse(b[10]);  // 11
            Assert.IsFalse(b[11]);  // 12
            Assert.IsFalse(b[12]);  // 13
            Assert.IsFalse(b[13]);  // 14
            Assert.IsFalse(b[14]);  // 15
            Assert.IsTrue(b[15]);   // 16 == 2^4
        }
    }

    public static Array GetEnums()
    {
        return Enum.GetValues(typeof(TestsJob.TestType));
    }

    [TestCaseSource("GetEnums")]
    public void Test(TestsJob.TestType type)
    {
        new TestsJob() { Type = type }.Run();
    }

}
