// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Debug/Export.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System.Globalization;
using System.IO;
using System.Text;

namespace BULA
{
    // Managed (allocating, NON-Burst) text / CSV exporters for float/double matrices and vectors.
    // Values are cast to the generated float/double type before formatting (the raw float proxy
    // only has a parameterless ToString). ToCsv uses "G9"/"G17" (round-trips exactly); ToText uses
    // "G7" (human preview, not required to round-trip). Unbounded StringBuilder -- unlike
    // Print.Log's 4 KB FixedString, these never truncate. Managed / editor code only, never from
    // inside a Burst job.
    public static partial class Print
    {
        public static string ToText(in floatMxN m)
        {
            var sb = new StringBuilder();
            for (int r = 0; r < m.M_Rows; r++)
            {
                for (int c = 0; c < m.N_Cols; c++)
                {
                    if (c > 0) sb.Append(' ');
                    sb.Append(((float)m[r, c]).ToString("G7", CultureInfo.InvariantCulture));
                }
                sb.Append('\n');
            }
            return sb.ToString();
        }

        public static string ToText(in floatN v)
        {
            var sb = new StringBuilder();
            for (int i = 0; i < v.N; i++)
            {
                if (i > 0) sb.Append('\n');
                sb.Append(((float)v[i]).ToString("G7", CultureInfo.InvariantCulture));
            }
            return sb.ToString();
        }

        public static string ToCsv(in floatMxN m)
        {
            var sb = new StringBuilder();
            for (int r = 0; r < m.M_Rows; r++)
            {
                for (int c = 0; c < m.N_Cols; c++)
                {
                    if (c > 0) sb.Append(',');
                    sb.Append(((float)m[r, c]).ToString("G9", CultureInfo.InvariantCulture));
                }
                sb.Append('\n');
            }
            return sb.ToString();
        }

        public static string ToCsv(in floatN v)
        {
            var sb = new StringBuilder();
            for (int i = 0; i < v.N; i++)
            {
                sb.Append(((float)v[i]).ToString("G9", CultureInfo.InvariantCulture));
                sb.Append('\n');
            }
            return sb.ToString();
        }

        public static void SaveCsv(in floatMxN m, string path) => File.WriteAllText(path, ToCsv(in m));
        public static void SaveCsv(in floatN v, string path) => File.WriteAllText(path, ToCsv(in v));
    }
}
