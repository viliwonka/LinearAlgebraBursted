// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Debug/Debug.Histogram.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Collections;
using Unity.Mathematics;

namespace LinearAlgebra
{
    public static partial class Print
    {
        // Quick ASCII horizontal histogram: bins [min, max] into `bins` equal buckets, each line
        // "<left edge> | ####### <count>" scaled to `width` chars; long output flushes in chunks.
        public static void Histogram(in floatN data, int bins = 16, int width = 40)
        {
            if (bins < 1) bins = 1;
            if (width < 1) width = 1;

            int n = data.N;
            if (n == 0)
            {
                UnityEngine.Debug.Log("Histogram: empty data");
                return;
            }

            float lo = data[0], hi = data[0];
            for (int i = 1; i < n; i++)
            {
                float v = data[i];
                if (v < lo) lo = v;
                if (v > hi) hi = v;
            }

            float range = hi - lo;

            var counts = new NativeArray<int>(bins, Allocator.Temp);
            for (int i = 0; i < n; i++)
            {
                int b;
                if (range <= 0)
                {
                    b = 0; // all values identical -> single populated bucket
                }
                else
                {
                    b = (int)(((data[i] - lo) / range) * bins);
                    if (b >= bins) b = bins - 1;
                    if (b < 0) b = 0;
                }
                counts[b] = counts[b] + 1;
            }

            int maxCount = 1;
            for (int b = 0; b < bins; b++)
                if (counts[b] > maxCount) maxCount = counts[b];

            FixedString4096Bytes str = new FixedString4096Bytes();
            FixedString128Bytes header = $"Histogram  n={n}  range=[{lo:G4}, {hi:G4}]  bins={bins}\n";
            str.Append(header);

            for (int b = 0; b < bins; b++)
            {
                float edge = lo + range * ((float)b / bins);
                int barLen = (int)(((float)counts[b] / maxCount) * width);

                FixedString128Bytes line = $"{edge:G4} | ";
                str.Append(line);
                for (int k = 0; k < barLen; k++)
                    str.Append('#');
                FixedString32Bytes tail = $" {counts[b]}\n";
                str.Append(tail);

                // flush before the FixedString can overflow (many bins / large counts)
                if (str.Length > 3500)
                {
                    UnityEngine.Debug.Log($"{str}");
                    str.Clear();
                }
            }

            if (str.Length > 0)
                UnityEngine.Debug.Log($"{str}");

            counts.Dispose();
        }
    }
}
