// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/BurstProbe.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Burst;

namespace LinearAlgebra
{
    // Throw-based Burst-execution guard: call RequireBursted() near the top of a [BurstCompile]
    // IJob.Execute() that must never silently fall back to Mono. FailIfMono is [BurstDiscard]'d, so
    // the call is elided entirely from Burst-compiled code -- RequireBursted() is a no-op under a
    // real Burst compile. Under a Mono fallback the elision does not apply, so the throw runs,
    // turning a silent interpreted-execution slowdown into an immediate, loud failure: Unity reports
    // a job exception as an unhandled log message, which NUnit's EditMode runner fails the current
    // test case for (the same channel the project's existing in-job Assert.IsTrue-abort tests rely
    // on) -- no managed-side catch is needed or possible, since a job's exception is never rethrown
    // synchronously to the .Run() caller.
    //
    // RanUnderMono exists for callers that DO need a synchronous, in-process signal (the benchmark
    // harness: it must catch a Mono fallback and keep sweeping, and a thrown exception cannot be
    // observed synchronously after .Run() returns). It is set immediately before the throw, so it
    // is reliable even though the throw itself is not catchable -- poll and reset it around a
    // .Run() call instead of using try/catch.
    internal static class BurstProbe
    {
        public static bool RanUnderMono;

        [BurstDiscard]
        static void FailIfMono()
        {
            RanUnderMono = true;
            throw new System.InvalidOperationException(
                "Job ran under Mono, not Burst -- a mono-forcing call slipped into a job that must be Bursted.");
        }

        public static void RequireBursted() => FailIfMono();
    }
}
