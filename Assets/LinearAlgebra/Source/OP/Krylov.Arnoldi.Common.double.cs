// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.Arnoldi.Common.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Collections;
using Unity.Collections.LowLevel.Unsafe;
using Unity.Mathematics;

namespace LinearAlgebra
{
    public static partial class Krylov {

        // ================= Scalar Arnoldi / Givens / Hessenberg core =================
        // Shared by gmres and fgmres (identical modified-Gram-Schmidt Arnoldi step, Givens
        // rotation apply/generate, and Hessenberg back-solve in both). gcrodr keeps its own copy:
        // its Arnoldi step interleaves a recycled-subspace projection and a pivotGuard-based
        // breakdown test that gmres/fgmres don't have, so folding it in would not be bit-identical.

        // Orthogonalizes w against the existing Arnoldi basis V[0..j] (modified Gram-Schmidt),
        // writing H's column j (H[i,j] for i in [0,j], H[j+1,j] the subdiagonal norm), then
        // normalizes the residual into V[j+1] when H[j+1,j] > 0. w is destroyed. n = A.Rows.
        static void ArnoldiMGSStep(doubleN w, in UnsafeList<doubleN> V, ref doubleMxN H, int j, int n)
        {
            for (int i = 0; i <= j; i++)
            {
                doubleN vi = V[i];
                double hij = Blas.dot(w, vi);
                H[i, j] = hij;
                w.addScaledInPlace(-hij, vi);
            }
            double hj1 = math.sqrt(Blas.dot(w, w));
            H[j + 1, j] = hj1;
            if (hj1 > (double)0)
            {
                doubleN vj1 = V[j + 1];
                double invh = (double)1 / hj1;
                vj1.CopyFrom(in w);
                for (int i = 0; i < n; i++) vj1[i] *= invh;
            }
        }

        // Applies the j already-generated Givens rotations to column j of H, then generates and
        // applies rotation j (zeroing H[j+1,j], rotating g[j]/g[j+1] by it). Returns false when the
        // rotated pivot H[j,j] and the raw Arnoldi subdiagonal H[j+1,j] are BOTH exactly zero -- a
        // genuine breakdown (column j carries no residual-reducing direction at all) distinct from
        // ordinary happy breakdown (subdiagonal zero, pivot nonzero), which is still resolved
        // normally below. Callers must not use column j as a back-substitution pivot when this
        // returns false -- H[j,j] is left at 0 and g is left unrotated.
        static bool GivensApplyAndGenerate(ref doubleMxN H, ref doubleN cs, ref doubleN sn, ref doubleN g, int j)
        {
            for (int i = 0; i < j; i++)
            {
                double t0 = cs[i] * H[i, j] + sn[i] * H[i + 1, j];
                H[i + 1, j] = -sn[i] * H[i, j] + cs[i] * H[i + 1, j];
                H[i, j] = t0;
            }

            double a = H[j, j], bb2 = H[j + 1, j];
            double rr = math.sqrt(a * a + bb2 * bb2);
            if (rr <= (double)0)
                return false;

            double c = a / rr, s = bb2 / rr;
            cs[j] = c; sn[j] = s;
            H[j, j] = rr;
            H[j + 1, j] = (double)0;

            double gj = g[j];
            g[j] = c * gj;
            g[j + 1] = -s * gj;
            return true;
        }

        // Back-substitutes the upper-triangular Hessenberg system H[0..k-1,0..k-1] y = g[0..k-1].
        static void HessenbergBackSolve(in doubleMxN H, in doubleN g, ref doubleN y, int k)
        {
            for (int i = k - 1; i >= 0; i--)
            {
                double sum = g[i];
                for (int l = i + 1; l < k; l++) sum -= H[i, l] * y[l];
                y[i] = sum / H[i, i];
            }
        }
    }
}
