// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/QueryOP.Alloc.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;
using Unity.Collections;

namespace LinearAlgebra
{
    // Standalone allocating wrappers for Query search operations: allocate their own buffers
    // via allocator.
    //
    // Zero-alloc ref-dest primitives (distancesToRow/Column etc.) stay in the rest of QueryOP;
    // this file only adds the allocating convenience surface.
    public static partial class Query
    {
        // -------------------------------------------------------------------------
        // distancesToRow / distancesToColumn — allocate floatN standalone
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates a fresh floatN (length A.M_Rows) and fills it
        /// with the distance/similarity from each row of A to query q under metric m.
        /// </summary>
        public static floatN floatDistancesToRow(in floatMxN A, in floatN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new floatN(A.M_Rows, allocator);
            Query.distancesToRow(in A, in q, m, ref dest);
            return dest;
        }

        /// <summary>
        /// Allocates a fresh floatN (length A.N_Cols) and fills it
        /// with the distance/similarity from each column of A to query q under metric m.
        /// </summary>
        public static floatN floatDistancesToColumn(in floatMxN A, in floatN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new floatN(A.N_Cols, allocator);
            Query.distancesToColumn(in A, in q, m, ref dest);
            return dest;
        }

        // -------------------------------------------------------------------------
        // nonzeroIndices — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count nonzero elements, allocate exact-sized Indices, fill indices.
        /// Returns the allocated Indices (length = count).
        /// </summary>
        public static Indices floatNonzeroIndices<T>(in T x, float tol, Allocator allocator = Allocator.Temp)
            where T : unmanaged, IUnsafefloatArray
        {
            int count = Query.countNonzero(in x, tol);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            int written = 0;
            for (int i = 0; i < x.Data.Length && written < idx.N; i++)
                if (math.abs(x.Data[i]) > tol) idx[written++] = i;
            return idx;
        }

        // -------------------------------------------------------------------------
        // rowsWithinRadius / columnsWithinRadius — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count + exact-alloc Indices of row indices within radius r.
        /// </summary>
        public static Indices floatRowsWithinRadius(in floatMxN A, in floatN q, float r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Cosine || m == Metric.Dot;
            int written = 0;
            for (int row = 0; row < A.M_Rows && written < idx.N; row++)
            {
                float s = Query.RowScore(in A, row, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = row;
            }
            return idx;
        }

        /// <summary>
        /// Two-pass: count + exact-alloc Indices of column indices within radius r.
        /// </summary>
        public static Indices floatColumnsWithinRadius(in floatMxN A, in floatN q, float r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinColumnRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Cosine || m == Metric.Dot;
            int written = 0;
            for (int c = 0; c < A.N_Cols && written < idx.N; c++)
            {
                float s = Query.ColScore(in A, c, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = c;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // kNearestRows / kNearestColumns — standalone-alloc Indices + floatN scores
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates clamped-k Indices + floatN, fills via kNearestRows.
        /// Returns idx; scores and count are out params. count = min(k, A.M_Rows).
        /// </summary>
        public static Indices floatKNearestRows(in floatMxN A, in floatN q, int k, Metric m, out floatN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new floatN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new floatN(clampedK, allocator);
            count = Query.kNearestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>
        /// Allocates clamped-k Indices + floatN, fills via kNearestColumns.
        /// </summary>
        public static Indices floatKNearestColumns(in floatMxN A, in floatN q, int k, Metric m, out floatN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new floatN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new floatN(clampedK, allocator);
            count = Query.kNearestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        // -------------------------------------------------------------------------
        // kFarthestRows / kFarthestColumns — standalone-alloc Indices + floatN scores
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates clamped-k Indices + floatN, fills via kFarthestRows.
        /// Returns idx; scores and count are out params. count = min(k, A.M_Rows).
        /// </summary>
        public static Indices floatKFarthestRows(in floatMxN A, in floatN q, int k, Metric m, out floatN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new floatN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new floatN(clampedK, allocator);
            count = Query.kFarthestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>
        /// Allocates clamped-k Indices + floatN, fills via kFarthestColumns.
        /// </summary>
        public static Indices floatKFarthestColumns(in floatMxN A, in floatN q, int k, Metric m, out floatN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new floatN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new floatN(clampedK, allocator);
            count = Query.kFarthestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }
    }
}
