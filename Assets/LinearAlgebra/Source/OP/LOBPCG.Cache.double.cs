// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/LOBPCG.Cache.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using Unity.Collections;

namespace LinearAlgebra
{
    public static partial class Eigen
    {
        /// <summary>
        /// Throws if <paramref name="ws"/> is not sized for an n-dimensional symmetric operator
        /// run for a <paramref name="k"/>-eigenpair LOBPCG solve -- the layout produced by
        /// <c>Arena.doubleLOBPCGCache(n, k)</c>.
        /// </summary>
        static void RequireLOBPCGWorkspace(in doubleLOBPCGCache ws, int n, int k)
        {
            if (ws.X.M_Rows != k || ws.X.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace X must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.AX.M_Rows != k || ws.AX.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace AX must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.W.M_Rows != k || ws.W.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace W must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.AW.M_Rows != k || ws.AW.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace AW must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.P.M_Rows != k || ws.P.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace P must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.AP.M_Rows != k || ws.AP.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace AP must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.R.M_Rows != k || ws.R.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace R must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.Xnext.M_Rows != k || ws.Xnext.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace Xnext must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.Pnext.M_Rows != k || ws.Pnext.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace Pnext must be k x n (use Arena.doubleLOBPCGCache(n, k))");

            if (ws.BX.M_Rows != k || ws.BX.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace BX must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.BW.M_Rows != k || ws.BW.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace BW must be k x n (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.BP.M_Rows != k || ws.BP.N_Cols != n)
                throw new ArgumentException("LOBPCG: workspace BP must be k x n (use Arena.doubleLOBPCGCache(n, k))");

            if (ws.lambda.N != k)
                throw new ArgumentException("LOBPCG: workspace lambda must have length k (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.residual.N != k)
                throw new ArgumentException("LOBPCG: workspace residual must have length k (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.resScale.N != k)
                throw new ArgumentException("LOBPCG: workspace resScale must have length k (use Arena.doubleLOBPCGCache(n, k))");
            if (ws.xBnorm.N != k)
                throw new ArgumentException("LOBPCG: workspace xBnorm must have length k (use Arena.doubleLOBPCGCache(n, k))");

            if (ws.rowIn.N != n || ws.rowOut.N != n)
                throw new ArgumentException("LOBPCG: workspace rowIn/rowOut must have length n (use Arena.doubleLOBPCGCache(n, k))");

            int cap = 3 * k;
            if (!ws.Gram.IsSquare || ws.Gram.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace Gram must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
            if (!ws.H.IsSquare || ws.H.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace H must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
            if (!ws.L.IsSquare || ws.L.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace L must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
            if (!ws.Atrans.IsSquare || ws.Atrans.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace Atrans must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
            if (!ws.Y.IsSquare || ws.Y.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace Y must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
            if (!ws.C.IsSquare || ws.C.M_Rows != cap)
                throw new ArgumentException("LOBPCG: workspace C must be 3k x 3k (use Arena.doubleLOBPCGCache(n, k))");
        }
    }

    /// <summary>
    /// Reusable scratch for <see cref="Eigen.lobpcg{TOp,TBOp,TPre}"/> (blocked LOBPCG for the k
    /// smallest eigenpairs of an n-dimensional symmetric operator, or the k smallest of the
    /// GENERALIZED pencil (A, B) -- the SAME cache and its SAME shape serve both, see
    /// <see cref="BX"/>/<see cref="BW"/>/<see cref="BP"/>). Sized for k eigenpairs over an
    /// n-dimensional operator. Allocate ONCE via <c>Arena.doubleLOBPCGCache(n, k)</c> and reuse it
    /// across same-shape calls so repeated solves are zero-alloc at the O(n) scale (see the
    /// class doc comment on <see cref="LOBPCG"/> for the one exception: the tiny O(k)-sized
    /// Rayleigh-Ritz eigensolve still uses a few small, bounded <c>Allocator.Temp</c> scratch
    /// vectors internally, exactly like <see cref="Eigen.symmetricInPlace(ref doubleMxN, ref doubleN, ref doubleMxN)"/>
    /// already does for its own callers).
    /// </summary>
    public struct doubleLOBPCGCache : IDisposable
    {
        /// <summary>k x n. Current eigenvector estimates (rows), ascending-sorted only at the
        /// final return -- during iteration rows [0, numActive) are the still-iterating pairs and
        /// rows [numActive, k) are locked (converged, frozen) pairs, in lock order.</summary>
        public doubleMxN X;

        /// <summary>k x n. A applied to each row of <see cref="X"/>; recomputed via a FRESH
        /// <c>A.Apply</c> every iteration for the active rows.</summary>
        public doubleMxN AX;

        /// <summary>k x n. Preconditioned residual directions; only rows [0, numActive) are
        /// meaningful in any given iteration.</summary>
        public doubleMxN W;

        /// <summary>k x n. A applied to each row of <see cref="W"/> (one matvec batch per
        /// iteration, over the active rows only).</summary>
        public doubleMxN AW;

        /// <summary>k x n. Previous search directions (the "conjugate" part of the basis); only
        /// rows [0, numActive) are meaningful, and only once at least one iteration has run.</summary>
        public doubleMxN P;

        /// <summary>k x n. A applied to each row of <see cref="P"/>; recomputed via a FRESH
        /// <c>A.Apply</c> every iteration for the active rows, same rationale as <see cref="AX"/>.</summary>
        public doubleMxN AP;

        /// <summary>k x n. Raw (unpreconditioned) residuals A x - lambda x for the active rows;
        /// scratch feeding the preconditioner Apply that produces <see cref="W"/>.</summary>
        public doubleMxN R;

        /// <summary>k x n each. Ping-pong destination buffers for the new X/P block computed each
        /// iteration (the combination reads the CURRENT X/W/P, so it cannot safely write in place)
        /// -- swapped into <see cref="X"/>/<see cref="P"/> at the end of every iteration (a cheap
        /// struct-handle swap, not a buffer copy).</summary>
        public doubleMxN Xnext, Pnext;

        /// <summary>k x n each. GENERALIZED-eigenproblem B-images of <see cref="X"/>/<see cref="W"/>/
        /// <see cref="P"/> (B applied row-wise) -- see the <c>LOBPCG</c> class doc comment's
        /// "Generalized eigenproblem" / "fresh-matvec principle extends to B" notes. <c>BX</c>/
        /// <c>BP</c> are recomputed via a FRESH <c>B.Apply</c> at the same points <see cref="AX"/>/
        /// <see cref="AP"/> are (never mirror-combined across an iteration boundary); <c>BW</c> gets
        /// ONE fresh <c>B.Apply</c> per iteration (mirroring <see cref="AW"/>) and is then carried
        /// through that SAME iteration's deflation/internal-orthonormalization via linearity. For
        /// the standard (B=I) forwarding path these are exact bit-copies of X/W/P respectively --
        /// see the class doc's "B=I strategy" note for why that keeps the standard path
        /// bit-identical to the pre-generalization implementation despite the extra buffers.</summary>
        public doubleMxN BX, BW, BP;

        /// <summary>Length k. Current Ritz value estimates (in/out; sorted ascending only at the
        /// final return).</summary>
        public doubleN lambda;

        /// <summary>Length k. Per-pair 2-norm residual ‖A x_i - lambda_i B x_i‖ (B=I reduces this
        /// to ‖A x_i - lambda_i x_i‖), latest computed value (frozen at its locking-time value for
        /// already-converged pairs).</summary>
        public doubleN residual;

        /// <summary>Length k. Per-pair convergence scale min(normAEst·‖x_i‖ +
        /// |lambda_i|·normBEst·‖x_i‖_B, max(|lambda_i|, 1)·‖x_i‖_B): the denominator of the
        /// scale-invariant residual test, computed alongside <see cref="residual"/> (frozen at its
        /// locking-time value for locked pairs).</summary>
        public doubleN resScale;

        /// <summary>Length k. Per-pair B-norm ‖x_i‖_B = sqrt(x_i·B x_i) (‖x_i‖ for B=I), computed
        /// alongside <see cref="residual"/> (frozen at its locking-time value for locked pairs).
        /// A pair below the solver's certification floor is degenerate: never locked, never counted
        /// converged.</summary>
        public doubleN xBnorm;

        /// <summary>Length n each. Scratch row buffers used whenever a single operator/
        /// preconditioner Apply call needs to read from or write into one row of a k x n block
        /// (Apply operates on <see cref="doubleN"/>, not a matrix row).</summary>
        public doubleN rowIn, rowOut;

        /// <summary>3k x 3k each. Backing store for the small dense Rayleigh-Ritz sub-problem
        /// (Gram = S^T S, H = S^T A S, L = Cholesky factor of Gram, Atrans = the transformed
        /// standard-form matrix L^-1 H L^-T, Y = Atrans's eigenvectors, C = the recovered
        /// combination coefficients L^-T Y). Only the leading m x m block is used in any given
        /// iteration (m = 2*numActive or 3*numActive &lt;= 3k) via a same-buffer, smaller-shaped
        /// LOGICAL view (see <c>Eigen.View</c>) -- never a fresh allocation.</summary>
        public doubleMxN Gram, H, L, Atrans, Y, C;

        /// <summary>Standalone allocation sized identically to <c>Arena.doubleLOBPCGCache(n, k)</c>. Pair with <see cref="Dispose"/>.</summary>
        public doubleLOBPCGCache(int n, int k, Allocator allocator)
        {
            int cap = 3 * k;
            X = new doubleMxN(k, n, allocator);
            AX = new doubleMxN(k, n, allocator);
            W = new doubleMxN(k, n, allocator);
            AW = new doubleMxN(k, n, allocator);
            P = new doubleMxN(k, n, allocator);
            AP = new doubleMxN(k, n, allocator);
            R = new doubleMxN(k, n, allocator);
            Xnext = new doubleMxN(k, n, allocator);
            Pnext = new doubleMxN(k, n, allocator);
            BX = new doubleMxN(k, n, allocator);
            BW = new doubleMxN(k, n, allocator);
            BP = new doubleMxN(k, n, allocator);
            lambda = new doubleN(k, allocator);
            residual = new doubleN(k, allocator);
            resScale = new doubleN(k, allocator);
            xBnorm = new doubleN(k, allocator);
            rowIn = new doubleN(n, allocator);
            rowOut = new doubleN(n, allocator);
            Gram = new doubleMxN(cap, cap, allocator);
            H = new doubleMxN(cap, cap, allocator);
            L = new doubleMxN(cap, cap, allocator);
            Atrans = new doubleMxN(cap, cap, allocator);
            Y = new doubleMxN(cap, cap, allocator);
            C = new doubleMxN(cap, cap, allocator);
        }

        /// <summary>Dispose only instances built with the Allocator ctor; arena-built instances are arena-owned.</summary>
        public void Dispose()
        {
            X.Dispose(); AX.Dispose(); W.Dispose(); AW.Dispose(); P.Dispose(); AP.Dispose();
            R.Dispose(); Xnext.Dispose(); Pnext.Dispose();
            BX.Dispose(); BW.Dispose(); BP.Dispose();
            lambda.Dispose(); residual.Dispose(); resScale.Dispose(); xBnorm.Dispose();
            rowIn.Dispose(); rowOut.Dispose();
            Gram.Dispose(); H.Dispose(); L.Dispose(); Atrans.Dispose(); Y.Dispose(); C.Dispose();
        }
    }

    public static partial class ArenaExtensions
    {
        /// <summary>
        /// Allocates an LOBPCG workspace for a k-eigenpair solve over an n-dimensional symmetric
        /// operator (standard B=I or generalized pencil (A, B) -- the SAME layout serves both, see
        /// <see cref="doubleLOBPCGCache"/>'s BX/BW/BP fields). See <see cref="doubleLOBPCGCache"/>
        /// for reuse guidance.
        /// </summary>
        public static doubleLOBPCGCache doubleLOBPCGCache(this ref Arena arena, int n, int k)
        {
            int cap = 3 * k;
            return new doubleLOBPCGCache
            {
                X = arena.doubleMat(k, n),
                AX = arena.doubleMat(k, n),
                W = arena.doubleMat(k, n),
                AW = arena.doubleMat(k, n),
                P = arena.doubleMat(k, n),
                AP = arena.doubleMat(k, n),
                R = arena.doubleMat(k, n),
                Xnext = arena.doubleMat(k, n),
                Pnext = arena.doubleMat(k, n),
                BX = arena.doubleMat(k, n),
                BW = arena.doubleMat(k, n),
                BP = arena.doubleMat(k, n),
                lambda = arena.doubleVec(k),
                residual = arena.doubleVec(k),
                resScale = arena.doubleVec(k),
                xBnorm = arena.doubleVec(k),
                rowIn = arena.doubleVec(n),
                rowOut = arena.doubleVec(n),
                Gram = arena.doubleMat(cap, cap),
                H = arena.doubleMat(cap, cap),
                L = arena.doubleMat(cap, cap),
                Atrans = arena.doubleMat(cap, cap),
                Y = arena.doubleMat(cap, cap),
                C = arena.doubleMat(cap, cap),
            };
        }
    }
}
