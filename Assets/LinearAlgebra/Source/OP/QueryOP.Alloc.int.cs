// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/QueryOP.Alloc.iProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;
using Unity.Collections;

namespace LinearAlgebra
{
    // Standalone allocating wrappers for Query search operations over integer types: allocate
    // their own buffers via allocator.
    //
    // Supported integer-exact metrics: Manhattan, Chebyshev, SqEuclidean, Dot.
    // Euclidean and Cosine throw ArgumentException (float-only).
    // Overflow note: element-wise differences must fit the proxy type (for short: ±16383
    // coordinates so differences fit ±32767). SqEuclidean/Dot: maxAbs²×dim must fit.
    // Use float/double for larger ranges.
    public static partial class Query
    {
        // -------------------------------------------------------------------------
        // distancesToRow / distancesToColumn — allocate intN standalone
        // -------------------------------------------------------------------------

        /// <summary>Allocates a fresh intN (length A.M_Rows) with row distances/similarities to q under metric m. See class doc for supported metrics and overflow limits.</summary>
        public static intN intDistancesToRow(in intMxN A, in intN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new intN(A.M_Rows, allocator);
            Query.distancesToRow(in A, in q, m, ref dest);
            return dest;
        }

        /// <summary>Allocates a fresh intN (length A.N_Cols) with column distances/similarities to q under metric m. See class doc for supported metrics and overflow limits.</summary>
        public static intN intDistancesToColumn(in intMxN A, in intN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new intN(A.N_Cols, allocator);
            Query.distancesToColumn(in A, in q, m, ref dest);
            return dest;
        }

        // -------------------------------------------------------------------------
        // nonzeroIndices — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count nonzero elements, allocate exact-sized Indices, fill indices.
        /// Returns the allocated Indices (length = count).
        /// </summary>
        public static Indices intNonzeroIndices<T>(in T x, int tol, Allocator allocator = Allocator.Temp)
            where T : unmanaged, IUnsafeintArray
        {
            int count = Query.countNonzero(in x, tol);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            int written = 0;
            for (int i = 0; i < x.Data.Length && written < idx.N; i++)
            {
                int v = x.Data[i];
                int av = v < (int)0 ? (v == int.MinValue ? int.MaxValue : (int)(-v)) : v;
                if (av > tol) idx[written++] = i;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // rowsWithinRadius / columnsWithinRadius — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>Two-pass: counts, then exact-allocates Indices of row indices within radius r under metric m.</summary>
        public static Indices intRowsWithinRadius(in intMxN A, in intN q, int r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Dot;
            int written = 0;
            for (int row = 0; row < A.M_Rows && written < idx.N; row++)
            {
                int s = Query.RowScore(in A, row, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = row;
            }
            return idx;
        }

        /// <summary>Two-pass: counts, then exact-allocates Indices of column indices within radius r under metric m.</summary>
        public static Indices intColumnsWithinRadius(in intMxN A, in intN q, int r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinColumnRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Dot;
            int written = 0;
            for (int c = 0; c < A.N_Cols && written < idx.N; c++)
            {
                int s = Query.ColScore(in A, c, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = c;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // kNearestRows / kNearestColumns — standalone-alloc Indices + intN scores
        // -------------------------------------------------------------------------

        /// <summary>Allocates clamped-k Indices + intN scores, filled via kNearestRows; count = min(k, A.M_Rows).</summary>
        public static Indices intKNearestRows(in intMxN A, in intN q, int k, Metric m, out intN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new intN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new intN(clampedK, allocator);
            count = Query.kNearestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>Allocates clamped-k Indices + intN scores, filled via kNearestColumns; count = min(k, A.N_Cols).</summary>
        public static Indices intKNearestColumns(in intMxN A, in intN q, int k, Metric m, out intN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new intN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new intN(clampedK, allocator);
            count = Query.kNearestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        // -------------------------------------------------------------------------
        // kFarthestRows / kFarthestColumns — standalone-alloc Indices + intN scores
        // -------------------------------------------------------------------------

        /// <summary>Allocates clamped-k Indices + intN scores, filled via kFarthestRows; count = min(k, A.M_Rows).</summary>
        public static Indices intKFarthestRows(in intMxN A, in intN q, int k, Metric m, out intN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new intN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new intN(clampedK, allocator);
            count = Query.kFarthestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>Allocates clamped-k Indices + intN scores, filled via kFarthestColumns; count = min(k, A.N_Cols).</summary>
        public static Indices intKFarthestColumns(in intMxN A, in intN q, int k, Metric m, out intN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new intN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new intN(clampedK, allocator);
            count = Query.kFarthestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }
    }
}
