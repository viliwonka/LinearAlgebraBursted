// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/RandomOP.bool.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using System.Runtime.CompilerServices;
using Unity.Mathematics;
using Random = Unity.Mathematics.Random;

namespace LinearAlgebra
{
    /// <summary>
    /// Zero-alloc random-fill operations for bool vectors and matrices.
    /// <c>nextBernoulliInPlace</c> — fills each element with an independent Bernoulli(p) draw:
    /// <c>element = rng.NextFloat() &lt; p</c>. Validates p ∈ [0, 1].
    /// <c>nextBoolInPlace</c> — convenience fair-coin wrapper (p = 0.5) using <c>rng.NextBool()</c>.
    /// Hand-written singular file; not generated per-type.
    /// </summary>
    public static partial class Rand
    {
        // ---- Bernoulli fill (vector) ----

        /// <summary>
        /// Overwrites every element of <paramref name="dest"/> with a Bernoulli(p) draw:
        /// <c>element = rng.NextFloat() &lt; p</c>. Advances <paramref name="rng"/> by
        /// <c>dest.N</c> steps. Throws if <paramref name="p"/> is outside [0, 1].
        /// <para><b>Float-resolution note:</b> <c>rng.NextFloat()</c> has ~1.19e-7 resolution
        /// (float ULP at 1). For p within ~1e-7 of 0 or 1, individual draws may lose fidelity:
        /// p = 1e-8f behaves as all-false; p = 1 - 1e-8f behaves as all-true. Use double if
        /// exact fidelity near the boundaries is required.</para>
        /// </summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static void nextBernoulliInPlace(ref Random rng, ref boolN dest, float p)
        {
            if (!(p >= 0f && p <= 1f))
                throw new ArgumentException("Rand.nextBernoulliInPlace: p must be in [0, 1]");
            int len = dest.Data.Length;
            for (int i = 0; i < len; i++)
                dest[i] = rng.NextFloat() < p;
        }

        /// <summary>
        /// Overwrites every element of <paramref name="dest"/> with a Bernoulli(p) draw:
        /// <c>element = rng.NextFloat() &lt; p</c>. Advances <paramref name="rng"/> by
        /// <c>dest.Length</c> steps. Throws if <paramref name="p"/> is outside [0, 1].
        /// <para><b>Float-resolution note:</b> see the vector overload for fidelity caveats
        /// near p = 0 and p = 1.</para>
        /// </summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static void nextBernoulliInPlace(ref Random rng, ref boolMxN dest, float p)
        {
            if (!(p >= 0f && p <= 1f))
                throw new ArgumentException("Rand.nextBernoulliInPlace: p must be in [0, 1]");
            int len = dest.Data.Length;
            for (int i = 0; i < len; i++)
                dest[i] = rng.NextFloat() < p;
        }

        // ---- fair coin flip fill (vector) ----

        /// <summary>
        /// Overwrites every element of <paramref name="dest"/> with an independent fair coin
        /// flip via <c>rng.NextBool()</c>, advancing <paramref name="rng"/> by <c>dest.N</c>
        /// steps. Convenience wrapper for Bernoulli(0.5).
        /// </summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static void nextBoolInPlace(ref Random rng, ref boolN dest)
        {
            int len = dest.Data.Length;
            for (int i = 0; i < len; i++)
                dest[i] = rng.NextBool();
        }

        /// <summary>Matrix overload of <see cref="nextBoolInPlace(ref Random, ref boolN)"/>; advances
        /// <paramref name="rng"/> by <c>dest.Length</c> steps.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static void nextBoolInPlace(ref Random rng, ref boolMxN dest)
        {
            int len = dest.Data.Length;
            for (int i = 0; i < len; i++)
                dest[i] = rng.NextBool();
        }
    }
}
