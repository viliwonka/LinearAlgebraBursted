// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.GMRES.Common.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Collections;
using Unity.Collections.LowLevel.Unsafe;
using Unity.Mathematics;

namespace LinearAlgebra
{
    public static partial class Krylov
    {
        // ================= Shared restarted-GMRES(m) core =================
        // The SINGLE body behind gmres (requires a constant M) and fgmres (allows a per-step
        // varying M). Right-preconditioned Arnoldi (modified Gram–Schmidt) + incrementally
        // Givens-rotated least-squares + restart every `restart` inner steps. Branches on two
        // compile-time-constant preconditioner flags, both Burst-folded per specialization so each
        // concrete (TOp, TPre) compiles to exactly one path:
        //   * IsIdentity  -> no M apply, no extra basis, solution accumulated straight into x
        //                    (bit-for-bit plain GMRES).
        //   * flexible (= !IsConstant) -> store the preconditioned basis Z = [z_1 … z_m]
        //                    (z_j = M⁻¹ v_j) and update x += Σ y_i z_i, valid even when M changes
        //                    every step (FGMRES; e.g. an AMG K-cycle or an inner Krylov solve).
        //   * constant real M -> a single zt vector: precondition per step into zt, and update
        //                    x += M⁻¹(Σ y_i v_i) with ONE M apply per restart. m-1 fewer basis
        //                    vectors than the Z path, and one M apply per restart instead of per
        //                    step -- the standard right-preconditioned GMRES.
        // Consequence (the "switch on the preconditioner"): a CONSTANT preconditioner takes the
        // cheap standard path whether it arrived through gmres OR fgmres; only a genuinely varying
        // M pays for the Z basis. For a constant M, fgmres is therefore bit-identical to gmres.
        //
        // rnorm is a freshly recomputed ‖b−Ax‖ (VerifyTrueResidual), not the raw Arnoldi/Givens
        // estimate: a claimed convergence is verified before being reported, falling through to
        // another restart cycle if the estimate turned out optimistic. An exact-zero Arnoldi/Givens
        // pivot with zero usable columns this cycle is a dead end (x unchanged) -> Breakdown.
        static SolveInfo GmresCore<TOp, TPre>(in TOp A, in TPre M, in doubleN b, ref doubleN x,
                                              int restart, int maxIter, double tol)
            where TOp : struct, IdoubleLinearOperator
            where TPre : struct, IdoublePreconditioner
        {
            int n = A.Rows;
            int m = restart;
            bool flexible = !M.IsConstant;

            double bb = Blas.dot(b, b);
            if (bb == (double)0)
            {
                x.CopyFrom(in b);
                return MakeSolveInfo(IterativeSolveStatus.Converged, 0, (double)0);
            }
            double bnorm = math.sqrt(bb);
            double thresh = tol * bnorm;

            // Workspace (Temp): basis V (m+1 x n as rows), Hessenberg H (m+1 x m), Givens cs/sn,
            // rotated rhs g, least-squares solution y, and an Arnoldi work vector w. The
            // preconditioned workspace depends on the folded flags: flexible non-identity -> Z
            // (m vectors); constant non-identity -> zt (1 vector); identity -> neither.
            var V = new UnsafeList<doubleN>(m + 1, Allocator.Temp);
            for (int i = 0; i <= m; i++) V.Add(new doubleN(n));
            UnsafeList<doubleN> Z = default;
            doubleN zt = default;
            if (!M.IsIdentity)
            {
                if (flexible)
                {
                    Z = new UnsafeList<doubleN>(m, Allocator.Temp);
                    for (int i = 0; i < m; i++) Z.Add(new doubleN(n));
                }
                else
                {
                    zt = new doubleN(n);
                }
            }
            var H = new doubleMxN(m + 1, m, Allocator.Temp, false);   // cleared: read only written entries
            var cs = new doubleN(m);
            var sn = new doubleN(m);
            var g = new doubleN(m + 1);
            var y = new doubleN(m);
            var w = new doubleN(n);

            int total = 0;
            double resnorm = bnorm;
            bool converged = false;
            // Set only when an Arnoldi/Givens step breaks down (H[j,j] and the Arnoldi subdiagonal
            // both exactly zero) with ZERO usable columns produced this cycle -- x is then
            // unchanged, so retrying would reproduce an identical residual forever.
            bool deadEnd = false;

            while (total < maxIter && !converged && !deadEnd)
            {
                doubleN v0 = V[0];
                A.Apply(in x, ref w);                        // w = A x
                v0.CopyFrom(in b);
                v0.addScaledInPlace((double)(-1), w);         // v0 = b - A x
                double beta = math.sqrt(Blas.dot(v0, v0));
                resnorm = beta;
                if (beta <= thresh) { converged = true; break; }

                double invBeta = (double)1 / beta;
                for (int i = 0; i < n; i++) v0[i] *= invBeta;    // v0 /= beta
                for (int i = 0; i <= m; i++) g[i] = (double)0;
                g[0] = beta;

                int k = 0;
                bool breakdown = false;
                for (int j = 0; j < m && total < maxIter; j++)
                {
                    doubleN vj = V[j];
                    // w = A (M⁻¹ v_j). Identity: w = A v_j directly. Flexible: z_j = M⁻¹ v_j stored
                    // in Z for the per-step-varying solution update. Constant: reuse the single zt.
                    if (M.IsIdentity)
                    {
                        A.Apply(in vj, ref w);
                    }
                    else if (flexible)
                    {
                        doubleN zj = Z[j];
                        M.Apply(in vj, ref zj);               // z_j = M⁻¹ v_j
                        A.Apply(in zj, ref w);                // w   = A z_j
                    }
                    else
                    {
                        M.Apply(in vj, ref zt);               // zt = M⁻¹ v_j
                        A.Apply(in zt, ref w);                // w  = A M⁻¹ v_j
                    }

                    // Arnoldi step: orthogonalize w against V[0..j], normalize into V[j+1].
                    ArnoldiMGSStep(w, in V, ref H, j, n);
                    total++;   // one Arnoldi/matvec step consumed, whether or not it yields a pivot

                    // Apply/generate the Givens rotations, rotating H's column j and rhs g. False
                    // means H[j,j] and the Arnoldi subdiagonal are both exactly zero -- no usable
                    // pivot (would divide by zero in HessenbergBackSolve). Exclude column j.
                    if (!GivensApplyAndGenerate(ref H, ref cs, ref sn, ref g, j))
                    {
                        breakdown = true;
                        break;
                    }

                    resnorm = math.abs(g[j + 1]);
                    k = j + 1;
                    if (resnorm <= thresh) { converged = true; break; }
                }

                // Back-substitute H[0..k-1,0..k-1] y = g[0..k-1] (k == 0 only on an immediate,
                // zero-column breakdown -- the loops below are then no-ops).
                if (k > 0)
                {
                    HessenbergBackSolve(in H, in g, ref y, k);
                    if (M.IsIdentity)
                    {
                        // x += Σ y_i v_i (accumulated straight into x -- matches plain GMRES).
                        for (int i = 0; i < k; i++)
                        {
                            doubleN vi = V[i];
                            x.addScaledInPlace(y[i], vi);
                        }
                    }
                    else if (flexible)
                    {
                        // x += Σ y_i z_i using the STORED per-step preconditioned vectors (valid
                        // even when M varied across j = 0..k-1).
                        for (int i = 0; i < k; i++)
                        {
                            doubleN zi = Z[i];
                            x.addScaledInPlace(y[i], zi);
                        }
                    }
                    else
                    {
                        // x += M⁻¹(Σ y_i v_i) -- accumulate into w, apply M⁻¹ once (M fixed).
                        for (int i = 0; i < n; i++) w[i] = (double)0;
                        for (int i = 0; i < k; i++)
                        {
                            doubleN vi = V[i];
                            w.addScaledInPlace(y[i], vi);
                        }
                        M.Apply(in w, ref zt);
                        x.addScaledInPlace((double)1, zt);
                    }
                }

                // Verify-at-exit (converged path) OR final honesty check on a dead-end breakdown:
                // the rotated-rhs estimate |g[j+1]| can drift from the true ‖b-Ax‖ once MGS loses
                // orthogonality (an aggressive inner-iterative M widens the gap further). w and V[0]
                // are both about to be fully overwritten at the top of the next cycle, so they're
                // free scratch here; a failed verify on the converged path falls through to another
                // restart cycle instead of a false Converged.
                if (converged || (breakdown && k == 0))
                {
                    doubleN v0v = V[0];
                    double trueRR = VerifyTrueResidual(in A, in b, in x, ref w, ref v0v);
                    resnorm = math.sqrt(trueRR);
                    converged = resnorm <= thresh;
                    if (breakdown && k == 0) deadEnd = true;
                }
            }

            for (int i = 0; i <= m; i++) V[i].Dispose();
            V.Dispose();
            if (!M.IsIdentity)
            {
                if (flexible)
                {
                    for (int i = 0; i < m; i++) Z[i].Dispose();
                    Z.Dispose();
                }
                else
                {
                    zt.Dispose();
                }
            }
            H.Dispose(); cs.Dispose(); sn.Dispose(); g.Dispose(); y.Dispose(); w.Dispose();

            var status = converged ? IterativeSolveStatus.Converged
                       : deadEnd ? IterativeSolveStatus.Breakdown
                       : IterativeSolveStatus.MaxIterations;
            return MakeSolveInfo(status, total, resnorm);
        }
    }
}
