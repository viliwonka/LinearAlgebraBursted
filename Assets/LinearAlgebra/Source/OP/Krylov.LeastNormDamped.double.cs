// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Krylov.LeastNormDamped.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using BULA.Sparse;

namespace BULA
{
    /// <summary>
    /// Augmented operator [A | damp·I] (Rows × (Cols+Rows)) behind the Tikhonov-DAMPED least-norm
    /// solvers. The minimum-norm solution of [A | damp·I]·(x,s) = b minimizes ‖x‖² + ‖s‖² subject to
    /// A x + damp·s = b, i.e. x = Aᵀ(A Aᵀ + damp²·I)⁻¹ b -- the ridge-regularized least-norm solution.
    /// So running an UNDAMPED least-norm solver (craig/craigmr/lnlq) over this operator yields the
    /// damped solve with NO recurrence change; the solver's solution vector is (x, s) and x is its
    /// first Cols entries. Needs one Cols-length scratch (the x-part copy / Aᵀx). Apply/ApplyT only
    /// (Golub-Kahan bidiagonalization uses no ApplyDot/ApplyBlock).
    /// </summary>
    public readonly struct doubleDampedLeastNormOperator<TOp> : IdoubleLinearOperator
        where TOp : struct, IdoubleLinearOperator
    {
        readonly TOp _A;
        readonly double _damp;
        readonly doubleN _xScratch;   // length _A.Cols

        public doubleDampedLeastNormOperator(in TOp A, double damp, in doubleN xScratch)
        {
            _A = A;
            _damp = damp;
            _xScratch = xScratch;
        }

        public int Rows => _A.Rows;
        public int Cols => _A.Cols + _A.Rows;

        // y = [A | damp·I] z = A·z[0:Ac] + damp·z[Ac:Ac+Ar]
        public void Apply(in doubleN z, ref doubleN y)
        {
            int ac = _A.Cols, ar = _A.Rows;
            var xp = _xScratch;
            for (int i = 0; i < ac; i++) xp[i] = z[i];
            _A.Apply(in xp, ref y);
            for (int i = 0; i < ar; i++) y[i] += _damp * z[ac + i];
        }

        // z = [A | damp·I]ᵀ x = (Aᵀx, damp·x)
        public void ApplyT(in doubleN x, ref doubleN z)
        {
            int ac = _A.Cols, ar = _A.Rows;
            var xp = _xScratch;
            _A.ApplyT(in x, ref xp);
            for (int i = 0; i < ac; i++) z[i] = xp[i];
            for (int i = 0; i < ar; i++) z[ac + i] = _damp * x[i];
        }

        public double ApplyDot(in doubleN x, ref doubleN y)
            => throw new NotSupportedException("doubleDampedLeastNormOperator: ApplyDot unused (Golub-Kahan uses Apply/ApplyT only)");

        public void ApplyBlock(in doubleMxN Vrows, ref doubleMxN AVrows, int rows)
            => throw new NotSupportedException("doubleDampedLeastNormOperator: ApplyBlock unused");
    }

    public static partial class Krylov
    {
        // Extract x (first Ac entries of the augmented solution z) into caller x, then re-audit
        // diagnostics in ORIGINAL coordinates (like RightPreFinish / damped cgne): rnorm = ‖b-Ax‖
        // (= damp·‖s‖ at the optimum, legitimately nonzero), Arnorm = ‖Aᵀr - damp²x‖ (→0 -- the real
        // convergence cert), xnorm = ‖x‖; iterations/status from the augmented solve.
        static LstsqInfo DampedLeastNormFinish<TOp>(in TOp origOp, in doubleN b, in doubleN z, ref doubleN x,
                                                    double damp, int iterations, IterativeSolveStatus status,
                                                    ref doubleN mScratch, ref doubleN nScratch)
            where TOp : struct, IdoubleLinearOperator
        {
            int ac = origOp.Cols;
            for (int i = 0; i < ac; i++) x[i] = z[i];
            var info = lstsqResidual(in origOp, in b, in x, damp, ref mScratch, ref nScratch);
            info.iterations = iterations;
            info.status = status;
            return info;
        }

        // ===== craig: Tikhonov-damped least-norm =====

        /// <summary>
        /// Damped CRAIG: minimum-norm solve of the ridge-regularized underdetermined system,
        /// x = Aᵀ(A Aᵀ + damp²·I)⁻¹ b. Runs UNDAMPED <see cref="craig{TOp}"/> over the augmented
        /// operator [A | damp·I] (no recurrence change) and returns x. damp == 0 delegates to plain
        /// craig. The <see cref="LstsqInfo"/> is re-audited in original coords: rnorm = ‖b-Ax‖
        /// (nonzero at the damped optimum), Arnorm = ‖Aᵀr - damp²x‖ (→0), read status/Arnorm not rnorm.
        /// </summary>
        public static LstsqInfo craig(in doubleMxN A, in doubleN b, ref doubleN x, int maxIter, double tol, double damp)
        {
            if (damp == (double)0) return craig(in A, in b, ref x, maxIter, tol);
            return CraigDampedCore(new doubleDenseOperator(in A), in b, ref x, A.M_Rows, A.N_Cols, maxIter, tol, damp);
        }

        /// <summary>Damped CRAIG over a BSR matrix -- see the dense overload. damp == 0 delegates.</summary>
        public static LstsqInfo craig(in doubleBSR A, in doubleN b, ref doubleN x, int maxIter, double tol, double damp)
        {
            if (damp == (double)0) return craig(in A, in b, ref x, maxIter, tol);
            return CraigDampedCore(new doubleBSROperator(in A), in b, ref x, A.M_Rows, A.N_Cols, maxIter, tol, damp);
        }

        static LstsqInfo CraigDampedCore<TOp>(in TOp origOp, in doubleN b, ref doubleN x, int ar, int ac,
                                              int maxIter, double tol, double damp)
            where TOp : struct, IdoubleLinearOperator
        {
            int augCols = ac + ar;
            var xScratch = b.doubleTempVec(ac);
            var aug = new doubleDampedLeastNormOperator<TOp>(in origOp, damp, in xScratch);

            var z    = b.doubleTempVec(augCols);
            var u    = b.doubleTempVec(ar);        // aug.Rows
            var v    = b.doubleTempVec(augCols);   // aug.Cols
            var tmpM = b.doubleTempVec(ar);
            var tmpN = b.doubleTempVec(augCols);
            for (int i = 0; i < augCols; i++) z[i] = (double)0;

            var ci = craig(in aug, in b, ref z, ref u, ref v, ref tmpM, ref tmpN, maxIter, tol);

            var mScratch = b.doubleTempVec(ar);
            var nScratch = b.doubleTempVec(ac);
            return DampedLeastNormFinish(in origOp, in b, in z, ref x, damp, ci.iterations, ci.status, ref mScratch, ref nScratch);
        }

        // ===== craigmr: Tikhonov-damped least-norm (monotonic-residual CRAIG) =====

        /// <summary>
        /// Damped CRAIGMR: ridge least-norm x = Aᵀ(A Aᵀ + damp²·I)⁻¹ b via UNDAMPED
        /// <see cref="craigmr{TOp}"/> over the augmented operator [A | damp·I]. damp == 0 delegates.
        /// See <see cref="craig(in doubleMxN, in doubleN, ref doubleN, int, double, double)"/> for the
        /// original-coord diagnostics contract (rnorm = ‖b-Ax‖ nonzero, Arnorm = ‖Aᵀr-damp²x‖ →0).
        /// (lnlq is intentionally NOT damped this way: its certified forward-error bound applies to
        /// the augmented (x,s), not to x, so the augmentation defeats its distinctive feature.)
        /// </summary>
        public static LstsqInfo craigmr(in doubleMxN A, in doubleN b, ref doubleN x, int maxIter, double tol, double damp)
        {
            if (damp == (double)0) return craigmr(in A, in b, ref x, maxIter, tol);
            return CraigmrDampedCore(new doubleDenseOperator(in A), in b, ref x, A.M_Rows, A.N_Cols, maxIter, tol, damp);
        }

        /// <summary>Damped CRAIGMR over a BSR matrix -- see the dense overload. damp == 0 delegates.</summary>
        public static LstsqInfo craigmr(in doubleBSR A, in doubleN b, ref doubleN x, int maxIter, double tol, double damp)
        {
            if (damp == (double)0) return craigmr(in A, in b, ref x, maxIter, tol);
            return CraigmrDampedCore(new doubleBSROperator(in A), in b, ref x, A.M_Rows, A.N_Cols, maxIter, tol, damp);
        }

        static LstsqInfo CraigmrDampedCore<TOp>(in TOp origOp, in doubleN b, ref doubleN x, int ar, int ac,
                                                int maxIter, double tol, double damp)
            where TOp : struct, IdoubleLinearOperator
        {
            int augCols = ac + ar;
            var xScratch = b.doubleTempVec(ac);
            var aug = new doubleDampedLeastNormOperator<TOp>(in origOp, damp, in xScratch);

            var z    = b.doubleTempVec(augCols);
            var u    = b.doubleTempVec(ar);
            var v    = b.doubleTempVec(augCols);
            var d    = b.doubleTempVec(augCols);
            var tmpM = b.doubleTempVec(ar);
            var tmpN = b.doubleTempVec(augCols);
            for (int i = 0; i < augCols; i++) z[i] = (double)0;

            var ci = craigmr(in aug, in b, ref z, ref u, ref v, ref d, ref tmpM, ref tmpN, maxIter, tol);

            var mScratch = b.doubleTempVec(ar);
            var nScratch = b.doubleTempVec(ac);
            return DampedLeastNormFinish(in origOp, in b, in z, ref x, damp, ci.iterations, ci.status, ref mScratch, ref nScratch);
        }
    }
}
