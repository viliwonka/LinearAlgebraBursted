// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/Kalman.Info.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
//singularFile//
using Unity.Collections;

namespace BULA
{
    /// <summary>
    /// Terminal state of a <see cref="Kalman.update"/> / <see cref="Kalman.ekfUpdate{TMeas}"/> call,
    /// carried by <see cref="KFInfo"/>. Type-agnostic (no fProxy) on purpose -- lives in a non-
    /// templated file so codegen does not emit a duplicate definition into both the float and double
    /// partials (CS0102), exactly like <see cref="RiccatiStatus"/>.
    /// </summary>
    public enum KFStatus
    {
        /// <summary>The innovation-covariance solve succeeded (full rank, or a usable rank-deficient
        /// minimum-norm result); <c>x</c>/<c>P</c> describe the updated estimate.</summary>
        Ok = 0,

        /// <summary>H P Hᵀ + R was not even positive-semi-definite (a numerically broken P or R) --
        /// the update did not apply; <c>x</c>/<c>P</c> are UNCHANGED (the pre-update estimate).</summary>
        InnovationSolveFailed = 1,
    }

    /// <summary>
    /// Burst-safe enum-to-name helper for <see cref="KFStatus"/>, used by
    /// <see cref="KFInfo.ToFixedString"/>.
    /// </summary>
    public static class KFStatusExtensions
    {
        public static FixedString32Bytes Name(this KFStatus s)
        {
            switch (s)
            {
                case KFStatus.Ok: return "Ok";
                case KFStatus.InnovationSolveFailed: return "InnovationSolveFailed";
                default: return "Unknown";
            }
        }
    }

    /// <summary>
    /// Result of a <see cref="Kalman.update"/> / <see cref="Kalman.ekfUpdate{TMeas}"/> call. Returned
    /// by value; implicit <c>bool</c> conversion (== <see cref="Solved"/>) matches every other *_Info
    /// struct in this library.
    /// </summary>
    public struct KFInfo
    {
        /// <summary>‖z - Hx‖ (or ‖z - h(x)‖ for the EKF path) at the innovation this call computed --
        /// already-computed as part of the update, reported regardless of <see cref="status"/>.</summary>
        public double innovationNorm;

        /// <summary>Why the update stopped -- see <see cref="KFStatus"/>.</summary>
        public KFStatus status;

        /// <summary>True iff the update applied (<c>status == KFStatus.Ok</c>).</summary>
        public bool Solved => status == KFStatus.Ok;

        /// <summary>Implicit success test, so <c>if (Kalman.update(...))</c> reads as "did it apply".</summary>
        public static implicit operator bool(KFInfo info) => info.status == KFStatus.Ok;

        /// <summary>Burst-safe compact summary, e.g. <c>KFInfo(Ok, innovationNorm=1.23E+00)</c>. Never
        /// allocates managed memory.</summary>
        public FixedString128Bytes ToFixedString()
        {
            FixedString128Bytes str = "KFInfo(";
            str.Append(status.Name());
            FixedString128Bytes tail = $", innovationNorm={innovationNorm:G3})";
            str.Append(tail);
            return str;
        }

        /// <summary>Managed wrapper -- do not call from inside a [BurstCompile] job.</summary>
        public override string ToString() => ToFixedString().ToString();
    }
}
