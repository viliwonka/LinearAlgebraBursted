// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/OP/QueryOP.Alloc.iProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;
using Unity.Collections;

namespace LinearAlgebra
{
    // Standalone (non-arena) allocating wrappers for Query search operations over integer types.
    // Same semantics as ArenaExtensions.Query.short.cs but allocate their own buffers via
    // allocator instead of drawing from a matrix's arena.
    //
    // Supported integer-exact metrics: Manhattan, Chebyshev, SqEuclidean, Dot.
    // Euclidean and Cosine throw ArgumentException (float-only).
    // Overflow note: element-wise differences must fit the proxy type (for short: ±16383
    // coordinates so differences fit ±32767). SqEuclidean/Dot: maxAbs²×dim must fit.
    // Use float/double for larger ranges.
    public static partial class Query
    {
        // -------------------------------------------------------------------------
        // distancesToRow / distancesToColumn — allocate shortN standalone
        // -------------------------------------------------------------------------

        /// <summary>Allocates a fresh shortN (length A.M_Rows) with row distances/similarities to q under metric m. See class doc for supported metrics and overflow limits.</summary>
        public static shortN shortDistancesToRow(in shortMxN A, in shortN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new shortN(A.M_Rows, allocator);
            Query.distancesToRow(in A, in q, m, ref dest);
            return dest;
        }

        /// <summary>Allocates a fresh shortN (length A.N_Cols) with column distances/similarities to q under metric m. See class doc for supported metrics and overflow limits.</summary>
        public static shortN shortDistancesToColumn(in shortMxN A, in shortN q, Metric m, Allocator allocator = Allocator.Temp)
        {
            var dest = new shortN(A.N_Cols, allocator);
            Query.distancesToColumn(in A, in q, m, ref dest);
            return dest;
        }

        // -------------------------------------------------------------------------
        // nonzeroIndices — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count nonzero elements, allocate exact-sized Indices, fill indices.
        /// Returns the allocated Indices (length = count).
        /// </summary>
        public static Indices shortNonzeroIndices<T>(in T x, short tol, Allocator allocator = Allocator.Temp)
            where T : unmanaged, IUnsafeshortArray
        {
            int count = Query.countNonzero(in x, tol);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            int written = 0;
            for (int i = 0; i < x.Data.Length && written < idx.N; i++)
            {
                short v = x.Data[i];
                short av = v < (short)0 ? (v == short.MinValue ? short.MaxValue : (short)(-v)) : v;
                if (av > tol) idx[written++] = i;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // rowsWithinRadius / columnsWithinRadius — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>Two-pass: counts, then exact-allocates Indices of row indices within radius r under metric m.</summary>
        public static Indices shortRowsWithinRadius(in shortMxN A, in shortN q, short r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Dot;
            int written = 0;
            for (int row = 0; row < A.M_Rows && written < idx.N; row++)
            {
                short s = Query.RowScore(in A, row, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = row;
            }
            return idx;
        }

        /// <summary>Two-pass: counts, then exact-allocates Indices of column indices within radius r under metric m.</summary>
        public static Indices shortColumnsWithinRadius(in shortMxN A, in shortN q, short r, Metric m, Allocator allocator = Allocator.Temp)
        {
            int count = Query.countWithinColumnRadius(in A, in q, r, m);
            if (count == 0) return new Indices(0, allocator);
            var idx = new Indices(count, allocator);
            bool sim = m == Metric.Dot;
            int written = 0;
            for (int c = 0; c < A.N_Cols && written < idx.N; c++)
            {
                short s = Query.ColScore(in A, c, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = c;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // kNearestRows / kNearestColumns — standalone-alloc Indices + shortN scores
        // -------------------------------------------------------------------------

        /// <summary>Allocates clamped-k Indices + shortN scores, filled via kNearestRows; count = min(k, A.M_Rows).</summary>
        public static Indices shortKNearestRows(in shortMxN A, in shortN q, int k, Metric m, out shortN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new shortN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new shortN(clampedK, allocator);
            count = Query.kNearestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>Allocates clamped-k Indices + shortN scores, filled via kNearestColumns; count = min(k, A.N_Cols).</summary>
        public static Indices shortKNearestColumns(in shortMxN A, in shortN q, int k, Metric m, out shortN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new shortN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new shortN(clampedK, allocator);
            count = Query.kNearestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        // -------------------------------------------------------------------------
        // kFarthestRows / kFarthestColumns — standalone-alloc Indices + shortN scores
        // -------------------------------------------------------------------------

        /// <summary>Allocates clamped-k Indices + shortN scores, filled via kFarthestRows; count = min(k, A.M_Rows).</summary>
        public static Indices shortKFarthestRows(in shortMxN A, in shortN q, int k, Metric m, out shortN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = new shortN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new shortN(clampedK, allocator);
            count = Query.kFarthestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>Allocates clamped-k Indices + shortN scores, filled via kFarthestColumns; count = min(k, A.N_Cols).</summary>
        public static Indices shortKFarthestColumns(in shortMxN A, in shortN q, int k, Metric m, out shortN scores, out int count, Allocator allocator = Allocator.Temp)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = new shortN(0, allocator, true); count = 0; return new Indices(0, allocator); }
            var idx = new Indices(clampedK, allocator);
            scores = new shortN(clampedK, allocator);
            count = Query.kFarthestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }
    }
}
