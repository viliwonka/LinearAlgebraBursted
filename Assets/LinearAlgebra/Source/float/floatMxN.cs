// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/fProxy/fProxyMxN.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Collections;
using Unity.Collections.LowLevel.Unsafe;
using System;
using System.Runtime.InteropServices;

namespace BULA
{
    // A m x n matrix
    // m = rows
    // n = cols
    [StructLayout(LayoutKind.Sequential)]
    public partial struct floatMxN : IDisposable, IUnsafefloatArray {

        public int M_Rows;
        public int N_Cols;

        private UnsafeList<float> _data;

        public unsafe UnsafeList<float> Data
        {
            get => _data;
            private set => _data = value;
        }

        public readonly int Length;

        public bool IsSquare => M_Rows == N_Cols;

        /// <summary>True while this matrix has a live allocation, including views; false for
        /// default(floatMxN) and after Dispose().</summary>
        public unsafe bool IsCreated => _data.IsCreated;

        /// <summary>
        /// Creates a VIEW over <paramref name="viewOf"/>'s memory as a row-major
        /// M_rows x N_cols matrix (viewOf.Length must equal M_rows*N_cols) -- no copy, no
        /// ownership. Element reads/writes go straight to the array. Valid only while the source
        /// array is alive; Dispose() releases nothing (the array keeps ownership). The view is
        /// outside the job-safety system: it does not carry the array's safety handle, so the
        /// caller owns the aliasing/race discipline.
        /// </summary>
        public unsafe floatMxN(int M_rows, int N_cols, NativeArray<float> viewOf)
        {
            if (M_rows * N_cols != viewOf.Length)
                throw new ArgumentException("floatMxN view: viewOf.Length must equal M_rows * N_cols");

            M_Rows = M_rows;
            N_Cols = N_cols;
            Length = viewOf.Length;
            _data = new UnsafeList<float>((float*)viewOf.GetUnsafePtr(), viewOf.Length);
        }

        /// <summary>
        /// Creates a VIEW over the leading rows*cols elements of <paramref name="source"/>'s own
        /// backing storage, repacked row-major as a rows x cols matrix -- a tightly-packed reinterpretation of
        /// source's flat storage prefix, not a strided sub-block that preserves source's own N_Cols pitch. No
        /// copy, no ownership; Dispose() on the view releases nothing. Aliases source's memory: reads/writes
        /// through the view touch source's own storage in place, visible through any other handle to the same
        /// storage. Valid only while source's backing memory is alive; outside the job-safety system, same
        /// contract as the NativeArray view constructor above -- caller owns the aliasing/lifetime discipline.
        /// Throws if rows*cols exceeds source's own logical length.
        /// </summary>
        public unsafe floatMxN(in floatMxN source, int rows, int cols)
        {
#if ENABLE_UNITY_COLLECTIONS_CHECKS
            if (!source.IsCreated)
                throw new InvalidOperationException("floatMxN view: source is not created / has been disposed");
#endif
            if (rows < 0 || cols < 0 || rows * cols > source.Data.Length)
                throw new ArgumentException("floatMxN view: rows*cols must not exceed source's own logical length");

            M_Rows = rows;
            N_Cols = cols;
            Length = rows * cols;
            _data = new UnsafeList<float>(source.Data.Ptr, Length);
        }

        public unsafe floatMxN(int M_rows, int N_cols, Allocator allocator = Allocator.Temp, bool uninit = false)
        {
            _data = default;
            M_Rows = M_rows;
            N_Cols = N_cols;
            Length = M_Rows * N_Cols;
            var data = new UnsafeList<float>(Length, allocator, uninit ? NativeArrayOptions.UninitializedMemory : NativeArrayOptions.ClearMemory);
            data.Resize(Length, NativeArrayOptions.UninitializedMemory);
            Data = data;
        }

        /// <summary>
        /// Creates a copy of the matrix with a new allocation.
        /// </summary>
        public unsafe floatMxN(in floatMxN orig, Allocator allocator = Allocator.Temp)
        {
            _data = default;

            M_Rows = orig.M_Rows;
            N_Cols = orig.N_Cols;
            Length = orig.Length;
            var data = new UnsafeList<float>(Length, allocator, NativeArrayOptions.UninitializedMemory);
            data.Resize(Length, NativeArrayOptions.UninitializedMemory);
            data.CopyFrom(orig.Data);
            Data = data;
        }

        /// <summary>Returns an Allocator.Temp copy.</summary>
        public unsafe floatMxN Copy()
        {
            return new floatMxN(in this, Allocator.Temp);
        }

        /// <summary>Returns an Allocator.Temp copy.</summary>
        public unsafe floatMxN TempCopy()
        {
            return new floatMxN(in this, Allocator.Temp);
        }

        /// <summary>Copies every element into <paramref name="mat"/> (dimensions must match). Fixed-size: never resizes <paramref name="mat"/>.</summary>
        public unsafe void CopyTo(in floatMxN mat)
        {
            if (M_Rows != mat.M_Rows || N_Cols != mat.N_Cols)
                throw new ArgumentException("CopyTo: dimensions do not match!");

            UnsafeUtility.MemCpy(mat.Data.Ptr, Data.Ptr, (long)M_Rows * N_Cols * sizeof(float));
        }

        /// <summary>Copies every element from <paramref name="mat"/> (dimensions must match). Fixed-size: never resizes this matrix.</summary>
        public unsafe void CopyFrom(in floatMxN mat)
        {
            if (M_Rows != mat.M_Rows || N_Cols != mat.N_Cols)
                throw new ArgumentException("CopyFrom: dimensions do not match!");

            UnsafeUtility.MemCpy(Data.Ptr, mat.Data.Ptr, (long)M_Rows * N_Cols * sizeof(float));
        }

        /// <summary>Copies every element (row-major) into <paramref name="dst"/> (dst.Length must equal M_Rows*N_Cols).</summary>
        public unsafe void CopyTo(NativeArray<float> dst)
        {
            if (Length != dst.Length)
                throw new ArgumentException("CopyTo: dst.Length must equal M_Rows * N_Cols");

            UnsafeUtility.MemCpy(dst.GetUnsafePtr(), Data.Ptr, (long)Length * sizeof(float));
        }

        /// <summary>Copies every element (row-major) from <paramref name="src"/> (src.Length must equal M_Rows*N_Cols).</summary>
        public unsafe void CopyFrom(NativeArray<float> src)
        {
            if (Length != src.Length)
                throw new ArgumentException("CopyFrom: src.Length must equal M_Rows * N_Cols");

            UnsafeUtility.MemCpy(Data.Ptr, src.GetUnsafeReadOnlyPtr(), (long)Length * sizeof(float));
        }

        public unsafe void Dispose() {
            _data.Dispose();
        }

        public override string ToString()
        {
            var sb = new System.Text.StringBuilder();
            for (int r = 0; r < M_Rows; r++)
            {
                sb.Append("[ ");
                for (int c = 0; c < N_Cols; c++)
                {
                    if (c > 0) sb.Append("  ");
                    sb.Append(this[r, c]);
                }
                sb.Append(" ]");
                if (r < M_Rows - 1) sb.AppendLine();
            }
            return sb.ToString();
        }
    }
}
