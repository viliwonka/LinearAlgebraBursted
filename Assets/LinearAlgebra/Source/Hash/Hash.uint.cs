// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Hash/Hash.iProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using System.Runtime.CompilerServices;


namespace LinearAlgebra
{
    public static partial class Hash
    {
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static uint hash(in uintN v, uint seed = 0)
        {
            unsafe { return hash((byte*)v.Data.Ptr, v.Data.Length * sizeof(uint), seed); }
        }

        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static uint hash(in uintMxN v, uint seed = 0)
        {
            unsafe { return hash((byte*)v.Data.Ptr, v.Data.Length * sizeof(uint), seed); }
        }

        // Row/col hash dest here is always uint (all 4 generated slots: int/short/long/uint), not
        // A's own element type; see Hash/DEVLOG.md and the matching note in Hash.fProxy.cs for why
        // it's spelled via a choose marker instead of a real type.

        /// <summary>
        /// Writes one xxHash32 value per row of <paramref name="A"/> into <paramref name="dest"/> -
        /// dest[r] equals Hash.hash of row r extracted as a standalone vector, given the same seed.
        /// Zero-alloc: rows are contiguous (row-major storage), so each row is hashed directly out of
        /// A's backing buffer with no gather/copy. <paramref name="dest"/> is a uint buffer (a
        /// uintN) sized A.M_Rows, regardless of A's own element type.
        /// </summary>
        public static void rowHashes(in uintMxN A, ref uintN dest, uint seed = 0)
        {
            if (dest.N != A.M_Rows)
                throw new ArgumentException("Hash.rowHashes: dest.N must equal A.M_Rows");

            unsafe
            {
                int rowBytes = A.N_Cols * sizeof(uint);
                byte* rowPtr = (byte*)A.Data.Ptr;
                uint* destPtr = (uint*)dest.Data.Ptr;
                for (int r = 0; r < A.M_Rows; r++)
                {
                    destPtr[r] = hash(rowPtr, rowBytes, seed);
                    rowPtr += rowBytes;
                }
            }
        }

        /// <summary>Allocating wrapper: same as the ref-dest <c>rowHashes</c> overload, but returns a
        /// fresh uint buffer (a uintN, from Allocator.Temp) instead of writing into a caller-provided one.</summary>
        public static uintN rowHashes(in uintMxN A, uint seed = 0)
        {
            var dest = A.uintVec(A.M_Rows);
            rowHashes(in A, ref dest, seed);
            return dest;
        }

        /// <summary>
        /// Writes one xxHash32 value per column of <paramref name="A"/> into <paramref name="dest"/> -
        /// dest[c] equals Hash.hash of column c extracted as a standalone vector, given the same
        /// seed. Columns are STRIDED (not contiguous, under row-major storage), so each column is
        /// first gathered into a reused scratch vector (drawn once from Allocator.Temp, refilled
        /// per column) before hashing - this makes colHashes slower than rowHashes (an O(M) gather per
        /// column vs. a direct pointer slice per row), but it is required for correctness: streaming
        /// the strided bytes through xxHash32's block algorithm in column order would NOT produce the
        /// same hash as hashing a real contiguous vector of that column's values, so the gather-then-
        /// hash approach is what makes the "same result as a standalone vector" guarantee above hold.
        /// </summary>
        public static void colHashes(in uintMxN A, ref uintN dest, uint seed = 0)
        {
            if (dest.N != A.N_Cols)
                throw new ArgumentException("Hash.colHashes: dest.N must equal A.N_Cols");

            if (A.N_Cols == 0) return;

            var col = A.uintTempVec(A.M_Rows);
            unsafe
            {
                int byteLen = A.M_Rows * sizeof(uint);
                uint* destPtr = (uint*)dest.Data.Ptr;
                for (int c = 0; c < A.N_Cols; c++)
                {
                    for (int r = 0; r < A.M_Rows; r++)
                        col[r] = A[r, c];
                    destPtr[c] = hash((byte*)col.Data.Ptr, byteLen, seed);
                }
            }
        }

        /// <summary>Allocating wrapper: same as the ref-dest <c>colHashes</c> overload, but returns a
        /// fresh uint buffer (a uintN, from Allocator.Temp) instead of writing into a caller-provided one.</summary>
        public static uintN colHashes(in uintMxN A, uint seed = 0)
        {
            var dest = A.uintVec(A.N_Cols);
            colHashes(in A, ref dest, seed);
            return dest;
        }

        

        // bool-sourced rowHashes/colHashes live HERE (not in Hash.bool.cs, which is a genuinely
        // singular file with no per-type rotation to hang a fixed-uintN-dest override on) - see
        // Hash.bool.cs's own note. The per-type skip marker above keeps this block ONLY for
        // the "uint" slot of this file's own int/short/long/uint rotation (dropped entirely from
        // Hash.int.cs/Hash.short.cs/Hash.long.cs), so it ends up emitted exactly once, in
        // Hash.uint.cs, where the plain `uintN`/`uintVec` placeholder tokens below resolve to
        // the real `uintN`/`uintVec` with no choose marker needed (the surviving slot already IS
        // the uint one). This comment lives INSIDE the skip zone specifically so it only ships in
        // Hash.uint.cs, where "the plain tokens below resolve to uintN" is actually true - outside
        // the zone it would leak into Hash.int.cs/Hash.short.cs/Hash.long.cs and be factually wrong
        // there (the block it refers to was stripped, not resolved to intN/shortN/longN).

        /// <summary>
        /// Writes one xxHash32 value per row of <paramref name="A"/> into <paramref name="dest"/> -
        /// dest[r] equals Hash.hash of row r extracted as a standalone vector, given the same seed.
        /// Zero-alloc: rows are contiguous (row-major storage), so each row is hashed directly out of
        /// A's backing buffer with no gather/copy.
        /// </summary>
        public static void rowHashes(in boolMxN A, ref uintN dest, uint seed = 0)
        {
            if (dest.N != A.M_Rows)
                throw new ArgumentException("Hash.rowHashes: dest.N must equal A.M_Rows");

            unsafe
            {
                int rowBytes = A.N_Cols * sizeof(bool);
                byte* rowPtr = (byte*)A.Data.Ptr;
                uint* destPtr = (uint*)dest.Data.Ptr;
                for (int r = 0; r < A.M_Rows; r++)
                {
                    destPtr[r] = hash(rowPtr, rowBytes, seed);
                    rowPtr += rowBytes;
                }
            }
        }

        /// <summary>Allocating wrapper: same as the ref-dest <c>rowHashes</c> overload, but returns a
        /// fresh uint buffer (a uintN, from Allocator.Temp) instead of writing into a caller-provided one.</summary>
        public static uintN rowHashes(in boolMxN A, uint seed = 0)
        {
            var dest = A.uintVec(A.M_Rows);
            rowHashes(in A, ref dest, seed);
            return dest;
        }

        /// <summary>
        /// Writes one xxHash32 value per column of <paramref name="A"/> into <paramref name="dest"/> -
        /// dest[c] equals Hash.hash of column c extracted as a standalone vector, given the same
        /// seed. Columns are STRIDED (not contiguous, under row-major storage), so each column is
        /// first gathered into a reused scratch vector (drawn once from Allocator.Temp, refilled
        /// per column) before hashing - this makes colHashes slower than rowHashes (an O(M) gather per
        /// column vs. a direct pointer slice per row), but it is required for correctness: streaming
        /// the strided bytes through xxHash32's block algorithm in column order would NOT produce the
        /// same hash as hashing a real contiguous vector of that column's values, so the gather-then-
        /// hash approach is what makes the "same result as a standalone vector" guarantee above hold.
        /// </summary>
        public static void colHashes(in boolMxN A, ref uintN dest, uint seed = 0)
        {
            if (dest.N != A.N_Cols)
                throw new ArgumentException("Hash.colHashes: dest.N must equal A.N_Cols");

            if (A.N_Cols == 0) return;

            var col = A.boolTempVec(A.M_Rows);
            unsafe
            {
                int byteLen = A.M_Rows * sizeof(bool);
                uint* destPtr = (uint*)dest.Data.Ptr;
                for (int c = 0; c < A.N_Cols; c++)
                {
                    for (int r = 0; r < A.M_Rows; r++)
                        col[r] = A[r, c];
                    destPtr[c] = hash((byte*)col.Data.Ptr, byteLen, seed);
                }
            }
        }

        /// <summary>Allocating wrapper: same as the ref-dest <c>colHashes</c> overload, but returns a
        /// fresh uint buffer (a uintN, from Allocator.Temp) instead of writing into a caller-provided one.</summary>
        public static uintN colHashes(in boolMxN A, uint seed = 0)
        {
            var dest = A.uintVec(A.N_Cols);
            colHashes(in A, ref dest, seed);
            return dest;
        }

        
    }
}
