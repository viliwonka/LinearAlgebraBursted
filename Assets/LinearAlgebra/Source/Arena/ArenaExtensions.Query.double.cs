// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Arena/ArenaExtensions.Query.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;

namespace LinearAlgebra
{
    // Allocating (arena) wrappers for Query search operations.
    // Zero-alloc ref-dest primitives (distancesToRow/Column) are in QueryOP;
    // these wrappers allocate from the matrix's own arena so callers don't
    // have to size buffers manually for the double-typed outputs.
    //
    // Indices-returning wrappers (nonzero, rowsWithinRadius, kNearestRows, etc.)
    // are in this file using Indices as the shared buffer type, generated via
    // the double template into floatXxx / doubleXxx variants.
    public static partial class ArenaExtensions
    {
        // -------------------------------------------------------------------------
        // distancesToRow / distancesToColumn — allocate doubleN from A's arena
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates a fresh doubleN (length A.M_Rows) from A's arena and fills it
        /// with the distance/similarity from each row of A to query q under metric m.
        /// </summary>
        public static doubleN doubleDistancesToRow(in doubleMxN A, in doubleN q, Metric m)
        {
            var dest = A.doubleVec(A.M_Rows);
            Query.distancesToRow(in A, in q, m, ref dest);
            return dest;
        }

        /// <summary>
        /// Allocates a fresh doubleN (length A.N_Cols) from A's arena and fills it
        /// with the distance/similarity from each column of A to query q under metric m.
        /// </summary>
        public static doubleN doubleDistancesToColumn(in doubleMxN A, in doubleN q, Metric m)
        {
            var dest = A.doubleVec(A.N_Cols);
            Query.distancesToColumn(in A, in q, m, ref dest);
            return dest;
        }

        // -------------------------------------------------------------------------
        // nonzeroIndices — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count nonzero elements, allocate exact-sized Indices, fill indices.
        /// Returns the allocated Indices (length = count). Arena-owned.
        /// </summary>
        public static Indices doubleNonzeroIndices<T>(this ref Arena arena, in T x, double tol)
            where T : unmanaged, IUnsafedoubleArray
        {
            int count = Query.countNonzero(in x, tol);
            if (count == 0) return arena.Indices(0);
            var idx = arena.Indices(count);
            int written = 0;
            for (int i = 0; i < x.Data.Length && written < idx.N; i++)
                if (math.abs(x.Data[i]) > tol) idx[written++] = i;
            return idx;
        }

        // -------------------------------------------------------------------------
        // rowsWithinRadius / columnsWithinRadius — count-pass + exact-alloc Indices
        // -------------------------------------------------------------------------

        /// <summary>
        /// Two-pass: count + exact-alloc Indices of row indices within radius r.
        /// </summary>
        public static Indices doubleRowsWithinRadius(this ref Arena arena, in doubleMxN A, in doubleN q, double r, Metric m)
        {
            int count = Query.countWithinRadius(in A, in q, r, m);
            if (count == 0) return arena.Indices(0);
            var idx = arena.Indices(count);
            bool sim = m == Metric.Cosine || m == Metric.Dot;
            int written = 0;
            for (int row = 0; row < A.M_Rows && written < idx.N; row++)
            {
                double s = Query.RowScore(in A, row, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = row;
            }
            return idx;
        }

        /// <summary>
        /// Two-pass: count + exact-alloc Indices of column indices within radius r.
        /// </summary>
        public static Indices doubleColumnsWithinRadius(this ref Arena arena, in doubleMxN A, in doubleN q, double r, Metric m)
        {
            int count = Query.countWithinColumnRadius(in A, in q, r, m);
            if (count == 0) return arena.Indices(0);
            var idx = arena.Indices(count);
            bool sim = m == Metric.Cosine || m == Metric.Dot;
            int written = 0;
            for (int c = 0; c < A.N_Cols && written < idx.N; c++)
            {
                double s = Query.ColScore(in A, c, in q, m);
                if (sim ? s >= r : s <= r) idx[written++] = c;
            }
            return idx;
        }

        // -------------------------------------------------------------------------
        // kNearestRows / kNearestColumns — arena-alloc Indices + doubleN scores
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates clamped-k Indices + doubleN from arena, fills via kNearestRows.
        /// Returns idx; scores and count are out params. count = min(k, A.M_Rows).
        /// </summary>
        public static Indices doubleKNearestRows(this ref Arena arena, in doubleMxN A, in doubleN q, int k, Metric m, out doubleN scores, out int count)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = A.doubleVec(0, true); count = 0; return arena.Indices(0); }
            var idx = arena.Indices(clampedK);
            scores = A.doubleVec(clampedK);
            count = Query.kNearestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>
        /// Allocates clamped-k Indices + doubleN from arena, fills via kNearestColumns.
        /// </summary>
        public static Indices doubleKNearestColumns(this ref Arena arena, in doubleMxN A, in doubleN q, int k, Metric m, out doubleN scores, out int count)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = A.doubleVec(0, true); count = 0; return arena.Indices(0); }
            var idx = arena.Indices(clampedK);
            scores = A.doubleVec(clampedK);
            count = Query.kNearestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        // -------------------------------------------------------------------------
        // kFarthestRows / kFarthestColumns — arena-alloc Indices + doubleN scores
        // -------------------------------------------------------------------------

        /// <summary>
        /// Allocates clamped-k Indices + doubleN from arena, fills via kFarthestRows.
        /// Returns idx; scores and count are out params. count = min(k, A.M_Rows).
        /// </summary>
        public static Indices doubleKFarthestRows(this ref Arena arena, in doubleMxN A, in doubleN q, int k, Metric m, out doubleN scores, out int count)
        {
            int clampedK = math.min(k, A.M_Rows);
            if (clampedK <= 0) { scores = A.doubleVec(0, true); count = 0; return arena.Indices(0); }
            var idx = arena.Indices(clampedK);
            scores = A.doubleVec(clampedK);
            count = Query.kFarthestRows(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }

        /// <summary>
        /// Allocates clamped-k Indices + doubleN from arena, fills via kFarthestColumns.
        /// Returns idx; scores and count are out params. count = min(k, A.N_Cols).
        /// </summary>
        public static Indices doubleKFarthestColumns(this ref Arena arena, in doubleMxN A, in doubleN q, int k, Metric m, out doubleN scores, out int count)
        {
            int clampedK = math.min(k, A.N_Cols);
            if (clampedK <= 0) { scores = A.doubleVec(0, true); count = 0; return arena.Indices(0); }
            var idx = arena.Indices(clampedK);
            scores = A.doubleVec(clampedK);
            count = Query.kFarthestColumns(in A, in q, clampedK, m, ref idx, ref scores);
            return idx;
        }
    }
}
