// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Arena/ArenaExtensions.Generators.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
namespace LinearAlgebra
{
    // Allocating (arena) wrappers for the Generate generators — each allocates a fresh persistent
    // vector/matrix and delegates to the zero-alloc ref-dest primitive. Use these for one-off /
    // setup-time builds (tween LUTs, kernels, wavetables); use the Generate.xxx(ref dest, …) form
    // inside per-frame loops.
    public static partial class ArenaExtensions
    {
        #region AXIS / SAMPLE

        /// <summary>N evenly spaced values over [a, b] inclusive (linspace). N==1 yields {a}.</summary>
        public static doubleN doubleLinspace(this ref Arena arena, double a, double b, int N)
        {
            var vec = arena.doubleVec(N);
            Generate.linspace(ref vec, a, b);
            return vec;
        }

        /// <summary>N-element arithmetic ramp: vec[i] = start + i*step.</summary>
        public static doubleN doubleArange(this ref Arena arena, double start, double step, int N)
        {
            var vec = arena.doubleVec(N);
            Generate.arange(ref vec, start, step);
            return vec;
        }

        /// <summary>Samples the functor f at N points over [t0, t1] (linspace piped through f).</summary>
        public static doubleN doubleSample<F>(this ref Arena arena, ref F f, int N, double t0, double t1)
            where F : struct, IdoubleScalarFunction
        {
            var vec = arena.doubleVec(N);
            Generate.sample(ref f, ref vec, t0, t1);
            return vec;
        }

        /// <summary>Samples the functor f at N points over the default domain [0, 1].</summary>
        public static doubleN doubleSample<F>(this ref Arena arena, ref F f, int N)
            where F : struct, IdoubleScalarFunction
            => arena.doubleSample(ref f, N, (double)0, (double)1);

        /// <summary>Bakes an easing curve into an N-entry LUT over [0, 1] (== doubleSample on [0,1]).</summary>
        public static doubleN doubleEasingLUT<F>(this ref Arena arena, ref F ease, int N)
            where F : struct, IdoubleScalarFunction
            => arena.doubleSample(ref ease, N, (double)0, (double)1);

        #endregion

        #region KERNELS / WINDOWS

        /// <summary>1D normalized Gaussian kernel (sum 1). sigma must be &gt; 0.</summary>
        public static doubleN doubleGaussianKernel(this ref Arena arena, int N, double sigma)
        {
            var vec = arena.doubleVec(N);
            Generate.gaussianKernel(ref vec, sigma);
            return vec;
        }

        /// <summary>1D uniform (box) kernel: every weight 1/N.</summary>
        public static doubleN doubleBoxKernel(this ref Arena arena, int N)
        {
            var vec = arena.doubleVec(N);
            Generate.boxKernel(ref vec);
            return vec;
        }

        /// <summary>1D triangular (tent) kernel, normalized to sum 1.</summary>
        public static doubleN doubleTentKernel(this ref Arena arena, int N)
        {
            var vec = arena.doubleVec(N);
            Generate.tentKernel(ref vec);
            return vec;
        }

        /// <summary>N×N separable Gaussian kernel = outer(g, g) of the 1D Gaussian (sum 1). sigma &gt; 0.</summary>
        public static doubleMxN doubleGaussianKernel2D(this ref Arena arena, int N, double sigma)
        {
            var mat = arena.doubleMat(N, N);
            Generate.gaussianKernel2D(ref mat, sigma);
            return mat;
        }

        /// <summary>DSP window of length N (Box/Hann/Hamming/Blackman).</summary>
        public static doubleN doubleWindow(this ref Arena arena, int N, WindowType type)
        {
            var vec = arena.doubleVec(N);
            Generate.window(ref vec, type);
            return vec;
        }

        #endregion

        #region RANK-1 (1D × 1D) MATRICES

        /// <summary>Outer product matrix M[i,j] = u[i]*v[j] (u.N × v.N), persistent allocation.</summary>
        public static doubleMxN doubleOuter(this ref Arena arena, in doubleN u, in doubleN v)
        {
            var mat = arena.doubleMat(u.N, v.N);
            Generate.outer(in u, in v, ref mat);
            return mat;
        }

        /// <summary>Additive outer matrix M[i,j] = u[i]+v[j] (u.N × v.N), persistent allocation.</summary>
        public static doubleMxN doubleOuterSum(this ref Arena arena, in doubleN u, in doubleN v)
        {
            var mat = arena.doubleMat(u.N, v.N);
            Generate.outerSum(in u, in v, ref mat);
            return mat;
        }

        #endregion
    }
}
