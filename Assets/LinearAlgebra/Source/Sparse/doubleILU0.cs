// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Sparse/fProxyILU0.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;
using LinearAlgebra;
using Unity.Mathematics;
using Unity.Collections;

namespace LinearAlgebra.Sparse
{
    /// <summary>
    /// Block incomplete-LU ILU(0) preconditioner over a square BSR: A ≈ L·U constrained to A's
    /// full block pattern — all fill is discarded (Saad ch. 10.3). L is unit-lower (its diagonal
    /// blocks implicit), U upper; the nonsymmetric sibling of <see cref="doubleIC0"/>, intended
    /// for <see cref="LinearAlgebra.Krylov"/>.biCGStab. On a fill-free pattern (block-tridiagonal)
    /// ILU(0) is the exact LU factorization.
    ///
    /// Storage: one full-pattern copy of A — strictly-lower slots hold L blocks, upper slots hold
    /// U blocks, and diagonal slots hold U_ii INVERSES (inverted once during factorization, so
    /// Apply multiplies instead of solving). Breakdown (a numerically singular pivot block) retries
    /// with an escalating diagonal shift, recorded in <see cref="Shift"/>; throws if the largest
    /// shift still fails. Symmetric-storage A pays a one-time mirror-to-full copy. A must store
    /// every diagonal block. Built via a ref Arena ctor, F is arena-tracked and the arena owns
    /// disposal -- do not call Dispose() on that instance. Built via an Allocator ctor, this
    /// instance owns F standalone and Dispose() must be called when done.
    /// </summary>
    public readonly struct doubleILU0 : IdoublePreconditioner, IDisposable
    {
        public readonly doubleBSR F;      // A's full pattern: L (strict lower), U_ii^{-1} (diag), U (upper)
        public readonly double Shift;

        public int Rows => F.M_Rows;

        /// <summary>Builds the ILU(0) factorization; throws ArgumentException if the factorization
        /// still breaks down at the largest diagonal shift. Use the out-info overload to receive
        /// the outcome as a <see cref="PreconditionerInfo"/> instead of an exception.</summary>
        public doubleILU0(in doubleBSR a, ref Arena arena)
        {
            this = new doubleILU0(in a, ref arena, out PreconditionerInfo info);
            if (!info.Solved)
                throw new ArgumentException("doubleILU0: factorization broke down at every diagonal shift — pivot blocks numerically singular");
        }

        /// <summary>
        /// Non-throwing build: info.status is Success, or Singular when the factorization broke
        /// down at every diagonal shift (the preconditioner is then unusable — do not Apply);
        /// info also carries the rescuing shift and the attempts consumed.
        /// Caller-contract violations (non-square, BR &gt; 16, missing diagonal block) still throw.
        /// </summary>
        public doubleILU0(in doubleBSR a, ref Arena arena, out PreconditionerInfo info)
        {
            if (a.BlockRows != a.BlockCols || a.BR != a.BC)
                throw new ArgumentException("doubleILU0: A must be square (BlockRows==BlockCols, BR==BC)");
            if (a.BR > 16)
                throw new ArgumentException("doubleILU0: BR must be <= 16 (fixed-size pivot scratch)");

            var A = a.Symmetric ? arena.doubleBSRMirrorToFull(in a) : a;

            int nb = A.BlockRows;
            int BR = A.BR;
            int blockLen = BR * BR;

            // Every diagonal block must exist (it becomes the pivot).
            double diagMax = 0;
            for (int i = 0; i < nb; i++)
            {
                int s = A.RowPtr[i], e = A.RowPtr[i + 1];
                bool hasDiag = false;
                for (int k = s; k < e; k++)
                {
                    if (A.ColInd[k] != i) continue;
                    hasDiag = true;
                    int off = k * blockLen;
                    for (int r = 0; r < BR; r++)
                    {
                        double av = math.abs(A.Values[off + r * BR + r]);
                        if (av > diagMax) diagMax = av;
                    }
                    break;
                }
                if (!hasDiag)
                    throw new ArgumentException("doubleILU0: missing diagonal block in A");
            }
            if (diagMax <= (double)0) diagMax = (double)1;

            // Working copy with A's full pattern.
            var Fm = arena.doubleBSR(nb, nb, BR, BR, A.Nnzb, true);
            {
                var fRowPtr = Fm.RowPtr; var fColInd = Fm.ColInd;
                for (int i = 0; i <= nb; i++) fRowPtr[i] = A.RowPtr[i];
                for (int k = 0; k < A.Nnzb; k++) fColInd[k] = A.ColInd[k];
            }

            double shift = 0;
            bool ok = false;
            int attempts = 0;
            for (int attempt = 0; attempt < 6; attempt++)
            {
                attempts = attempt + 1;
                CopyValues(in A, in Fm, shift);
                if (FactorizeInPlace(in Fm, diagMax)) { ok = true; break; }
                shift = shift == (double)0 ? (double)1e-3 * diagMax : shift * (double)10;
            }
            F = Fm;
            Shift = shift;
            info = new PreconditionerInfo
            {
                status = ok ? DirectSolveStatus.Success : DirectSolveStatus.Singular,
                shift = (double)shift,
                attempts = attempts,
            };
        }

        /// <summary>
        /// Standalone twin of <see cref="doubleILU0(in doubleBSR, ref Arena)"/>: allocates F from
        /// <paramref name="allocator"/> instead of an arena (a Symmetric-storage input's mirror-to-
        /// full copy is Temp-allocated and disposed before returning -- it is setup-only, F holds
        /// its own independent copy of the values). Same throw contract. Dispose the result with
        /// <see cref="Dispose"/> -- only call Dispose on an instance built via an Allocator ctor,
        /// never on one built via a ref Arena ctor (that instance is arena-owned).
        /// </summary>
        public unsafe doubleILU0(in doubleBSR a, Allocator allocator)
        {
            this = new doubleILU0(in a, allocator, out PreconditionerInfo info);
            if (!info.Solved)
            {
                Dispose();
                throw new ArgumentException("doubleILU0: factorization broke down at every diagonal shift — pivot blocks numerically singular");
            }
        }

        /// <summary>Standalone twin of <see cref="doubleILU0(in doubleBSR, ref Arena, out PreconditionerInfo)"/>.
        /// info carries the build outcome exactly as the ref Arena overload does (F is allocated and
        /// left populated with the failed factorization even on breakdown -- Dispose is still valid).</summary>
        public unsafe doubleILU0(in doubleBSR a, Allocator allocator, out PreconditionerInfo info)
        {
            if (a.BlockRows != a.BlockCols || a.BR != a.BC)
                throw new ArgumentException("doubleILU0: A must be square (BlockRows==BlockCols, BR==BC)");
            if (a.BR > 16)
                throw new ArgumentException("doubleILU0: BR must be <= 16 (fixed-size pivot scratch)");

            bool ownsMirror = a.Symmetric;
            var A = ownsMirror ? a.MirrorToFull(Allocator.Temp) : a;

            int nb = A.BlockRows;
            int BR = A.BR;
            int blockLen = BR * BR;

            double diagMax = 0;
            for (int i = 0; i < nb; i++)
            {
                int s = A.RowPtr[i], e = A.RowPtr[i + 1];
                bool hasDiag = false;
                for (int k = s; k < e; k++)
                {
                    if (A.ColInd[k] != i) continue;
                    hasDiag = true;
                    int off = k * blockLen;
                    for (int r = 0; r < BR; r++)
                    {
                        double av = math.abs(A.Values[off + r * BR + r]);
                        if (av > diagMax) diagMax = av;
                    }
                    break;
                }
                if (!hasDiag)
                {
                    if (ownsMirror) A.Dispose();
                    throw new ArgumentException("doubleILU0: missing diagonal block in A");
                }
            }
            if (diagMax <= (double)0) diagMax = (double)1;

            var Fm = new doubleBSR(nb, nb, BR, BR, A.Nnzb, allocator, true);
            {
                var fRowPtr = Fm.RowPtr; var fColInd = Fm.ColInd;
                for (int i = 0; i <= nb; i++) fRowPtr[i] = A.RowPtr[i];
                for (int k = 0; k < A.Nnzb; k++) fColInd[k] = A.ColInd[k];
            }

            double shift = 0;
            bool ok = false;
            int attempts = 0;
            for (int attempt = 0; attempt < 6; attempt++)
            {
                attempts = attempt + 1;
                CopyValues(in A, in Fm, shift);
                if (FactorizeInPlace(in Fm, diagMax)) { ok = true; break; }
                shift = shift == (double)0 ? (double)1e-3 * diagMax : shift * (double)10;
            }
            F = Fm;
            Shift = shift;
            info = new PreconditionerInfo
            {
                status = ok ? DirectSolveStatus.Success : DirectSolveStatus.Singular,
                shift = (double)shift,
                attempts = attempts,
            };

            if (ownsMirror) A.Dispose();
        }

        /// <summary>Disposes F. Only call on an instance built via an Allocator ctor -- an instance
        /// built via a ref Arena ctor is arena-owned and must not be disposed directly.</summary>
        public unsafe void Dispose() => F.Dispose();

        static void CopyValues(in doubleBSR A, in doubleBSR Fm, double shift)
        {
            int BR = A.BR, blockLen = BR * BR, nb = A.BlockRows;
            var src = A.Values; var dst = Fm.Values;
            for (int t = 0; t < src.Length; t++) dst[t] = src[t];

            if (shift == (double)0) return;
            for (int i = 0; i < nb; i++)
            {
                int s = A.RowPtr[i], e = A.RowPtr[i + 1];
                for (int k = s; k < e; k++)
                {
                    if (A.ColInd[k] != i) continue;
                    int off = k * blockLen;
                    for (int r = 0; r < BR; r++)
                        dst[off + r * BR + r] += shift;
                    break;
                }
            }
        }

        // IKJ block ILU(0). After processing row k, its diagonal slot holds U_kk^{-1}.
        static bool FactorizeInPlace(in doubleBSR Fm, double diagMax)
        {
            int nb = Fm.BlockRows;
            int BR = Fm.BR;
            int blockLen = BR * BR;
            double pivotFloor = (double)16 * Consts.doubleEpsilon * diagMax;

            var rowPtr = Fm.RowPtr; var colInd = Fm.ColInd; var values = Fm.Values;

            for (int i = 0; i < nb; i++)
            {
                int s = rowPtr[i], e = rowPtr[i + 1];
                int diagIdx = -1;

                for (int kIdx = s; kIdx < e; kIdx++)
                {
                    int k = colInd[kIdx];
                    if (k >= i) { if (k == i) diagIdx = kIdx; break; }

                    // L_ik = A_ik · U_kk^{-1}  (row k's diagonal already holds the inverse).
                    int kkIdx = FindDiag(rowPtr, colInd, k);
                    BlockMulRight(values, kIdx * blockLen, kkIdx * blockLen, BR);

                    // Trailing update: for each U_kj (j > k) present in BOTH row k and row i:
                    // A_ij -= L_ik · U_kj.
                    int p = kIdx + 1;                    // row i, cols > k
                    int q = kkIdx + 1;                   // row k, cols > k
                    int pEnd = e, qEnd = rowPtr[k + 1];
                    while (p < pEnd && q < qEnd)
                    {
                        int pc = colInd[p], qc = colInd[q];
                        if (pc < qc) { p++; continue; }
                        if (qc < pc) { q++; continue; }
                        BlockMulSubAB(values, p * blockLen, kIdx * blockLen, q * blockLen, BR);
                        p++; q++;
                    }
                }

                if (diagIdx < 0) diagIdx = FindDiag(rowPtr, colInd, i);
                if (!InvertBlockInPlace(values, diagIdx * blockLen, BR, pivotFloor))
                    return false;
            }
            return true;
        }

        static int FindDiag(Unity.Collections.LowLevel.Unsafe.UnsafeList<int> rowPtr,
                            Unity.Collections.LowLevel.Unsafe.UnsafeList<int> colInd, int row)
        {
            int s = rowPtr[row], e = rowPtr[row + 1];
            for (int k = s; k < e; k++)
                if (colInd[k] == row) return k;
            return -1;
        }

        // X = X · Minv for BR x BR row-major blocks at the given offsets.
        static unsafe void BlockMulRight(Unity.Collections.LowLevel.Unsafe.UnsafeList<double> v, int xOff, int mOff, int BR)
        {
            // Row buffer stackalloc'd ONCE (stackalloc frees at return, not loop exit).
            double* tmp = stackalloc double[16];
            for (int r = 0; r < BR; r++)
            {
                for (int c = 0; c < BR; c++)
                {
                    double sum = 0;
                    for (int t = 0; t < BR; t++)
                        sum += v[xOff + r * BR + t] * v[mOff + t * BR + c];
                    tmp[c] = sum;
                }
                for (int c = 0; c < BR; c++)
                    v[xOff + r * BR + c] = tmp[c];
            }
        }

        // C -= A · B for BR x BR row-major blocks.
        static void BlockMulSubAB(Unity.Collections.LowLevel.Unsafe.UnsafeList<double> v, int cOff, int aOff, int bOff, int BR)
        {
            for (int r = 0; r < BR; r++)
                for (int c = 0; c < BR; c++)
                {
                    double sum = 0;
                    for (int t = 0; t < BR; t++)
                        sum += v[aOff + r * BR + t] * v[bOff + t * BR + c];
                    v[cOff + r * BR + c] -= sum;
                }
        }

        // In-place Gauss-Jordan inverse with partial pivoting of the BR x BR block at off.
        // Returns false when a pivot falls at or below the floor.
        static bool InvertBlockInPlace(Unity.Collections.LowLevel.Unsafe.UnsafeList<double> v, int off, int BR, double pivotFloor)
        {
            unsafe
            {
                double* m = stackalloc double[16 * 16];
                int* perm = stackalloc int[16];
                for (int t = 0; t < BR * BR; t++) m[t] = v[off + t];
                for (int t = 0; t < BR; t++) perm[t] = t;

                // Gauss-Jordan on [m | I], I tracked implicitly by transforming m into the inverse.
                double* inv = stackalloc double[16 * 16];
                for (int r = 0; r < BR; r++)
                    for (int c = 0; c < BR; c++)
                        inv[r * BR + c] = r == c ? (double)1 : (double)0;

                for (int col = 0; col < BR; col++)
                {
                    int piv = col;
                    double best = math.abs(m[col * BR + col]);
                    for (int r = col + 1; r < BR; r++)
                    {
                        double av = math.abs(m[r * BR + col]);
                        if (av > best) { best = av; piv = r; }
                    }
                    if (!(best > pivotFloor)) return false;

                    if (piv != col)
                        for (int c = 0; c < BR; c++)
                        {
                            (m[col * BR + c], m[piv * BR + c]) = (m[piv * BR + c], m[col * BR + c]);
                            (inv[col * BR + c], inv[piv * BR + c]) = (inv[piv * BR + c], inv[col * BR + c]);
                        }

                    double d = (double)1 / m[col * BR + col];
                    for (int c = 0; c < BR; c++) { m[col * BR + c] *= d; inv[col * BR + c] *= d; }

                    for (int r = 0; r < BR; r++)
                    {
                        if (r == col) continue;
                        double f = m[r * BR + col];
                        if (f == (double)0) continue;
                        for (int c = 0; c < BR; c++)
                        {
                            m[r * BR + c] -= f * m[col * BR + c];
                            inv[r * BR + c] -= f * inv[col * BR + c];
                        }
                    }
                }

                for (int t = 0; t < BR * BR; t++) v[off + t] = inv[t];
            }
            return true;
        }

        /// <summary>z = (L·U)⁻¹ r: unit-lower forward sweep into z, then upper backward sweep in
        /// place (diagonal solves are multiplies by the stored U_ii inverses). z must not alias r.</summary>
        public bool IsIdentity => false;
        // L/U come from A's full (non-symmetric-constrained) pattern -- M is not symmetric in
        // general, even for symmetric A. Never a valid cg/minres preconditioner.
        public bool IsSpd => false;
        public bool IsConstant => true;

        public unsafe void Apply(in doubleN r, ref doubleN z)
        {
            int n = Rows;
            if (r.N != n) throw new ArgumentException("doubleILU0.Apply: r.N must equal Rows");
            if (z.N != n) throw new ArgumentException("doubleILU0.Apply: z.N must equal Rows");
            if (z.Data.Ptr == r.Data.Ptr) throw new ArgumentException("doubleILU0.Apply: z must not alias r");

            int nb = F.BlockRows;
            int BR = F.BR;
            int blockLen = BR * BR;

            var rowPtr = F.RowPtr; var colInd = F.ColInd; var values = F.Values;

            // Forward (unit lower): z_i = r_i - sum_{j<i} L_ij z_j.
            for (int i = 0; i < nb; i++)
            {
                int s = rowPtr[i], e = rowPtr[i + 1];
                int rowBase = i * BR;
                for (int t = 0; t < BR; t++) z[rowBase + t] = r[rowBase + t];

                for (int idx = s; idx < e; idx++)
                {
                    int j = colInd[idx];
                    if (j >= i) break;
                    int jBase = j * BR, off = idx * blockLen;
                    for (int lr = 0; lr < BR; lr++)
                    {
                        double sum = 0;
                        for (int lc = 0; lc < BR; lc++)
                            sum += values[off + lr * BR + lc] * z[jBase + lc];
                        z[rowBase + lr] -= sum;
                    }
                }
            }

            // Backward (upper): z_i = U_ii^{-1} (z_i - sum_{j>i} U_ij z_j).
            double* w = stackalloc double[16];   // stackalloc'd ONCE (stackalloc frees at return, not loop exit)
            for (int i = nb - 1; i >= 0; i--)
            {
                int s = rowPtr[i], e = rowPtr[i + 1];
                int rowBase = i * BR;
                int diagIdx = -1;

                for (int idx = e - 1; idx >= s; idx--)
                {
                    int j = colInd[idx];
                    if (j <= i) { if (j == i) diagIdx = idx; break; }
                    int jBase = j * BR, off = idx * blockLen;
                    for (int lr = 0; lr < BR; lr++)
                    {
                        double sum = 0;
                        for (int lc = 0; lc < BR; lc++)
                            sum += values[off + lr * BR + lc] * z[jBase + lc];
                        z[rowBase + lr] -= sum;
                    }
                }

                int dOff = diagIdx * blockLen;
                for (int lr = 0; lr < BR; lr++)
                {
                    double sum = 0;
                    for (int lc = 0; lc < BR; lc++)
                        sum += values[dOff + lr * BR + lc] * z[rowBase + lc];
                    w[lr] = sum;
                }
                for (int lr = 0; lr < BR; lr++) z[rowBase + lr] = w[lr];
            }
        }
    }
}
