// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/Generate/ConvertOP.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;
using Unity.Collections;



namespace BULA
{
    // Conversions between fixed-size Unity.Mathematics types and dynamic vectors/matrices. Forward
    // converters allocate a fresh floatN/floatMxN via allocator; reverse converters read into a
    // fixed-size value and allocate nothing.
    public static partial class ConvertOP
    {
        #region CONVERSIONS_FROM_MATH
        public static floatN Convert(in float2 mathVec, Allocator allocator = Allocator.Temp)
        {
            var vec = new floatN(2, allocator, true);

            vec[0] = mathVec.x;
            vec[1] = mathVec.y;

            return vec;
        }

        public static floatN Convert(in float3 mathVec, Allocator allocator = Allocator.Temp)
        {
            var vec = new floatN(3, allocator, true);

            vec[0] = mathVec.x;
            vec[1] = mathVec.y;
            vec[2] = mathVec.z;

            return vec;
        }

        public static floatN Convert(in float4 mathVec, Allocator allocator = Allocator.Temp)
        {
            var vec = new floatN(4, allocator, true);

            vec[0] = mathVec.x;
            vec[1] = mathVec.y;
            vec[2] = mathVec.z;
            vec[3] = mathVec.w;

            return vec;
        }

        public static floatMxN Convert(in float2x2 mathMat, Allocator allocator = Allocator.Temp)
        {
            var mat = new floatMxN(2, 2, allocator, true);

            mat[0, 0] = mathMat.c0.x;
            mat[1, 0] = mathMat.c0.y;
            mat[0, 1] = mathMat.c1.x;
            mat[1, 1] = mathMat.c1.y;

            return mat;
        }

        public static floatMxN Convert(in float3x3 mathMat, Allocator allocator = Allocator.Temp)
        {
            var mat = new floatMxN(3, 3, allocator, true);

            mat[0, 0] = mathMat.c0.x;
            mat[1, 0] = mathMat.c0.y;
            mat[2, 0] = mathMat.c0.z;
            mat[0, 1] = mathMat.c1.x;
            mat[1, 1] = mathMat.c1.y;
            mat[2, 1] = mathMat.c1.z;
            mat[0, 2] = mathMat.c2.x;
            mat[1, 2] = mathMat.c2.y;
            mat[2, 2] = mathMat.c2.z;

            return mat;
        }

        public static floatMxN Convert(in float4x4 mathMat, Allocator allocator = Allocator.Temp)
        {
            var mat = new floatMxN(4, 4, allocator, true);

            mat[0, 0] = mathMat.c0.x;
            mat[1, 0] = mathMat.c0.y;
            mat[2, 0] = mathMat.c0.z;
            mat[3, 0] = mathMat.c0.w;
            mat[0, 1] = mathMat.c1.x;
            mat[1, 1] = mathMat.c1.y;
            mat[2, 1] = mathMat.c1.z;
            mat[3, 1] = mathMat.c1.w;
            mat[0, 2] = mathMat.c2.x;
            mat[1, 2] = mathMat.c2.y;
            mat[2, 2] = mathMat.c2.z;
            mat[3, 2] = mathMat.c2.w;
            mat[0, 3] = mathMat.c3.x;
            mat[1, 3] = mathMat.c3.y;
            mat[2, 3] = mathMat.c3.z;
            mat[3, 3] = mathMat.c3.w;

            return mat;
        }

        #endregion

        #region CONVERSIONS_TO_MATH
        public static float2 Convert(in floatN mathVec) {
            if (mathVec.N < 2)
                throw new System.ArgumentException("Convert(floatN -> float2): source vector must have length >= 2");

            var vec = new float2();

            vec.x = mathVec[0];
            vec.y = mathVec[1];

            return vec;
        }
        #endregion


    }
}
