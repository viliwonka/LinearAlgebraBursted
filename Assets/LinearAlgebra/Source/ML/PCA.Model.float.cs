// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSource/ML/PCA.Model.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System;

using Unity.Collections;

using LinearAlgebra.ML;

namespace LinearAlgebra.ML
{
    /// <summary>
    /// A fitted PCA model: the axes/variances needed to project new data (<see cref="PCA.transform"/>)
    /// or to read off variances for a reduction decision. Every <c>PCA</c> fit route (fitCov /
    /// fitSvd / fitSvdTruncated / fitRandomized) fills one of these. Allocate via
    /// <c>Arena.floatPCAModel(p, k)</c> (p = X.N_Cols features, k = number of components) and reuse across
    /// same-shape fits (realtime pattern: fit each frame into the same model, <c>ClearTemp()</c> reclaims the
    /// internal scratch each fit allocates from the arena's temp pool).
    /// </summary>
    public struct floatPCAModel : IDisposable
    {
        /// <summary>p x k. Column i is the i-th principal axis (unit-norm, sign-fixed — see
        /// <see cref="PCA"/>'s sign convention). Undefined if <see cref="converged"/> is false.</summary>
        public floatMxN components;

        /// <summary>Length k. Variance captured by each component, DESCENDING. Undefined if
        /// <see cref="converged"/> is false.</summary>
        public floatN explainedVariance;

        /// <summary>Length k. explainedVariance[i] / totalVariance (totalVariance computed directly from
        /// the fit data, never as the sum of a possibly-truncated explainedVariance). Undefined if
        /// <see cref="converged"/> is false.</summary>
        public floatN explainedVarianceRatio;

        /// <summary>Length p. Per-feature mean, needed to center new data in <c>transform</c>.</summary>
        public floatN mean;

        /// <summary>Length p. Per-feature divisor applied before projecting: all ones (PCAScaling.Covariance)
        /// or the per-feature SAMPLE std-dev (PCAScaling.Correlation; a zero-variance feature gets 1, never
        /// a divide-by-zero).</summary>
        public floatN scale;

        /// <summary>Number of components: == p (X.N_Cols) for the full routes (fitCov/fitSvd), or the
        /// requested top-k for fitSvdTruncated/fitRandomized.</summary>
        public int k;

        /// <summary>Underlying eigensolve/SVD convergence flag. All other fields (except mean/scale, which
        /// are filled unconditionally) are undefined when this is false.</summary>
        public bool converged;

        /// <summary>Same value as <see cref="converged"/>; use whichever reads better at the call site.</summary>
        public bool Solved => converged;

        /// <summary>Implicit success test, so <c>if (model)</c> reads naturally after a fit.</summary>
        public static implicit operator bool(floatPCAModel m) => m.converged;

        /// <summary>Burst-safe compact summary, e.g. <c>floatPCAModel(k=3, p=8, converged=true)</c> --
        /// dims/k/converged only, NEVER the components matrix (unbounded / not Burst-safe to dump).
        /// p is read from <c>components.M_Rows</c> (the fitted feature count). Never allocates
        /// managed memory.</summary>
        public FixedString128Bytes ToFixedString()
        {
            FixedString128Bytes str = $"floatPCAModel(k={k}, p={components.M_Rows}, converged=";

            if (converged)
            {
                FixedString32Bytes flag = "true)";
                str.Append(flag);
            }
            else
            {
                FixedString32Bytes flag = "false)";
                str.Append(flag);
            }

            return str;
        }

        /// <summary>Managed wrapper -- do not call from inside a [BurstCompile] job.</summary>
        public override string ToString() => ToFixedString().ToString();

        /// <summary>Standalone allocation sized identically to <c>Arena.floatPCAModel(p, k)</c>. Pair with <see cref="Dispose"/>.</summary>
        public floatPCAModel(int p, int k, Allocator allocator)
        {
            components             = new floatMxN(p, k, allocator);
            explainedVariance      = new floatN(k, allocator);
            explainedVarianceRatio = new floatN(k, allocator);
            mean                   = new floatN(p, allocator);
            scale                  = new floatN(p, allocator);
            this.k                 = k;
            converged              = false;
        }

        /// <summary>Dispose only instances built with the Allocator ctor; arena-built instances are arena-owned.</summary>
        public void Dispose()
        {
            components.Dispose();
            explainedVariance.Dispose();
            explainedVarianceRatio.Dispose();
            mean.Dispose();
            scale.Dispose();
        }
    }
}

namespace LinearAlgebra
{
    public static partial class ArenaExtensions
    {
        /// <summary>
        /// Allocates a PCA model sized for <paramref name="p"/> features and <paramref name="k"/>
        /// components. All buffers are persistent in this arena (disposed with it).
        /// </summary>
        public static LinearAlgebra.ML.floatPCAModel floatPCAModel(this ref Arena arena, int p, int k)
        {
            return new LinearAlgebra.ML.floatPCAModel
            {
                components             = arena.floatMat(p, k),
                explainedVariance       = arena.floatVec(k),
                explainedVarianceRatio  = arena.floatVec(k),
                mean                    = arena.floatVec(p),
                scale                   = arena.floatVec(p),
                k                       = k,
                converged               = false
            };
        }
    }
}
