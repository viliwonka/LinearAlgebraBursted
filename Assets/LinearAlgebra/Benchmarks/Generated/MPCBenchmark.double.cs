// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceBenchmarks/MPCBenchmark.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System.Text;

using Unity.Burst;
using Unity.Collections;
using Unity.Jobs;
using Unity.Mathematics;

using LinearAlgebra;
using LinearAlgebra.Control;

namespace LinearAlgebra.Benchmarks
{
    // GENERATED per-dtype half of MPCBenchmark (the timed IJobs + the instance builder + build+measure
    // methods). The dtype-agnostic harness (sizes/seeds, row formatter, Run, Section) is hand-written in
    // Assets/LinearAlgebra/Benchmarks/MPCBenchmark.cs.
    //
    // Three job shapes:
    //   - MpcWarmFrameJobDouble: ONE receding-horizon frame per Execute() call (solve, then advance the
    //     REAL plant x <- A x + B u0). s/x/u0 are NativeArray-backed fields, so state survives across
    //     repeated job.Run() calls from managed code -- the harness runs this job untimed
    //     (MPCBenchmarkFmt.WarmPrewarmFrames times) to burn off the cold-start/churn transient, THEN
    //     times it via Bench.Time, so each of Bench.Time's own 1 warmup + 4 timed calls is one already-
    //     warm frame.
    //   - MpcColdJobDouble: resets the warm-start carry (z/uPlan/wstatus/populated) to its fresh-
    //     construction values before every rep, so every solve pays the first-ever-solve cost at the
    //     SAME x0 -- doubleMPCState's own fields are public for exactly this purpose (mirrors
    //     LqrColdRecursionJobDouble's force-cold-started state in LQRBenchmark.double.cs).
    //   - MpcConstructJobDouble: times doubleMPCState's constructor itself (terminal DARE + Phi/Gamma/H
    //     condensing), constructing and disposing a fresh instance every rep.
    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct MpcWarmFrameJobDouble : IJob
    {
        public doubleMPCState s;
        public doubleMxN A, B;      // REAL (not condensed) dynamics, to advance the plant after each solve
        public doubleN x;            // current plant state -- carried across repeated job.Run() calls
        public doubleN u0;
        public doubleN reference;
        public doubleN xNext, Bu;    // plant-advance scratch
        public NativeArray<int> itersOut;
        public NativeArray<int> changesOut;
        public NativeArray<int> statusOut;

        public void Execute()
        {
            var info = MPC.solve(ref s, in x, in reference, ref u0);
            itersOut[0] = info.iterations;
            changesOut[0] = info.activeSetChanges;
            statusOut[0] = (int)info.status;

            Blas.dot(in A, in x, ref xNext);
            Blas.dot(in B, in u0, ref Bu);
            xNext.addInPlace(Bu);
            x.CopyFrom(xNext);
        }
    }

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct MpcColdJobDouble : IJob
    {
        public doubleMPCState s;
        public doubleN x0;
        public doubleN reference;
        public doubleN u0;
        public int reps;
        public NativeArray<int> itersOut;
        public NativeArray<int> changesOut;
        public NativeArray<int> statusOut;

        public void Execute()
        {
            for (int r = 0; r < reps; r++)
            {
                // Reset the warm-start carry to its fresh-construction state (zero z/uPlan, every
                // wstatus row Inactive -- WorkingSetStatus.Inactive == 0 -- populated=false) so this rep
                // pays the FIRST-EVER-solve cost at x0, not a warm-started one.
                for (int i = 0; i < s.z.N; i++) s.z[i] = (double)0;
                for (int i = 0; i < s.uPlan.N; i++) s.uPlan[i] = (double)0;
                for (int i = 0; i < s.wstatus.Length; i++) s.wstatus[i] = 0;
                s.populated = false;

                var info = MPC.solve(ref s, in x0, in reference, ref u0);
                itersOut[0] = info.iterations;
                changesOut[0] = info.activeSetChanges;
                statusOut[0] = (int)info.status;
            }
        }
    }

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct MpcConstructJobDouble : IJob
    {
        public doubleMxN A, B, Q, R;
        public doubleN uLo, uHi;
        public int N;
        public int reps;
        public NativeArray<double> checksumOut;   // consumed so the construction can't be DCE'd away

        public void Execute()
        {
            double checksum = 0;
            for (int r = 0; r < reps; r++)
            {
                var s = new doubleMPCState(A.M_Rows, B.N_Cols, N, Allocator.Temp, in A, in B, in Q, in R, in uLo, in uHi);
                checksum += (double)s.Kinf[0, 0];
                s.Dispose();
            }
            checksumOut[0] = checksum;
        }
    }

    public static partial class MPCBenchmark
    {
        // Trivially stabilizable random plant (diagonal in [0.2,0.4), off-diagonal scaled 0.2/n) -- the
        // SAME recipe LQRBenchmark.double.cs's own BuildInstanceDouble uses, so the terminal DARE
        // doubleMPCState's constructor solves is guaranteed to converge at every size below. Q=I, R=I.
        static void BuildPlantDouble(int n, int m, uint seed,
                                     out doubleMxN A, out doubleMxN B, out doubleMxN Q, out doubleMxN R)
        {
            var rng = new Unity.Mathematics.Random(seed);
            double off = (double)(0.2 / n);

            A = new doubleMxN(n, n, Allocator.Persistent);
            for (int i = 0; i < n; i++)
                for (int j = 0; j < n; j++)
                    A[i, j] = (i == j) ? rng.NextDouble(0.2f, 0.4f) : rng.NextDouble(-1f, 1f) * off;

            B = new doubleMxN(n, m, Allocator.Persistent);
            for (int i = 0; i < n; i++)
                for (int j = 0; j < m; j++)
                    B[i, j] = rng.NextDouble(-1f, 1f);

            Q = new doubleMxN(n, n, Allocator.Persistent);
            for (int i = 0; i < n; i++) Q[i, i] = (double)1;

            R = new doubleMxN(m, m, Allocator.Persistent);
            for (int i = 0; i < m; i++) R[i, i] = (double)1;
        }

        // Builds the box-only or box+soft-wall doubleMPCState for a given size. The soft wall (when
        // enabled) is a single row selecting state[0] (C=[1,0,...,0], d=2) -- with x0[0]=4 (see
        // WarmFrameDouble/ColdSolveDouble), the wall is violated along the initial transient, exercising
        // the soft-row general-constraint machinery before the plant's own contractive dynamics settle
        // it near the origin.
        static doubleMPCState BuildStateDouble(int n, int m, int N, in doubleMxN A, in doubleMxN B,
                                               in doubleMxN Q, in doubleMxN R, in doubleN uLo, in doubleN uHi,
                                               bool softWall)
        {
            if (!softWall)
                return new doubleMPCState(n, m, N, Allocator.Persistent, in A, in B, in Q, in R, in uLo, in uHi);

            var C = new doubleMxN(1, n, Allocator.Persistent);
            C[0, 0] = (double)1;
            var d = GenerateOP.doubleVec(1, (double)2, Allocator.Persistent);
            var s = new doubleMPCState(n, m, N, Allocator.Persistent, in A, in B, in Q, in R, in uLo, in uHi, in C, in d);
            C.Dispose(); d.Dispose();
            return s;
        }

        // ==== Section 1: warm steady-state per-frame cost (the headline) ====
        static string WarmFrameDouble(int N, int n, int m, uint seed, bool softWall)
        {
            BuildPlantDouble(n, m, seed, out var A, out var B, out var Q, out var R);
            var uLo = GenerateOP.doubleVec(m, (double)(-1), Allocator.Persistent);
            var uHi = GenerateOP.doubleVec(m, (double)1, Allocator.Persistent);
            var s = BuildStateDouble(n, m, N, in A, in B, in Q, in R, in uLo, in uHi, softWall);

            var x = new doubleN(n, Allocator.Persistent);
            x[0] = (double)4;
            var reference = new doubleN(n, Allocator.Persistent);   // zero -- track to the origin
            var u0 = new doubleN(m, Allocator.Persistent, true);
            var xNext = new doubleN(n, Allocator.Persistent, true);
            var Bu = new doubleN(n, Allocator.Persistent, true);

            var itersOut = new NativeArray<int>(1, Allocator.Persistent);
            var changesOut = new NativeArray<int>(1, Allocator.Persistent);
            var statusOut = new NativeArray<int>(1, Allocator.Persistent);
            var job = new MpcWarmFrameJobDouble
            {
                s = s, A = A, B = B, x = x, u0 = u0, reference = reference, xNext = xNext, Bu = Bu,
                itersOut = itersOut, changesOut = changesOut, statusOut = statusOut,
            };

            // Untimed: burn off the cold-start + active-set-churn transient before any timed call.
            for (int i = 0; i < MPCBenchmarkFmt.WarmPrewarmFrames; i++) job.Run();

            var stat = Bench.Time(() => job.Run());
            string row = MPCBenchmarkFmt.Row("double", softWall ? "warm+wall" : "warm-box", N, n, m, 1, stat,
                                             itersOut[0], changesOut[0], statusOut[0]);

            itersOut.Dispose(); changesOut.Dispose(); statusOut.Dispose();
            s.Dispose();
            A.Dispose(); B.Dispose(); Q.Dispose(); R.Dispose(); uLo.Dispose(); uHi.Dispose();
            x.Dispose(); reference.Dispose(); u0.Dispose(); xNext.Dispose(); Bu.Dispose();
            return row;
        }

        // ==== Section 2: cold solve cost (fresh warm-start carry every call) ====
        static string ColdSolveDouble(int N, int n, int m, uint seed, int reps, bool softWall)
        {
            BuildPlantDouble(n, m, seed, out var A, out var B, out var Q, out var R);
            var uLo = GenerateOP.doubleVec(m, (double)(-1), Allocator.Persistent);
            var uHi = GenerateOP.doubleVec(m, (double)1, Allocator.Persistent);
            var s = BuildStateDouble(n, m, N, in A, in B, in Q, in R, in uLo, in uHi, softWall);

            var x0 = new doubleN(n, Allocator.Persistent);
            x0[0] = (double)4;
            var reference = new doubleN(n, Allocator.Persistent);
            var u0 = new doubleN(m, Allocator.Persistent, true);

            var itersOut = new NativeArray<int>(1, Allocator.Persistent);
            var changesOut = new NativeArray<int>(1, Allocator.Persistent);
            var statusOut = new NativeArray<int>(1, Allocator.Persistent);
            var job = new MpcColdJobDouble
            {
                s = s, x0 = x0, reference = reference, u0 = u0, reps = reps,
                itersOut = itersOut, changesOut = changesOut, statusOut = statusOut,
            };
            var stat = Bench.Time(() => job.Run());
            string row = MPCBenchmarkFmt.Row("double", softWall ? "cold+wall" : "cold-box", N, n, m, reps, stat,
                                             itersOut[0], changesOut[0], statusOut[0]);

            itersOut.Dispose(); changesOut.Dispose(); statusOut.Dispose();
            s.Dispose();
            A.Dispose(); B.Dispose(); Q.Dispose(); R.Dispose(); uLo.Dispose(); uHi.Dispose();
            x0.Dispose(); reference.Dispose(); u0.Dispose();
            return row;
        }

        // ==== Section 3: doubleMPCState construction cost (one-shot) ====
        static string ConstructDouble(int N, int n, int m, uint seed, int reps)
        {
            BuildPlantDouble(n, m, seed, out var A, out var B, out var Q, out var R);
            var uLo = GenerateOP.doubleVec(m, (double)(-1), Allocator.Persistent);
            var uHi = GenerateOP.doubleVec(m, (double)1, Allocator.Persistent);

            var checksumOut = new NativeArray<double>(1, Allocator.Persistent);
            var job = new MpcConstructJobDouble { A = A, B = B, Q = Q, R = R, uLo = uLo, uHi = uHi, N = N, reps = reps, checksumOut = checksumOut };
            var stat = Bench.Time(() => job.Run());
            string row = MPCBenchmarkFmt.ConstructionRow("double", N, n, m, reps, stat);

            checksumOut.Dispose();
            A.Dispose(); B.Dispose(); Q.Dispose(); R.Dispose(); uLo.Dispose(); uHi.Dispose();
            return row;
        }
    }
}
