// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceBenchmarks/BlockCGBenchmark.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System.Text;

using Unity.Burst;
using Unity.Collections;
using Unity.Jobs;
using Unity.Mathematics;

using LinearAlgebra;

namespace LinearAlgebra.Benchmarks
{
    // GENERATED per-dtype half of BlockCGBenchmark: two timed IJobs (block-CG over s RHS at once, vs
    // the scalar loop of s independent cg solves) + the build+measure method. The dtype-agnostic
    // harness (Run, Section) is hand-written in Assets/LinearAlgebra/Benchmarks/BlockCGBenchmark.cs.
    //
    // Both jobs solve the SAME s systems (SPD A, s x n block B) to the SAME tolerance, so the wall-clock
    // ratio is the true block-vs-scalar payoff: block-CG shares one Krylov subspace (fewer iterations)
    // and streams A over the whole block once per iteration via ApplyBlock (one GEMM vs s GEMVs), at the
    // cost of O(s^2 n) block updates + a tiny s x s Cholesky per iteration.

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct BlockCgTolJobFloat : IJob
    {
        public floatMxN A;               // n x n SPD
        public floatMxN B;               // s x n block RHS
        public floatMxN X, R, P, Q;      // s x n block scratch (X = solution)
        public int K; public float Tol;
        public Indices Iters;

        public void Execute()
        {
            int s = B.M_Rows, n = B.N_Cols;
            for (int i = 0; i < s; i++) for (int c = 0; c < n; c++) X[i, c] = (float)0;
            var info = Krylov.bcg(new floatDenseOperator(in A), in B, ref X, ref R, ref P, ref Q, K, Tol);
            Iters[0] = info.iterations;
        }
    }

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct ScalarLoopTolJobFloat : IJob
    {
        public floatMxN A;               // n x n SPD
        public floatMxN B;               // s x n block RHS
        public floatN x, r, p, Ap, bcol; // reused per column
        public int S, K; public float Tol;
        public Indices Iters;             // total iterations summed over the s columns

        public void Execute()
        {
            int n = B.N_Cols;
            var op = new floatDenseOperator(in A);
            int total = 0;
            for (int j = 0; j < S; j++)
            {
                for (int c = 0; c < n; c++) { bcol[c] = B[j, c]; x[c] = (float)0; }
                var info = Krylov.cg(in op, in bcol, ref x, ref r, ref p, ref Ap, K, Tol);
                total += info.iterations;
            }
            Iters[0] = total;
        }
    }

    public static partial class BlockCGBenchmark
    {
        static string BenchFloat(int n, int s)
        {
            var arena = new Arena(Allocator.Persistent);
            const string fmt = "{0,-7}{1,-7}{2,-4}{3,-14}{4,10:F4}{5,12:F4}{6,8}";

            var M = arena.floatMat(n, n);
            var A = arena.floatMat(n, n);
            var rng = new Unity.Mathematics.Random(2654435761u ^ (uint)(n * 131 + s));
            for (int row = 0; row < n; row++)
                for (int col = 0; col < n; col++) M[row, col] = rng.NextFloat(-1f, 1f);
            Blas.dot(in M, in M, ref A, true);                    // A = M^T M
            for (int d = 0; d < n; d++) A[d, d] += n;             // + n I  -> SPD, cond grows with n

            var B = arena.floatMat(s, n);
            for (int i = 0; i < s; i++)
                for (int c = 0; c < n; c++) B[i, c] = rng.NextFloat(-1f, 1f);

            float tol = Consts.floatSqrtEps;
            int cap = 4 * n;
            var iters = arena.Indices(1);
            var sb = new StringBuilder();

            // block-CG
            var X = arena.floatMat(s, n); var R = arena.floatMat(s, n);
            var P = arena.floatMat(s, n); var Q = arena.floatMat(s, n);
            var blockJob = new BlockCgTolJobFloat { A = A, B = B, X = X, R = R, P = P, Q = Q, K = cap, Tol = tol, Iters = iters };
            var blockStat = Bench.Time(() => blockJob.Run());
            sb.AppendLine(string.Format(System.Globalization.CultureInfo.InvariantCulture, fmt,
                "float", n, s, "block-CG", blockStat.Median, blockStat.Min, iters[0]));

            // scalar loop of s independent cg solves
            var x = arena.floatVec(n); var r = arena.floatVec(n); var p = arena.floatVec(n);
            var Ap = arena.floatVec(n); var bcol = arena.floatVec(n);
            var loopJob = new ScalarLoopTolJobFloat { A = A, B = B, x = x, r = r, p = p, Ap = Ap, bcol = bcol, S = s, K = cap, Tol = tol, Iters = iters };
            var loopStat = Bench.Time(() => loopJob.Run());
            sb.AppendLine(string.Format(System.Globalization.CultureInfo.InvariantCulture, fmt,
                "float", n, s, "scalar x s", loopStat.Median, loopStat.Min, iters[0]));

            arena.Dispose();
            return sb.ToString();
        }
    }
}
