// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceBenchmarks/Determinism.Shared.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using Unity.Mathematics;

using BULA;

namespace BULA.Benchmarks
{
    // Dtype-agnostic (no fProxy/long token) scalar-folding helpers for the determinism conformance
    // harness's generated per-dtype job Execute() bodies (see Determinism*.fProxy.cs/.long.cs and
    // docs/dev/spec-determinism-conformance-harness.md section 3's op-hash contract). Not proxy-
    // templated: copied through by codegen unchanged (singular output), so it lives once regardless
    // of how many dtype variants call it. Every overload is a thin, Burst-safe fold of one scalar's
    // raw bits onto a running op hash via Hash.combine, matching the library's own Hash contract.
    public static class DetHash
    {
        public static unsafe uint Combine(uint h, double d) => Hash.combine(h, Hash.hash((byte*)&d, sizeof(double)));
        public static uint Combine(uint h, float f) => Hash.combine(h, math.asuint(f));
        public static uint Combine(uint h, int i) => Hash.combine(h, (uint)i);
        public static uint Combine(uint h, uint u) => Hash.combine(h, u);
        public static unsafe uint Combine(uint h, long l) => Hash.combine(h, Hash.hash((byte*)&l, sizeof(long)));
        public static uint Combine(uint h, bool b) => Hash.combine(h, b ? 1u : 0u);
        public static unsafe uint Combine(uint h, byte* data, int byteLength) => Hash.combine(h, Hash.hash(data, byteLength));

        // Pivot/Indices expose no raw pointer (see docs/dev/spec-determinism-conformance-harness.md
        // binding rules) - fold their permutation element-by-element instead.
        public static uint CombinePivot(uint h, in Pivot p)
        {
            for (int i = 0; i < p.N; i++) h = Hash.combine(h, (uint)p[i]);
            return h;
        }

        public static uint CombineIndices(uint h, in Indices idx)
        {
            for (int i = 0; i < idx.N; i++) h = Hash.combine(h, (uint)idx[i]);
            return h;
        }
    }
}
