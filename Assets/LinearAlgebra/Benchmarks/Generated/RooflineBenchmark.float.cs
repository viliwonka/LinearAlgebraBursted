// <auto-generated>
//   Generated from Assets/LinearAlgebra/CodeGen/TemplateSourceBenchmarks/RooflineBenchmark.fProxy.cs
//   DO NOT EDIT BY HAND - edit the template and run Tools/regen.ps1.
// </auto-generated>
using System.Runtime.CompilerServices;
using System.Text;

using Unity.Burst;
using Unity.Collections;
using Unity.Collections.LowLevel.Unsafe;
using Unity.Jobs;
using Unity.Mathematics;

using BULA;


namespace BULA.Benchmarks
{
    // GENERATED per-dtype half of RooflineBenchmark (timed IJobs + build+measure methods). The
    // dtype-agnostic harness (Section, size list) is hand-written in
    // Assets/LinearAlgebra/Benchmarks/RooflineBenchmark.cs.

    public static class RooflineKernelsFloat
    {
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static float4 Splat(float v)
        {
            float4 r = default;
            r.x = v; r.y = v; r.z = v; r.w = v;
            return r;
        }

        // 8 independent width-4 chains, each stepping a = a*s + b: enough parallel chains to hide
        // FP latency, so this measures ISSUE THROUGHPUT, not dependency latency. s/b are runtime
        // values (1 and 0 at every call site) so the compiler can neither fold the chain nor
        // change any value — no overflow, no denormals, at any step count. Inlined into each
        // timed job so it compiles under THAT job's FloatMode (Default = no FMA contraction,
        // Fast = FMA allowed). One step = 8 chains x (mul + add) x 4 lanes = 64 scalar flops.
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public static float ComputeChains(int steps, float scale, float bias)
        {
            float4 s = Splat(scale);
            float4 b = Splat(bias);
            float4 a0 = Splat((float)1.01);
            float4 a1 = Splat((float)1.02);
            float4 a2 = Splat((float)1.03);
            float4 a3 = Splat((float)1.04);
            float4 a4 = Splat((float)1.05);
            float4 a5 = Splat((float)1.06);
            float4 a6 = Splat((float)1.07);
            float4 a7 = Splat((float)1.08);

            for (int i = 0; i < steps; i++)
            {
                a0 = a0 * s + b;
                a1 = a1 * s + b;
                a2 = a2 * s + b;
                a3 = a3 * s + b;
                a4 = a4 * s + b;
                a5 = a5 * s + b;
                a6 = a6 * s + b;
                a7 = a7 * s + b;
            }

            float4 r = ((a0 + a1) + (a2 + a3)) + ((a4 + a5) + (a6 + a7));
            return (r.x + r.y) + (r.z + r.w);
        }
    }

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public struct RooflineComputeJobFloat : IJob
    {
        public int Steps;
        public float Scale;
        public float Bias;
        public floatN Result;   // written so the chains cannot be dead-code-eliminated

        public void Execute() => Result[0] = RooflineKernelsFloat.ComputeChains(Steps, Scale, Bias);
    }

    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Fast)]
    public struct RooflineComputeFastJobFloat : IJob
    {
        public int Steps;
        public float Scale;
        public float Bias;
        public floatN Result;

        public void Execute() => Result[0] = RooflineKernelsFloat.ComputeChains(Steps, Scale, Bias);
    }

    // STREAM-style triad y = x*s + y over width-4 vectors: 2 loads + 1 store per element with
    // trivial math — pure bandwidth at any N past the caches.
    [BurstCompile(CompileSynchronously = true, FloatPrecision = FloatPrecision.High, FloatMode = FloatMode.Default)]
    public unsafe struct RooflineMemoryJobFloat : IJob
    {
        public floatN X;   // read stream
        public floatN Y;   // read-modify-write stream
        public float Scale;

        public void Execute()
        {
            var px = (float4*)X.Data.Ptr;
            var py = (float4*)Y.Data.Ptr;
            float4 s = RooflineKernelsFloat.Splat(Scale);
            int nQ = X.N >> 2;
            for (int q = 0; q < nQ; q++)
                py[q] = px[q] * s + py[q];
        }
    }

    public static partial class RooflineBenchmark
    {
        static string BenchComputeFloat(int millionFlops, bool fast)
        {
            int steps = (int)((long)millionFlops * 1000000 / 64);
            var res = new floatN(4, Allocator.Persistent);

            Bench.Stat stat;
            if (fast)
            {
                var job = new RooflineComputeFastJobFloat { Steps = steps, Scale = (float)1, Bias = (float)0, Result = res };
                stat = Bench.Time(() => job.Run());
            }
            else
            {
                var job = new RooflineComputeJobFloat { Steps = steps, Scale = (float)1, Bias = (float)0, Result = res };
                stat = Bench.Time(() => job.Run());
            }

            res.Dispose();
            return Bench.Row("float", millionFlops, stat, (double)millionFlops * 1e6);
        }

        static string BenchMemoryFloat(int millionElems)
        {
            int n = (int)((long)millionElems * 1000000);
            var X = new floatN(n, Allocator.Persistent);
            var Y = new floatN(n, Allocator.Persistent);
            X.fillInPlace((float)1);
            Y.fillInPlace((float)1);

            var job = new RooflineMemoryJobFloat { X = X, Y = Y, Scale = (float)1 };
            var stat = Bench.Time(() => job.Run());

            X.Dispose();
            Y.Dispose();

            // The "flops" argument is fed nominal BYTES MOVED (2 reads + 1 write per element), so
            // the report's last column reads as GB/s for these rows.
            double bytes = 3.0 * n * UnsafeUtility.SizeOf<float>();
            return Bench.Row("float", millionElems, stat, bytes);
        }
    }
}
